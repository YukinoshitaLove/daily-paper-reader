Title: Multilevel Preconditioning Strategies for Convex Optimization Methods in Image Deblurring

URL Source: https://arxiv.org/pdf/2607.10864v1

Published Time: Tue, 14 Jul 2026 01:33:26 GMT

Number of Pages: 39

Markdown Content:
# Multilevel Preconditioning Strategies for Convex Optimization Methods in Image Deblurring 

## Stefano Aleotti 1, Claudia Binda 1, Marco Donatelli 1, Rolf Krause 2,3 

> 1

Department of Science and High Technology, University of Insubria, Via Valleggio 11, Como, 22100, Italy. 

> 2

AMCS, CEMSE, King Abdullah University of Science and Technology, Thuwal, 23955-6900, Kingdom of Saudi Arabia. 

> 3

Euler Institute and Faculty of Informatics, Universit` a della Svizzera italiana, Via Giuseppe Buffi 10, Lugano, 6900, Switzerland. Contributing authors: stefano.aleotti@uninsubria.it ;

cbinda2@uninsubria.it ; marco.donatelli@uninsubria.it ;

rolf.krause@kaust.edu.sa ;

Abstract 

Proximal gradient methods are widely used in imaging, and their speed of conver-gence can be accelerated by incorporating variable metrics and/or extrapolation steps. Recent works have shown that preconditioning strategies can significantly enhance this acceleration, in particular, for image deblurring problems. In paral-lel, a multilevel framework has been introduced to speed up inertial and inexact forward–backward schemes for image restoration problems. In this paper, we combine preconditioning and multilevel strategies to design a robust and consistent acceleration framework for both standard and inexact forward-backward schemes applied to regularized convex optimization problems. Numerical experiments in image deblurring confirm that our approach yields a substantial improvement in convergence speed compared to standard methods. 

Keywords: Ill-posed problems, Multilevel convex optimization, Image deblurring, Preconditioning, Proximal gradient methods 

1arXiv:2607.10864v1 [math.NA] 12 Jul 2026 1 Introduction 

In this manuscript, we focus on image deblurring problems described by the model equation 

Au = bδ , (1) where A ∈ Rs×d represents the discretization of a space-invariant convolution operator, 

bδ ∈ Rs denotes the observed image corrupted by white Gaussian noise ηδ , and u ∈ Rd

is the unknown two-dimensional image with d pixels. We assume that bδ satisfies 

bδ = b + ηδ , ‖bδ − b‖ ≤ δ, (2) where b denotes the unobserved noise-free data, ‖ · ‖ is the Euclidean norm, and δ > 0is an upper bound on the noise level. The ill-posedness of the operator A, together with the presence of noise, requires the use of regularization in order to solve ( 1); see [ 1–3]. Regularization based on a standard variational approach consists of solving the optimization problem argmin 

> u∈Rd

f (u) + g(W u ), (3) where f : Rd → R is convex and smooth, g : Rd′

→ R ∪ {∞} is convex and possibly non-smooth, and W ∈ Rd′ ×d is a linear operator. Here, f denotes the data-fidelity 

term, depending on A and measuring the discrepancy between the observed data and the model, while the possibly non-differentiable term g◦W = g(W ·) serves as a penalty that incorporates prior information about the reconstructed image. Traditionally, this penalty term is weighted by an explicit regularization parameter λ > 0 to balance the two terms. In this paper, in agreement with the white Gaussian noise assumption on the observed data, we consider the particular instance of the model problem ( 3) where 

f (u) = 1

2 ‖Au − bδ ‖2. (4) A particularly effective way to compute approximate solutions of ( 3) is to employ proximal gradient methods [ 4–6], which are first-order iterative algorithms. These methods typically achieve mild-to-moderate accuracy at a relatively low cost per iteration, especially when the dimension d is large. Their basic iteration consists of alternating a gradient step on the differentiable part f with a proximal step on the non-smooth term g ◦ W , namely 

uk+1 = prox αg ◦W (uk − α∇f (uk )) , (5) where α > 0 is the step-length parameter along the descent direction −∇ f (uk), and prox αg ◦W denotes the proximal operator associated with the non-smooth term αg ◦W .Convergence of ( 5) to a solution of ( 3) is guaranteed whenever f has an L-Lipschitz continuous gradient and α is chosen smaller than 2 /L (see, e.g., [ 5]). 2However, proximal gradient methods suffer from two main drawbacks. The first is that scheme ( 5) assumes that prox αg ◦W is available in closed form. This assump-tion can be restrictive in real-world applications, where it is often not available in closed form, as is the case, for example, of the Total Variation [ 7, 8]. To overcome this issue, splitting approaches such as primal-dual methods avoid the explicit com-putation of prox αg ◦W by reformulating ( 3) as a convex-concave saddle-point problem [9–11 ]. Furthermore, if the convex conjugate g∗(v) = sup w∈Rd′ 〈v, w 〉 − g(w) admits an easily computable proximal operator, then the desired proximal approximation can be obtained by means of a primal-dual inner routine [ 12 –14 ], involving only the computation of ∇f (uk ), prox g∗ , and matrix-vector products with W and W T [14 –16 ]. The second drawback is that, if the step length α is chosen too small, conver-gence may become very slow, especially when only a rough estimate of the Lipschitz constant L is available. One possible remedy is to accelerate the method either by adopting a variable-metric strategy, which incorporates second-order information from the differentiable part [ 12 , 17 –20 ], or by introducing an extrapolation step that exploits information from previous iterations [ 21 , 22 ]. For instance, when g is the ℓ1-norm, a well-known extrapolated proximal gradient method is the Fast Iterative Soft Thresholding Algorithm (FISTA) [ 4]. Another strategy that has proven effective in accelerating convergence is the use of preconditioning techniques [ 23 –25 ]. In imag-ing problems, acceleration can often be achieved by preconditioning the linear system associated with ( 1) by means of a suitable operator P . In particular, when a left pre-conditioning approach is considered, the resulting method is also known as Iterated Tikhonov Thresholding Algorithm (ITTA). More recently, [ 26 ] introduced a multilevel strategy as an alternative approach for accelerating proximal gradient methods. The main idea is to approximate the objective function by a sequence of lower-dimensional models defined on coarse scales, where descent steps can be computed at a lower cost and then transferred back to the fine level. These coarse corrections are incorpo-rated into the fine-level descent step before the proximal operator is approximated to combine the benefits of both inertial and multilevel acceleration. The combination of the two acceleration strategies based on extrapolation and preconditioning may lead to instability, particularly when the proximity operator is not available in closed form and must be approximated, see [ 24 ]. On the other hand, combining preconditioning with multilevel strategies can accelerate convergence while preserving the stability of the method [ 27 ]. Therefore, in this manuscript, we aim to further investigate acceleration techniques by combining the multilevel strategy introduced in [ 26 , 28 ] with the preconditioning approach proposed in [ 23 , 24 ]. Since our focus is on image deblurring problems, the variational problem ( 3) can be naturally defined also on coarser levels by exploiting numerical linear algebra tools to construct both the blurring operator A and the preconditioner P on the corresponding coarse grids. By choosing a suitable preconditioner P , we can eliminate the need for an extrapolation step not only at the finest level but also on the coarser ones. The use of the preconditioner also at the coarse levels allows us to compute more accurate coarse corrections with fewer iterations than in the standard multilevel strategy, where non-preconditioned methods are employed. As a consequence, the descent directions generated on the coarse grids are expected to be more effective once transferred back to 3the fine level. Thereby reducing both the number of iterations required for convergence and the overall computational time. Several multilevel methods for image deblurring have previously been proposed in the literature [ 27 , 29 –32 ]. Some of these methods share our choice of grid-transfer and coarse-level operators; however, the underlying iterative regularization schemes are not based on proximal gradient methods. In comparison with the two papers [ 26 , 28 ]that inspired our work, we not only consider different proximal gradient methods, but also introduce a different correction criterion and an adaptive strategy for selecting the number of coarse-level iterations and multilevel cycles, thereby making the proposed multilevel framework fully automated. The remainder of the manuscript is organized as follows. Section 2 reviews stan-dard notions and results from convex analysis that will be used throughout the paper. Section 3 presents the proximal gradient methods considered in this work, distinguish-ing between those for which the proximal operator is available in closed form and those in which an approximate proximal point is computed via a dual formulation. Section 4 introduces the multilevel framework and describes the strategies adopted in our approach. The convergence properties of the resulting methods are analyzed in Section 5. Finally, Sections 6 and 7 are devoted to the experimental setup and the numerical results for image deblurring problems. 

# 2 Preliminaries 

This section recalls some well-known definitions and properties of convex analysis that will be used throughout the paper. For a more in-depth discussion, we refer the reader to [ 33 ]. Before that, we introduce some useful notations that will be consistently used. Denoting by S+(Rd) the set of all d × d real symmetric positive definite matrices and given η > 0, we define the subset Dη ⊆ S +(Rd) as containing all matrices in 

S+(Rd) with eigenvalues greater than or equal to η. Moreover, given M ∈ S +(Rd), the norm induced by M is defined as ‖u‖M = √uT M u , ∀u ∈ Rd.

## 2.1 Convex Analysis                                                                                 

> Definition 1 (Relative interior) Let Ω⊆Rnbe a set. We define the relative interior of Ωas
> relint(Ω) := {u∈Ω| ∃ ǫ > 0s.t. B(u, ǫ )∩aff(Ω) ⊂Ω},
> where B(u, ǫ )is the ball centered in uwith radius ǫwhile aff(Ω) is the affine hull of Ω.
> Definition 2 (Effective domain) The effective domain of a proper function f:Rd→R∪{∞}
> is the set
> dom( f) :=
> {
> u∈Rd:f(u)<∞
> }
> .
> Note that since fis proper, there exists u∈Rdsuch that f(u)6= + ∞and thus dom( f)6=∅.
> Definition 3 A function f:Rd→Ris said to have an L−Lipschitz continuous gradient if the following property holds
> ‖∇ f(˜ u)− ∇ f(¯ u)‖ ≤ L‖˜u−¯u‖,∀˜u, ¯u∈Rd.

4Example 1 A classic example of a function with L−Lipschitz continuous gradient is the least squares data-fidelity term (4). Since ∇f (u) = AT (Au − bδ), we have 

‖∇ f (˜ u) − ∇ f (¯ u)‖ = ‖AT (A˜u − bδ ) − AT (A¯u − bδ)‖ ≤ ‖ AT A‖‖ ˜u − ¯u‖,

and, in particular, the Lipschitz constant of ∇f is L = ‖AT A‖ = ‖A‖2.

Definition 4 Let ϕ : Rd → R ∪ {∞} be a proper convex and lower semicontinuous function. The subdifferential of ϕ at point u ∈ Rd is defined as the set 

∂ϕ = {w ∈ Rd : ϕ(v) ≥ ϕ(u) + 〈w, v − u〉 , ∀v ∈ Rd}.

Lemma 1 [33 , Theorem 23.8, Theorem 23.9] Let ϕ1, ϕ 2 : Rd′

→ R ∪ {∞} be proper, convex and lower semicontinuous functions and W ∈ Rd′×d.

• If ϕ(v) = ϕ1(v) + ϕ2(v) and there exists v0 ∈ Rd′

such that v0 ∈ relint(dom( ϕ1)) ∩

relint(dom( ϕ2)) , then 

∂(ϕ1 + ϕ2)( v) = ∂ϕ 1(v) + ∂ϕ 2(v), ∀v ∈ dom( ϕ).

• If ϕ = ϕ1(W u ) and there exist u0 ∈ Rd such that W u 0 ∈ relint(dom( ϕ1)) , then 

∂ϕ (u) = W T ∂ϕ 1(W u ) = {W T v : v ∈ ∂ϕ 1(W u )}, ∀u ∈ dom( ϕ).

Definition 5 [33 , p. 104] Given a proper, convex, lower semicontinuous function ϕ : Rd →

R ∪ {∞} , the convex conjugate of ϕ is the function 

ϕ∗ : Rd → R ∪ {∞} , ϕ∗(w) = sup 

> u∈Rd

〈w, u 〉 − ϕ(u), ∀ w ∈ Rd.

For the biconjugate function ( ϕ∗)∗, the following well-known result holds. 

Theorem 2 [33 , Theorem 12.2] Let ϕ : Rd → R ∪ {∞} be proper, convex, and lower semicontinuous. Then ϕ∗ is convex and lower semicontinuous, and (ϕ∗)∗ = ϕ.

Definition 6 Let ϕ : Rd → R ∪ { +∞} be a proper, convex, lower semicontinuous function. The proximity operator associated to ϕ in the metric induced by a symmetric positive definite matrix P ∈ D η with parameter α > 0 is the map prox αϕ : Rd → Rd defined as 

prox Pαϕ (a) = arg min  

> u∈Rd

ϕ(u) + 1

2α ‖u − a‖2 

> P

, ∀ a ∈ Rd.

Remark 1 When P = In, we write prox In 

> αϕ

= prox αϕ .

Proposition 3 (Moreau decomposition) Let ϕ : Rd → R ∪ {∞} be a proper, convex, lower semicontinuous function, and let ϕ∗ : Rd → R ∪ {∞} be its conjugate. Given α ∈ R++ , and 

P ∈ S +(Rd), then the following identity holds 

prox Pαϕ (u) + αP −1 prox P −1

> α−1ϕ∗

(α−1P u ) = u, ∀u ∈ Rd. (6) 

5Lemma 4 [34 , Section 11–12] Let ϕ : Rd → R ∪ {∞} be proper, convex, and lower semicontinuous. For all α, β > 0, the following statements are equivalent: 1. u = prox αϕ (u + αw );2. w ∈ ∂ϕ (u);3. ϕ(u) + ϕ∗(w) = 〈w, u 〉;4. u ∈ ∂ϕ ∗(w);5. w = prox βϕ ∗ (βu + w).

Definition 7 (Smoothed convex function) Let R be a convex, lower semi-continuous, and proper function on Rd. For every γ > 0, a continuously differentiable function Rγ is called a smoothed convex approximation of R if there exist two finite-valued scalars η1 and η2

satisfying η1 + η2 > 0 such that it holds 

∀u ∈ Rd R(u) − η1γ ≤ Rγ (u) ≤ R(u) + η2γ. (7) 

A classical and widely adopted approach to constructing such a smoothed approximation is via the Moreau envelope, which we define next. 

Definition 8 (Moreau envelope) Let γ > 0 and ϕ : Rd → R ∪ {∞} be a convex, lower semi-continuous, and proper function. The Moreau envelope of ϕ, denoted by γ ϕ, is the convex, continuous, real-valued function defined as  

> γ

ϕ(u) = inf 

> v∈Rd

{

ϕ(v) + 1

2γ ‖u − v‖2

}

. (8) 

The Moreau envelope γ ϕ is strictly related to prox γϕ by  

> γ

ϕ(u) = ϕ(prox γϕ (u)) + 1

2γ ‖u − prox γϕ (u)‖2. (9) Moreover, γ ϕ is Fr´ echet differentiable on Rd, and its gradient is γ−1-Lipschitz continuous and it holds that 

∇(γ ϕ) = γ−1(Id − prox γϕ 

). (10) In light of these regularizing properties, we will employ the Moreau envelope as our standard smoothed convex approximation throughout this work. From now on, we consider the optimization problem ( 3) under the following 

Assumption 1 

1. f : Rd → R is convex and differentiable with L-Lipschitz continuous gradient; 2. g : Rd′

→ R ∪ {∞} is a proper convex lower semicontinuous function; 3. W ∈ Rd′×d and exists u0 such that W u 0 ∈ relint(dom( g)) ;4. Problem in equation (3) has at least one solution. 

6We remark that the function f (u) in ( 4) satisfies Assumption 1.1. Moreover, Assumption 1.3 on W is required to guarantee that the subdifferential rule ∂(g ◦

W )( u) = W T ∂g (W u ) holds. 

# 3 Proximal gradient methods 

In this section, we review different proximal gradient or forward-backward methods that can effectively exploit the structure of the variational problem ( 3). These are first-order iterative methods that alternate, at each iteration, a forward gradient step on the differentiable part f , followed by a backward proximal step on the convex, non-smooth term g ◦ W . The general iterative scheme of these methods is given by 

uk+1 = prox αk g◦W (uk − αk ∇f (uk)) , k = 1 , 2, . . . , (11) where αk ∈ R++ is the steplength parameter. In the first part of this section, we assume that the proximal operator of g ◦ W is available in closed form. In contrast, the second part is devoted to numerical methods that address the inexact computation of the proximal operator. 

## 3.1 Exact proximal gradient methods 

A particular instance of proximal gradient methods appears when we consider g(u) = 

λ‖u‖1, λ > 0, and W is a semiorthogonal linear operator, i.e., W T W = I, for instance, a framelet operator (see Appendix A). In this case, the proximity operator of λ‖ · ‖ 1 ◦

W is the combination of the soft-thresholding operator with the linear operator W ,namely prox λ‖·‖ 1◦W (u) = W T Sλ(W u ), (12) where ( Sλ(u)) i := sign( ui) max {| ui| − λ, 0}, for i = 1 , . . . d , is the soft–thresholding operator. When we apply the proximal gradient method to this specific case, we obtain the so-called Iterative Soft–Thresholding Algorithm (ISTA) [ 35 ]. 

FISTA 

Due to the slow convergence rate typical of first-order methods, a Nesterov-type acceleration strategy can be incorporated into the iterations. This consists of adding a preliminary step, called the extrapolation step, to the iterative scheme under consideration. The resulting Fast ISTA (FISTA) [ 4] is described in Algorithm 1.7Algorithm 1 FISTA  

> 1:

Choose u0 ∈ Rd, 0 < α < 2 

> L

, maxit ∈ N, W ∈ Rd′ ×d. 

> 2:

t0 = 1, u−1 = u0 

> 3:

for k = 1 , . . . , maxit do  

> 4:

tk+1 = 1 + √1 + 4 t2

> k

2 

> 5:

yk = uk + tk − 1

tk+1 

(uk − uk−1) 

> 6:

uk+1 = W T Sαλ 

(W (yk − α∇f (yk )) ) 

> 7:

end for 

ITTA 

A different approach to overcoming the slow convergence of proximal gradient methods relies on introducing a suitable preconditioner P ∈ S +(Rd). This strategy is inspired by the iterated Tikhonov method [ 36 ], which can also be interpreted as a precondi-tioned gradient descent method and is therefore referred to as the Iterated Tikhonov Thresholding Algorithm (ITTA) in [ 23 ]. Similarly to equation ( 3), given S ∈ S +(Rs), we consider the optimization problem argmin 

> u∈Rd

fS (u) + g(W u ) = argmin 

> u∈Rd

1

2 ‖Au − bδ ‖2 

> S−1

+ g(W u ). (13) In the data fidelity term 

fS (u) = 1

2 ‖S− 1 

> 2

(Au − bδ)‖2, (14) the linear operator S 1 

> 2

can be interpreted as a left preconditioner for the linear system ( 1). If we further assume that there exists P ∈ S +(Rd) such that 

P −1AT = AT S−1, (15) then it holds 

∇fS (u) = AT S−1(Au − bδ ) = P −1∇f (u), (16) where f (u) is defined as in equation ( 4). A possible choice for the preconditioner operator P and the associated matrix S

that satisfies the condition ( 15 ), is 

P = AT A + νI, S = AA T + νI, (17) with ν > 0, as proposed in [ 24 ]. This choice is inspired by the iterated Tikhonov method [ 36 ], which coincides with the Levenberg–Marquardt method applied to the minimization of f . Indeed, P is a regularized approximation of the Hessian of f in 

S+(Rd). 8Applying the forward-backward method ( 11 ) to ( 13 ), where f is replaced by fS ,and choosing P as in ( 17 ), such that we can fix αk = 1, the corresponding forward-backward method can be summarized as in Algorithm 2.

Algorithm 2 ITTA  

> 1:

Choose u0 ∈ Rd, maxit ∈ N, W ∈ Rd′ ×d. 

> 2:

for k = 1 , . . . , maxit do  

> 3:

uk+1 = W T Sλ

(W (uk − P −1∇f (uk)) ) 

> 4:

end for 

## 3.2 Inexact proximal gradient methods 

The availability of a closed form for the proximity operator of g◦W is a strong assump-tion that is not satisfied by many regularization terms. In what follows, we describe a possible remedy for this issue by introducing a primal-dual strategy. Furthermore, we briefly present a preconditioned iteration, which is a generalization of the ITTA scheme within a broader framework. 

NPD 

Several iterative methods based on an inexact inertial forward–backward splitting scheme have been proposed in the literature, for instance in [ 13 –15 , 37 ]. In this work, we focus on the Nested Primal–Dual (NPD) method proposed in [ 37 ], in which a nested iterative procedure is used to approximate the proximity operator of g ◦ W . Its iterative scheme can be summarized as 

{

¯uk = uk + γk (uk − uk−1),uk+1 ≈ prox αk g◦W (¯ uk − αk∇f (¯ uk)) , (18) where ≈ indicates an approximation of the proximal-gradient point. Here, ¯ uk is usually referred to as the inertial point and γk ∈ [0 , 1] is the inertial parameter. Let L be the Lipschitz constant of ∇f , then the step length αk can be chosen stationary as 

α ∈ (0 , 2/L ). We can compute a fairly accurate approximation of the proximity operator in ( 18 )by exploiting the following 

Lemma 5 [37 , Theorem 1] Suppose that g : Rd′

→ R ∪ {∞} and W ∈ Rd′×d satisfy Assump-tion 1 items 2. and 3. Given a ∈ Rd, α > 0, 0 < β < 2/‖W ‖2, and v0 ∈ Rd′

, define the sequence 

vk+1 = prox βα −1g∗ (vk + βα −1W (a − αW T vk)) , ∀k ≥ 0. (19) 

Then there exists ˆv ∈ Rd′

such that: (i) lim k→∞ vk = ˆ v;(ii) prox αg ◦W (a) = a − αW T ˆv. 

9Note that by combining items ( i) and ( ii ) of Lemma 5 with equation ( 19 ), it holds prox αg ◦W (a) = a − αW T lim  

> k→∞

prox βα −1 g∗ (vk + βα −1W (a − αW T vk )) .

In this way, we can compute an approximation of the proximal operator of g ◦ W

by a finite number of steps of a primal-dual procedure, provided that the opera-tor prox βα −1 g∗ and the matrix-vector product with the linear operators W and W T

are easily computable. Let jmax be the maximum number of inner iterations used to compute the dual sequence ( 19 ), the resulting NPD algorithm is summarized in Algorithm 3.

Algorithm 3 NPD  

> 1:

Choose u0 ∈ Rd, 0 < α < 2 

> L

, 0 < β < 2  

> ‖W‖2

, jmax ∈ N, {γk}k∈N ⊆ R≥0. 

> 2:

u−1 = u0 

> 3:

v0,0 = 0  

> 4:

for k = 0 , 1, . . . do  

> 5:

Compute γk 

> 6:

¯uk = uk + γk(uk − uk−1) 

> 7:

uk+ 1 

> 2

= ¯ uk − α∇f (¯ uk ) 

> 8:

for j = 0 , 1, . . . , j max − 1 do  

> 9:

uk,j = uk+ 1 

> 2

− αW T vk,j  

> 10:

vk,j +1 = prox βα −1g∗ (vk,j + βα −1W u k,j ) 

> 11:

end for  

> 12:

vk+1 ,0 = vk,j max  

> 13:

uk,j max = uk+ 1 

> 2

− αW T vk,j max  

> 14:

uk+1 = 1

> jmax

∑jmax  

> i=1

uk,i  

> 15:

end for 

The convergence of the iterations to a solution of the variational problem ( 3) can be proved under suitable assumptions on the extrapolation parameter γk, provided that Assumption 1 holds. For further details, we refer the reader to [ 37 ]. 

PNPD 

In [ 24 ], the authors prove that preconditioning strategies can be applied to the NPD framework to achieve faster convergence. In what follows, we consider only left pre-conditioning; for further details on the right preconditioning approach, the reader is referred to [ 24 , 25 ]. The main idea is similar to the one used to derive ITTA from ISTA. Therefore, by applying the NPD algorithm to problem ( 13 ) instead of problem ( 3) under assump-tion ( 15 ), we obtain a two-step iterative scheme, called the Preconditioned Nested 

10 Primal-Dual (PNPD) [ 24 ], defined as 

{

¯uk = uk + γk(uk − uk−1),uk+1 ≈ prox αk g◦W (¯ uk − αk P −1∇f (¯ uk)) . (20) The preconditioner P and the extrapolation strategy are both intended to accelerate the iterative scheme. Indeed, as shown in [ 24 ], the acceleration provided by the extrap-olation term can also be achieved through a suitable choice of the preconditioner, and an inaccurate choice of γk could lead to instability. Therefore, in the numerical experiments we set γk = 0. Algorithm 4 reports the pseudocode of the proposed PNPD method without extrapolation, where αk = α ∈ (0 , 2/L ) and LS denotes the Lipschitz constant of ∇fS .This constant depends on the choice of the preconditioner S (i.e., P ), as a consequence of ( 15 ). In particular, thanks to equation ( 16 ), we have 

‖∇ fS (x) − ∇ fS (y)‖ = ‖P −1∇f (x) − P −1∇f (y)‖ ≤ ‖ P −1AT A‖‖ x − y‖,

and it holds 

LS = ‖P −1AT A‖ = ‖AT S−1A‖.

Remark 2 If P and S are chosen as in (17 ), then it can be shown, by means of the singular value decomposition (SVD) of A, that LS ≤ 1 for all ν > 0. Therefore, the step length in 

(20 ) can be fixed as αk = 1 throughout the iterations. 

The convergence of the iterates generated by the PNPD scheme can be established under the same assumptions as in the NPD case, together with condition ( 15 ) on the preconditioner P . For further details, we refer the reader to [ 24 ]. 

Algorithm 4 PNPD  

> 1:

Choose u0 ∈ Rd, P ∈ S +(Rd), 0 < α < 2 

> LS

, 0 < β < 2  

> ‖W‖2

, jmax ∈ N. 

> 2:

v0,0 = 0  

> 3:

for k = 0 , 1, . . . do  

> 4:

uk+ 1 

> 2

= uk − αP −1∇f (uk) 

> 5:

for j = 0 , 1, . . . , j max − 1 do  

> 6:

uk,j = uk+ 1 

> 2

− αW T vk,j  

> 7:

vk,j +1 = prox βα −1 g∗ (vk,j + βα −1W u k,j ) 

> 8:

end for  

> 9:

vk+1 ,0 = vjmax  

> n
> 10:

uk,j max = uk+ 1 

> 2

− αW T vk,j max  

> 11:

uk+1 = 1

> jmax

∑jmax  

> i=1

uk,i  

> 12:

end for 

11 4 Multilevel framework 

In this section, we introduce the multilevel framework adopted in the numerical exper-iments. The proposed approach is designed to enhance computational efficiency by combining multilevel strategies with suitable preconditioning techniques. The inter-play between these two components improves both the convergence properties and the scalability of the overall method. The adopted strategy follows the multilevel scheme proposed in [ 28 ], suitably adapted to the structure of the problem under consideration. In what follows, we introduce all the tools required to implement an effective multilevel scheme, together with the theoretical assumptions needed to guarantee convergence to the solution. First, for the sake of simplicity and without loss of generality, we consider the two-level case. Accordingly, we use the index h (respectively, H) for all quantities defined at the fine (respectively, coarse) level. Referring to the variational problem ( 3), we define Fh : RNh → (−∞ , +∞] as the objective function at the fine level, where Nh = d,and such that 

Fh = fh + gh, fh = f, g h = g ◦ W. 

We then associate with this fine-level functions a coarse-level approximation, where 

NH < N h, denoted by FH : RNH → (−∞ , +∞], fH , and gH , respectively. 

## 4.1 Coarse model and transfer information operators 

Firstly, a multilevel scheme requires the transfer of information from one level to another. To this end, we introduce two transfer operators. The first one is the linear operator IHh : RNh → RNH , referred to as the restriction operator , which transfers information from the fine level to the coarse level. Conversely, we define the operator 

IhH : RNH → RNh , called the prolongation operator , which transfers information from the coarse level back to the fine level. We require these transfer operators to satisfy the following definition.                      

> Definition 9 Two operators IHh:RNh→RNHand IhH:RNH→RNhare called coherent information transfer (CIT) operators if there exists ξ > 0such that
> IhH=ξ(IHh)T.(21)

There are many different ways to construct CIT operators, for instance, by means of full weighting and bilinear interpolation, as adopted in the numerical results. The basic idea of a multilevel strategy is to compute inexpensive corrections at a coarse resolution, which can then be used to update the current iterate after being prolonged to the fine level. We now focus on how to define the variational model at a coarser level. Following [ 28 , 38 ], we smooth the non-differentiable term g ◦ W in the sense of Definition 8, which satisfies Definition 7.12 Definition 10 (Coarse model FH for non-smooth functions) The coarse model FH is defined at the point uh ∈ RNh as 

FH = fH + gH + 〈vH , · 〉 , (22) 

where 

vH = IHh

(∇fh(uh) + ∇γh gh(uh)) − (∇fH (IHh uh) + ∇γH gH (IHh uh)). (23) 

The two functions γh gh and γH gH are smoothed versions of gh and gH , respectively, and they satisfy Definition 11 with smoothing parameters γh > 0 and γH > 0, respectively. The smoothed coarse model γH FH is defined at the point uh ∈ RNh as  

> γH

FH = fH +γH gH + 〈vH , · 〉 . (24) 

Definition 11 (First order coherence) The first-order coherence between the smoothed ver-sion of the objective function Fh at the fine level and the coarse level objective function FH

is verified in a neighborhood of uh if it holds 

∇γH FH (IHh uh) = IHh ∇γh Fh(uh). (25) 

Lemma 6 If FH is given by Definition 10 , then it satisfies the first order coherence (Definition 11 ). Proof Considering the gradient of the coarse smoothed model γH FH in Equation ( 24 ) and combining it with the definition of vH in Equation ( 23 ), yields 

∇γH FH (IHh uh) = ∇fH (IHh uh) + ∇γH gH (IHh uh) + vH

= IHh

(∇fh(uh) + ∇γH gh(uh))

= IHh ∇γH Fh(uh).



Once the coarse model has been defined, the next step in a multilevel strategy consists of computing a suitable solution of the coarse problem and using it to obtain a better approximation at the fine level. More specifically, we require that a decrease in the objective function at the coarser level also implies a decrease in the fine-level objective function when the coarse solution is interpolated onto the finer grid. This consistency between levels is ensured by the linear term vH defined in ( 23 ), which allows the coarse model to satisfy the first-order coherence condition of Definition 11 .The smoothing approach aims to preserve fidelity to the original non-smooth problem while introducing desirable properties into the coarse model. Depending on the regularization term, two different cases may arise. If g◦W is smooth, one can simply apply gradient-based methods without requiring any smoothing in the coarse model, that is, γh = γH = 0. This is also the case when g◦W is non-smooth, but its proximity operator can be computed in closed form, since approximate solutions of the coarse model can be computed by means of proximal gradient methods. However, when the proximity operator cannot be computed in closed form, as in the case of TV regularization, the use of the Moreau envelope may lead to some difficulties. 13 Indeed, in order to compute the linear term vH defined in ( 25 ), we need to evaluate 

∇γ gh = ∇γ (g ◦ W ). By ( 10 ), we have 

∇γ (g ◦ W ) = γ−1(Id − prox γg ◦W ).

Since prox γg ◦W does not admit a closed-form expression, we are unable to compute the linear term vH . To overcome this issue, we adopt the same strategy proposed in [28 ]: instead of directly applying the Moreau envelope to g ◦ W , we first compute the Moreau envelope of g and then compose it with W . This smoothing strategy satisfies the following interesting properties.                                    

> Lemma 7 ([ 28 ], Lemma 3.2) γg◦Wis a smoothed convex function approximating g◦Win the sense of Definition 7.
> Lemma 8 For any u∈Rd,W:Rd→Rd′
> and g:Rd′
> →Ra convex, lower semi-continuous, and proper function, we have
> ∇(γg◦W)( u) = γ−1WT(W u −prox γg (W u )).(26)

This means that an explicit expression for prox γg is sufficient to characterize the gradient of γ (g ◦ W ). However, if no explicit information on the proximity operator of 

g is available, but only on its convex dual function g∗, then we can exploit the Moreau decomposition ( 6), with P = Id, and rewrite, according to Lemma 7, the gradient of  

> γ

(g ◦ W ) as 

∇(γ g ◦ W )( u) = γ−1W ∗(W u − prox γg (W u ))

= γ−1W ∗(W u − (W u − γ prox γ−1g∗ (γ−1W u )) )

= W T prox γ−1g∗ (γ−1W u ).

Within the multilevel framework, fine-level iterations are enhanced by coarse-level corrections, which are computed through V-cycles (see Algorithms 5 and 6). Specifi-cally, during a V-cycle, a sequence of iterations is performed at the coarse level. The difference between the final and initial coarse-level iterates, which acts as an error-correction term, is then prolonged to the fine level and used to update the current approximation. Between successive V-cycles, standard fine-level iterations may be per-formed. Finally, the multilevel acceleration is applied only during the first p iterations, after which the algorithm proceeds using the standard method alone. 

## 4.2 Decrease of the objective function 

The last ingredient needed to properly define a multilevel strategy is the choice of the solver Φ H to be used for the coarse variational problem. Ideally, this solver should generate a sequence {umH }m≥0 that yields a sufficient decrease of γH FH after a finite number of iterations. 14 Algorithm 5 Vcycle iter (2 levels)  

> 1:

uk, 0 

> H

= IHh ukh 

> 2:

vkH = IHh ∇γ Fh(ukh) − ∇ γ FH (uk, 0 

> H

) 

> 3:

uk,m H = Φ( uk, 0 

> H

, m, v kH ) 

> 4:

ck,m h = IhH

(

uk,m H − uk, 0

> H

) 

> 5:

τ k = 5 , t = 0  

> 6:

while Fh(ukh + τ k ck,m h ) > F h(ukh) + cτ k ∇γ F Th (ukh)ck,m h ∧ t < max bt do  

> 7:

τ k = (0 .85) t τ k , t = t + 1  

> 8:

end while  

> 9:

uk+1  

> h

= ukh + τ k ck,m h

Algorithm 6 MULTILEVEL (2 levels)  

> 1:

Set u0 

> h

∈ RN , p, max bt , m, Φ, maxit.  

> 2:

for k = 1 , . . . , p do  

> 3:

ukh =Vcycle iter( uk−1 

> h

, m, ΦH , max bt ) 

> 4:

end for 5: 6: umaxit  

> h

= Φ h(uph, maxit − p)

Assumption 2 Let (Φ H,ℓ )ℓ∈N be a sequence of operators such that there exists an integer 

m > 0 with the following property: if umH = Φ H,m −1 ◦ . . . ◦ ΦH, 0(u0 

> H

), then  

> γH

FH (umH ) ≤ γH FH (u0 

> H

).

Moreover, the correction umH − u0 

> H

is bounded. 

Typical choices for the sequence of operators {ΦH,ℓ }ℓ∈N include gradient descent methods and forward–backward schemes. For simplicity, in the numerical experiments section, we choose Φ H,ℓ to be the same solver used at the fine level. These solver operators guarantee that umH − u0 

> H

is a bounded descent direction for γH FH (due to convergence of the sequence [ 39 ]). 

Lemma 9 (Descent direction for the fine-level smoothed function) . Let us assume that 

IHh and IhH are CIT operators, that γH FH satisfies Definition 10 , and that ΦH satisfies Assumption 2. Then, IhH (umH − u0 

> H

) is a descent direction for fh + γh gh.Proof Let uh ∈ RNh and define pH := umH − u0 

> H

. Recall that u0 

> H

= IHh uh. By the definition of descent direction, we have 

〈pH , ∇FH (u0 

> H

)〉 ≤ 0.

By the first-order coherence condition and imposing IHh = ξ−1 (

IhH

)T

, we obtain 

〈pH , ∇FH (u0 

> H

)〉 = 〈pH , I Hh ∇(fh + γh gh)( uh)〉 = ξ−1〈IhH (pH ), ∇(fh + γh gh)( uh)〉 ≤ 0.



15 The previous lemma shows that IhH (umH − u0 

> H

) is a descent direction for the smoothed objective function γh Fh at the fine level. We conclude this part on the decrease of the objective function by deriving a bound for the decrease of the non-smooth objective function Fh at the fine level. In [ 26 ], the authors search for a suitable step size τ that prevents excessively large corrections from the coarse level by guaranteeing that γ Fh(uh + τ I hH (umH − u0 

> H

)) ≤ γ Fh(uh). (27) In our case, however, we replace condition ( 27 ) with the more restrictive Armijo condition. More precisely, we require the parameter τ to satisfy  

> γ

Fh(uh + τ I hH (umH − u0 

> H

)) ≤ γ Fh(uh) + c1τ 〈∇ γ Fh(uh), I hH (umH − u0 

> H

)〉, (28) for some positive constant c1 ∈ (0 , 1). 

Lemma 10 (Fine-level decrease) If the assumptions of Lemma 9 hold, then the iterations of Algorithm 7 satisfy 

Fh(uh + τ I hH (umH − u0 

> H

)) ≤ Fh(uh) + ( μ1 + μ2)γh. (29) 

Proof This follows directly from the definition of smoothed convex function given in Definition 7. Since there exists a value of τ satisfying the Armijo condition, we have 

Fh(uh + τ I hH (umH − u0 

> H

)) ≤ γ Fh(uh + τ I hH (umH − u0 

> H

)) + μ1γh

≤ γ Fh(uh) + c1τ 〈∇ γ Fh(uh), I hH (umH − u0 

> H

)〉 + μ1γh

≤ Fh(uh) + ( μ1 + μ2)γh.



The last inequality follows from the fact that, by Lemma 9, the term 

c1τ 〈∇ γ Fh(uh), I hH (umH − u0 

> H

)〉 is negative, since IhH (umH − u0 

> H

) is a descent direction for  

> γ

Fh. This shows that a coarse-level minimization step leads to a decrease of Fh, up to the constant ( μ1 + μ2)γh, which can be made arbitrarily small by letting γh tend to zero. 

## 4.3 Extension to a multilevel scheme 

The extension of the previously presented results to the multilevel framework is straightforward. Indeed, when considering J levels in the multilevel approach, the analysis developed above can be applied to each pair of consecutive levels. Proceed-ing recursively, one can show that the coarsest level J produces a bounded correction that defines a descent direction for the upper level. This correction can then be recur-sively exploited to generate a new descent direction for level J − 1, and so on, until a refinement of the initial point at the finest level is obtained. In terms of classical multilevel terminology, all these computations can be interpreted as a single V–cycle of the proposed multilevel strategy. 16 In principle, several V–cycles could be performed consecutively. The general mul-tilevel scheme is summarized in Figure 1, where we consider J = 4 levels, perform m

iterations on each coarse grid, and apply s fine-level iterations after each V–cycle. We also design an adaptive strategy to select the number of coarse iterations and V–cycles automatically, based on whether the Armijo condition ( 28 ) is satisfied, as reported in Algorithms 7 and 8. This condition is used to estimate the parameter τ ,which is computed through a backtracking procedure so that ( 28 ) holds. The idea is as follows: (a) Perform m iterations on the coarse grid and store all intermediate reconstructions. (b) Check whether the computed reconstruction satisfies the Armijo condition. (c) If the condition is not satisfied, select the reconstruction obtained after ⌊ m

> 2

⌋

iterations and repeat the Armijo test. Continue by progressively reducing the number of iterations (e.g., 4 → 2 → 1). (d) If the reconstruction obtained after a single iteration still does not satisfy the Armijo condition, then no V–cycle is performed and no additional V–cycles are attempted.   

> m
> iterations
> m
> iterations
> m
> iterations
> s
> iterations
> m
> iterations
> m
> iterations
> m
> iterations
> maxit −s−2iterations

1 V-cycle Level 1 Level 2 Level 3 Level 4 

Fig. 1 : General scheme of the multilevel approach with 4 levels, m coarse iterations and maxit fine iterations. 

Algorithm 7 MULTILEVEL AUTO (2 levels)  

> 1:

Set u0 

> h

∈ RN , p, max bt , m, Φ, cycle = true.  

> 2:

while cycle == true do  

> 3:

[ukh, cycle] = Vcycle iter auto( uk−1 

> h

, m, ΦH , max bt ) 

> 4:

end while  

> 5:

umaxit  

> h

= Φ h(uph, maxit − p)

# 5 Convergence 

In this section, we show that the considered multilevel framework generates a conver-gent sequence of iterates {uk} converging to a solution of the original problem ( 3). 17 Algorithm 8 Vcycle iter auto (2 levels)  

> 1:

uk, 0 

> H

= IHh ukh 

> 2:

vkH = IHh ∇Fh(ukh) − ∇ γ FH (uk, 0 

> H

) 

> 3:

uk,m H = Φ H (uk, 0 

> H

, m, v kH ) 

> 4:

success = false  

> 5:

while success = false and m ≥ 1 do  

> 6:

ck,m h = IhH

(

uk,m H − uk, 0

> H

) 

> 7:

τ k = 5 , t = 0  

> 8:

while Fh(ukh + τ k ck,m h ) > F h(ukh) + cτ k ∇F Th (ukh)ck,m h ∧ t < max bt do  

> 9:

τ k = (0 .85) t τ k , t = t + 1  

> 10:

end while  

> 11:

if t = max bt then  

> 12:

m = ⌊ m

> 2

⌋ 

> 13:

else  

> 14:

success = true  

> 15:

end if  

> 16:

end while  

> 17:

if success = true then  

> 18:

uk+1  

> h

= ukh + τ k ck,m h 

> 19:

else  

> 20:

cycle = false  

> 21:

end if 

For a more detailed discussion of the convergence analysis, we refer the reader to [ 28 ]. In order to study the convergence of the multilevel approach, it is necessary to under-stand the type of approximation introduced when computing the proximity operator of the regularization term. To this end, we first extend the notion of subdifferential through the following definition: 

Definition 12 The ǫ-subdifferential of R at u ∈ dom( R) is defined as: 

∂ǫR(u) = {y ∈ Rd | R(u) ≥ R(u) + 〈x − u, y 〉 − ǫ, ∀x ∈ Rd}.

Based on this definition, different types of approximations can be introduced. How-ever, typical image restoration problems in which dual optimization is employed are based on what is called type 2 approximation. 

Definition 13 We say that x ∈ Rd is a type 2 approximation of prox γR (u) with precision ǫ,and we write x ≈2,ǫ prox γR (u), if and only if 

γ−1(u − x) ∈ ∂ǫR(x).

The convergence results corresponding to all the approximation types, as well as the case in which the proximal operator can be computed in closed form, are discussed in 18 detail in [ 28 ]. In what follows, we show that the strategy adopted here to approximate the proximity operator coincides with the approach proposed in [ 28 ], and therefore yields a type 2 approximation. Firstly, recall that in our case the regularization term is given by R(u) = ( g◦W )( u). The NPD and PNPD methods, described in Section 3.2 , compute an approximation of the proximal operator based on Lemma 5 that is they compute a dual sequence vk

defined as 

vk+1 = prox βα −1g∗ (vk + βα −1W (a − αW T vk)) , ∀k ≥ 0, (30) where α > 0, 0 < β < 2/‖W ‖2 and v0 ∈ Rd′

. To show that this coincides with a type 2 approximation, we will show that this coincides with applying any proximal gradient methods to a particular minimization problem. Indeed, starting from the standard definition of the proximity operator, we switch to the dual formulation by using the convex conjugate defined in Definition 5. Namely, prox γg ◦W (u) = argmin 

> x∈Rd

1

2 ‖x − u‖22 + γ(g(W x )) = argmin 

> x∈Rd

max 

> w∈Rd

1

2 ‖x − u‖22 + γ(xT W T w − g∗(w)) = argmax 

> w∈Rd

− 1

2γ ‖u − γW T w‖22 + 1

2γ ‖u‖22 − g∗(w)= argmin 

> w∈Rd

1

2γ ‖u − γW T w‖22 + g∗(w). (31) Problem ( 31 ) is usually referred to as the dual problem. An approximate solution can be computed by applying any suitable optimization method. For instance, by using a proximal gradient method, one obtains the iterative scheme 

wk+1 = prox μg ∗ (wk + μW (u − γW T wk)) ,

where the parameter μ must be chosen according to the Lipschitz constant of the gradient of the differentiable term 1 

> 2γ

‖u − γW T w‖22. In particular, one requires μ <    

> 2
> γ‖W W T‖

. Therefore, by writing μ = βγ −1 with 0 < β < 2/‖W ‖22, we recover exactly the same iterative scheme as in ( 30 ). Dual optimization thus provides a simple way to approximate the proximity operator, while also offering theoretical guarantees on the computed approximation, as stated in the following proposition. 

Proposition 11 Assume that (wk)k∈N is a minimizing sequence for the dual problem (31 ).Then: 

• The sequence (u − γW T wk)k∈N converges to the exact solution of prox γg ◦W (u).

• This sequence provides a type 2 approximation of the proximity operator. 

19 Proof See [ 28 ], Proposition 3.5. 

The convergence guarantees of the overall algorithm are directly related to the accuracy with which the proximity operator is approximated at each iteration. Typi-cally, a smaller approximation error requires a greater computational effort, which is why some approaches use a fixed budget of inner iterations to estimate the proxim-ity operator. This choice, however, comes at the price of limited accuracy and may eventually lead to divergence when a large number of outer iterations are performed. In [ 28 ], the authors propose a heuristic strategy based on a tolerance parame-ter controlling the relative distance between two consecutive inner iterates used in the approximation of the proximity operator. Whenever no decrease of the objective function is observed over two consecutive iterations, the tolerance is reduced, thereby enforcing a more accurate computation of the proximal point. In our numerical experiments, we choose to keep the number of inner iterations fixed when computing the proximity operator. More precisely, we perform only one inner iteration. This choice is motivated by the results in [ 24 , 37 ], where the NPD and PNPD methods do not exhibit any substantial improvement when the number of nested iterations is increased. We also empirically verified this behavior by testing a larger number of inner iterations. Although, in principle, this should yield a more accurate approximation of the proximity operator, in all our experiments, the results remain essentially unchanged compared with the case in which only one inner iteration is performed. 

# 6 Experimental setup 

In this section, we describe the setting and the implementation details of the multilevel framework used in our numerical experiments. 

Variational model 

In the numerical experiments, we consider image problems of the form ( 1) solving the optimization problem ( 3). We consider two different regularization terms. In the first case, we take g(W u ) = 

‖W u ‖1, where W ∈ R3d×d is the framelet operator defined in Appendix A. In this case, the optimization problem ( 3) takes the form argmin 

> u∈Rd

1

2 ‖Au − bδ‖22 + λ‖W u ‖1. (32) This type of variational problem can be efficiently addressed by exact proximal gradient methods such as FISTA and ITTA, since the proximity operator for the reg-ularization term can be computed in closed form. For this reason, in the numerical experiments, we analyze the performance of the multilevel framework when FISTA or ITTA are used as solvers at both the fine and the coarse levels. 20 The second regularization term we consider is the total variation (TV) functional [40 ], that is argmin 

> u∈Rd

1

2 ‖Au − bδ ‖22 + λT V (u). (33) In this case, the proximity operator of the TV does not admit a closed-form expression, and therefore only approximate solutions of the optimization problem ( 33 ) can be computed by means of inexact proximal gradient methods such as NPD and PNPD, see Section 3.2 . Also in this setting, we analyze the performance achieved by the multilevel approach when inexact proximal gradient methods are used as solvers at both the fine and coarse levels, and compare it with the corresponding plain strategy. To this end, we conclude this preliminary part by recalling that the TV functional can be written in the form g ◦ W , where 

• W : Rd → R2d is the discrete gradient operator, 

W u =

[DxuDy u

]

,

with Dx and Dy denoting the finite-difference operators in the horizontal and vertical directions, respectively. 

• h : R2d → R is the convex function 

h(z) = 

> d

∑

> j=1

‖zj ‖2,

where zj ∈ R2 is the two-dimensional gradient vector at pixel j.              

> Remark 3 The choice of βin the NPD and PNPD algorithms must be consistent with Algorithms 3and 4. In the TV case, it can be shown that β=1
> 8satisfies the requirement
> 0< β < 2
> ‖W‖2.

Transfer operators 

As mentioned in Section 4.1 , a crucial ingredient of multilevel methods is the ability to transfer information between different levels. From a theoretical point of view, the only assumption required is that the transfer operators be coherent according to Definition 21 . In this section, we describe a classic choice of CIT transfer operators based on linear B-splines. The restriction operator is the full-weighting associated with the stencil 

R = 1

16 



1 2 1 2 4 2 1 2 1 

 ,

which is centered on the target pixel and computes a weighted average of the neighboring pixels. 21 To define the coarser problem, we combine the linear operator associated with the stencil R with the downsampling operator. Consequently, the resulting restriction operator can be written as 

IHh = 1

16 



2 1 0 · · · 00 1 2 1 0 · · · 0... . . . . . . . . . ...0 · · · 0 1 2 1

 ⊗



2 1 0 · · · 00 1 2 1 0 · · · 0... . . . . . . . . . ...0 · · · 0 1 2 1

 ,

where ⊗ denotes the Kronecker product. Note the similarity with the matrix W00 in Appendix A.Correspondingly, the stencil associated with the prolongation operator is defined as 

P = 1

4

1 2 1 2 4 2 1 2 1 

 = 4 RT ,

which defines the bilinear interpolation operator 

IhH = 4( IHh )T .

The pair ( IHh , I hH ) provides a simple and intuitive construction of CIT operators. 

Coarse problem 

In Section 4.1 , we described how to properly define the variational problem at coarser levels starting from the fine-level formulation. When dealing with image deblurring problems, a further crucial aspect is the definition of the blurring operator A on the coarser grids. To this end, we briefly describe below the strategy adopted in our numerical experiments. We consider the two-level setting and let 

d = dh > d H = d

4 ,

so that IHh ∈ RdH ×dh and IhH ∈ Rdh×dH . Then, we denote by Ah = A the blurring operator at the fine level, and we construct the coarse-level operator according to the Galerkin approach, as 

AH = IHh AhIhH . (34) The interpretation of this formulation for AH is that applying AH to a vector is equivalent to interpolating the vector to the finer level, applying the operator Ah, and then restricting the result to the coarser level. 

Computational cost 

In the numerical experiments, we compare the standard methods FISTA, ISTA, NPD, and PNPD with their multilevel counterparts. To ensure a fair comparison, we explicitly analyze the computational cost in all cases. 22 Since the coarse problem has size d/ 4, estimates suggest that four iterations at the coarse level are approximately equivalent, in terms of floating-point operations, to one iteration at the fine level. For this reason, the number of coarse iterations in the mul-tilevel methods is chosen to be a multiple of four. In particular, in the plots showing the error versus the iteration count, one multilevel iteration with four coarse itera-tions is compared with one fine-level iteration, whereas one multilevel iteration with eight coarse iterations is compared with two fine-level iterations. For a comprehensive analysis of the computational cost, we refer the reader to Appendix B.

# 7 Numerical results 

All numerical tests were carried out in MATLAB 25.2.0.2998904 (R2025b) on a laptop running Windows 11 Home (version 25H2), equipped with an Intel Core i7-1065G7 processor (1.30 GHz) and 16 GB of RAM. The test problems were generated using the IRtools toolbox [ 41 ]. For all algorithms, the initial guess is chosen as the observed image. The shifting parameter of the preconditioner is fixed at ν = 0 .1 throughout all numerical exper-iments. In the multilevel framework, the number of levels is set to 2, the Moreau parameter is chosen as γ = 1 .1, and a total of 100 iterations are performed. Since the coarse-level problem is better conditioned, the regularization parameter and the preconditioner shift at the coarse level are set to λH = λh/4 and νH = νh/4, respectively. Moreover, periodic boundary conditions are assumed in the construction of the matrix A, so that all computations can be performed by means of the Fast Fourier Transform (FFT). We tested several values of m, corresponding to the number of coarse-grid itera-tions, and of p, corresponding to the number of V-cycles. Among the various numerical experiments performed, the choice m = 8 and p = 8 provided a good compromise across all examples, independently of the blur and noise levels. Therefore, in the mul-tilevel framework (Algorithm 6), we perform 8 iterations on the coarse grid for 8 V-cycles. No intermediate fine-level iterations after a V-cycle iteration are performed, since our experiments indicate that they slow down convergence. Our main goal is to show that the multilevel and preconditioned algorithms are more efficient than their non-multilevel and non-preconditioned counterparts for solving deblurring problems, without compromising the quality of the computed recon-structions, which remains comparable across all algorithms. The performance of the methods is assessed using two metrics. The first one is the Relative Reconstruction Error (RRE), defined as RRE( urec ) = ‖utrue − urec ‖

‖utrue ‖ , (35) where urec denotes the reconstructed image and utrue the ground-truth image. The second metric is the Peak Signal-to-Noise Ratio (PSNR), defined as PSNR( urec ) = 10 log 10 

( 1

‖utrue − urec ‖2

)

, (36) 23 (a) (b) (c) 

Fig. 2 : Example 1: ( a) True image, ( b) Gaussian PSF of 35 × 35 pixels, ( c) Observed image. since our gray-scaled images are scaled in [0 , 1]. Higher PSNR values correspond to more accurate reconstructions. 

## 7.1 Exact case 

This section focuses on the solution of the minimization problem ( 32 ), where the proximity operator can be computed in closed form. In the numerical experiments, we compare the standard proximal methods ITTA and FISTA with their multilevel counterparts, namely MITTA and MM FISTA. In addition, we test the automatic strategy presented in Section 4.3 for estimating the optimal number of coarse itera-tions and V-cycles. The multilevel method based on this adaptive strategy is called MITTA auto. To ensure convergence of the methods, at each level we set the step-length param-eter α = 1 for ITTA, while for FISTA we adopt the classic choice α = 1 /L , where L

denotes the Lipschitz constant of the gradient of the data-fidelity term. To test the different methods, we consider a grayscale satellite image of 512 × 512 pixels, blurred by a Gaussian PSF. The final observed image bδ is obtained by adding 2% white Gaussian noise ηδ , that is, 

‖ηδ ‖ = 0 .02 ‖b‖.

7.1.1 Example 1 

In this first example, the PSF is of 35 × 35 pixels. Figure 2 shows the ground-truth image, the PSF, and the corresponding observed image. The regularization parameter is λ = 3 · 10 −4 for FISTA and MM FISTA, while 

λ = 2 · 10 −3 for ITTA, MITTA, and MITTA auto. MITTA auto performs 8 coarse iterations and 3 V-cycles. Figure 3 shows the RRE values obtained by the considered methods, varying the number of iterations, and plotted according to the previous cost analysis. We observe that, in terms of iteration count, both MITTA and MITTA auto converge faster than ITTA. In particular, the initial coarse-grid iterations already provide a significant 24 0 20 40 60 80 100 120    

> 0.1 0.12 0.14 0.16 0.18 0.2 0.22 0.24 FISTA ITTA MM FISTA MITTA MITTA auto

Number of iterations RRE 

Fig. 3 : Example 1: comparison of the RREs varying the number of iterations for FISTA, ITTA, and their multilevel versions.                       

> Iter FISTA ITTA MM FISTA MITTA MITTA auto
> 429.3752 30.7140 30.7939 32.1489 32.1001 15 31.0242 31.8205 31.5673 33.0966 32.5803 60 33.1690 32.9968 32.9448 33.3729 33.2069

Table 1 : Example 1: PSNR values obtained at different iterations with different methods. improvement of the approximation. Moreover, thanks to the presence of the precon-ditioner P , both ITTA and its multilevel counterpart show a faster decrease of the RRE than FISTA and MM FISTA. Although the automatic multilevel procedure is slightly slower than the multilevel version in which the number of coarse iterations and V-cycles is manually optimized, it still outperforms all the other methods. Since it avoids the need to tune two additional parameters, it provides a competitive and robust alternative in practice. To better emphasize the acceleration of the convergence in the first iterations, Table 1 shows the PSNR for the considered methods after 4, 15, and 60 iterations. Note that, for instance, the reconstruction produced by MITTA after only 4 V-cycles attains a higher PSNR than the ITTA reconstruction after 15 iterations, confirming the improved performance of the multilevel approach. This is further highlighted by the detail of the reconstructions shown in Figure 4.

7.1.2 Example 2 

In this second example, we choose a more severe Gaussian PSF, namely, we consider a PSF of size 51 × 51. Figure 5 shows the ground-truth image, the corresponding PSF, and the corrupted observation used in this experiment. 25 24.9971 25.9777 26.3410 

Fig. 4 : Example 1: detailed view of the reconstructed images. From left to right, we show the ITTA reconstruction after 4 iterations, after 15 iterations, and the MITTA reconstruction after 4 iterations. The corresponding PSNR values are reported below each image.   

> (a) (b) (c)

Fig. 5 : Example 2:( a) True image, ( b) Gaussian PSF of dimension 51 ×51, ( c) Observed image. The regularization parameter is λ = 5 · 10 −4 for FISTA and MM FISTA, while it is λ = 3 · 10 −3 for ITTA, MITTA and MITTA auto. The automatic strategy selects the same number of coarse iterations and V-cycles as in the previous example, namely 8 coarse iterations and 3 V-cycles. Figure 6 reports the comparison in terms of RRE at each iteration. The plot clearly shows that the iterations performed on the coarse grid significantly accelerate convergence in the multilevel schemes. As in the previous example, MITTA achieves the highest PSNR values among all the methods under comparison. Comparing our MITTA with MM FISTA, we note that the improvement in the reconstructed images is visible after just a few iterations. In fact, as highlighted in the zoomed-in view in Figure 7, the satellite image appears sharper, particularly along the edges, when using MITTA rather than MM FISTA. 

## 7.2 Inexact case 

The present section focuses on the solution of the optimization problem ( 33 ), where the proximity operator is not available in closed form. In the numerical experiments, we 26 0 20 40 60 80 100 120    

> 0.14 0.16 0.18 0.2 0.22 0.24 0.26 0.28 0.3 FISTA ITTA MM FISTA MITTA MITTA auto

Number of iterations RRE 

Fig. 6 : Example 2: comparison of the RREs varying the number of iterations for FISTA, ITTA, and their multilevel versions. 

Fig. 7 : Example 2: detailed view of the reconstructed images. MM FISTA reconstruc-tion after 4 iterations (left) and MITTA reconstruction after 4 iterations (right). compare the standard inexact proximal methods NPD and PNPD with their multilevel counterparts, namely MNPD and MPNPD. In addition, we also test the automatic strategy introduced in Section 4.3 for estimating the optimal number of coarse iter-ations and V-cycles in the MPNPD method. The multilevel method based on this adaptive strategy is called MPNPD auto. To guarantee convergence at both levels, we set the step length in the gradient-descent step equal to α = 1 for PNPD and α = 1 

> L

for NPD, while for both algorithms, we choose β = 1 

> 8

according to the estimation in Remark 3.In the NPD method, for the extrapolation sequence {γk}, we adopt the same strategy proposed in [ 37 ], denoted by {γN P D k } and defined as 

{γN P D  

> 0

= 0 

γN P D k = min 

{

γF IST A k , Cρ k  

> ‖uk−uk−1‖

}

, ∀k ≥ 1,

27 (a) (b) (c) 

Fig. 8 : Example 3: ( a) true image, ( b) defocus PSF of size 31 ×31, ( c) observed image. where C > 0 is a positive constant, set here to C = 100, and {ρk}k∈N is a fixed positive summable sequence chosen as ρk = 1 

> k1.1

, as suggested in [ 37 ]. The sequence γF IST A k

corresponds to the classical FISTA extrapolation parameter, namely 

γF IST A k = tk − 1

tk+1 

, where 

{t0 = 0 ,tk+1 = 1+ √1+4 t2 

> k
> 2

, ∀k ≥ 1.

In the following experiments, we consider a 256 × 256 grayscale image of pep-pers, blurred with two different PSFs generated by the IRtools toolbox [ 41 ], and contaminated with white Gaussian noise with intensity level equal to 1%. 

7.2.1 Example 3 

In this example, we consider a defocus PSF of size 31 × 31. Figure 8 depicts the ground-truth image, the corresponding PSF, and the corrupted observation. The regularization parameter is chosen as λ = 1 · 10 −4 for NPD and MNPD, and as λ = 1 · 10 −3 for PNPD, MPNPD, and MPNPD auto. In this experiment, the MPNPD auto algorithm performs 8 coarse iterations for 19 V-cycles, followed by 4 coarse iterations for 38 V-cycles. Figure 9 shows the comparison in terms of RRE for the five considered methods. The plot confirms that, also in the inexact case, the initial coarse-grid iterations per-formed by the multilevel methods significantly improve the overall performance. As for the MPNPD auto algorithm, increasing the number of V-cycles leads to a fur-ther reduction of the RRE; however, this improvement becomes only marginal after approximately the 20th iteration. The comparison among the reconstructions obtained with NPD, PNPD, and their multilevel counterparts is reported in Figure 10 , where the results after 4, 15, and 60 iterations are shown. As in the previous examples, the multilevel preconditioned methods achieve the highest PSNR values among all the considered algorithms. In particular, after only 4 iterations, MPNPD already provides a reconstruction of higher quality than the one obtained by standard PNPD after 15 iterations. 28 0 20 40 60 80 100 120   

> 0.05 0.1 0.15 0.2 0.25 NPD PNPD MNPD MPNPD MPNPD auto

Number of iterations RRE 

Fig. 9 : Example 3: comparison of the RRE varying the number of iterations for NPD, PNPD, and their multilevel versions. 

NPD PNPD MNPD MPNPD MPNPD auto 

> Iter: 4

22.5627 24.4368 24.7746 26.9806 26.9806 

> Iter: 15

24.8281 26.0527 25.9426 28.2409 28.8953 

> Iter: 60

27.9005 27.7485 27.6144 28.8691 29.6418 

Fig. 10 : Example 3: reconstructions obtained at different iterations with different methods. 29 7.2.2 Example 4 

In this example, we consider a shake PSF of size 42 × 42. Figure 11 displays the ground-truth image, the corresponding PSF, and the observed image.   

> (a) (b) (c)

Fig. 11 : Example 4: ( a) true image, ( b) shake PSF of size 42 × 42, ( c) observed image. The regularization parameter is chosen as λ = 4 · 10 −4 for NPD and MNPD, and as λ = 3 · 10 −3 for PNPD, MPNPD, and MPNPD auto. In this example, the MPNPD auto algorithm performs 8 coarse-level iterations for 7 V-cycles, followed by 4 additional coarse iterations for 2 more V-cycles. Figure 12 reports the comparison in terms of RRE for the considered methods. As in the previous examples, the plot confirms that the initial coarse-grid iterations performed by the multilevel methods provide a substantial acceleration of convergence. The same conclusions can be derived by looking at the restored images in Figure 13 .        

> 020 40 60 80 100 120
> 00.05 0.1 0.15 0.2 0.25 0.3 NPD PNPD MNPD MPNPD MPNPD auto

Number of iterations RRE 

Fig. 12 : Example 4: comparison of the RRE varying the number of iterations for NPD, PNPD, and their multilevel versions. 30 NPD PNPD MNPD MPNPD MPNPD auto             

> Iter: 4
> 23.1924 26.9759 27.4100 31.35599 31.3559
> Iter: 15
> 27.9786 30.8264 30.2138 33.7781 33.7233
> Iter: 60
> 34.1109 33.9253 33.8464 34.6223 34.6302

Fig. 13 : Example 4: reconstructions obtained at different iterations with different methods. The main takeaway from these experiments is that the proposed methods are able to produce accurate approximations of the solution with only a small number of coarse-grid corrections and the aid of a suitable preconditioner, while remaining competitive with existing approaches. 

# 8 Conclusion 

Inspired by the IML FISTA method proposed in [ 28 ], we investigated the possibility of combining preconditioning strategies for proximal gradient methods with a multilevel framework. We proposed a convergent preconditioned multilevel framework for both exact and inexact proximal algorithms. Furthermore, we replaced condition ( 27 ) with an Armijo condition in order to better control the corrections arising from the coarse level. Finally, we introduced an adaptive strategy for selecting the number of coarse iterations and V-cycles, based on the satisfaction of the Armijo condition. We tested our approach on several image deblurring problems involving observations corrupted by white Gaussian noise. The numerical experiments confirm the advantages of incorporating a precondi-tioner within a multilevel framework. Indeed, our methods perform very well across a wide range of point spread functions of different types and sizes. Specifically, the mul-tilevel ITTA and PNPD algorithms provide high-quality reconstructions faster than their standard counterparts. This opens up promising opportunities for addressing 31 large-scale problems. Furthermore, the automatic algorithm we propose allows us to reduce the number of parameters without compromising performance. Future work will explore alternative choices for the preconditioning matrix, particu-larly for applications where our proposal may be computationally demanding to invert, such as in computed tomography. Moreover, investigating the link between coarse and fine-level optimization remains key to determining the most effective strategies for constructing lower-level models. 

# Declarations 

• Funding: The first three authors are members of the GNCS group of INdAM and are partially supported by the PRIN 2022 project “Inverse Problems in the Imaging Sciences (IPIS)” (2022ANC8HL) and by the INdAM-GNCS 2024 Project “Metodi strutturati per il signal processing avanzato” (CUP E53C25002010001). The last author thanks the Swiss National Science Foundation for the support through the project “CardioTwin - Precision Cardiology based on Digital Twins” (Grant number 214817). 

• Conflict of interest: All authors certify that they have no affiliations with or involvement in any organization or entity with any financial interest or non-financial interest in the subject matter or materials discussed in this manuscript. The authors have no Conflict of interest to declare that are relevant to the content of this article. 

• Ethics approval: The authors declare that research ethics approval was not required for this study. 

• Consent to participate: The authors declare that consent to participate was not required for this study. 

• Data availability: The test problems were generated using the IRtools toolbox [41 ]. The peppers.png test image is a standard dataset distributed with the MATLAB environment. 

• Code availability: The custom code generated during the current study is available from the corresponding author on reasonable request. 

• Author contribution: The authors contributed equally to this work. 

# References 

[1] Hansen, P.C., Nagy, J.G., O’Leary, D.P.: Deblurring Images: Matrices, Spectra, and Filtering. SIAM, Philadelphia (2006) [2] Engl, H.W., Hanke, M., Neubauer, G.: Regularization of Inverse Problems, 1996 edn. Mathematics and Its Applications. Springer, Dordrecht, Netherlands (1996) [3] Bertero, M., Boccacci, P.: Introduction to Inverse Problems in Imaging. Institute of Physics Publishing, Bristol (1998) [4] Beck, A., Teboulle, M.: A fast iterative shrinkage-thresholding algorithm for linear inverse problems. SIAM journal on imaging sciences 2(1), 183–202 (2009) 32 [5] Combettes, P.L., Wajs, V.R.: Signal recovery by proximal forward-backward splitting. Multiscale Model. Simul. 4(4), 1168–1200 (2005) [6] Daubechies, I., Defrise, M., Mol, C.D.: An iterative thresholding algorithm for linear inverse problems with a sparsity constraint. Commun. Pure Appl. Math. 

57 (11), 1413–1457 (2004) [7] Bach, F., Jenatton, R., Mairal, J., Obozinski, G.: Structured sparsity through convex optimization. Stat. Sci. 27 (4), 450–468 (2012) [8] Polson, N.G., Scott, J.G., Willard, B.T.: Proximal Algorithms in Statistics and Machine Learning. Stat. Sci. 30 (4), 559–581 (2015) [9] Chambolle, A., Pock, T.: A first–order primal–dual algorithm for convex problems with applications to imaging. J. Math. Imaging Vis. 40 , 120–145 (2011) [10] Malitsky, Y., Pock, T.: A First-Order Primal-Dual Algorithm with Linesearch. SIAM J. Optim. 28 (1), 411–432 (2018) [11] Chambolle, A., Delplancke, C., Ehrhardt, M.J., Sch¨ onlieb, C.-B., Tang, J.: Stochastic Primal–Dual Hybrid Gradient Algorithm with Adaptive Step Sizes. J. Math. Imaging Vis. 66 , 294–313 (2024) [12] Bonettini, S., Porta, F., Ruggiero, V.: A variable metric forward-backward method with extrapolation. SIAM J. Sci. Comput. 38 , 2558–2584 (2016) [13] Schmidt, M., Roux, N.L., Bach, F.: Convergence rates of inexact proximal-gradient methods for convex optimization. arXiv:1109.2415v2 (2011) [14] Villa, S., Salzo, S., Baldassarre, L., Verri, A.: Accelerated and inexact forward-backward algorithms. SIAM J. Optim. 23 (3), 1607–1633 (2013) [15] Bonettini, S., Rebegoldi, S., Ruggiero, V.: Inertial variable metric techniques for the inexact forward–backward algorithm. SIAM J. Sci. Comput. 40 (5), 3180–3210 (2018) [16] Chen, J., Loris, I.: On starting and stopping criteria for nested primal-dual iterations. Numer. Algorithms 82 , 605–621 (2019) [17] Chouzenoux, E., Pesquet, J.-C., Repetti, A.: Variable metric forward-backward algorithm for minimizing the sum of a differentiable function and a convex function. J. Optim. Theory Appl. 162 , 107–132 (2014) [18] Frankel, P., Garrigos, G., Peypouquet, J.: Splitting methods with variable met-ric for Kurdyka–Lojasiewicz functions and general convergence rates. J. Optim. Theory Appl. 165 (3), 874–900 (2015) [19] Ghanbari, H., Scheinberg, K.: Proximal quasi-Newton methods for regularized 33 convex optimization with linear and accelerated sublinear convergence rates. Comput. Optim. Appl. 69 , 597–627 (2018) [20] Lee, C., Wright, S.J.: Inexact successive quadratic approximation for regularized optimization. Comput. Optim. Appl. 72 , 641–674 (2019) [21] Beck, A., Teboulle, M.: Fast gradient-based algorithms for constrained total vari-ation image denoising and deblurring problems. IEEE Trans. Image Processing 

18 (11), 2419–34 (2009) [22] Ochs, P., Chen, Y., Brox, T., Pock, T.: iPiano: Inertial proximal algorithm for non-convex optimization. SIAM J. Imaging Sci. 7(2), 1388–1419 (2014) [23] Huang, J., Donatelli, M., Chan, R.H.: Nonstationary iterated thresholding algo-rithms for image deblurring. Inverse Problems and Imaging 7(3), 717–736 (2013) 

https://doi.org/10.3934/ipi.2013.7.717 

[24] Aleotti, S., Donatelli, M., Krause, R., Scarlato, G.: A preconditioned version of a nested primal-dual algorithm for image deblurring. Journal of Scientific Computing 103 (3), 85 (2025) https://doi.org/10.1007/s10915-025-02863-8 

[25] Aleotti, S., Bonettini, S., Donatelli, M., Prato, M., Rebegoldi, S.: Anested primal–dual iterated tikhonov method for regularized convex opti-mization. Computational Optimization and Applications 91 , 357–395 (2024) 

https://doi.org/10.1007/s10589-024-00613-4 

[26] Lauga, G., Riccietti, E., Pustelnik, N., Gon¸ calves, P.: Multilevel fista for image restoration. In: ICASSP 2023 - 2023 IEEE International Confer-ence on Acoustics, Speech and Signal Processing (ICASSP), pp. 1–5 (2023). 

https://doi.org/10.1109/ICASSP49357.2023.10094710 

[27] Buccini, A., Donatelli, M.: A multigrid frame based method for image deblurring. Electron. Trans. Numer. Anal. 53 , 283–312 (2020) https://doi.org/10.1553/etna 

vol53s283 

[28] Lauga, G., Riccietti, E., Pustelnik, N., Gon¸ calves, P.: Iml fista: A mul-tilevel framework for inexact and inertial forward-backward. application to image restoration. SIAM Journal on Imaging Sciences 17 (3), 1347–1376 (2024) 

https://doi.org/10.1137/23M1582345 

[29] Donatelli, M.: A multigrid for image deblurring with tikhonov regularization. Numerical linear algebra with applications 12 (8), 715–729 (2005) [30] Donatelli, M., Serra-Capizzano, S.: On the regularizing power of multigrid-type algorithms. SIAM Journal on Scientific Computing 27 (6), 2053–2076 (2006) [31] Morigi, S., Reichel, L., Sgallari, F., Shyshkov, A.: Cascadic multiresolution 34 methods for image deblurring. SIAM Journal on Imaging Sciences 1(1), 51–74 (2008) [32] Chan, R.H., Chen, K.: A multilevel algorithm for simultaneously denoising and deblurring images. SIAM Journal on Scientific Computing 32 (2), 1043–1063 (2010) [33] Rockafellar, R.T.: Convex Analysis. Princeton Mathematical Series. Princeton University Press, Princeton, N. J. (1970) [34] Moreau, J.J.: Proximit´ e et dualit´ e dans un espace hilbertien. Bul-letin de la Soci´ et´ e Math´ ematique de France 93 , 273–299 (1965) 

https://doi.org/10.24033/bsmf.1625 

[35] Daubechies, I., Defrise, M., De Mol, C.: An iterative thresholding algorithm for linear inverse problems with a sparsity constraint. Communications on Pure and Applied Mathematics: A Journal Issued by the Courant Institute of Mathematical Sciences 57 (11), 1413–1457 (2004) [36] Engl, H.W., Hanke, M., Neubauer, A.: Regularization of Inverse Problems. Math-ematics and Its Applications, vol. 375. Kluwer Academic Publishers, Dordrecht, The Netherlands (1996). https://doi.org/10.1007/978-94-009-1740-8 

[37] Bonettini, S., Prato, M., Rebegoldi, S.: A nested primal–dual fista-like scheme for composite convex optimization problems. Computational Optimization and Applications 84 , 1–39 (2022) https://doi.org/10.1007/s10589-022-00410-x 

[38] Beck, A., Teboulle, M.: Smoothing and first order methods: A uni-fied framework. SIAM Journal on Optimization 22 (2), 557–580 (2012) 

https://doi.org/10.1137/100818327 

[39] Aujol, J.-F., Dossal, C.: Stability of over-relaxations for the forward-backward algorithm, application to fista. SIAM Journal on Optimization 25 , 2408–2433 (2015) https://doi.org/10.1137/140994964 

[40] Rudin, L.I., Osher, S., Fatemi, E.: Nonlinear total variation based noise removal algorithms. J. Phys. D. 60 (1–4), 259–268 (1992) [41] Gazzola, S., Hansen, P.C., Nagy, J.: Ir tools: A matlab package of iterative regu-larization methods and large-scale test problems. Numerical Algorithms 81 (2019) 

https://doi.org/10.1007/s11075-018-0570-7 

[42] Cai, J.-F., Chan, R.H., Shen, Z.: A framelet-based image inpainting algorithm. Applied and Computational Harmonic Analysis 24 (2), 131–149 (2008) 35 A Framelets 

Let W denote the tight frame associated with linear B-splines [42 ]. The one-dimensional linear B-spline system consists of a low-pass filter W0 and two high-pass filters W1 and W2. The corresponding masks are 

w(0) = 1 

> 4

(1 , 2, 1) , w(1) = √2 

> 4

(−1, 0, 1) , w(2) = 1 

> 4

(−1, 2, −1) .

Imposing reflexive boundary conditions, the resulting filter matrices are 

W0 = 1

4



3 1 0 · · · 01 2 1 · · · 0... ... . . . ... ...0 · · · 1 2 10 · · · 0 1 3



, W1 =

√2

4



−1 1 0 · · · 0

−1 0 1 · · · 0... ... . . . ... ...0 · · · − 1 0 10 · · · 0 −1 1 



,W2 = 1

4



1 −1 0 · · · 0

−1 2 −1 · · · 0... ... . . . ... ...0 · · · − 1 2 −10 · · · 0 −1 1



,

and 

W =

W0

W1

W2

 . (37) The operators Wi, i = 0 , 1, 2, are designed for one-dimensional signals. The two-dimensional framelet filters are obtained via tensor products as 

Wij = Wi ⊗ Wj , i, j = 0 , 1, 2,

and the corresponding 2D operator is 

W2D = ( W ⊗ W ) = 



W00 

W01 

...

W22 

 . (38) The low-pass component is W00 , while all remaining matrices Wij contain at least one high-pass filter in at least one spatial direction. 

# B Computational cost 

For simplicity, we set d = n2, allowing us to represent a square image X of size n × n

at the fine level and an image of size n 

> 2

× n 

> 2

at the coarse level. Accordingly, the 36 blurring matrix A has size n2 × n2 at the fine level and n2 

> 4

× n2 

> 4

at the coarse level. The computational cost of one cycle of the multilevel scheme is given by 

C(CY CLE (n, m )) = C (SOLV ER ( n 

> 2

, n 

> 2

, m )) + P,

where C (SOLV ER ( n 

> 2

, n 

> 2

, m )) denotes the number of flops for performing m itera-tions of the solver at the coarse level, while P accounts for the cost of transferring information between fine and coarse levels. 

• Transfer operators 

Each row of the restriction operator IHh contains 9 nonzero entries. Therefore, each row requires 8 additions, 5 multiplications, and 1 division. For the prolon-gation operator IhH , one quarter of the fine-grid points coincide with coarse-grid points and are obtained by direct injection, requiring no arithmetic operations. For half of the points in the fine grid, which lie between two coarse grid nodes, we compute the weighted averages of 2 neighbors, requiring 2 operations per point. The remaining points lie in the center of a coarse grid cell, so they are computed as averages of 4 neighbors, requiring 4 operations. Therefore, it holds 

C(IHh x) = 14 n2

4 = 7

2 n2, C(IhH x) = 0 + 4 n2

4 + 2 n2

2 = 2 n2,

obtaining 

C(IhH x) + C(IHh x) = 2 n2 + 7

2 n2 = 11 

2 n2,

and 

P = 2( C(IhH x) + C(IHh x)) = 11 n2,

since both the current iterate and the gradient of the objective function at the current iterate must be transferred. Since the most expensive operations in all the considered methods are FFTs and multiplications by framelet operators, the cost analysis mainly focuses on these two components. 

• Fast Fourier transform 

The cost of a one-dimensional FFT of a vector x ∈ Rn is 

C(F F T (n)) = 5 n log 2 n. 

Therefore, applying the two-dimensional FFT to a matrix X ∈ Rn×n requires 

C(F F T 2( n, n )) = n

(

5n log 2 n

)

+ n

(

5n log 2 n

)

= 10 n2 log 2 n, 

since one-dimensional FFT is applied to each row and column. 37 • Framelet multiplications 

The framelet operator W ∈ R3n×n is a sparse matrix defined in ( 37 ), and the corresponding two-dimensional operator is defined in ( 38 ). By standard properties of the Kronecker product, 

W2Dx = ( W ⊗ W )x = vec( W XW T ), W T 

> 2D

y = ( W T ⊗ W T )y = vec( W T Y W ),

where x = vec( X) and y = vec( Y ). Since the number of operations required to compute W XW T and W T Y W is the same, it is sufficient to estimate the cost of 

W XW T . Exploiting the structure of the filters W0, W1, and W2, a direct count of the arithmetic operations yields 

C(W X, n ) = n(4 n + 3 n + 4 n) = 11 n2, C(Y W T , n ) = 3 n(4 n + 3 n + 4 n) = 33 n2.

In conclusion, we have 

C(W2Dx, n ) = C(W X, n ) + C(Y W T , n ) = 44 n2.

• Solvers 

Under periodic boundary conditions, the matrix A is block circulant with circu-lant blocks (BCCB) and is therefore diagonalized by the two-dimensional Fourier matrix, namely A = F H 

> 2

ΛF2, where F2 = Fn ⊗ Fn and Λ is a diagonal matrix containing the eigenvalues of A. As a consequence, the eigenvalues of A can be obtained by computing the FFT of its first column. Exploiting this structure, the gradient ∇f = AT (Ax − b) can be computed using two two-dimensional FFTs. The same cost applies to the computation of 

P −1AT (Ax − b). Hence, 

C(F IST A (n, n, m )) = m(2 C(F F T 2( n, n )) + kF n2 + 2 C(W2Dx, n )) = m(20 n2 log 2 n + kF n2 + 88 n2)

C(IST A (n, n, m )) = m(2 C(F F T 2( n, n )) + kI n2 + 2 C(W2Dx, n )) = m(20 n2 log 2 n + kI n2 + 88 n2)

C(N P D (n, n, m )) = m(2 C(F F T 2( n, n )) + kN n2 + 3 C(W2Dx, n )) = m(20 n2 log 2 n + kN n2 + 132 n2)

C(P N P D (n, n, m )) = m(2 C(F F T 2( n, n )) + kP n2 + 3 C(W2Dx, n )) = m(20 n2 log 2 n + kP n2 + 132 n2)where ki, for i = F, I, N, P , collects the additional element-wise operations required by each algorithm, and m denotes the number of iterations. For m

coarse-level iterations, it hold 

C (SOLV ER ( n 

> 2

, n 

> 2

, m )) = m

(

20 n2

4 log 2

n

2 + k1

n2

4 + k2

n2

4

)

38 = m

4

(

20 n2 log 2

n

2 + k1n2 + k2n2)

= m

4

(20 n2 log 2 n − 20 n2 + k1n2 + k2n2)

= 1

4 C (SOLV ER (n, n, m )) − 5n2m. 

Combining the previous estimates, the computational cost of one multilevel cycle is 

C(CY CLE (n, m )) = C

(

SOLV ER ( n 

> 2

, n 

> 2

, m )

)

+ P,

= 1

4 C (SOLV ER (n, n, m )) − 5n2m + 9 n2

≈ 1

4 C(SOLV ER (n, n, m )) .

In particular, C(CY CLE (n, m )) < 1 

> 4

C(SOLV ER (n, n, m )) whenever m ≥ 3. 39