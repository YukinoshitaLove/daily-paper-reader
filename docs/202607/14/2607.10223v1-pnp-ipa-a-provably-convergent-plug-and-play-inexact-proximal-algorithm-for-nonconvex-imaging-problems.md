---
title: "PnP-IPA: A Provably Convergent Plug-and-Play Inexact Proximal Algorithm for Nonconvex Imaging Problems"
title_zh: "PnP-IPA: 用于非凸成像问题的可证收敛即插即用非精确近端算法"
authors: "Cristiano Parenti, Silvia Bonettini, Marco Prato"
date: 2026-07-11
pdf: "https://arxiv.org/pdf/2607.10223v1"
tags: ["query:real-ir"]
score: 9.0
evidence: 用于图像恢复逆问题的即插即用方法，使用深度去噪器
tldr: Plug-and-Play方法通过深度降噪器替代近端算子求解成像反问题，虽性能优越，但其在非凸目标下的收敛性证明一直是难题。现有基于梯度步骤的可证明方案存在正则参数严格界、步长固定等限制。本文提出PnP-IPA，一种可证明收敛的即插即用非精确近端算法，引入非精确分裂策略评估隐式正则项近端算子，并设计替代价值函数实现无精确目标评估的自适应回溯线搜索。借助KL性质，无需正则参数假设即证明全局收敛到稳定点。在图像去模糊等任务上，该方法突破理论瓶颈，实现最优参数调节与顶尖去噪效果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10223v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1533, \"height\": 1740, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10223v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1531, \"height\": 1647, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10223v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1526, \"height\": 1958, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10223v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 794, \"height\": 244, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10223v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 903, \"height\": 281, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10223v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 941, \"height\": 209, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10223v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 894, \"height\": 426, \"label\": \"Table\"}]"
motivation: 现有PnP方法在非凸成像问题中缺乏收敛保证，且基于梯度步骤的证明方案受限于参数界和步长规则，无法处理非凸数据项。
method: 提出PnP-IPA算法，采用非精确近端分裂和替代价值函数驱动的自适应线搜索，无需精确目标评估即可调整步长。
result: 借助KL性质证明全局收敛到非凸目标稳定点，并在高斯和柯西噪声去模糊实验中获得先进复原质量。
conclusion: PnP-IPA有效解除了理论约束，实现最优参数调节与鲁棒收敛，为即插即用方法提供了可证明收敛性框架。
---

## 摘要
即插即用（PnP）方法通过用具有高度表达能力的深度去噪器替代传统正则化项的邻近算子，已成为解决成像逆问题的极为有效的范式。尽管在经验上取得了成功，但为PnP算法建立严格的收敛性保证仍然是一个重大挑战。现有的基于梯度步（GS）去噪器的可证明方法存在理论和实践上的局限性，例如对正则化参数的严格限制、僵化的步长规则以及无法处理非凸数据保真项。在本文中，我们引入了PnP-IPA（即插即用非精确近端算法），这是一种克服这些瓶颈的新型优化方案。我们提出了一种新的分裂策略，该策略非精确地评估缩放隐式正则化器的近端算子。为了在不进行精确目标评估的情况下实现自适应步长选择，我们设计了一种新颖的代理价值函数，该函数成功地驱动了类似Armijo的回溯线搜索。借助Kurdyka-Lojasiewicz性质，我们在不对正则化参数施加任何假设的情况下，建立了到非凸目标静止点的全局收敛性。在高斯和柯西噪声下的图像去模糊方面的大量数值实验展示了PnP-IPA的实际优势。通过有效地解除先前的理论约束，我们的方法允许进行最优参数调优，从而产生最先进的复原质量，并且即使在非凸情况下也能实现稳健的收敛。

## Abstract
Plug-and-Play (PnP) methods have emerged as a highly effective paradigm for solving imaging inverse problems by replacing traditional proximity operators of regularization terms with highly expressive deep denoisers. While empirically successful, establishing rigorous convergence guarantees for PnP algorithms remains a major challenge. Existing provable approaches based on the Gradient-Step (GS) denoiser suffer from theoretical and practical limitations, such as restrictive bounds on the regularization parameter, rigid step-size rules, and the inability to handle nonconvex data-fidelity terms. In this paper, we introduce PnP-IPA (Plug-and-Play Inexact Proximal Algorithm), a novel optimization scheme that overcomes these bottlenecks. We propose a new splitting strategy that evaluates the proximal operator of the scaled implicit regularizer inexactly. To enable adaptive step-size selection without exact objective evaluations, we design a novel surrogate merit function that successfully drives an Armijo-like backtracking line-search. Relying on the Kurdyka-Lojasiewicz property, we establish global convergence to a stationary point of the nonconvex objective without imposing any assumption on the regularization parameter. Extensive numerical experiments on image deblurring under both Gaussian and Cauchy noise demonstrate the practical advantages of PnP-IPA. By effectively lifting previous theoretical constraints, our method allows for optimal parameter tuning, yielding state-of-the-art restoration quality and robust convergence even in nonconvex regimes.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：Plug-and-Play（PnP）方法在成像逆问题中性能优异，但其收敛性理论严重滞后。特别是基于梯度步（Gradient-Step, GS）去噪器的可证明 PnP 方法，存在正则化参数有严格上界、步长固定、只能处理凸数据保真项等根本性限制。
- **整体含义**：本文旨在突破上述瓶颈，提出一种可证明收敛的 PnP 算法（PnP-IPA），通过非精确近端分裂和自适应线搜索，在无参数假设的情况下实现全局收敛，并能稳定处理非凸数据保真项，从而使理论保证与最优参数调节不再矛盾。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：利用 GS 去噪器对应隐式正则项 \( \phi_{\sigma} \) 的共轭函数形式，将目标函数重新分裂为平滑项 \( f_0(x) = f_{\text{data}}(x) - \frac{\lambda}{2}\|x\|^2 \) 和凸项 \( f_1(x) = \lambda h_{\sigma}^*(x) \)。基于此分裂，设计非精确的前向-后向算法，将对 \( h_{\sigma}^* \) 的近端计算转移至其原始函数 \( h_{\sigma} \) 上，通过内层梯度下降非精确求解，并用去噪器 \( D_{\sigma} = \nabla h_{\sigma} \) 直接获得近端梯度的近似点 \( \tilde{y}_k \)。
- **关键技术细节**：
  - **非精确近端计算**：通过 Moreau 分解，对近端 \( \mathrm{prox}_{\alpha_k \lambda h_{\sigma}^*} \) 的计算转化为对内层问题 \( \min_p G_k(p) \) 的近似求解，其中 \( \nabla G_k(p) = \frac{p - z_k}{\alpha_k \lambda} + \frac{1}{\alpha_k \lambda} D_{\sigma}(p) \)。内层迭代停止准则基于 \( \|\nabla G_k(\tilde{p}_k)\|^2 \le -\tilde{\Delta}_k/4 \)，确保方向下降且线搜索条件满足。
  - **代理价值函数与线搜索**：由于无法对所有解点精确计算 \( f_1 \)，定义上界序列 \( U^k \) 满足 \( f_1(x_k) \le U^k \)，并构造可计算的代理价值函数 \( \Phi(x_k, U^k) = f_0(x_k) + U^k \)。基于 Armijo 回溯条件与 \( \Phi \) 完成自适应步长选择。
  - **算法流程**（文字描述）：
    1. 梯度步：\( z_k = x_k - \alpha_k \nabla f_0(x_k) \)。
    2. 非精确后向步：通过内层梯度下降得到 \( \tilde{p}_k \) 满足停止准则，计算 \( \tilde{y}_k = D_{\sigma}(\tilde{p}_k) \)。
    3. 线搜索：沿方向 \( d_k = \tilde{y}_k - x_k \) 用回溯法确定步长 \( \eta_k \)，使代理函数满足充分下降。
    4. 更新：\( x_{k+1} = x_k + \eta_k d_k \)，\( U^{k+1} \) 根据凸组合更新。
- **收敛性分析**：将算法映射到抽象的 KL 框架下，证明所有极限点为稳定点，并在 KL 性质下保证整个序列收敛到稳定点。此过程不要求正则参数 \( \lambda \) 满足任何界。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

- **数据集与场景**：
  - 高斯噪声去模糊（高噪 \( \nu = 0.05 \) 和弱噪 \( \nu = 0.01 \)）。
  - 非凸柯西噪声去模糊（尺度参数 \( \gamma = 0.01 \)）。
  - 实验基于 CBSD68 数据集，取前 10 张 256×256 中心裁剪彩色图像（CBSD10），使用 8 种真实相机抖动模糊核。
- **对比方法**：
  - Prox-PnP、Relaxed-Prox-PnP 和 alpha-Prox-PnP（具有收敛保证的 GS 去噪器方案）。
  - DPIR（无收敛保证，但经验性能很强）。
  - 柯西噪声下额外对比了带回溯的梯度下降方法（GS-GD-Backtracking）和 Prox-PnP。

### 4. 资源与算力：用了多少算力（GPU 型号、数量、训练时长等）

- 文中未报告模型训练所使用的算力（GS 去噪器为前人工作训练好的权重，本文仅调用）。
- 推理/实验运行的机器配置：Intel Core i7-12700H CPU + 16 GB RAM + NVIDIA GeForce RTX 4050 (Laptop) GPU with 6 GB VRAM。所有方法使用同一设备以保证公平对比。

### 5. 实验数量与充分性：大概做了多少组实验，是否充分、客观、公平

- **实验组数**：共 3 个主要实验场景（高噪高斯、弱噪高斯、柯西噪声），每个场景都在 10 张图片 × 8 个模糊核上计算平均值，共 80 次独立运行。
- **充分性与公平性**：
  - 使用 BenchOpt 框架统一评估协议，所有算法使用相同的停止准则（相对误差 < 10⁻⁴），并固定随机种子。
  - 超参数通过网格搜索调至各方法在相应场景下的最佳 PSNR，确保公平比较。
  - 消融实验未单独开展，但通过对不同噪声类型和不同约束条件的对比，充分展示了方法在各种场景下的行为。
  - 实验覆盖了凸与非凸数据项、高噪声与低噪声，验证了所提方法的普适性，整体较为充分和客观。

### 6. 论文的主要结论与发现

- PnP-IPA 是首个在不依赖正则参数界的情况下，可证明收敛的 GS 去噪器式 PnP 算法。
- 在高噪高斯去模糊中，PnP-IPA 的 PSNR 与 Prox-PnP 等价，但速度更快；在弱噪高斯场景中，由于不受参数界限制，可选的更优 \( \lambda \) 使得 PnP-IPA 显著优于原方法。
- 在柯西噪声（非凸数据项）下，PnP-IPA 以明显优势战胜 Prox-PnP 和 GS-GD，且 DPIR 因发散完全无法工作，彰显了提出方法在非凸情况下的强鲁棒性。
- 非精确内层求解平均仅需 1–2 次去噪器评估，线搜索的回溯次数也很低，表明算法开销可控。

### 7. 优点：方法或实验设计上有哪些亮点

- **理论上**：首次统一解决了 GS 去噪器 PnP 方法中的三大限制（参数界、步长固定、非凸数据项），借助 KL 框架给出全局收敛证明。
- **算法设计上**：
  - 精巧的分裂策略使得近端计算完全基于原始去噪器，无需反演网络。
  - 创新的代理价值函数使线搜索在不直接评估原目标函数的情况下成为可能，为自适应步长铺平道路。
  - 内层停止准则与代理函数下降条件巧妙联动，保证了线搜索的有限终止。
- **实验上**：实验设计对比全面，包含不同噪声类型和强度，证明了方法在最佳参数设置下的性能提升。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **模型假设的理想化**：GS 去噪器的 Lipschitz 约束（\( L_{g_\sigma} < 1 \)）在实际训练中仅被软约束，严格保证未必成立；本文强调此时极小化的仍是完整定义的目标，但实际网络可能与理论设定不完全吻合。
- **实验覆盖**：
  - 仅在图像去模糊和两种噪声类型上测试，未涉及超分辨、CT 重建等更广泛的逆问题。
  - 使用的数据集规模较小（仅 10 张图），且限于 CBSD68 灰度/彩色自然图像，结论外推至其他成像模态需审慎。
- **未提供训练细节与模型复现**：GS 去噪器本身不是本文贡献，但若从头训练，其对 Lipschitz 常数的控制可能影响最终收敛行为，文中未讨论。
- **效率**：虽然内层步数少，但每次仍需要调用去噪器网络，整体运行时间高于 DPIR 等无收敛保证方法，在实时性要求较高的场景可能不占优。
- **步长策略启发式**：文中对外层步长 \( \alpha_k \) 采用了手动分阶段衰减策略，未探索更自动化的自适应方式。

（完）
