---
title: Reference-Based Face Super-Resolution Using the Spatial Transformer
title_zh: 基于参考的人脸超分辨率：使用空间变换器
authors: "Varun Ramesh Jois, Antonella DiLillo, James Storer"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.11025v1"
tags: ["query:real-ir"]
score: 10.0
evidence: 基于参考的人脸超分辨率
tldr: 人脸超分辨率是提升低分辨率人脸图像细节的重要技术，在消费摄影、视频监控等领域普遍应用，但该不适定问题极易导致重建图像模糊或失真。现有基于参考的方法虽能引入外部先验，却面临对齐不稳定和融合效率低等难题。本文提出基于空间变换器的对齐模块，显著优于可变形卷积的稳定性，并设计自适应聚合函数，根据参考质量选择性融合信息。实验证明，该方法在CelebA、VggFace2等数据集上以较小模型取得领先的PSNR/SSIM指标，实现了高质量、高效率的人脸超分辨率，为实际部署提供了可行方案，并已开源代码。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.11025v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1267, \"height\": 543, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11025v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1261, \"height\": 473, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11025v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1197, \"height\": 1231, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11025v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1221, \"height\": 593, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11025v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1198, \"height\": 595, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.11025v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1115, \"height\": 817, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.11025v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 586, \"height\": 224, \"label\": \"Table\"}]"
motivation: 针对现有参考超分方法中可变形卷积对齐不稳定、参考信息融合低效的问题，本文旨在提升对齐鲁棒性与融合质量。
method: 提出基于空间变换器的稳定对齐模块替换可变形卷积，并设计自适应聚合函数，依据参考图像质量动态融合或抑制信息。
result: 在多个数据集上，该方法以较小模型参数量取得了最先进的人脸超分辨率性能，同时验证了更高的稳定性。
conclusion: 本文的参考人脸超分方法利用空间变换器和自适应聚合有效克服了传统方法缺陷，实现了高效、鲁棒的高保真重建，代码已开源。
---

## 摘要
人脸超分辨率是提升含有人脸的图像分辨率以增加更精细细节的任务。它在许多计算机视觉应用中无处不在，用户甚至常常意识不到它在执行。然而，高保真地执行这项任务具有挑战性，因为它是一个不适定问题。本文提出了一种基于参考的人脸超分辨率解决方案，利用更高分辨率的参考图像来辅助任务。我们展示了一个基于空间变换器的对齐模块，它比流行的可变形卷积要稳定得多。我们还展示了一个聚合函数，当参考图像中有可用的高质量信息时，它可以提取这些信息；而当此类信息不可用时，则抑制该函数。最后，我们展示了相对较小的模型可以在多个数据集上达到最先进的结果。源代码可在https://github.com/varun-jois/FSRST获取。

## Abstract
Face super-resolution is the task of increasing the resolution of an image containing a face thereby adding finer detail. It is a ubiquitous task in many computer vision applications and quite often the user isn't even aware that it is being performed. However, doing it with high fidelity is challenging as it is an ill-posed problem. In this paper we present a reference-based solution for face super-resolution that uses higher resolution reference images to aid in the task. We show an alignment module based on the spatial transformer that is considerably more stable than the popular deformable convolutions. We also show an aggregation function that can take good quality information from the reference images when available or suppress the function when such information is unavailable. Finally, we show that our relatively smaller model can achieve state of the art results on multiple datasets. The source code is available at https://github.com/varun-jois/FSRST.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究背景**：人脸超分辨率（Face Super-Resolution）旨在从低分辨率人脸图像重建出高分辨率细节，广泛应用于视频会议、监控与消费摄影，但其本身是一个不适定（ill-posed）问题。
- **动机与问题**：引入高分辨率**参考图像**（如同一个人的其他图片）能提供额外先验，但现有方法主要面临两大挑战：
  - **对齐不稳定**：流行的可变形卷积（Deformable Convolution）用于图像对齐时训练极不稳定，容易崩溃，常需额外的光流引导。
  - **信息聚合低效**：如何从多个参考图像中自适应地提取有用纹理，同时忽略不匹配或低质量的参考，仍缺少优雅的解决方案。

### 2. 方法论
论文提出 **FSRST（Face Super-Resolution using the Spatial Transformer）** 模型，由四个核心组件构成：
- **特征提取器**：用5个残差块分别提取低分辨率输入特征 \(F_L\) 和参考特征 \(F_{R_i}\)（参考图先转灰度并通过空间到深度操作降至相同分辨率）。
- **空间变换器对齐（STA）**：
  - 将 \(F_L\) 与每张参考特征沿通道拼接，送入定位网络（两层5×5卷积+池化+全连接），输出2×3仿射变换矩阵 \(\theta\)。
  - 通过网格生成器和采样器对参考特征进行仿射变形，得到对齐后的特征 \(F_{R_i A}\)。
  - 该方法**无需外部光流**，训练远比可变形卷积稳定。
- **距离加权聚合（DWA）**：
  - 计算每个对齐参考与 \(F_L\) 的逐像素 L2 距离 \(D_i\)，拼接后对负距离做 softmax，得到各参考的权重图。
  - 将权重与对应参考特征加权求和，生成聚合特征 \(F_{agg}\)。
  - 通过距离截断（clamp）和数值稳定（\(\epsilon\)）技巧，当所有参考距离都很大时权重趋于零，实现**自适应忽略参考**。
- **输出构建器**：聚合特征与 \(F_L\) 相加后，经20个残差块和亚像素卷积上采样，再与输入的双三次插值图相加形成最终超分结果。损失函数仅使用 \(L_1\) 像素损失。

### 3. 实验设计
- **数据集与场景**：
  - **DFD**：DeeepFakeDetection 中22个身份训练、6个测试，模拟视频通话场景，共1300训练/340测试样本。
  - **CelebAMask‑HQ**：过滤身份后2600训练/287测试，代表多人脸图像库。
  - **VoxCeleb2**：118个身份的3617个测试样本，用仅在 CelebA 上训练的模型做跨库评估。
  - 所有任务均为4×超分（32×32 → 128×128），每样本使用3张参考图。
- **对比方法**：TTSR、C2‑Matching（单参考）、MRefSR、HIME（多参考），均仅使用\(L_1\)损失训练。
- **评价指标**：PSNR 与 SSIM（专注保真度而非感知质量）。

### 4. 资源与算力
- 论文**未明确说明**训练所用的GPU型号、数量或训练时长。仅提及各模型参数量（如FSRST 2.55M，小版本0.87M），缺少实际推理速度或FLOPs的测量。

### 5. 实验数量与充分性
- **实验组数**：
  - 3组主超分实验（DFD→DFD，CelebA→CelebA，CelebA→VoxCeleb2），覆盖自测试与跨库泛化。
  - 1组消融实验（0、1、3张参考图像对性能的影响）。
  - 2组对齐验证实验（反转已知仿射变换、对齐同人不同帧）。
- **充分性与公平性**：
  - 对比了4种近年 SOTA 方法，并均采用相同\(L_1\)训练条件和图像预处理，对比较为公平。
  - 消融实验清晰证明了多参考和模块设计的有效性。
  - 不足：缺少对计算量、推理速度的实测；HIME 为自行复现，可能存在实现偏差；未进行感知指标或人工评估。

### 6. 主要结论与发现
- FSRST 在全部三个数据集上均取得**最高 PSNR/SSIM**，且模型参数量较小（2.55M），小版本（0.87M）仍能击败大部分对比模型。
- 空间变压器对齐模块**无需光流即可稳定训练**，解决了可变形卷积的对齐崩溃问题。
- 距离加权聚合能在参考匹配时获取高质量细节，参考不匹配时自动抑制，实现了鲁棒的多参考融合。

### 7. 优点
- **对齐方案创新**：首次将空间变压器用于参考超分对齐，稳定、轻量且无需额外运动估计。
- **聚合机制灵活**：基于 L2 距离的自适应权重设计巧妙，有效处理多参考与无参考情形。
- **轻量高效**：极小的模型即可超越部分大模型，适合移动端部署。
- **实验扎实**：涵盖多个真实场景数据集，并进行了对齐、数量消融等详细分析，代码开源。

### 8. 不足与局限
- **分辨率固定**：空间变压器模块非全卷积，只能处理固定尺寸输入，实际部署需切图或训练多模型。
- **缺乏感知质量评估**：仅优化\(L_1\)损失，未与GAN或感知损失方法对比视觉真实感，可能在人眼观感上劣于感知驱动模型。
- **通用性未知**：实验仅限人脸数据，在其他基准（如通用参考超分CUFED5）上的性能未验证。
- **复现风险**：核心对比方法HIME为自实现，可能未完全达到原文精度。
- **计算效率不透明**：未报告推理时间、GPU内存占用等实际部署指标。

（完）
