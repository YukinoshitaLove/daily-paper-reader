---
title: From Score Approximation to Distribution Approximation in Score-Based Diffusion Models
title_zh: 从分数近似到分布近似：基于分数的扩散模型
authors: Lan V. Truong
date: 2026-07-24
pdf: "https://arxiv.org/pdf/2607.22199v1"
tags: ["query:real-ir"]
score: 8.0
evidence: 建立基于分数的扩散模型中分数逼近与分布逼近的严格量化联系
tldr: 基于得分的扩散模型在实践中表现优异，但其逼近理论尚不完善：神经网络的得分逼近能否保证生成分布逼近数据分布？本文严格建立了两者间的定量关系，证明若得分逼近误差足够小，则反向扩散过程生成的分布在KL散度下接近目标，误差显式依赖于得分误差、噪声策略及先验不匹配。该分析结合了Hornik定理、Girsanov定理和数据处理不等式，填补了从函数逼近到分布逼近的理论缺口。
source: arxiv
selection_source: fresh_fetch
motivation: 虽有经典通用逼近定理保证得分函数可逼近，但该逼近能否转化为生成分布的逼近仍不明确，缺乏严格分析。
method: 结合Hornik通用逼近定理、Girsanov测度变换和相对熵数据处理不等式，建立从得分逼近到KL散度分布误差的定量关系。
result: 证明了若神经网络得分逼近误差足够小，反向扩散生成的分布与真实分布在KL散度下接近，误差上界由得分误差、噪声调度和先验不匹配三种因素决定。
conclusion: 从逼近论角度完善了扩散模型的理论根基，为通过改善得分逼近来提升生成质量提供了严格依据。
---

## 摘要
基于分数的扩散模型在生成建模方面取得了显著的经验成功，但其逼近理论基础仍不完善。特别是，尽管经典的通用逼近定理保证了神经网络可以逼近分数函数，但这样的逼近保证是否能转化为反向扩散过程生成的概率分布的逼近，尚不明确。本文建立了这两个概念之间的严格定量联系。具体来说，我们证明如果神经网络足够准确地逼近真实的分数函数，那么由相应反向扩散模型生成的概率分布在 Kullback-Leibler (KL) 散度意义上接近目标数据分布，除去正向扩散过程的终端分布与初始化反向过程的先验之间不可消除的差异。更精确地，我们根据分数逼近误差、扩散噪声时间表和终端先验差异，推导出分布逼近误差的一个显式上界。我们的分析结合了 Hornik 的通用逼近定理、路径空间上的 Girsanov 定理以及相对熵的数据处理不等式。作为对近期在有限样本统计设置和数据分布结构假设下研究分数逼近的工作的补充，我们的工作发展了一种基于经典神经网络逼近理论的逼近论分析。所得定理提供了一个简单而显式的保证，将神经网络的分数函数逼近与反向扩散模型生成的概率分布的逼近联系起来。

## Abstract
Score-based diffusion models have achieved remarkable empirical success in generative modeling, yet their approximation-theoretic foundations remain incomplete. In particular, although classical universal approximation theorems guarantee that neural networks can approximate score functions, it remains unclear whether such approximation guarantees translate into approximation of the probability distributions generated by reverse diffusion processes. In this paper, we establish a rigorous quantitative connection between these two notions. Specifically, we prove that if a neural network approximates the true score function sufficiently accurately, then the probability distribution generated by the corresponding reverse diffusion model is close to the target data distribution in Kullback-Leibler (KL) divergence, up to an irreducible mismatch between the terminal distribution of the forward diffusion process and the prior used to initialize the reverse process. More precisely, we derive an explicit upper bound on the distribution approximation error in terms of the score approximation error, the diffusion noise schedule, and the terminal prior mismatch. Our analysis combines Hornik's universal approximation theorem, Girsanov's theorem on path space, and the data processing inequality for relative entropy. Complementary to recent work that studies score approximation under finite-sample statistical settings and structural assumptions on the data distribution, our work develops an approximation-theoretic analysis based on classical neural network approximation theory. The resulting theorem provides a simple and explicit guarantee linking neural network approximation of score functions to approximation of the probability distributions generated by reverse diffusion models.

---

## 论文详细总结（自动生成）

## 论文总结：《从分数近似到分布近似：基于分数的扩散模型》

### 1. 核心问题与整体含义
- **研究动机**：基于分数的扩散模型（Score-Based Diffusion Models）在图像生成等任务中表现卓越，但其逼近理论基础尚存在缺口。虽然经典的神经网络通用逼近定理保证分数函数（Score Function）可以被逼近，但**无法直接说明这种函数层面的逼近能否转化为生成分布层面的逼近**。
- **整体含义**：本文旨在严格建立两者之间的**量化桥梁**。论文证明，若神经网络对真实分数函数在 \(L^2\) 意义下逼近得足够好，则反向扩散过程生成的分布在 Kullback-Leibler (KL) 散度下必然接近真实数据分布，误差上界可明确表示为分数逼近误差、噪声时间表和先验不匹配三者的函数。这为扩散模型的表达能力提供了逼近论基础，与近年从统计学习角度出发的工作形成互补。

### 2. 方法论：核心思想与关键技术细节
- **核心思想**：将“分数逼近”到“分布逼近”的转化拆解为两步，并分别用三个经典理论工具进行严格定量。
  1.  **分数逼近 → 路径测度逼近**：应用 **Girsanov 定理** 在扩散路径空间上进行测度变换，将真实反向随机微分方程（SDE）与近似反向 SDE 两条路径的 KL 散度显式表达为：
      \[
      \text{KL}(P \| Q^{\theta}) = \text{KL}(p_T \| p_{\text{prior}}) + \frac{1}{2} \int_0^T g^2(t) \mathbb{E}_{p_t}\big[\|s_{\theta}(X_t, t) - s(X_t, t)\|^2\big] \, dt
      \]
      其中 \(s\) 与 \(s_\theta\) 分别为真实分数和神经网络近似分数，\(g(t)\) 为扩散系数，\(p_T\) 和 \(p_{\text{prior}}\) 分别为正向扩散终端分布与反向初始先验。
  2.  **路径测度逼近 → 分布逼近**：利用**相对熵的数据处理不等式**（Data Processing Inequality），证明在 \(t=0\) 时刻的边际分布 KL 散度不大于整条路径的 KL 散度：\(\text{KL}(\mu \| \mu^{\theta}) \le \text{KL}(P \| Q^{\theta})\)。
  3.  **分数逼近的可行性保障**：借助 **Hornik 通用逼近定理**，保证对任意 \(\varepsilon > 0\)，存在某类 Lipschitz 连续的前馈神经网络（如采用 Softplus 激活），使得 \(\|s_{\theta} - s\|_{L^2(\mu)^d}^2 < \varepsilon\)，且该网络满足后续分析所需的技术假设（线性增长、Novikov 条件等）。
- **最终定理**：对任意 \(\varepsilon>0\)，存在神经网络 \(s_\theta\)，使得生成分布 \(\mu^\theta\) 满足 \(\text{KL}(\mu \| \mu^\theta) < \delta + \frac{1}{2} g_{\max}^2 \varepsilon\)，其中 \(\delta = \text{KL}(p_T \| p_{\text{prior}})\)，\(g_{\max}\) 为噪声系数上界。

### 3. 实验设计
- **实验部分**：本文为**纯理论性论文**，**未包含任何实验**。文中没有使用数据集、benchmark，也未对比任何方法或报告生成样本质量。

### 4. 资源与算力
- **算力说明**：由于不涉及实验，**文中未提及 GPU 型号、数量或训练时长**等任何计算资源信息。

### 5. 实验数量与充分性
- **实验验证**：本文未做实验，因此不存在实验数量或充分性问题。其贡献完全体现在数学证明与理论框架的建立上。

### 6. 主要结论与发现
- **建立了分数逆扩散过程的分布逼近理论**：证明了对分数函数的 \(L^2\) 逼近可以**定量地**转化为生成分布在 KL 散度意义上的逼近上界。
- **误差来源分解**：生成误差明确分解为两项之和——正向终端与反向先验的不匹配（\(\delta\)）以及分数逼近误差的加权项（\(\frac{1}{2}g_{\max}^2\varepsilon\)）。若先验完美匹配（\(\delta=0\)），则分布逼近质量完全由分数逼近决定。
- **理论定位**：从**逼近论**而非统计估计的角度，为扩散模型“学好了分数就能学好分布”提供严格数学保证，与有限样本下的泛化分析形成互补。

### 7. 优点
- **理论简洁且普适**：桥梁仅由三个成熟经典结果搭建（Hornik 定理、Girsanov 定理、数据处理不等式），推导清晰，最终界的形式极为简洁。
- **假设广泛成立**：所要求的全局 Lipschitz、线性增长、Novikov 条件等在常见扩散模型（VP-SDE、VE-SDE）和标准神经网络架构（ReLU、Softplus、U-Net 等）下均可得到满足。
- **填补理论空白**：首次从通用逼近角度建立分数到分布的确定型定量关系，为理解扩散模型的表达能力提供了直接的理论支撑。

### 8. 不足与局限
- **缺乏实验验证**：作为纯理论论文，未通过任何实验证明其边界在实际中的有效性或紧致程度。
- **强假设限制**：假定真实分数全局 Lipschitz、满足 Novikov 条件，对于更复杂、多模态的真实数据分布可能不成立；分析局限于 KL 散度，未推广至 Wasserstein 距离等重要度量。
- **未涉及逼近速率**：仅给出存在性结果（存在网络使误差任意小），没有量化逼近误差与网络规模的收敛速率（如 \(\mathcal{O}(N^{-\alpha})\)）。
- **应用范围较窄**：分析建立在连续时间 SDE 和欧氏空间之上，未包含离散时间 DDPM、概率流 ODE 或非欧氏数据（流形、图）。
- **未结合统计学习**：未讨论有限样本下的估计误差、样本复杂度和泛化界，与近期 Chen et al. 等人的工作恰好形成互补的两极，缺乏统一的桥接。

（完）
