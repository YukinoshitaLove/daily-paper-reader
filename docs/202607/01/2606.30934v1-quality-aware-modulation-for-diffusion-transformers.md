---
title: Quality-Aware Modulation for Diffusion Transformers
title_zh: 面向扩散变换器的质量感知调制
authors: "Luke Budny, Yuhong Guo, Kevin Cheung"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.30934v1"
tags: ["query:real-ir"]
score: 9.0
evidence: 提出质量表示模块学习质量感知调制，增强扩散变压器生成图像的保真度
tldr: 针对扩散变压器等现代文本到图像模型仅依赖时间步或提示嵌入进行调制、缺少质量感知信息的问题，提出质量表示模块(QRM)。QRM是一种轻量级变压器模块，从模型已有输入中学习质量感知表征，并生成一组向量来调节DiT块内的自适应层归一化参数，从而向去噪参数注入质量敏感信号。实验表明，该方法显著提升了生成图像的对齐度、视觉一致性和保真度，为扩散模型增加了有效的质量驱动生成控制。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.30934v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1113, \"height\": 748, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30934v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1524, \"height\": 565, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30934v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1703, \"height\": 669, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30934v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1624, \"height\": 1088, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.30934v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1108, \"height\": 791, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30934v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 974, \"height\": 259, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30934v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 950, \"height\": 256, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30934v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 930, \"height\": 211, \"label\": \"Table\"}]"
motivation: 提出质量表示模块学习质量感知调制，增强扩散变压器生成图像的保真度。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
现代文本到图像扩散模型，如扩散变换器（DiT），依赖时间步或提示嵌入来调节每个时间步的去噪过程强度。虽然这种调制传达了当前的噪声水平，但并未提供任何质量感知信息，这可能导致生成的图像不一致、视觉上不连贯且保真度不足。在本文中，我们提出了质量表示模块（QRM），这是一个轻量级变换器模块，它基于现有模型输入学习质量感知表示，并生成一组向量 $M_{qrm}$。这些向量调整 DiT 变换器块内的自适应 LayerNorm 调制，从而将质量敏感信号注入去噪参数。QRM 对采样调度或扩散主干没有引入显著变化。实验包括对 QRM 训练损失和架构的消融研究，以及实证结果表明，与基线 DiT 模型相比，图像质量得到了一致的提升。

## Abstract
Modern text-to-image diffusion models, such as diffusion transformers (DiT), rely on timestep or prompt embeddings to modulate the strength of the denoising process in each timestep. While this modulation communicates the current noise level, it does not provide any quality-aware information, which can lead to generated images that are unaligned, visually inconsistent, and lacking in fidelity. In this paper, we propose the Quality Representation Module (QRM), a lightweight transformer module that learns a quality-aware representation based on existing model inputs, and produces a set of vectors $M_{qrm}$. These vectors adjust the adaptive LayerNorm modulation within the DiT transformer blocks, thereby injecting a quality-sensitive signal into the denoising parameters. The QRM introduces no significant changes to the sampling schedule or diffusion backbone. Experiments include ablations on QRM training losses and architectures, as well as empirical results demonstrating consistent image quality improvements over baseline DiT-based models.

---

## 论文详细总结（自动生成）

- **论文核心问题与整体含义**  
  - 现代文本到图像扩散变换器（如 Stable Diffusion 3.5）在每一步去噪中仅通过时间步嵌入和提示嵌入来调制自适应层归一化（AdaLN）参数，缺少对当前生成图像质量的直接感知信号。  
  - 这种“质量盲区”容易导致生成图像语义不对齐、视觉不一致或细节失真，因为调制完全由时间和文本决定，无法对中间潜在状态作出反应。  
  - 本文提出，若能在每一步去噪中注入与当前潜在图像质量相关的信息，有望在不改变扩散主干的前提下提升生成质量。

- **提出的方法论**  
  - **核心思想与模块设计**  
    - 设计一个轻量级的**质量表示模块（QRM）**，该模块是一个 Transformer 解码器，以当前潜在图像 \(x_t\)、汇总提示嵌入 \(p\)、时间步嵌入 \(t\) 以及扩散变换器各层原本的 AdaLN 调制参数 \(M_{\text{base}}\) 作为输入，预测一组调制偏移量 \(M_{\text{qrm}}\)。  
    - 最终的调制参数由基线和偏移量相加得到：\(M = M_{\text{base}} + M_{\text{qrm}}\)，从而将质量感知信号直接注入去噪过程。  
  - **关键技术细节**  
    - 对潜在图像 \(x_t\) 通过 \(1\times1\) 卷积和自适应池化转换为一组空间标记；对各层调制参数切片、归一化、投影后作为查询标记，与潜在标记、提示标记、时间步标记共同输入 Transformer 解码器层。  
    - 输出经层归一化和投影后，重新拼接为与各 AdaLN 层宽度匹配的偏移向量 \(M_{\text{qrm}}\)。  
  - **训练与推理策略**  
    - 训练：采用**奖励反馈学习（ReFL）**，冻结扩散主干、文本编码器、VAE 和奖励模型，仅更新 QRM 参数。在选定的时间步子集 \(K_{\text{qrm}}\) 上激活 QRM，并从中随机选择一个步骤 \(\hat{k}\)，使用 QRM 调制后的噪声预测进行一次去噪更新，将解码图像送入冻结的奖励模型获得分数，通过最大化奖励或使用 margin-seeking 损失（要求 QRM 图像比基线好于一定边缘）优化。  
    - 推理：仅为 \(K_{\text{qrm}}\)（通常是早期高噪声步）预测并添加调制偏移，其余步使用原始基线调制，从而在不改变采样器或模型权重的情况下引导去噪轨迹。

- **实验设计**  
  - **数据集与基准**  
    - 训练使用 ImageReward 提示集（10,000 条多样化提示）。  
    - 主要评估使用 Parti-Prompts 基准（1,632 条提示，涵盖构图和属性描述）。  
  - **基线方法**  
    - 直接推理：SD1.5、SD3.5（本工作的基础模型）。  
    - 提示精炼：BeautifulPrompt、Promptist、DNP。  
    - 扩散微调：DDPO‑Aesthetic、DDPO‑Alignment、Diffusion‑DPO、ReFL、TextCraftor。  
    - 负嵌入方法：ReNeg（全局负嵌入、样本级负嵌入）。  
    - 上述方法中部分结果取自 ReNeg 论文的基准报告，仅作参考；本工作的核心对比是 SD3.5 基线 vs. SD3.5+QRM。  
  - **评价指标**：Aesthetic Predictor、HPSv2.1（主要）；CLIPScore、ImageReward（消融分析）。  
  - **实验公平性**：所有 QRM 实验与基线使用相同主干、采样器、引导参数、提示集和随机种子，仅改变调制参数。

- **资源与算力**  
  - 所有实验在 **单个 RTX 5090** 上运行。  
  - 训练 25 个 epoch，每 epoch 399 步，批次大小 1，梯度累积 3，总更新次数约 9975 次。  
  - 推理时，512×512 分辨率、50 步采样下，基线约需 5 秒/图，加入 QRM 后约需 6 秒/图（增加约 20% 时间），显存占用从约 10 GB 增加到约 12.5 GB。

- **实验数量与充分性**  
  - **主要定量比较**：在 Parti-Prompts 上报告了 SD3.5 基线及 QRM 变体（使用 CLIPScore 或 HPSv2.1 奖励训练）相较于多种已有方法的 Aesthetic 和 HPSv2.1 分数，体系较全面。  
  - **消融实验** 共涵盖三个维度：  
    - 架构容量：小（4 层，384 维）、中（6 层，512 维）、大（8 层，640 维）。  
    - 激活时间步范围：早期（{50,…,26}）、中期（{38,…,13}）、晚期（{25,…,1}）。  
    - 训练奖励信号：CLIPScore vs. HPSv2.1。  
  - 每个消融都在相同训练设置下独立完成，并报告 CLIPScore、ImageReward、HPSv2.1 三种指标，对比全面，实验设计客观且公平。  
  - 此外还提供了**定性去噪轨迹对比**（同一噪声种子下，不同时间步区间激活 QRM 的效果），以及部分生成的图像示例。

- **主要结论与发现**  
  - QRM 仅通过调整 AdaLN 调制参数，即可在冻结的 SD3.5 主干上显著提升生成质量。  
  - 最佳 QRM 配置（大容量、早期时间步激活、HPSv2.1 奖励）相比 SD3.5 基线在 Parti-Prompts 上提升 Aesthetic +0.20，HPSv2.1 +0.82。  
  - 早期去噪步注入质量信号效果最显著，因为这决定了图像的全局结构和语义；晚期调制改进有限。  
  - 训练所用的奖励模型直接塑造 QRM 的优化方向：用 HPSv2.1 训练更利于 HPSv2.1 分数，用 CLIPScore 训练则提升 CLIP 相关指标。

- **优点**  
  - **无需修改扩散主干**：仅通过预测调制偏移工作，可在不微调任何原始参数的情况下提升预训练大模型的性能。  
  - **轻量级设计**：QRM 参数相对较小，推理增加的时间与显存开销适中，易于集成到现有扩散管道。  
  - **灵活的引导接口**：可根据不同奖励模型定制生成偏好，为质量感知生成提供了可扩展的控制器。  
  - **实验扎实**：多维度消融和基线对比清晰，消融结果解释了模块容量、激活时机、奖励信号的影响机制。

- **不足与局限**  
  - **奖励偏差继承**：QRM 的性能和偏好完全由冻结的奖励模型决定，若奖励模型存在偏差或忽略某些质量维度，模块也会继承这些缺陷。  
  - **评估局限**：仅在 SD3.5 一个扩散变换器上验证，未在 FLUX 等其他 DiT 架构上测试，泛化性尚待证实。  
  - **指标权衡**：当追求 HPSv2.1 高分数时，CLIPScore 略有下降，表明存在指标间的权衡，可能在部分应用场景中牺牲语义相似性。  
  - **训练数据规模小**：训练仅使用 1 万条提示，可能限制 QRM 对长尾或复杂描述的调制能力。  
  - **计算开销**：虽然相对较小，但仍增加约 20% 的推理时间和约 2.5 GB 显存，对实时或超大模型部署可能存在压力。  

（完）
