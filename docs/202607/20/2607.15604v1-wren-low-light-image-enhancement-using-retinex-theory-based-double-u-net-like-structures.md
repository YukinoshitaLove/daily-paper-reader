---
title: "WREN: Low Light Image Enhancement Using Retinex theory-based Double U-Net-like Structures"
title_zh: WREN：基于Retinex理论的双U型结构低光图像增强
authors: "Reina Kaneko, Junya Hara, Hiroshi Higashi, Yuichi Tanaka"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.15604v1"
tags: ["query:real-ir"]
score: 9.0
evidence: 提出基于Retinex理论和双U-Net的低光图像增强网络，直接解决图像恢复问题。
tldr: 低光图像增强中，基于Retinex理论的方法因反射与光照分解非唯一性，常导致过增强和对动态范围变化敏感。本文提出WREN，一种双U-Net结构网络，首个子网络将输入分解为反射图和光照图，第二个子网络通过定制Transformer模块仅增强光照图，严格遵循Retinex假设。采用尺度不变损失端到端训练，增强过程对光照缩放鲁棒。在多数据集上达到当前最优性能，有效抑制过增强，提升了低光增强的稳定性和泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15604v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 895, \"height\": 509, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15604v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1775, \"height\": 635, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15604v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1844, \"height\": 1961, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.15604v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 453, \"height\": 218, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15604v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1725, \"height\": 261, \"label\": \"Table\"}]"
motivation: 现有Retinex低光增强方法因分解不稳定，易产生过增强且对不同动态范围场景敏感，亟需一种鲁棒的分解‑增强方案。
method: 提出双U‑Net网络WREN，第一个U‑Net分解反射与光照，第二个含Transformer的U‑Net仅增强光照图，最后重组为增强图像。
result: 在多个基准数据集上，WREN均取得最先进的增强性能，有效避免过增强，生成更自然的低光增强结果。
conclusion: WREN通过稳定的分解和专用的光照增强，结合尺度不变损失，实现了对动态范围鲁棒、泛化性强的低光图像增强。
---

## 摘要
本文提出了一种基于Retinex理论的低光图像增强（LLIE）神经网络，使LLIE对各种动态范围场景具有鲁棒性。Retinex理论是一种受人眼色觉假说启发的图像形成模型，其中低光图像被分解为固有颜色上下文（即反射图）和场景相关的照度（即照度图）。由于其分解的非唯一性，现有的基于Retinex的LLIE方法往往无法实现稳定分解，导致过度增强。通常，它们对不同光照条件下变化的动态范围很敏感。为了解决这个问题，我们提出了WREN：一种具有双U型结构的LLIE神经网络。WREN由两个U型子网络组成。第一个网络具有一个编码器和两个解码器，将输入图像分解为反射图和照度图。第二个网络在编码器和解码器之间有一个定制的Transformer模块，仅增强从第一个网络获得的照度图：这完全遵循Retinex理论的假设。最后，增强后的照度图与反射图重新组合。该网络使用尺度不变损失函数进行端到端训练，使其对照度缩放具有鲁棒性。数值结果表明，我们的方法在多个数据集上实现了最先进的性能。我们的代码已在线提供。

## Abstract
This paper proposes a neural network for low light image enhancement (LLIE) based on retinex theory to make LLIE robust for various dynamic range scenes. The retinex theory is an image formulation model inspired by a human color perception hypothesis, where a low light image is decomposed into intrinsic color context (i.e., reflectance map) and scene-dependent illumination (i.e., illumination map). Due to non-uniqueness of its decomposition, existing retinex-based LLIE methods often fail to achieve stable decomposition, which lead to over-enhancement. Typically, they are sensitive to the dynamic ranges that vary in different lighting conditions. To tackle this issue, we propose WREN: An LLIE neural network with double U-Net-like structures. WREN consists of two U-Net-like sub-networks. The first network has one encoder and two decoders that decompose an input image into the reflectance and illumination maps. The second network with a customized Transformer block between an encoder and a decoder only enhances the illumination map obtained from the first network: This completely follows the assumption of the retinex theory. Finally, the enhanced illumination map is recombined with the reflectance map. The network is trained end-to-end with a scale-invariant loss function, which gives robustness against the illumination scaling. Numerical results show that our method achieves the state-of-the-art performance across multiple datasets. Our code is available online.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- 低光图像增强（LLIE）是夜间监控、搜救等任务的关键预处理步骤，但现有方法常面临三大挑战：
  - Retinex 理论下反射图与照度图的分解是非唯一的、病态的，难以稳定分解。
  - 端到端训练中，光照补偿与后处理（如去噪）分阶段设计易导致不稳定的联合训练。
  - 模型对不同光照条件的动态范围变化敏感，常出现过增强、颜色偏移或过平滑。
- 现有基于 Retinex 的深度学习方法常对合成后的正常光图像做整体精炼，隐含地假设反射图也带噪声，这与 Retinex 原理中“反射图应无噪声”相悖，会导致过平滑。
- 本文提出 **WREN**，一种基于 Retinex 理论的双 U 型结构网络，旨在严格遵循 Retinex 假设，实现稳定分解并仅增强照度图，从而提升对不同动态范围场景的鲁棒性。

### 2. 论文提出的方法论
#### 2.1 整体思想
- WREN 由两个 U-Net 类子网络组成：
  - **第一个 U-Net（Retinex 分解网络）**：共享编码器 + 两个独立解码器，将低光图像 \(I_{\text{low}}\) 分解为反射图 \(R\) 与照度图 \(L_{\text{low}}\)，并 **保持反射图不变**。
  - **第二个 U-Net（照度增强网络）**：仅对 \(L_{\text{low}}\) 进行增强，输出正常光照度图 \(L_{\text{normal}}\)，最后通过元素乘得到增强结果 \(I_{\text{recon}} = R \odot L_{\text{normal}}\)。
- 网络结构上图 2 中有图示（编码器‑Transformer‑定制解码器的级联）。

#### 2.2 关键技术细节
- **照度增强网络的定制解码器**：在编码器与解码器之间插入 **Transformer 模块**（多自注意 + MLP + 层归一化），用以捕获特征的非局部平滑性；解码器包含 **注意力模块、可训练导向滤波器** 和残差块，以在增强照度图时保留边缘和纹理。
- **损失函数设计**：
  - **分解网络损失**（\( \mathcal{L}_{\text{decomp}} \)）：重构损失 + **尺度不变损失**（\( \mathcal{L}_{\text{SI}} \)）
    \[
    \mathcal{L}_{\text{SI}} = \text{var}\big(\log(I_{\text{normal}}) - \log(I_{\text{recon}})\big)
    \]
    该损失对全局照度缩放不变，保证对不同动态范围的鲁棒性。
  - **增强网络损失**（\( \mathcal{L}_{\text{enh}} \)）：组合了 MSE、MS‑SSIM、感知损失（VGG‑16）、\(L^*a^*b^*\) 空间的 ℓ1 距离、以及亮度超出 1 的惩罚项。
  - 总损失为上述两项直接相加，实现端到端训练。

#### 2.3 算法流程（文字描述）
1. 输入低光图像 \(I_{\text{low}}\) 通过第一个 U‑Net 分解出 \(R\) 和 \(L_{\text{low}}\)。
2. \(L_{\text{low}}\) 进入第二个 U‑Net，先经编码器提取多尺度特征，再通过 Transformer 块捕获全局依赖，最终由定制解码器输出增强照度 \(L_{\text{normal}}\)。
3. 合成增强图像 \(I_{\text{recon}} = R \odot L_{\text{normal}}\)。
4. 使用尺度不变损失与多分量损失联合优化所有网络参数。

### 3. 实验设计
- **数据集**：
  - 训练集：仅使用 **LOLv1**（485 对低光/正常光图像）。
  - 测试集：跨五个不同数据集评估泛化能力 —— LOLv1 验证集（15 张）、SICE Part1（360 张）、LIEQ（100 张）、NTIRE 2024 验证集（15 张）、MIT‑Adobe FiveK 验证集（500 张）。
- **基准对比方法**（共 6 个）：
  - 深度学习方法：ReDDiT、Retinexformer、RetinexNet、SNR‑Net、KinD++、Zero‑DCE。
  - 传统模型方法：LIME。
  - 所有方法均用原作者代码重新训练并在相同数据划分上评估，保证公平性。
- **评价指标**：PSNR、SSIM、LPIPS、MAE。

### 4. 资源与算力
- 论文提供了训练配置（表 1）：批量大小 4、训练 400 轮、优化器 AdamW、学习率 \(3\times 10^{-4}\)、调度器 OneCycleLR。
- **未明确说明使用的 GPU 型号、数量及训练时长**，因此无法获知具体计算开销。

### 5. 实验数量与充分性
- **主要定量实验**：在 5 个数据集上计算 4 项指标的均值，得到 1 张总结表（表 2）。
- **定性实验**：展示来自各数据集的 5 组增强结果对比图（图 3）。
- **实验充分性评估**：
  - 覆盖了合成/真实、不同动态范围的多个 domain，具有一定的泛化验证。
  - **未进行消融实验**：没有分析各子网络、损失项或模块（如 Transformer、导向滤波器）的单独贡献，缺少超参数敏感性分析。
  - 对比方法涵盖传统模型、CNN 和 Transformer 架构，但对比数量有限（6 个），且未包含所有最新的 SoTA（如 2024‑2025 的某些扩散模型，但 ReDDiT 已为 2025 工作）。
  - 整体实验设计较为直接，对核心设计选择的验证不足，但评估设定（统一训练集、统一测试划分）是客观且相对公平的。

### 6. 论文的主要结论与发现
- WREN 在所有测试集的平均度量上均取得最优性能，PSNR 比次优方法 ReDDiT 高出 >1 dB，LPIPS 低 >0.1。
- 视觉结果表明，WREN 能有效增强暗区，同时保持正常光照区域的自然度，避免了其他方法常见的过度增强、颜色偏移和噪声放大问题。
- 采用尺度不变损失和严格保持反射图不变的架构，显著提升了对不同动态范围场景的鲁棒性。

### 7. 优点
- **严格遵循 Retinex 理论**：不改动反射图，仅增强照度图，减少了过平滑风险。
- **双 U‑Net 结构简明**：分解与补偿解耦，设计直接且逻辑清晰。
- **尺度不变损失**：使模型对训练集和测试集之间的全局亮度差异不敏感，跨数据集泛化能力强。
- **定制照度增强模块**：引入 Transformer 和可训练导向滤波器，兼顾非局部去噪和边缘保持。
- **端到端训练且代码开源**，具有良好的可复现性。

### 8. 不足与局限
- **缺乏消融研究**：未验证各组件（如 Transformer 块、导向滤波器、尺度不变损失）的实际贡献，难以判断每个设计的重要性。
- **算力信息缺失**：未提供 GPU 型号、训练时间等，无法评估计算效率与实际部署成本。
- **训练数据单一**：仅用 LOLv1（485 张图像）训练，该数据集主要为室内简单场景，可能限制对复杂室外或极端噪声情况的建模能力。
- **对比方法数量有限**：虽包含传统和深度方法，但未与更多基于扩散模型或最新 Transformer 变体（除 ReDDiT 外）细致对比。
- **仅考虑照度增强**：未显式建模传感器噪声或颜色通道间的 SNR 失衡，尽管反射图保持不变，但原始低光图中的噪声可能通过照度图传递，网络虽设计了去噪能力，但缺乏噪声建模的理论保证。
- **评估仅用全参考指标**：未探讨下游任务（如目标检测、分割）上的实用性，也未使用无参考感知质量指标，实际应用价值有待进一步验证。

（完）
