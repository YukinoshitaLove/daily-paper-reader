---
title: Domain Adaptation of Mismatched Proximal Denoiser for Plug-and-Play Image Reconstruction
title_zh: 面向插件式图像重建的不匹配近端去噪器的域自适应
authors: "Guixian Xu, Jinglai Li, Junqi Tang"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.14894v1"
tags: ["query:real-ir"]
score: 9.0
evidence: 即插即用图像重建中降噪器的领域失配
tldr: 在即插即用图像重建中，域不匹配导致去噪器偏离目标先验，但现有分析未考虑其对收敛的影响。本文引入proximal mismatch量化该偏差，推导出带误差项的Ο(1/K)收敛界，并提出proximal matching域适应策略。实验表明，该方法在去模糊和超分辨率任务上显著优于传统MSE适应，小样本场景下提升最大。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.14894v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1250, \"height\": 741, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14894v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1613, \"height\": 1200, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14894v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1694, \"height\": 1162, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14894v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1588, \"height\": 1133, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14894v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1685, \"height\": 1043, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14894v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1693, \"height\": 862, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14894v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1603, \"height\": 770, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14894v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1602, \"height\": 777, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14894v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1592, \"height\": 706, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14894v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1589, \"height\": 698, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.14894v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1761, \"height\": 612, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14894v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1318, \"height\": 1287, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.14894v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1318, \"height\": 1287, \"label\": \"Table\"}]"
motivation: 实际部署中，去噪器常面临训练域外的偏移，但缺乏对PnP收敛影响的量化与有效适应方法。
method: 定义proximal mismatch度量去噪器与目标邻近算子的差异，并采用proximal matching使去噪器对齐目标邻近算子。
result: 理论上得到收敛速率Ο(1/K)加平均mismatch误差项，实验证明proximal matching在域偏移下重建质量明显优于MSE适应。
conclusion: proximal matching能有效缓解域不匹配，提高PnP重建的鲁棒性，尤其适用于少样本域适应场景。
---

## 摘要
插件式近端梯度下降（PnP-PGD）通过使用去噪器作为隐式先验，实现了灵活的图像重建。在实际应用中，这些去噪器常常被部署在其训练域之外。现有的分析在部署的去噪器上建立了结构假设下的收敛性，例如要求它是近端映射或收缩映射。然而，这些分析并未衡量域不匹配如何影响PnP-PGD的收敛。我们将这种影响定义为\emph{近端不匹配}：即部署的去噪器$\widehat{\mathsf D}$与目标域参考映射$\mathsf D_\star=\operatorname{prox}_{R_\star}$（与底层正则项$R_\star$相关联）之间的差异。在这种不匹配下，每次去噪更新都变成了目标函数的一个不精确近端步骤。我们进一步推导出一个平稳性界，其衰减速率为$\mathcal{O}(1/K)$，并带有一个与平均平方近端不匹配成比例的附加项。这一结果促使我们采用基于近端匹配的自适应，而非仅基于MSE的自适应。我们研究了两种主流去噪器家族的这一方法：学习得到的近端网络和梯度步去噪器。在存在显著域偏移的高斯去模糊和超分辨率实验表明，近端匹配自适应相比基于MSE的自适应显著改善了重建质量，在少样本情形下获得了最大的数值增益。

## Abstract
Plug-and-play proximal gradient descent (PnP-PGD) enables flexible image reconstruction by using denoisers as implicit priors. In practice, these denoisers are often deployed outside their training domains. Existing analyses establish convergence under structural assumptions on the deployed denoiser, such as requiring it to be a proximal map or a contraction. However, they do not measure how domain mismatch affects convergence of PnP-PGD. We define this effect as \emph{proximal mismatch}: the discrepancy between a deployed denoiser $\widehat{\mathsf D}$ and a target-domain reference map $\mathsf D_\star=\operatorname{prox}_{R_\star}$ associated with the underlying regularizer $R_\star$. Under this mismatch, each denoising update becomes an inexact proximal step for the target objective. We further derive a stationarity bound that decays at a rate of $\mathcal{O}(1/K)$, with an additive term proportional to the average squared proximal mismatch. This result motivates adaptation via proximal matching rather than MSE-based adaptation alone. We study this approach with two established denoiser families: learned proximal networks and gradient-step denoisers. Experiments on Gaussian deblurring and super-resolution under substantial domain shift show that proximal matching adaptation improves reconstruction quality significantly over MSE-based adaptation, yielding the largest numerical gains in the few-shot regime.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **核心问题**：在即插即用图像重建（PnP-PGD）中，预训练的去噪器通常会部署在与训练域分布不同的目标域上，造成“先验不匹配”。传统的收敛分析并未量化这种不匹配对优化目标收敛性的具体影响，也未提供专门针对该不匹配的域自适应策略。
- **整体含义**：该工作将域不匹配形式化为“近端不匹配”（proximal mismatch），即部署去噪器与目标域理想近端算子之间的输出偏差。通过将其建模为不精确近端步骤，理论揭示了不匹配会成为目标函数平稳性的持久误差项。基于此，提出了以近端匹配（Proximal Matching）替代传统MSE的少样本域自适应方法，以直接减小近端误差，从而在小样本场景下显著提升重建质量。

### 2. 论文提出的方法论
- **核心思想**：将域偏移下的PnP-PGD视作对目标目标函数的不精确近端梯度下降，用“近端不匹配”度量每次去噪更新偏离真实近端映射的程度，推导收敛界后，指导设计一种以对齐目标近端映射为目的的自适应损失，而非仅最小化去噪MSE。
- **关键技术细节**：
    - **近端不匹配定义**：设目标域理想近端映射$\mathsf D_\star = \operatorname{prox}_{R_\star}$，部署去噪器$\widehat{\mathsf D}$，在迭代点$z_k$处的近端误差$\xi_k = \widehat{\mathsf D}(z_k) - \mathsf D_\star(z_k)$，不匹配量化为$d_k = \lVert\xi_k\rVert^2$。由此，部署更新$\widehat{\mathsf D}(z_k)$可写成$\mathsf D_\star(z_k) + \xi_k$，成为目标近端子问题的不精确解。
    - **收敛理论**：在数据保真项光滑、目标函数下有界、近端子问题具有一定光滑性等假设下，证明了PnP-PGD的平均平方梯度范数满足：
        $\frac{1}{t}\sum_{k=1}^t \lVert\nabla F_\star(x_k)\rVert^2 \le \frac{C_0}{t} + C_1 \frac{1}{t}\sum_{k=1}^t d_k^2$
        即$\mathcal{O}(1/t)$的优化项加上一个与平均近端不匹配成比例的附加项。若不匹配平方和有限，则梯度范数收敛到0。
    - **近端匹配自适应**：
        - 对学习近端网络（LPN）：维持其$\mathsf D_\theta = \nabla \Psi_\theta$的近端结构，在少量目标域干净图像$\{x_i\}$和加噪版本$\{y_{i,j}\}$上最小化近端匹配损失：$L_{\text{PM}}(\theta;\gamma) = \frac{1}{n_{\text{adapt}} M}\sum_{i,j} \rho_\gamma(\mathsf D_\theta(y_{i,j}), x_i)$，其中$\rho_\gamma(u,v) = 1 - \exp(-\lVert u-v\rVert^2/(2\gamma^2))$。理论表明，当$\gamma\to0$时，该损失的总体最小化解趋近于目标域的MAP/近端去噪器。
        - 对梯度步（GS）去噪器：利用其形式$\mathsf D_\sigma = \mathrm{Id} - \nabla g_\sigma$，将输出匹配转换为梯度场匹配：$L_{\text{GS-PM}}(\theta;\gamma) = \frac{1}{n_{\text{adapt}} M}\sum_{i,j} \rho_\gamma(\nabla g_\theta(y_{i,j}), y_{i,j}-x_i)$，并附加一个惩罚项$R_{\text{con}}$来约束$\nabla g_\theta$的Lipschitz常数小于1，以保持近端解释所需的收缩性。

### 3. 实验设计
- **数据集与域偏移**：源域为BreCaHAD（乳腺组织病理图像），目标域为CelebA（人脸对齐图像），构成语义内容和图像统计量的大幅度偏移。测试集为100张CelebA图像，自适应图像数量$n_{\text{adapt}}\in\{1,5,25,50\}$（与测试集不重合）。
- **任务与比较方法**：
    - 图像复原任务：高斯去模糊（核5×5，噪声$\sigma=0.02$）和×4超分辨率（抗混叠下采样，无噪声）。
    - 对比基线：
        - 源模型（仅在BreCaHAD上训练，不进行自适应）。
        - 目标参考模型（在CelebA上充分训练，视为目标域理想近端参考）。
        - M SE自适应（fine-tune MSE损失）。
        - 近端匹配自适应（PM，GS版本额外加收缩正则）。
    - 去噪器家族：LPN和GS两种结构近端去噪器，在相同自适应图片、加噪过程和训练轮数下比较。
- **评价指标**：重建PSNR、SSIM，以及沿PnP-PGD轨迹的“相对近端失配” gap，即部署去噪器输出与对应家族目标参考去噪器输出的相对偏差。

### 4. 资源与算力
- 论文正文及附录未明确提及使用的GPU型号、数量、训练时长或显存消耗等算力信息。

### 5. 实验数量与充分性
- **实验组数**：两个逆问题 × 两个去噪器家族 × 四个自适应变体（源、目标参考、MSE自适应、PM自适应） × 四种自适应图片数量，至少进行了2×2×4×4=64组主要对比实验，此外还有轨迹层面分析和随自适应数量变化的可视化。
- **充分性与公平性**：对比在同一框架下进行，自适应设置（训练轮数、初始点等）对MSE和PM方法保持一致；评价指标包含重建质量和与理论直接相关的proximal mismatch；分析了小样本到较多样本下的性能变化趋势，展示了PM在极端少样本（1或5张）时的显著优势。实验设计较为充分、客观，能够支撑方法的有效性和理论动机。

### 6. 论文的主要结论与发现
- 理论层面：近端不匹配直接转化为目标函数平稳性的附加误差项，即使优化项$\mathcal{O}(1/K)$衰减，域不匹配仍会留下残余误差。
- 方法层面：近端匹配自适应能更有效地纠正部署去噪器与目标近端映射的偏差，在两种结构去噪器（LPN和GS）上均有效。
- 实验发现：在BreCaHAD到CelebA的大域偏移下，PM自适应相较于MSE自适应大幅提升重建PSNR（例如去模糊任务中，GS从不足3dB提升至25dB左右），尤其当自适应数据极少（1~5张）时增益最大，且失配gap的降低与PSNR提升高度一致。随着数据增多，两种自适应方法差距缩小，但PM仍保持优势。

### 7. 优点
- **理论创新**：将PnP域不匹配严格定义为“近端不匹配”，并首次给出收敛性分析，直接量化其对目标函数平稳性的影响，弥补了现有分析的空白。
- **方法动机明确**：基于理论结论直接导出自适应策略，用近端匹配代替MSE，从原理上更贴合PnP迭代中作为近端算子的角色。
- **通用性验证**：在两个结构不同的去噪器家族（LPN和GS）上应用相同的自适应原理，并都取得显著效果，证明了方法的普适性。
- **突出的少样本性能**：在仅有少量目标域图像时能获得巨大提升，实用价值高。

### 8. 不足与局限
- **参考依赖**：方法需要假设目标域存在一个理想近端参考$\mathsf D_\star$，尽管可用充分训练的参考模型来评估，但在实际中若无目标域大量真值来训练此参考，其实施和评测均受限。
- **实验域偏移单一**：仅测试了一种跨域场景（组织病理→人脸），缺乏对其他类型分布偏移（如不同噪声水平、不同成像模态、合成到真实等）的验证。
- **计算资源未报告**：没有提供算力信息，使得复现成本和实用性难以评估。
- **依赖干净目标图像**：自适应过程需要目标域的干净图像及其加噪对，无法直接应用于只有含噪观测的场景。
- **超参数敏感性**：近端匹配中的带宽$\gamma$和GS的收缩惩罚系数$\lambda_{\text{con}}$可能需要针对不同任务调节。

（完）
