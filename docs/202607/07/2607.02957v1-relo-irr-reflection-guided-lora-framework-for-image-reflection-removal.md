---
title: "ReLo-IRR: Reflection-Guided LoRA Framework for Image Reflection Removal"
title_zh: ReLo-IRR：基于反射引导的LoRA框架用于图像反射去除
authors: "Chaoqun Wang, Yuehuan Wei, Haoxiang Cao, Shaobo Min"
date: 2026-07-03
pdf: "https://arxiv.org/pdf/2607.02957v1"
tags: ["query:real-ir"]
score: 9.0
evidence: 使用扩散模型和LoRA的图像反射去除
tldr: 单图像反射去除（SIRR）旨在从反射污染图像中恢复清晰背景，是复原领域的关键挑战。现有基于扩散模型的方法采用图像不可知的适配策略，对不同反射强度统一抑制，导致强反射残留、弱反射细节丢失。本文提出ReLo-IRR，一种基于rectified flow的反射引导LoRA框架，创新地引入反射强度先验。首先用轻量估计器预测每幅图像的反射主导描述子，再通过时间条件机制将其与去噪时间步融合，动态调制LoRA权重，实现与图像和去噪过程一致的精细控制。在多个SIRR基准上（含真实与合成），ReLo-IRR均取得最佳性能，有效去除强反射且不损害弱反射细节，定性定量结果充分证实其优越性与鲁棒泛化，代码已公开。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02957v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 831, \"height\": 955, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02957v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 860, \"height\": 418, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02957v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1835, \"height\": 512, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02957v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 888, \"height\": 161, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02957v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 885, \"height\": 308, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02957v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 893, \"height\": 166, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.02957v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1836, \"height\": 301, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02957v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 832, \"height\": 201, \"label\": \"Table\"}]"
motivation: 现有方法对不同反射强度统一处理，导致重反射残存、弱反射细节丢失，需自适应调制策略。
method: 基于rectified flow模型，设计轻量估计器预测反射强度描述子，并引入时间条件机制融合描述子与时间步，实现图像依赖的LoRA调制。
result: 在挑战性基准上大量实验表明，ReLo-IRR有效抑制多样反射，去反射性能优越且泛化鲁棒。
conclusion: ReLo-IRR通过反射引导的LoRA调制，实现自适应反射去除，为扩散模型在图像恢复任务中应用提供新思路。
---

## 摘要
单图像反射去除（SIRR）旨在从受反射污染的图像中恢复干净的透射层。尽管近期方法利用大型扩散模型取得了可喜成果，但它们依赖于与图像无关的适应策略（如微调或ControlNet），无论反射严重程度如何都强行施加均匀抑制。结果，强反射往往留下残余，而弱反射则遭受细节损失。为此，我们提出ReLo-IRR，一个建立在矫正流模型上的反射引导LoRA框架。首先，设计一个轻量估计器来预测反射强度描述符，为每张图像提供显式的反射优势先验，并实现图像依赖的LoRA调制。其次，引入时间条件机制，将该反射描述符与时间步嵌入融合，使LoRA调制能够与从粗到精的去噪过程一致地演化。通过联合建模反射强度和去噪动态，我们的ReLo-IRR实现了对多种反射情况的鲁棒抑制。在具有挑战性的基准测试上的大量实验验证了ReLo-IRR的有效性，展示了卓越的去反射性能和鲁棒的泛化能力。代码已发布于https://github.com/KONGBAI-8080/ReLo-IRR。

## Abstract
Single-image reflection removal (SIRR) aims to recover the clean transmission layer from a reflection-contaminated image. Although recent methods achieve promising results with large diffusion models, they rely on image-agnostic adaptation strategies, e.g., fine-tuning or ControlNet, that enforce uniform suppression regardless of reflection severity. As a result, heavy reflections often leave residuals, while weak ones suffer from detail loss. To this end, we propose ReLo-IRR, a reflection-guided LoRA framework built upon the rectified flow model. First, a lightweight estimator is designed to predict the reflection strength descriptor, providing an explicit prior of reflection dominance for each image and enabling image-dependent LoRA modulation. Second, we introduce a time-conditioned mechanism that fuses this reflection descriptor with timestep embeddings, enabling LoRA modulation to evolve consistently with the coarse-to-fine denoising process. By jointly modeling reflection strength and denoising dynamics, our ReLo-IRR achieves robust suppression of diverse reflection conditions. Extensive experiments on challenging benchmarks validate the effectiveness of ReLo-IRR, demonstrating superior dereflection performance and robust generalization. The code is released at https://github.com/KONGBAI-8080/ReLo-IRR.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：单图像反射去除（SIRR）旨在从受反射污染的单张图像中恢复清晰的背景（透射层），这是一个高度不适定问题，因为反射与透射层在空间上交叠且均未知。
- **研究动机**：现有基于大型扩散模型的方法虽然性能优越，但普遍采用“图像不可知”的适应策略（如统一微调或固定ControlNet），对所有输入图像施加相同强度的反射抑制，忽视了反射严重程度的巨大差异。
  - 强反射情况下，统一抑制容易残留反射痕迹；
  - 弱反射情况下，则可能导致过度平滑，丢失透射层细节。
- **整体含义**：本文提出的ReLo-IRR框架通过显式估计每张图像的反射强度，并据此自适应地调节LoRA参数的更新幅度，从而实现对不同反射程度的精细化、差异化处理，同时引入时间步条件机制，使调制强度与矫正流模型的由粗到细去噪过程相协调，大幅提升反射去除的鲁棒性与细节保持能力。

### 2. 论文提出的方法论

- **模型基础**：基于预训练的矫正流（rectified flow）模型Flux.1 Kontext，使用低秩适应（LoRA）进行高效参数调整。
- **核心思想**：将反射先验与去噪时间步信息同时注入LoRA调制过程，生成图像依赖且时间演化的LoRA系数，替代传统统一更新。
- **关键组件与流程**：
  1. **反射引导描述符生成**：
     - 使用预训练的反射检测网络 \(R_\psi\) 预测软反射图 \(M \in [0,1]^{H \times W}\)。
     - 通过轻量卷积投影器 \(g_\varphi\)、全局平均池化和前馈网络，将 \(M\) 压缩为紧凑描述符 \(h \in \mathbb{R}^{1 \times d_h}\)（\(d_h = r^2\)，\(r\) 为LoRA秩）。
     - 经层归一化和残差前馈块后，变形为反射引导系数 \(C \in \mathbb{R}^{r \times r}\)。
  2. **标准LoRA与反射调制注入**：
     - 标准LoRA更新：\(W^\star = W + BA\)。
     - ReLo-IRR扩展：\(W^\star = W + BCA\)，系数 \(C\) 依输入图像动态缩放更新强度。
  3. **时间条件反射引导**：
     - 用正弦位置编码嵌入去噪时间步 \(t\)，生成时间嵌入 \(E_t\)。
     - 通过MLP \(\Phi\) 输出调制参数 \((\beta_t, \gamma_t, \alpha_t)\)。
     - 对隐藏激活 \(h\) 进行自适应层归一化：\(\hat{h}_{norm} = LN(h) \cdot (1+\gamma_t) + \beta_t\)。
     - 时间调制后的反射描述符：\(C^*_t = \text{Reshape}(LN(h + \alpha_t \cdot \text{FFN}(\hat{h}_{norm})), r, r)\)。
     - 最终权重更新：\(W^\star = W + BC^*_t A\)。
  - **整体设计**：反射引导负责输入依赖的强度控制，时间条件负责去噪过程的阶段性调制，两者结合实现了“强反射强抑制、弱反射弱抑制，且随去噪阶段递进”的自适应策略。

### 3. 实验设计

- **数据集与场景**：
  - **Real**[27]：110对自然场景图像，90对训练，20对测试。
  - **Nature**[28]：220对真实拍摄图像对，200对训练，20对测试。
  - **RR4K**[29]：1326对高分辨率4K图像，1230对训练，96对测试。
  - 评估指标：PSNR、SSIM、LPIPS、DISTS，覆盖像素精度、结构相似性、感知距离和纹理相似度。
- **对比方法**：ERRNet[6]、DSRNet[5]、RAGNet[24]、RDNetRRNet[16]、RDNet[25]等近年来的SOTA反射去除方法。
- **实验类型**：
  - 定量对比实验（表1）：在三个数据集上与多个方法比较四项指标。
  - 定性视觉对比（图2）：展示各方法的去反射效果。
  - 用户主观偏好研究（图3）：39名参与者对结果进行盲选，比较感知质量。
  - 消融实验（表2、图4、5、6）：
    - LoRA基准、加反射引导、再加时间引导的逐步叠加效果（表2）。
    - 反射估计器设计的变体对比（图4）：Sobel边缘+CNN/MLP、直接CNN+MLP vs 本文反射检测网络。
    - 反射引导调制的注意力图可视化（图5）：验证不同反射强度下的注意力分配。
    - 时间条件机制的变体对比（图6）：手工调度、MLP直接融合 vs AdaNorm。

### 4. 资源与算力

- **硬件配置**：8块NVIDIA A100 GPU。
- **训练设置**：总训练500 epochs，每GPU批大小为7，初始学习率 \(1 \times 10^{-4}\)，采用Adam优化器（\(\beta_1=0.9, \beta_2=0.999\)）。
- **训练时长**：论文未明确给出具体总训练时长或单次迭代时间，仅提供epoch数与硬件数量。据此可估算总计算量较大，但缺乏小时级或GPU小时的精确测量。

### 5. 实验数量与充分性

- **实验组数**：
  - 主实验：3个数据集 × 约5个对比方法 × 4项指标，形成完整对比。
  - 用户研究：39名参与者评估约20组图像，覆盖多种反射场景。
  - 消融研究：至少3组独立的消融实验（组件堆叠、反射估计器设计、时间调制方式），每组内又含多种变体。
- **充分性与公平性**：
  - 数据集涵盖真实场景、自然拍摄和高分辨率条件，覆盖面较广。
  - 对比方法包含经典CNN方法和基于扩散模型的最新工作，比较公平。
  - 消融实验清晰剥离各模块贡献，变体设计具有针对性。
  - 主观用户研究弥补了客观指标的感知盲区。
  - 所有实验均在同一训练配置和代码框架下复现或使用官方发布结果，保障客观性。
  - 总体而言，实验设计较为系统，论证充分，结论可靠。

### 6. 论文的主要结论与发现

- ReLo-IRR通过联合建模反射强度先验和去噪动态过程，实现了图像依赖且时间演化的LoRA调制，显著优于统一抑制策略。
- 在Real、Nature、RR4K三个数据集上，ReLo-IRR在PSNR、SSIM、LPIPS、DISTS等多项指标上达到或接近最优，去反射效果和细节保真度兼备。
- 消融实验证实：反射引导和时间条件模块各自且组合带来性能增益；基于反射检测网络的估计器优于简单边缘或直接CNN方案；AdaNorm时间融合比手工调度或MLP直接融合更有效。
- 用户研究显示ReLo-IRR的视觉质量最受认可，证明其自适应机制不仅提升数值指标，也改善人眼感知。

### 7. 优点

- **轻量高效的反射先验提取**：利用现成的反射检测网络生成软反射图，并通过紧凑的卷积投影器得到低维描述符，既保持语义信息又计算代价小。
- **双重自适应机制**：图像依赖+时间依赖的LoRA调制，使模型能针对不同反射强度、不同去噪阶段无缝调整去反射力度，避免了过平滑和残留问题。
- **与矫正流模型自然集成**：在预训练Flux.1 Kontext上叠加LoRA适配器，训练高效且不破坏基座模型的生成能力。
- **实验全面且证据充分**：多数据集、多指标、多类型消融，尤其包含主观用户研究，验证方法的鲁棒性和感知质量。
- **代码开源**，可复现性高。

### 8. 不足与局限

- **依赖外部反射检测器**：反射描述符的质量取决于预训练反射检测网络 \(R_\psi\) 的准确性，若该网络在特定域上失效，可能引入错误的调制信号。
- **LoRA秩固定**：实验中LoRA秩设为16，没有探索不同秩对性能的影响，可能忽略了效率与效果的进一步权衡。
- **训练成本未完全透明**：虽然给出了GPU数量和epoch数，但未报告总训练时间（如GPU小时），难以精确评估计算开销。
- **复杂反射场景的鲁棒性未专设实验**：如多重叠反射、非均匀反射、或有色反射等极端情形未被单独评测，可能在更复杂的真实退化中性能下降。
- **时间调制设计较简单**：仅依靠MLP生成缩放和偏置，可能不足以完美匹配矫正流模型复杂的时间动态。
- **对比方法可能未涵盖最新极大型模型**：虽然对比了RDNet等2025年工作，但扩散模型领域发展迅速，可能存在同期更强劲的基线未纳入。

（完）
