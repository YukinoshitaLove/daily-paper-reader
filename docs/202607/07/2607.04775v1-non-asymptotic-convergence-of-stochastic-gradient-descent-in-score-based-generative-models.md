---
title: Non-asymptotic Convergence of Stochastic Gradient Descent in Score-based Generative Models
title_zh: 基于评分的生成模型中随机梯度下降的非渐近收敛性
authors: "Stanislas Strasman, Sobihan Surendran, Sylvain Le Corff"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.04775v1"
tags: ["query:real-ir"]
score: 9.0
evidence: 基于分数的生成模型属于扩散模型，本文研究了SGD训练的策略收敛性
tldr: 基于分数的生成模型在数据生成中表现优异，但其训练阶段的随机梯度下降优化动态缺乏理论保证。本文研究SGD在加权去噪分数匹配目标上的非凸收敛性质，显式刻画了调度相关权重因子的影响，并针对两层ReLU网络建立神经正切核分析框架，给出SGD轨迹上的分数近似误差界。该理论揭示了重加权因子在得分近似中的关键作用，为实际权重选择提供指导。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04775v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 798, \"height\": 589, \"label\": \"Figure\"}]"
motivation: 基于分数的生成模型训练中，随机梯度下降优化动态的收敛性理论仍不完善。
method: 针对一般参数化分析SGD在加权去噪分数匹配目标上的非凸收敛，并对两层ReLU网络建立神经正切核框架，推导分数近似误差界。
result: 证明了SGD的收敛速率显式依赖调度权重，并得到两层网络SGD轨迹上的分数近似误差界，量化了重加权因子的影响。
conclusion: 重加权因子对分数近似误差有重要影响，理论结果为实际训练中的权重选择提供了指导。
---

## 摘要
基于评分的生成模型（SGMs）在众多应用的数据生成中展现了令人瞩目的性能。尽管其采样过程的统计特性日益得到深入理解，但支撑其训练的优化动力学却仍未得到充分探索。SGMs通常通过最小化加权去噪评分匹配目标进行训练，然而，使用随机梯度的优化保证仍然有限。在本工作中，我们研究了SGMs中的随机梯度下降（SGD），并在两个互补的设置中取得了成果。首先，对于一般的评分参数化，我们建立了SGD在加权去噪评分匹配目标上的非凸收敛率，明确刻画了对依赖于调度方案的加权因子的依赖性。其次，对于过参数化的两层ReLU网络，我们开发了一种针对扩散训练和随机梯度的神经正切核分析，从而得到了沿SGD轨迹的评分近似误差界。最后，我们的分析量化了重新加权因子在评分近似误差中的作用，为实践中使用的加权选择提供了理论指导。

## Abstract
Score-based Generative Models (SGMs) have achieved impressive performance in data generation across a wide range of applications. While the statistical properties of their sampling procedures are increasingly well understood, the optimization dynamics underlying their training remain less explored. SGMs are typically trained by minimizing a weighted denoising scorematching objective, yet optimization guarantees with stochastic gradients remain limited. In this work, we study Stochastic Gradient Descent (SGD) for SGMs, contributing results in two complementary regimes. First, for general score parameterizations, we establish a non-convex convergence rate for SGD on the weighted denoising score-matching objective, with explicit dependence on the schedule-dependent weighting factors. Second, for overparameterized two-layer ReLU networks, we develop a Neural Tangent Kernel analysis tailored to diffusion training with stochastic gradients, yielding score-approximation error bounds along the SGD trajectory. Finally, our analysis quantifies the role of the reweighting factor in the score approximation error, providing theoretical guidance for weighting choices used in practice.

---

## 论文详细总结（自动生成）

好的，以下是基于提供的论文内容生成的结构化深度总结。

### 论文核心问题与整体含义

本文的核心问题在于填补**基于评分的生成模型（Score-based Generative Models, SGMs）**在**优化动力学**方面的理论空白。虽然 SGMs 在数据生成（如图像、视频等）上取得了巨大成功，且其采样过程的统计性质已得到广泛研究，但其**训练阶段**的理论保证，特别是使用**随机梯度下降（SGD）**进行优化时的收敛性，却鲜有探讨。

-   **研究动机**：SGMs 的训练（通过最小化加权去噪评分匹配目标）通常依赖 SGD，但缺乏非渐近收敛保证。理解 SGD 的优化动态对于控制最终生成误差中的“近似误差（approximation error）”部分至关重要。
-   **整体含义**：本文从优化角度为 SGMs 提供了理论根基，量化了 SGD 的收敛速度，并解释了实践中广泛采用的重加权策略（如信噪比加权）为何有效，为模型设计和训练提供了理论指导。

### 论文提出的方法论

本文在两种互补的理论设置下展开分析：

1.  **通用评分参数化下的非凸收敛分析**
    -   **核心思想**：将 SGD 应用于一般的评分网络，假设网络满足数据依赖的李普希茨和平滑性条件，利用经典的非凸随机优化理论进行分析。
    -   **关键技术细节**：
        -   证明了加权去噪评分匹配目标 \( L_{DSM}(\theta) \) 是 \( L \)-平滑的。
        -   推导出 SGD 迭代在期望平方梯度范数下的收敛速率，为 \(\mathcal{O}(\log n / \sqrt{n})\)。
        -   该界**显式地**刻画了小批量大小（\(B\)）、随机梯度方差（\(\tau^2\)）以及时间依赖的损失权重 \(\lambda(t)\) 和时间采样密度 \(q(t)\) 的影响。
    -   **结论**：揭示了权重选择 \(\lambda(t) = \sigma_t^2\) 能够在高噪声和低噪声区域之间达到平衡，从优化角度为实践中常用的信噪比加权提供了理论支撑。

2.  **过参数化两层 ReLU 网络的 NTK 分析**
    -   **核心思想**：针对常用的 ReLU 激活函数，采用神经正切核理论，在过参数化条件下，将复杂的网络训练动态线性化为一个更易于分析的线性模型动态。
    -   **关键技术细节**：
        -   **模型设置**：使用一个输出层固定、隐藏层权重可训练的两层 ReLU 网络来参数化预测函数 \( f_{\theta} \)，该函数与评分函数 \( s_{\theta} \) 直接相关。
        -   **训练目标**：将去噪评分匹配目标重写为对“噪声目标（\( X_0 \)）”的回归问题。
        -   **分析框架**：推导了**预测误差 \(\Delta_k\)** 沿 SGD 轨迹的递归关系：\( \Delta_{k+1} = (I - \gamma_k K_k) \Delta_k - v_k + \epsilon_k \)，其中 \(K_k\) 是经验 NTK，\(v_k\) 是 SGD 噪声，\(\epsilon_k\) 是非线性残差。
        -   **关键证明步骤**：
            1.  **局部化**：将无界噪声输入限制在一个半径为 \(R\) 的截断区域内，控制塔尾概率。
            2.  **误差分解**：将投影后的 DSM 损失分解为四项：NTK 极限的谱收缩项（主导项）、核漂移项、随机波动项和非线性余项。
            3.  **各项控制**：用谱方法控制收缩项；用VC维和反集中不等式控制由于权重更新导致的核漂移（\(D_k\)）和非线性余项（\(\epsilon_k\)）；用鞅差序列控制随机波动项（\(v_k\)）。
    -   **结论**：最终得到了一个关于**投影去噪评分匹配损失 \( \mathcal{L}_{\Pi}^{DSM}(W^{(n)}) \)** 的非渐近上界，该界明确依赖于网络宽度 \(m\)、输入维度 \(d\)、截断半径 \(R\) 和步长等参数，并同样支持了 \(\lambda(t) = \sigma_t^2\) 的最优性。

### 实验设计

实验旨在**定性地验证**理论分析中关于损失重加权因子作用的结论。

-   **数据集**：使用了 **Leeds Butterfly** 数据集。
-   **基准与对比方法**：没有对比其他算法，而是进行了一项**单变量消融实验**。固定所有其他训练和采样设置（如 1000 步 Euler-Maruyama 采样器），仅改变损失加权函数 \(\lambda(t)\)。
-   **对比的加权方案**：共测试了五种权重，\( \lambda(t) \in \{1, \sigma_t, \sigma_t^2, \sigma_t^3, \sigma_t^4\} \)，并采用均匀时间采样（\(q(t)=1\)）。
-   **评价指标**：使用 **Fréchet Inception Distance (FID)** 来评估生成质量，FID 越低表示质量越好。

### 资源与算力

-   **硬件配置**：实验使用了 **NVIDIA RTX 6000 GPU**，显存为 **48 GB**。
-   **训练时长**：所有模型均训练了 **200,000 次迭代**。

### 实验数量与充分性

-   **实验数量**：论文仅报告了在 **1 个数据集**上针对 **1 种网络架构**进行的 **5 组对照实验**（1 次实验 × 5 种加权 × 5 次独立运行取平均）。
-   **充分性与客观性**：该实验被作者明确描述为“示意性的（illustrative）”。其规模较小，是为了佐证理论分析中关于 \(\lambda(t) = \sigma_t^2\) 为“自然选择”的定性结论，而非进行全面的基准测试。从验证特定理论观点的角度看，设计是客观、公平的，但其结论的泛化能力（例如在其他数据集或更大模型上的表现）尚未被充分验证。

### 论文的主要结论与发现

1.  **通用优化保证**：对于平滑的评分网络，SGD 能以 \(\mathcal{O}(\log n / \sqrt{n})\) 的速率收敛到加权去噪评分匹配目标的一个平稳点。
2.  **两层 ReLU 网络的近似保证**：对于过参数化的两层 ReLU 网络，通过 NTK 分析，本文首次给出了在 SGD 训练下的**非渐近分数近似误差界**。该误差界由谱收缩、有限宽度和随机梯度波动共同决定。
3.  **权重因子的作用**：理论分析一致表明，加权函数选择 \(\lambda(t) = \sigma_t^2\) 能平衡高低噪声区域的优化难度。实验在 Leeds Butterfly 数据集上验证了这一点，该加权方案取得了最低的 FID，优于 \(\lambda(t) = 1, \sigma_t^3, \sigma_t^4\) 等其他选择。

### 优点

-   **理论深度**：首次为 SGMs 的 SGD 训练提供了非渐近收敛保证，并刻画了 NTK 动态，填补了关键理论空白。
-   **分析严谨性**：成功地将 NTK 分析框架从有界输入和梯度下降扩展到**无界噪声输入**、**向量值输出**和**随机梯度下降**的设置，技术贡献显著。
-   **实践指导性**：从优化理论的角度，为 SGMs 中广泛使用的“信噪比加权”策略（\(\lambda(t) = \sigma_t^2\)）提供了合理的解释和支持。
-   **模块化**：其得出的分数近似误差界可作为插件，与现有的采样误差分析（基于 Girsanov 定理）相结合，形成一个完整的误差分解。

### 不足与局限

-   **模型局限性**：
    -   **通用分析部分**：依赖的平滑性假设（Assumption 1）排除了像 ReLU 这样的非平滑激活函数。
    -   **NTK 分析部分**：分析仅限于**两层、过参数化的 ReLU 网络**，而实际 SOTA 方法（如基于扩散模型的图像/视频生成）通常使用深层的 U-Net 架构。
-   **实验充分性不足**：
    -   实验仅在 **1 个规模较小的数据集**上进行，且网络结构是相对简单的 NCSN++，缺乏在大规模数据集（如 CIFAR-10, ImageNet）上的验证。
    -   未进行更广泛的消融实验，例如研究不同网络宽度 \(m\)、不同截断半径 \(R\) 或不同扩散时间 \(T\) 的影响。
-   **理论假设限制**：
    -   NTK 分析要求数据分布为**亚高斯**，虽然比有界假设更宽泛，但仍不能覆盖所有真实数据。
    -   分析依赖于**早停（early stopping）**来保证 \(c(t)\) 有界，这在实际训练中可能并非总是最优策略。

（完）
