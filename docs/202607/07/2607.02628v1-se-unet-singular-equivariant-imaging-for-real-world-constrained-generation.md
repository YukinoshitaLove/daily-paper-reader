---
title: "SE-UNet: Singular Equivariant Imaging for Real-World Constrained Generation"
title_zh: SE-UNet：面向现实约束生成的奇异等变成像
authors: Kanishk Awadhiya
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02628v1"
tags: ["query:real-ir"]
score: 9.0
evidence: 利用几何等变性和奇异值门控的扩散模型零样本图像修复
tldr: "扩散模型在真实逆问题中面临数据需求大、物理约束难施加的挑战。SE-UNet通过引入D4群几何等变性与奇异值门控作为优化约束，将生成转化为约束优化问题，实现无预训练的零样本图像修复。在CIFAR-10上80%缺失像素的修复任务中，PSNR超越Deep Image Prior基线超4dB，并展现独特的“奇异快照”收敛现象。该框架为约束生成提供了数据高效路径，推动理论先验与实用部署的结合。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02628v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1423, \"height\": 505, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02628v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1134, \"height\": 702, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02628v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1127, \"height\": 700, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.02628v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 844, \"height\": 265, \"label\": \"Table\"}]"
motivation: 扩散模型用于真实逆问题时常需海量数据和难以施加严格物理约束，限制了其实际应用。
method: SE-UNet将生成视为优化问题，引入D4群几何等变性和奇异值门控作为约束，无需预训练即可规范解空间。
result: "在CIFAR-10数据集80%像素缺失的修复中，PSNR超越Deep Image Prior基线4dB以上，并出现快速锁定信号流形的“奇异快照”收敛。"
conclusion: SE-UNet展示了强归纳偏置可实现高效零样本约束生成，为理论先验的实际部署提供了新路径。
---

## 摘要
尽管扩散模型彻底改变了图像合成，但它们在应用于现实世界逆问题时，常常受到海量数据集需求以及难以施加严格物理约束的限制。本文提出SE-UNet（奇异等变UNet），这是一种无需大量预训练即可解决病态成像任务的框架。通过将生成视为受几何等变性（$D_4$群）和奇异值门控约束的优化问题，SE-UNet有效地标准化了解空间。我们证明，这些强归纳偏置能够在CIFAR-10上实现最先进的零样本修复结果（80%缺失像素）。我们的方法在PSNR上比深度图像先验（DIP）基线高出4 dB以上，并表现出特有的“奇异快照”收敛——迅速锁定信号流形。因此，SE-UNet为约束生成提供了一条数据高效的途径，与ReALM-GEN连接理论先验与实际部署的目标相一致。

## Abstract
While diffusion models have revolutionized image synthesis, their application to real-world inverse problems is often hampered by the need for massive datasets and the difficulty of imposing strict physical constraints. In this work, we introduce \textbf{SE-UNet} (Singular Equivariant UNet), a framework designed to solve ill-posed imaging tasks without extensive pre-training. By treating generation as an optimization problem constrained by geometric equivariance ($D_4$ group) and singular value gating, SE-UNet effectively standardizes the solution space. We demonstrate that these strong inductive biases allow for state-of-the-art zero-shot inpainting results (80\% missing pixels) on CIFAR-10. Our method surpasses Deep Image Prior (DIP) baselines by over 4 dB in PSNR and exhibits a characteristic "singular snap" convergence -- rapidly locking into the signal manifold. SE-UNet thus offers a data-efficient pathway for constrained generation, aligning with the ReALM-GEN goal of bridging theoretical priors with practical deployment.

---

## 论文详细总结（自动生成）

### 1. 论文核心问题与整体含义

- **研究背景**：扩散模型在图像生成上虽有突破，但面对真实世界的逆问题（如图像修复、去噪）时，通常需要海量训练数据，且难以嵌入严格的物理或几何约束。
- **核心问题**：如何在**不依赖大规模预训练**的前提下，高质量地解决高度病态的成像逆问题（如缺失 80% 像素的修复），并保证生成结果满足可解释的约束。
- **整体含义**：将生成任务重新定义为**受强归纳偏置约束的优化问题**，展示了一条数据高效、物理一致、适用于零样本约束生成的可行路径，契合 ReALM-GEN 研讨会“用现实约束调制生成”的目标。

### 2. 方法论

#### 2.1 整体框架
恢复图像 $x^* = f_\theta(z)$，其中 $f_\theta$ 为 U‑Net 生成器，$z$ 为固定噪声。优化目标为最小化 $\|A f_\theta(z) – y\|^2$ 并附加正则化 $R(f_\theta(z))$。

#### 2.2 关键技术细节
- **奇异值门控（Singular Gating）**
  - 对每层特征图进行 SVD，观察到信号集中于前导奇异向量，而噪声占满尾部。
  - 通过一个可学习的门控函数，抑制低于阈值 $\tau$ 的奇异值，使网络在每次前向传播时都将内部表示投影到“干净”的信号流形上，从而防止后期噪声过拟合。

- **几何等变性（$D_4$ 群对称性）**
  - 利用自然图像固有的旋转与反射对称性（$D_4$ 群，共 8 个元素），在损失函数中对所有群变换 $g$ 求和：
    \[
    L_{eq} = \sum_{g \in D_4} \| A f_\theta(g \cdot z) - g \cdot y \|^2
    \]
  - 输入噪声 $z$ 和目标 $y$ 同时经过群变换，相当于将监督信号乘以 8 倍，迫使网络学习旋转不变的特征，实现几何自监督。

- **“奇异快照”收敛现象**
  - 由于同时施加谱约束与几何约束，优化动态在早期（约第 50 次迭代）出现**急剧相位转变**，模型快速锁定高 PSNR 区域，而传统 DIP 收敛缓慢渐进。

### 3. 实验设计

- **数据集**：CIFAR‑10 测试集。
- **任务**：零样本图像修复，随机掩码移除 **80% 像素**，仅依靠给定观测和网络自身的归纳偏置，不使用外部训练数据。
- **对比方法**：
  - 标准 Deep Image Prior (DIP)
  - DIP + 全变分 (TV) 正则化
  - SG‑UNet（仅奇异门控，用作消融）
  - SE‑UNet（完整方法）
  - 还给出了全监督扩散模型的 PSNR 参考线（绿色虚线）。
- **评估指标**：PSNR (dB) 和 SSIM。

### 4. 资源与算力

- 论文**未明确说明**所使用的 GPU 型号、数量、训练时长或 FLOPs 等算力信息。
- 仅提及优化器为 Adam，学习率 0.01。

### 5. 实验数量与充分性

- **主要实验组**：
  - 定量对比表格（1 张表）：4 种方法的 PSNR/SSIM 对比。
  - 收敛动态图（1 张）：对比 SE‑UNet 与 DIP 的优化曲线，展示“奇异快照”。
  - 可视化示例（1 张）：原图、掩码输入、SE‑UNet 修复结果。
- **消融实验**：对比完整 SE‑UNet 与仅带奇异门控的 SG‑UNet，证明几何等变性的额外增益。
- **公平性与充分性**：
  - 对比基线合理（DIP 及其变体），指标通用，但实验局限于 **单一数据集（CIFAR‑10）**、**单一掩码比例（80%）**、**单一任务（修复）**。缺少对噪声类型、不同稀疏度、不同数据集的泛化性验证，也未研究超参数敏感性或不同网络深度的表现。因此，实验覆盖较窄，结论的普适性有待进一步检验。

### 6. 主要结论与发现

- SE‑UNet 通过奇异值门控与几何等变性双重约束，在 **80% 缺失像素** 的零样本修复任务上取得 **25.34 dB PSNR**，超越标准 DIP **4 dB 以上**。
- 奇异门控单独可带来 **+3 dB** 增益；加入 $D_4$ 群约束后进一步提升性能。
- 优化过程呈现 **“奇异快照”** 现象，约第 50 次迭代迅速收敛至理想解，表明约束有效重塑了损失景观。
- 框架为数据稀缺场景下的约束生成提供了一种轻量级、无需外部数据的解决方案，并可与扩散模型等预训练系统结合作为几何引导。

### 7. 优点

- **强物理/几何可解释性**：将 $D_4$ 对称性和谱稀疏性直接纳入损失与架构，生成结果更可控。
- **卓越的零样本能力**：完全无需外部训练数据，仅凭单张退化图像恢复高保真结果，适合数据匮乏的应用。
- **创新的“奇异快照”收敛**：约束优化带来果断的相位转变，收敛速度快，避免渐近式漂移。
- **模块化设计**：奇异门控与几何约束可独立工作，也可扩展至其他生成框架。

### 8. 不足与局限

- **实验验证范围有限**：
  - 仅测试了 CIFAR‑10 数据集，未在更高分辨率或更复杂的真实图像上验证。
  - 仅针对单一修复任务（随机掩码 80%），缺乏对其他逆问题（去噪、超分辨率、压缩感知等）的评测。
  - 未提供不同缺失比例下的性能趋势或对不同噪声水平的鲁棒性分析。
- **算力与效率未报告**：无 GPU 型号、推理/优化时间等，难以评估实际部署成本。
- **理论分析深度不足**：对“奇异快照”的机制和为何 D4 群约束能精确重塑损失景观缺乏严格数学论证。
- **尺度泛化风险**：对称性假设在自然图像中可能不普遍成立（如文本、人脸不对称等），可能限制其适用范围。
- **对比基线少**：未与近期其他零样本方法（如 Score‑based DDNM、DDRM 等）比较，难以确认相对前沿的水平。

（完）
