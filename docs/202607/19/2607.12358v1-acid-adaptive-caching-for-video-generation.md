---
title: "ACID: Adaptive Caching for vIDeo generation"
title_zh: ACID：面向视频生成的自适应缓存
authors: "Om Agrawal, Saurabh Agarwal, Aditya Akella"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12358v1"
tags: ["query:real-ir"]
score: 10.0
evidence: 自适应缓存加速视频扩散模型推理，直接提升扩散模型效率
tldr: "视频扩散模型生成速度慢，现有动态缓存方法用固定阈值跳过计算，导致速度与质量的人为折衷。作者发现存在漂移快速变化的关键步骤，提出ACID自适应调整阈值：在关键步骤保守缓存，其他步骤激进缓存。ACID在多个缓存方法和视频模型上扩展了帕累托前沿，如在TeaCache上实现2.16倍加速，比保守阈值快38%且质量损失可忽略。ACID训练无关、即插即用，为视频生成加速提供新范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12358v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1444, \"height\": 368, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1439, \"height\": 479, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1435, \"height\": 551, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1438, \"height\": 548, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1438, \"height\": 549, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1454, \"height\": 1394, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1450, \"height\": 924, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.12358v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1453, \"height\": 961, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12358v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1507, \"height\": 1315, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1217, \"height\": 382, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1229, \"height\": 381, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1229, \"height\": 418, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1287, \"height\": 918, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1268, \"height\": 814, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1234, \"height\": 813, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.12358v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1249, \"height\": 668, \"label\": \"Table\"}]"
motivation: 现有动态缓存方法采用固定阈值，在推理速度与生成质量间存在非根本性折衷，希望同时实现高速与高质。
method: 提出ACID，一种训练无关的包装器，通过监测现有缓存方法的漂移信号变化率，在关键步骤自动采用低阈值，其余步骤采用高阈值。
result: "在TeaCache、EasyCache、DiCache和HunyuanVideo等模型上，ACID扩展帕累托前沿，实现最高2.16倍加速及比保守基线高38%的速度，质量损失可忽略。"
conclusion: ACID首次揭示关键步骤的存在，通过自适应阈值突破固定阈值限制，可作为通用模块提升现有缓存方案，推动视频扩散模型实时生成。
---

## 摘要
视频扩散模型能生成高质量内容，但因顺序去噪过程，推理速度仍然较慢。基于缓存的加速方法通过重用中间模型输出来解决这一问题：主流的动态方法如 TeaCache、EasyCache 和 DiCache 会累积漂移信号，并在累积漂移低于固定阈值 τ 时跳过昂贵的模型计算。这一阈值控制着明显的权衡——提高阈值可加快生成速度但牺牲视觉质量，降低阈值则保留质量但牺牲速度。我们证明这种权衡并非根本性的，而是整个去噪过程中保持 τ 不变所导致的人为现象。我们识别出关键步骤的存在——即漂移信号快速变化的时间步——并表明在这些步骤上有选择地应用低阈值，同时在其余步骤进行激进缓存，能在显著提高推理速度的同时，恢复保守缓存的大部分质量。基于这一洞察，我们提出 ACID，一个轻量级、无需训练的包装器，它监控每种方法现有漂移信号的变化率，从而动态切换低阈值与高阈值。ACID 与信号无关且模块化：它无需重新训练，可直接集成到现有动态缓存方法中，而不修改其核心机制。在三种缓存方法（TeaCache、EasyCache、DiCache）和三个开源视频扩散模型（HunyuanVideo、Wan 2.1、CogVideoX）上的评估表明，ACID 持续扩展了视觉质量与推理速度的帕累托前沿，超越任何固定阈值所能达到的效果。特别是，在 TeaCache 和 HunyuanVideo 上，ACID 相对于无缓存基线实现高达 2.16 倍的加速，相对于保守固定阈值基线实现高达 38% 的额外加速，同时质量下降可忽略不计（PSNR < 0.3 dB，SSIM < 0.01，LPIPS < 0.01）。

## Abstract
Video diffusion models produce high-quality generations but remain slow at inference due to their sequential denoising procedure. Caching-based acceleration methods address this by reusing intermediate model outputs: leading dynamic approaches such as TeaCache, EasyCache, and DiCache accumulate a drift signal and skip expensive model evaluations when accumulated drift stays below a fixed threshold τ. This threshold controls an apparent tradeoff - raising it yields faster generation at the cost of visual quality, while lowering it preserves quality but sacrifices speed. We show this tradeoff is not fundamental; it is an artifact of holding τ constant throughout denoising. We identify the existence of critical steps - timesteps where the drift signal changes rapidly - and show that applying a low threshold selectively at these steps while caching aggressively elsewhere recovers most of the quality of conservative caching at substantially higher inference speeds. Building on this insight, we propose ACID, a lightweight, training-free wrapper that monitors the rate of change of each method's existing drift signal to dynamically switch between a low and a high threshold. ACID is signal-agnostic and modular: it requires no retraining and plugs directly into existing dynamic caching methods without modifying their core mechanisms. Evaluated across three caching methods (TeaCache, EasyCache, DiCache) and three open-source video diffusion models (HunyuanVideo, Wan 2.1, CogVideoX), ACID consistently expands the Pareto frontier of visual quality versus inference speed beyond what any fixed threshold achieves. In particular, on TeaCache and HunyuanVideo, ACID achieves up to 2.16x speedup over the no-caching baseline, and up to 38% additional speedup over the conservative fixed-threshold baseline with negligible (<0.3 dB PSNR, <0.01 SSIM, <0.01 LPIPS) quality degradation.

---

## 论文详细总结（自动生成）

# ACID: Adaptive Caching for vIDeo generation 论文详细总结

## 1. 论文的核心问题与整体含义
- **研究背景**：视频扩散模型（如 HunyuanVideo、Wan 2.1、CogVideoX）虽然生成质量高，但推理速度缓慢，主要瓶颈在于顺序去噪过程中的大量模型前向计算。
- **现有方法的局限**：动态缓存方法（TeaCache、EasyCache、DiCache）通过累积漂移信号并设定固定阈值 τ 来决定是否跳过模型计算。然而，固定阈值造成了推理速度与生成质量间的“看似固有的权衡”——高阈值更快但质量差，低阈值质量好但速度慢。
- **核心发现**：这种权衡并非根本性的，而是由于在整个去噪过程中使用单一固定阈值所产生的人为后果。作者发现存在“关键步骤”（critical steps），即漂移信号变化剧烈的时刻，在这些步骤上过于激进的缓存会严重损害质量。
- **整体含义**：若能自适应地调整阈值——在关键步骤保守缓存，在平稳步骤激进缓存——则可以在维持高质量的同时大幅提升推理速度，突破固定阈值方法的帕累托前沿。

## 2. 论文提出的方法论
### 2.1 核心思想
- 观察缓存信号的**二阶导数（变化率）**，当变化率较小（稳定阶段）时采用**高阈值**以加速；当变化率剧烈（关键步骤）时采用**低阈值**以保真。
- 该方法不修改原有缓存方法的核心机制，仅在其外围添加动态阈值切换逻辑，是**训练无关、即插即用**的包装器。

### 2.2 ACID 算法流程
- **输入**：采样步数 T，基础缓存方法 M，低阈值 τ_low，高阈值 τ_high，稳定区间参数 δ。
- **步骤**（伪代码表述）：
  1. 初始化累积漂移 Σ←0，上一次信号 s_prev←None。
  2. 对于每个时间步 t 从 T 到 0：
     - 若处于预热阶段，直接运行完整模型并更新 M。
     - 否则，计算当前信号 s_t（例如 TeaCache 的 ∆TEMNI，EasyCache 的 k×∆input/∥v_t−1∥，DiCache 的浅层特征差异）。
     - 累积漂移 Σ ← Σ + s_t。
     - 若 s_prev 存在且 |s_t - s_prev| ≤ δ，则选择 τ = τ_high，否则选择 τ = τ_low。
     - 若 Σ ≤ τ，则跳过完整前向，重用上一输出（由 M 的具体重用策略决定）；否则执行完整模型前向并重置 Σ←0。
     - 更新 s_prev ← s_t。

### 2.3 关键设计要点
- **信号无关性**：直接利用各缓存方法已有的漂移信号计算变化率，无需引入额外模型或修改信号定义。
- **阈值对 (τ_low, τ_high)**：等价于原固定阈值的扩展，低阈值值常设为保守配置，高阈值值对应激进配置。
- **稳定性判断**：通过参数 δ 控制“稳定区间”宽度，当信号变化量小于 δ 时视为稳定，否则视为关键步骤。

## 3. 实验设计
### 3.1 模型与数据集
- **视频生成模型**：HunyuanVideo T2V 720P、Wan 2.1 T2V 1.3B、CogVideoX 1.5-5B 三个开源模型。
- **提示词数据集**：从 VBench 的 16 个评估维度中选取 33 条提示词（每个维度组 3 条，共 11 组），覆盖多样场景。
- **配置**：每条提示词生成一段视频，固定随机种子，确保不同方法间可公平对比。

### 3.2 对比的基线方法
- **缓存方法**：TeaCache、EasyCache、DiCache。其中 EasyCache 和 DiCache 仅在其官方支持的模型上评估（HunyuanVideo、Wan 2.1），CogVideoX 仅作 TeaCache 评估。
- **固定阈值基线**：对每种缓存方法分别设定一系列离散阈值（低、中、高），绘制帕累托前沿。
- **ACID 自适应模式**：选择不同的 (τ_low, τ_high) 组合，以及不同的稳定间隔 δ，构成一系列自适应点。

### 3.3 评估指标
- **效率**：推理延迟（秒）和加速比（相对于无缓存的基线）。
- **生成质量**：PSNR、SSIM、LPIPS，均以无缓存输出作为参考标准。

## 4. 资源与算力
- **硬件**：所有实验在 NVIDIA A100 80GB PCIe GPU 上运行，使用 PyTorch 框架。
- 论文并未详细说明总实验 GPU 小时数或训练时长（方法本身无需训练）。然而，从表格中的推理延迟数据可估算单次生成耗时：例如 HunyuanVideo 无缓存需 1359 秒（约 23 分钟），Wan 2.1 约 212 秒，CogVideoX 约 1019 秒。若每个配置生成 33 个视频且全部阈值组合被测试，总 GPU 时间可能较为可观，但未给出精确统计。

## 5. 实验数量与充分性
- **总组合数**：7 组方法 × 模型组合（TeaCache×3 模型 + EasyCache×2 模型 + DiCache×2 模型），每组均包含多个固定阈值和多个自适应阈值配置。
- **详细结果**（附录表格）：列出了每种组合下所有测试点的延迟、加速比及质量指标，数量在 10～30 个点不等，覆盖了广泛的阈值设置。
- **充分性**：实验设计较为充分，覆盖三种缓存方法、三种模型、两种信号模式（U 型与非 U 型），并对每个组合提供了完整的帕累托对比图。消融方面未细分 δ 的独立影响，但通过不同阈值对和 δ 的组合间接展示了稳定性。
- **公平性**：使用固定种子、统一提示集和统一评估指标，对比同一缓存方法的固定阈值与自适应模式，保证可比性。未针对某些方法优化超参数，所有配置均直接应用。

## 6. 论文的主要结论与发现
- **关键步骤的存在**：去噪过程中存在漂移信号剧烈变化的步骤，缓存方法若在这些步骤过度缓存会严重损害质量。
- **自适应阈值优势**：ACID 通过简单的信号变化率监测（二阶导数特性），动态切换阈值，能够在不牺牲质量的前提下显著提升速度。
- **帕累托前沿扩展**：在所有 7 个方法-模型组合中，自适应点均位于固定阈值帕累托前沿之上（或明显向右上方扩展），证明了方法的通用性。
- **具体性能**：例如 TeaCache + HunyuanVideo 实现 2.16 倍加速，比保守固定阈值快 38%，质量损失可忽略（PSNR < 0.3 dB，SSIM < 0.01，LPIPS < 0.01）；其他组合同样表现出类似的“接近低阈值质量、接近高阈值速度”的特点。

## 7. 优点
- **问题洞察深刻**：首次揭示固定阈值造成的非根本性折衷，并指出关键在于自适应应对关键步骤。
- **方法简洁通用**：仅基于现有漂移信号的变化率，无需训练、修改模型结构或缓存核心逻辑，可作为外挂模块直接集成。
- **广泛的兼容性验证**：在三种主流动态缓存方法和三个视频生成模型上均有效，表现出强鲁棒性。
- **实验扎实**：使用统一的 VBench 提示词集、固定种子、多种指标，提供了全面的帕累托前沿对比。

## 8. 不足与局限
- **超参数敏感性**：方法引入了 τ_low、τ_high 和 δ 等新参数，虽然现有结果已展示合理配置，但未系统分析 δ 的变化对性能的影响，也未给出自动化选择这些参数的方法。
- **信号依赖性**：ACID 完全依赖底层缓存方法的漂移信号质量；若原信号存在较大估计误差，自适应切换可能失效（论文未深入讨论此风险）。
- **模型覆盖**：仅测试了三个开源视频模型，未涉及图像扩散模型或更大规模的商业模型（如 Sora 等），其推广性尚待验证。
- **评估样本量**：每个配置仅使用 33 条提示词，样本规模偏小，结论的代表性可能受限于提示词的多样性。
- **计算环境单一**：只在 A100 GPU 上验证，对其他硬件（如消费级 GPU）的表现未说明，实用性评估不足。
- **缓存预热与特殊处理**：文中指出 EasyCache 前几步必须运行完整模型（相当于一种手动阈值调整），但未比较 ACID 与这种特定手工策略的异同。

（完）
