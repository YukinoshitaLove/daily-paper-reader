---
title: Semi-Supervised Conditional Diffusion via Label Augmentation
title_zh: 基于标签增强的半监督条件扩散模型
authors: "Jin Su, Yuan Gao, Yong Zhou, Jian Huang"
date: 2026-07-18
pdf: "https://arxiv.org/pdf/2607.16685v1"
tags: ["query:real-ir"]
score: 10.0
evidence: 提出一种通过为无标签数据分配平凡标签的半监督条件扩散学习方法。
tldr: 条件扩散模型依赖大量高质量标注数据，但标注成本高昂。为解决这一问题，本文提出标签增强条件扩散（LACD）方法，为无标注数据引入虚拟标签并联合训练。理论证明LACD在无标注数据足够时，生成分布收敛速度优于纯监督方法，并在合成、图像和表格数据上验证了其样本效率和生成性能的提升。该工作为半监督条件生成提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16685v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1589, \"height\": 995, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16685v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1546, \"height\": 422, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16685v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1200, \"height\": 482, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16685v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1471, \"height\": 694, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16685v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1199, \"height\": 483, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16685v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1540, \"height\": 532, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16685v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1589, \"height\": 958, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16685v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1295, \"height\": 234, \"label\": \"Table\"}]"
motivation: 获取高质量标注数据成本高、耗时，大量无标注数据未被有效利用，限制了条件扩散模型的实用性。
method: 提出LACD方法，为无标注样本分配虚拟标签，在增强数据集上进行联合去噪得分匹配。
result: 理论上，当无标注样本充足时，LACD分布收敛速度在TV距离上严格优于纯监督，W1距离上至少相当；实验证实样本效率与生成性能显著提升。
conclusion: LACD通过标签增强有效利用无标注数据，为低成本半监督条件扩散提供理论与实用方案，未来可继续扩展。
---

## 摘要
条件扩散模型已成为从标记数据中学习复杂条件分布的一个强大且灵活的框架。然而，在实践中，获取高质量标签代价高昂且耗时，导致大量未标记数据未被利用。为解决这一问题，我们提出了标签增强条件扩散模型（LACD），这是一种简单有效的方法，通过为未标记样本分配一个指定的平凡标签，并在增强数据集上进行联合去噪得分匹配来纳入未标记样本。我们给出了充分条件，保证在该方案下目标条件分布在总体层面上的可识别性。此外，我们建立了严格的统计保证：当有足够多的未标记样本时，LACD 产生的采样分布在总变差距离上比纯监督估计器收敛得更快，在 Wasserstein-1 距离上至少一样快。在合成、图像和表格基准上的大量实验验证了我们的理论，并显示与纯监督估计器相比，在样本效率和生成性能上有显著提升。

## Abstract
Conditional diffusion models have become a powerful and flexible framework for learning complex conditional distributions from labeled data. In practice, however, acquiring high-quality labels is costly and time-consuming, leaving large volumes of unlabeled data unused. To address this, we introduce label-augmented conditional diffusion (LACD), a simple and effective approach that incorporates unlabeled examples by assigning them a designated trivial label and performing joint denoising score matching over the augmented dataset. We provide sufficient conditions guaranteeing population-level identifiability of the target conditional distribution under this scheme. Moreover, we establish rigorous statistical guarantees: when sufficiently many unlabeled samples are available, the sampling distribution produced by LACD converges strictly faster than the purely supervised estimator in total variation distance, and at least as fast in Wasserstein-1 distance. Extensive experiments on synthetic, image, and tabular benchmarks corroborate our theory and show substantial gains in sample efficiency and generative performance compared with the purely supervised estimator.

---

## 论文详细总结（自动生成）

好的，以下是对已提供论文内容的结构化、深入、客观的总结。

### 1. 论文的核心问题与整体含义

*   **核心问题**：在实际应用中，获取高质量标注数据（成对的 \(X, Y\)）成本高昂且耗时，导致大量无标注数据（只有 \(X\)）被浪费。现有的条件扩散模型在标注样本稀缺时，容易出现过拟合，性能受限。
*   **研究动机**：半监督学习（SSL）提供了一种自然的解决思路，利用廉价的、大量的无标注数据来辅助少量有标注数据进行学习。
*   **整体含义**：论文研究在半监督设定下，如何使用无标注协变量 \(X\) 的观测值来改进条件生成模型 \(P_{X|Y}\) 的估计。这与传统半监督学习（用 \(X\) 和 \(Y\) 估计 \(P_{Y|X}\) 或其泛函）问题设定相反，目标不同。

### 2. 论文提出的方法论

*   **核心思想**：标签增强（Label Augmentation）。将无标注样本也纳入模型训练，通过为其引入一个特殊的“平凡标签”（\(\emptyset\)），在一个统一的“增强概率空间”中同时处理有标注和无标注数据。
*   **关键技术细节**：
    *   **增强概率空间**：定义增强变量 \(\tilde{Y} \in \mathcal{Y} \cup \{\emptyset\}\)。当样本有标签时，\(\tilde{Y}=Y\)；无标签时，\(\tilde{Y}=\emptyset\)。通过测度论构造，保证在该空间下，\(P_{X|\tilde{Y}=y} = P_{X|Y=y}\)（条件分布可识别），且 \(P_{X|\tilde{Y}=\emptyset} = P_X\)（边缘分布可识别）。
    *   **模型结构假设**：假设条件密度具有共享基线的可加形式：\(p_{X|Y=y}(x) = p(x; b_0 + \Phi_0 e_y)\)，且无标签时 \(p_X(x) = p(x; b_0)\)。其中 \(b_0\) 是所有类共享的基线向量，\(\Phi_0 e_y\) 是标签 \(y\) 对应的独热编码经嵌入矩阵变换后的特定偏移向量。
    *   **联合去噪得分匹配**：采用基于 OU 过程的得分扩散模型。通过在增强数据集（有标+无标）上最小化去噪得分匹配（DSM）损失，联合学习得分网络 \(s_\theta\) 以及参数 \(b\) 和 \(\Phi\)。无标注数据的损失项只取决于共享参数 \(b\)，而有标注数据的损失项则同时优化 \(b\) 和 \(\Phi\)。
*   **算法流程**：通过最小化经验 DSM 损失得到估计参数 \(\hat{\theta}, \hat{b}, \hat{\Phi}\)，然后对于目标类别 \(y\)，设定条件 \(\tilde{y}=y\)，模拟反向 SDE 过程，从初始噪声分布生成样本。

### 3. 实验设计

*   **数据集与场景**：
    *   **合成数据集**：9 种不同的二维合成数据集（如 `large_4gaussians`, `rings`, `2spirals`），具有不同的复杂几何结构。
    *   **图像基准**：
        *   **CIFAR-10**: 6 万张 \(32 \times 32\) 的低分辨率彩色自然图像，10 类。
        *   **Intel Scenes**: 17,034 张 \(64 \times 64\) 的自然场景图像，6 类。
    *   **表格基准**：
        *   **BEED (Bangalore EEG Epilepsy Dataset)**: 8000 个脑电图（EEG）样本，16维特征，4个类别。
*   **对比方法（Benchmark）**：
    *   **纯监督估计器**：在相同的实验设定下，仅使用有标签数据训练的扩散模型。
*   **评估指标**：
    *   合成数据：Total Variation (TV)，Maximum Mean Discrepancy (MMD)，Wasserstein-1 (W1) 距离。
    *   图像数据：总体和类平均的 Fréchet Inception Distance (FID)。
    *   表格数据：特征级 W1 距离、切片 W1 距离、KS 互补指数、边界遵循度（BA）。

### 4. 资源与算力

*   论文在已提供的文本内容中，**未明确提及具体的 GPU 型号、数量或训练总时长的算力信息**。

### 5. 实验数量与充分性

*   **实验数量**：论文进行了相对充分的实验，覆盖了三种不同类型的数据（合成、图像、表格）共 4 个主要任务，总计超过 10 种不同的数据集/配置。对于每个数据集，都设计了不同大小 \(N_\ell\) 和 \(N_u\) 的对比实验。
*   **充分性与客观性**：
    *   **充分性**：实验设计较为全面，从多个维度（视觉效果、多种距离/质量指标、最近邻图、特征嵌入投影）验证了方法在有限样本下的性能优势和理论预测。在 CIFAR-10 上，与完全监督的 EDM 基线进行比较，进一步展示其性能差距。
    *   **客观性与公平性**：对比基准清晰，即同等标注量下的纯监督方法。对于 CIFAR-10 实验，还诊断了生成样本的多样性（t-SNE）和训练稳定性 (FID 曲线)，以排除过拟合记忆化（memorization）对 FID 分数的干扰，增强了结论的可靠性。

### 6. 论文的主要结论与发现

*   **理论结论**：建立了 LACD 在总变差（TV）和 Wasserstein-1 (W1) 距离上的有限样本收敛速度，并严格证明当无标注样本足够多时，LACD 的收敛速度在 TV 距离下**严格优于**纯监督方法，在 W1 距离下**至少不相上下**。
*   **实验发现**：
    *   在各种设定下，增加无标注样本数量 (\(N_u\)) 能持续提升生成样本的质量（FID, TV, MMD 等指标下降）。
    *   无标注数据可以弥补标注样本的不足。例如，少量标注数据配合大量无标注数据，可以达到甚至超过使用更多标注数据但无无标注数据时的性能。在使用 10% 标签的 CIFAR-10 实验中，LACD 大幅缩小了与使用全部标签的全监督模型的性能差距。
    *   无标注数据有助于在低标注量下减轻模型对训练样本的记忆，提升生成样本的多样性。

### 7. 优点

*   **方法论简洁有效**：通过引入虚拟标签这一简单操作，巧妙地将无监督数据融入条件扩散模型的训练框架，无需复杂的伪标签迭代或自训练过程。
*   **理论与实验并重**：不仅提供了严格的统计收敛性理论保证，还通过覆盖合成、图像、表格数据的广泛实验充分验证了方法的有效性和理论优势。
*   **设定新颖明确**：清晰地将自己定位于“使用无标签 \(X\) 改善 \(P_{X|Y}\) 的生成”这一特定问题，与传统的半监督学习研究问题区分开。

### 8. 不足与局限

*   **结构性假设较强**：模型依赖于“条件分布共享基线向量 \(b_0\)”这一关键假设（Assumption 3），这在一些高度异质任务中可能不成立，限制了方法的通用性。
*   **算力信息缺失**：文中未提及任何算力消耗信息，无法评估方法的计算成本与可复现性。
*   **标签缺失机制假设**：目前假设标签完全随机缺失（MCAR），这在实践中不一定总是成立，限制了其适用范围。
*   **对连续 \(Y\) 的讨论有限**：正文的理论和主要实验聚焦于离散标签 \(Y\)，仅提及了结果可扩展到连续 \(Y\) 的情况，但未提供实验验证。
*   **实验对比方法不足**：实验仅与纯监督方法进行了对比，未与其他可能的半监督生成模型（如基于伪标签、一致性正则化或生成对抗网络的半监督方法）进行比较，无法体现LACD在同类方法中的相对优势。

（完）
