---
title: "The Devil is in the Dark Pixels: Toward Brightness Bias-Robust Denoising"
title_zh: 暗像素中的魔鬼：迈向亮度偏差鲁棒去噪
authors: "Sungjun Cho, Zhuangzhuang Chen, Xiaomeng Li"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.16320v1"
tags: ["query:real-ir"]
score: 9.0
evidence: 解决图像去噪中的亮度偏差问题，核心图像修复任务
tldr: 信号依赖噪声下暗区细节恢复困难，MSE训练加剧此偏差。提出BBRD，通过亮度分段、方差归一化和Group DRO动态平衡不同区域的训练权重。在多种模型和数据集上，BBRD是唯一同时提升所有亮度段的方法，暗段增益最高+0.45dB，整体PSNR提升+0.65dB。该即插即用损失函数零参数零推理开销，显著提升暗区细节恢复。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16320v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1146, \"height\": 926, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16320v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1258, \"height\": 355, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16320v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1262, \"height\": 432, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16320v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1243, \"height\": 265, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16320v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1144, \"height\": 666, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16320v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1246, \"height\": 329, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16320v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1273, \"height\": 760, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16320v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1265, \"height\": 726, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16320v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1271, \"height\": 164, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16320v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1280, \"height\": 515, \"label\": \"Table\"}]"
motivation: MSE损失下信号依赖噪声导致亮区主导训练，暗区重建误差可达6倍。
method: 按亮度分段，用噪声方差归一化误差，并通过Group DRO动态上调最差段权重。
result: 唯一在所有亮度段同时提升的方法，SIDD上整体PSNR提升0.65dB，暗区最高+0.45dB。
conclusion: BBRD即插即用无额外开销，有效矫正亮度偏差，增强暗区细节恢复。
---

## 摘要
本文揭示了图像去噪中一个重要但被忽视的问题：在信号依赖的相机噪声模型下，暗区域因信号强度衰减远快于噪声方差的减小而固有地具有低信噪比（SNR），使得暗区的细节恢复本质上极具挑战性。然而，以均方误差（MSE）训练的去噪器非但没有补偿这一困难，反而加剧了它——相对于各波段的噪声基底，暗像素的重建误差最差可达6倍。这种偏差源于两个复合因素：信号依赖噪声扩大了亮像素的残差，且网络的雅可比范数随亮度单调递增。二者共同导致亮区长期主导梯度更新，而牺牲了暗区。为此，我们提出了亮度偏差鲁棒去噪（BBRD），这是一种即插即用的MSE损失替代方案：它将像素划分为亮度带，用经验噪声方差对各带误差进行归一化，并应用组分布鲁棒优化（Group-DRO）动态增加当前表现最差带的权重，同时不增加额外参数或推理成本。在实验涉及的8种架构和2个数据集上，BBRD是13种测试方法中唯一同时改善每个亮度带的方法，在暗带上增益高达+0.45 dB，亮带上+0.32 dB，在SIDD数据集上的聚合峰值信噪比（PSNR）提升达+0.65 dB，其中细节恢复最为关键的暗区的每带增益最大。代码见 https://github.com/xmed-lab/BBRD。

## Abstract
In this paper, we reveal an important yet overlooked problem in image denoising: under signal-dependent camera noise models, dark regions suffer from inherently low Signal-to-Noise Ratio (SNR), as signal intensity decays far faster than noise variance diminishes, making detail recovery in dark areas fundamentally challenging. Yet rather than compensating for this difficulty, MSE-trained denoisers exacerbate it -- reconstructing dark pixels up to 6x worse relative to their per-band noise floor. This bias stems from two compounding factors: signal-dependent noise inflates bright-pixel residuals, and the network's Jacobian norm increases monotonically with brightness. Together, these cause bright regions to chronically dominate gradient updates at the expense of dark ones. To this end, we propose Brightness Bias-Robust Denoising (BBRD), a drop-in replacement for MSE loss that partitions pixels into brightness bands, normalizes per-band error by empirical noise variance, and applies Group Distributionally Robust Optimization (Group-DRO) to dynamically upweight whichever band is currently worst, with zero additional parameters or inference cost. Across 8 architectures and 2 datasets in our experiments, BBRD is the only method among 13 tested alternatives that improves each brightness band simultaneously, achieving up to +0.45 dB on dark bands, +0.32 dB on bright bands, and +0.65 dB aggregate Peak Signal-to-Noise Ratio (PSNR) on SIDD, with the largest per-band gains in the darkest regions where detail recovery matters most. Code is available at https://github.com/xmed-lab/BBRD

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与研究动机
- **问题：MSE训练下的亮度偏差**  
  在信号依赖的相机噪声模型下，暗区信噪比 (SNR) 天生极低。以 MSE 训练的去噪器非但没有弥补这一困难，反而进一步牺牲暗区——暗像素的重建误差相对于各自的噪声基底可高出 6 倍。  
- **成因分析**  
  偏差来自两个相互加强的因素：  
  1. 信号依赖噪声使亮像素的残差自然更大；  
  2. 网络的雅可比范数随输入亮度单调递增（CNN、Transformer、SSM 无一例外），使得亮区梯度远强于暗区。  
  两者叠加导致亮区长期主导参数更新，暗区被系统性地欠优化。  
- **核心动机**  
  现有重加权损失（Focal Loss、OHEM 等）反而误将亮像素当作“困难样本”而加倍偏斜；频率域损失、鲁棒损失等也无法跟踪当前哪个亮度带真正滞后。缺少标准化的亮度分层评估，聚合 PSNR 完全掩盖这一问题。需要一种既矫正偏差、又不引入额外参数和推理成本的训练方案。

### 2. 方法论
**BBRD (Brightness Bias-Robust Denoising)** 是一个即插即用的 MSE 替代损失，包含三个阶段：

- **阶段一：亮度带构建**
  - 从干净图像 `y` 计算平滑亮度图（7×7 平均池化，sRGB 灰度）。
  - 在训练集上拟合一维高斯混合模型 (GMM)，用 BIC 自动选择最优分量数 `K*`（SIDD 上为 5，PolyU 上为 3）。
  - 采用**高斯软分配**：像素 i 对波段 k 的权重为 `exp(-(I_i - c_k)² / 2σ_g²)`，避免硬边界引起的梯度不连续性。由此得到每带加权 MSE：  
    `MSE_k = Σ_i φ_ik (ŷ_i - y_i)² / Σ_i φ_ik`。

- **阶段二：经验噪声归一化**
  - 预计算每带的经验噪声方差 `σ̂_k²`（训练前一次性完成，取自噪声‑干净对），固定不变。
  - 定义噪声归一化误差：`R_k = MSE_k / max(σ̂_k², ε)`，使得所有带置于噪声校正后的共同尺度上。`R_k ≈ 1` 表示几乎没有去噪进展，`R_k → 0` 表示完美恢复。

- **阶段三：Softmax Group DRO**
  - 动态调整各带权重，让当前最落后的带获得更大优化压力：  
    `L_BBRD = Σ_k w_k R_k`，其中 `w_k = exp(η·R_k) / Σ_j exp(η·R_j)`，温度 `η=5`。
  - 权重每一步重新计算，始终将梯度流指向噪声校正下表现最差的波段，全程可微分。
- **关键特性**  
  零额外参数，零推理开销（带划分和 DRO 仅用于训练），计算开销仅增加约 1.7%。

### 3. 实验设计
- **数据集**
  - **真实世界基准**：SIDD（智能手机，偏暗分布）；PolyU（DSLR，亮暗像素比 ~18.5×）。
  - **合成评估基准 LumaSet‑700**：700 个自然图像块，均匀覆盖 5 个亮度分位数，三种噪声类型（信号依赖 SIDD‑style、同方差高斯、随机方差）各 11 个严重等级，专为亮度分层与噪声泛化评估设计。
- **架构覆盖**  
  8 种骨干网络横跨三大范式：CNN（NAFNet、DRUNet、KBNet、SCUNet）、Transformer（SwinIR、Restormer）、State Space Model（MambaIR、MambaIRv2）。  
- **对比方法**  
  与 13 种损失/训练策略对比：像素回归（ℓ₂、ℓ₁、Charbonnier、Huber）、结构相似度（SSIM、MS‑SSIM）、频域（FFT 损失、ℓ₁+FFT、Focal Frequency、Guided Frequency）、重加权/难例挖掘（Focal Loss、OHEM、Heteroscedastic NLL）。  
- **基准指标**  
  提出基于亮度分带的 PSNR‑D（最暗带）和 PSNR‑B（最亮带），并保持聚合 PSNR 与 SSIM，所有模型统一训练设置（Adam、100 epoch、fp32），BBRD 的超参数跨所有骨架固定不变。

### 4. 资源与算力
文中详细训练配置选择 Adam 优化器，100 个 epoch，fp32 精度，并指出 BBRD 额外训练开销仅为 +1.7%，GMM 拟合与噪声方差估计在训练前一次性完成（SIDD 约 2 分钟）。但是，**未明确给出所用的 GPU 型号、数量或具体训练时长**，属于本文在计算资源描述上的缺失。

### 5. 实验数量与充分性
实验规模广泛且系统化，包括但不限于：
- 2 个真实数据集 × 8 种架构的 MSE vs BBRD 全面对比（16 组主实验）。
- 13 种损失函数在 NAFNet 上的横向比较（SIDD 和 PolyU 双数据集）。
- 组件消融（DRO 仅、归一化仅、均匀分位替代 GMM、带源来自噪声/干净图像、温度 η 调节）。
- 训练动态分析（带间 PSNR 演变、DRO 权重收敛、归一化误差下降趋势）。
- LumaSet‑700 跨噪声类型、跨强度等级的系统评估（信号依赖、高斯、随机三种噪声，11 个等级）。
- 额外分析：Jacobian 范数验证（8 架构）、噪声方差 σ̂² 稳定性、分带阈值敏感性等。

这些实验覆盖多模型、多损失、多噪声类型，消融与分析链条完整，且所有模型使用相同训练协议，对比公平客观。

### 6. 主要结论与发现
- **亮度偏差是普遍且严重的损失层次缺陷**：所有 8 种架构和 13 种常见损失均存在暗/亮带显著 PSNR 差距，现有重加权或鲁棒损失无法缓解，甚至恶化。
- **BBRD 是唯一同时提升所有亮度带的方法**：在 SIDD 上，暗带提升达 +0.45 dB，亮带 +0.32 dB，聚合 PSNR +0.65 dB，且增益在细节最关键的暗部最大。
- **噪声归一化是矫正偏差的关键**：若无此步骤，Group DRO 反而会误将高噪声亮带作为最差带并放大原始偏差。
- **BBRD 跨架构与噪声类型泛化能力强**：对 CNN、Transformer、SSM 均一致提升，且在合成基准的三种噪声类型（包括同方差高斯噪声）下均优于 MSE，证明其同时应对了信号依赖噪声和雅可比偏斜两个源头。

### 7. 优点
- **问题定义清晰且深刻**：揭示了一个被聚合指标掩盖的根本性训练偏差，并通过梯度分析和 Jacobian 测量给出严谨的因果解释。
- **方法设计精巧、实用性强**：三步设计（GMM 软分区 → 噪声归一化 → DRO 动态重加权）环环相扣，零参数、零推理开销，可无缝嵌入现有去噪训练流程。
- **评估体系完善**：提出了标准化的亮度分带评估协议和专门的合成基准 LumaSet‑700，为后续研究提供工具。
- **实验极为扎实**：跨 8 种 SOTA 架构、13 种损失函数、2 个真实数据集、额外合成噪声基准，消融与分析详尽，充分证明了方法的普适性和有效性。
- **理论‑实验闭环**：梯度命题与 Jacobian 测量直接支撑了方法动机，训练动态曲线恰好证实了偏差的早期锁定与 BBRD 的持续纠正。

### 8. 不足与局限
- **算力信息缺失**：全文未报告训练使用的 GPU 型号、数量及具体耗时，仅提及百分比开销，不便于复现时的资源评估。
- **干净目标依赖**：亮度带划分需从干净图像 `y` 计算亮度图，在某些盲去噪或缺乏对应高清图的应用场景中需额外处理。
- **仅限于 sRGB 域**：验证基于 sRGB 图像，未在 RAW 域或其他颜色空间测试，噪声方差归一化的直接迁移性尚待验证。
- **温度超参数固定**：`η=5` 虽在文中表现稳定（0.5‑10 范围均可），但在极端训练动态下可能不是全局最优。
- **真实场景数据集较少**：仅在 SIDD 和 PolyU 两个数据集上测试，虽互补但尚未覆盖医学影像、遥感等噪声特性差异大的领域。
- **与扩散模型等新范式的结合未涉及**：扩散模型常用噪声预测 MSE，BBRD 的集成方式与效果仍需进一步探索。

（完）
