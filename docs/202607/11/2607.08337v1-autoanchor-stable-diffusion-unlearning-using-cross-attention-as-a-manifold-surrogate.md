---
title: "AutoAnchor: Stable Diffusion Unlearning Using Cross-Attention as a Manifold Surrogate"
title_zh: AutoAnchor：以交叉注意力作为流形代理的稳定扩散模型遗忘
authors: "Siyuan Wen, Jiahao Zeng, Ningning Ding"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08337v1"
tags: ["query:real-ir"]
score: 8.0
evidence: 通过交叉注意力流形代理进行稳定扩散反学习以消除有害内容
tldr: "当前的扩散遗忘方法中，基于锚点的方法因手工选择锚点而易引入偏置，无锚点方法则因潜在空间不受约束更新而导致遗忘不稳定。本文从流形假设出发，理论证明了缺乏流形近邻锚点会导致显著的法空间漂移，从而损害遗忘性能。为解决该问题，提出AutoAnchor框架，自动合成流形近邻锚点，并创新性地采用交叉注意力一致性损失作为流形邻近的高效代理，克服了直接几何优化的计算难题。实验结果表明，AutoAnchor能够在多个基线模型上实现鲁棒且无偏的遗忘，目标概念CLIP分数提升高达31.04%，非目标效用提升4.18%，同时可即插即用增强现有方法，平均提升6.30%的移除与6.65%的效用。该工作为扩散模型的安全遗忘提供了理论依据与实用方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08337v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1154, \"height\": 536, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08337v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1095, \"height\": 362, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.08337v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1229, \"height\": 639, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.08337v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1259, \"height\": 918, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08337v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1169, \"height\": 381, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08337v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1449, \"height\": 411, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08337v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1389, \"height\": 1313, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08337v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1458, \"height\": 788, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08337v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1013, \"height\": 1481, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08337v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1169, \"height\": 1389, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08337v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1456, \"height\": 824, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08337v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 699, \"height\": 375, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.08337v1/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1384, \"height\": 377, \"label\": \"Table\"}]"
motivation: 现有扩散遗忘方法因手工选锚偏置或无约束更新而不稳定，亟需稳健方案。
method: 提出AutoAnchor两阶段框架，自动合成流形近邻锚点，并以交叉注意力一致性损失高效代理流形邻近，避免直接几何优化。
result: "AutoAnchor显著提升目标遗忘（CLIP+31.04%）和效用（+4.18%），并可增强他法，平均移除+6.30%，效用+6.65%。"
conclusion: 本研究为扩散遗忘提供理论指导与即插即用工具，实现了更稳定无偏的概念移除。
---

## 摘要
扩散遗忘对于减少文本到图像模型中有害或受版权保护内容的生成至关重要。当前的扩散遗忘技术通过使用目标概念的替代物作为锚点或使用空提示来确定模型更新方向。基于锚点的方法依赖人工且语义选择的锚点，存在有偏遗忘的风险；而无锚点的方法由于不受约束的潜在更新，本质上遭受不稳健的遗忘。本工作从理论上在流形假设下形式化了这种不稳定的扩散遗忘问题，并证明缺乏流形近端锚点不可避免地会引发显著的法空间漂移，从而降低遗忘性能。为了实现稳定遗忘，我们提出\mysysn，一个自动合成流形近端锚点的两阶段框架。然而，直接的几何流形优化在计算上是不可行的。为了应对这一挑战，\mysys引入了一种新颖的交叉注意力一致性损失，作为流形邻近性的高效代理。实验结果表明，\mysys在各种最先进的基线方法上有效实现了鲁棒且无偏的遗忘，显著改善了目标概念的移除（CLIP分数提升高达31.04%）和非目标效用（CLIP分数提升高达4.18%）。此外，\mysys还可以轻松集成到现有的扩散遗忘方法中，以增强其遗忘性能（概念移除平均提升6.30%，效用平均提升6.65%）。

## Abstract
Diffusion unlearning is essential for mitigating the generation of harmful or copyrighted content in text-to-image models. Current diffusion unlearning techniques determine the model update direction by either using alternatives of the target concept as an anchor or using empty prompts. The anchor-based method relies on manually and semantically-chosen anchors that risk biased unlearning, while the anchor-free method inherently suffers from unrobust unlearning due to unconstrained latent updates. In this work, we theoretically formalize such unstable diffusion unlearning issues under the manifold hypothesis and prove that lacking a manifold-proximal anchor inevitably induces significant normal-space drift that degrades unlearning performance. To achieve stable unlearning, we propose \mysysn, a two-stage framework that automatically synthesizes manifold-proximal anchors. However, direct geometric manifold optimization is computationally intractable. To address this challenge, \mysys introduces a novel cross-attention consistency loss which serves as a highly efficient surrogate of manifold proximity. Experimental results demonstrate that \mysys effectively achieves robust and unbiased unlearning across various state-of-the-art baselines, significantly improving targeted concept removal (by up to 31.04\% in CLIP score) and non-target utility (by up to 4.18\% in CLIP score). Moreover, \mysys can also be easily integrated into existing diffusion unlearning methods to enhance their unlearning performance (by 6.30\% for concept removal and 6.65\% for utility on average).

---

## 论文详细总结（自动生成）

好的，以下是对这篇论文的详细中文总结。

---

### 1. 论文的核心问题与整体含义

- **核心问题**：当前的文本到图像扩散模型遗忘技术存在不稳定和有偏的问题。基于锚点的方法依赖人工选择的、语义上接近的替代概念作为“锚点”，这容易引入偏差（biased unlearning）；而无锚点的方法（如使用空提示）则因为更新方向不受约束，导致遗忘效果不稳健（unrobust unlearning），容易通过对抗性提示被绕过或损害模型生成质量。
- **研究背景与动机**：扩散模型因训练数据未经过滤，存在生成有害或侵权内容的风险。遗忘技术旨在移除模型生成特定目标概念（如“米老鼠”、“梵高风格”）的能力，但如何在有效擦除的同时保持模型对非目标概念的生成能力（即模型效用）是一个难题。
- **核心思想**：本文从**流形假设**出发，将有效图像视作位于高维潜在空间中的低维数据流形上的点。理想的遗忘更新应沿着流形表面“滑行”到邻近的替代概念，而非“跳离”流形。缺乏与目标概念在流形上邻近（manifold-proximal）的锚点，会导致更新方向包含大量的法空间（正交于流形）分量，将模型推向流形之外，从而引发性能退化。

### 2. 论文提出的方法论

#### 2.1 核心思想
提出 **\mysysn** 框架，自动合成一个与目标概念在数据流形上邻近的锚点，以指导稳定、无偏的遗忘更新。直接进行流形优化在计算上不可行，因此提出使用**交叉注意力一致性损失**作为流形邻近性的高效替代指标（surrogate）。

#### 2.2 关键技术细节：两阶段框架

- **第一阶段：高效锚点初始化**
    - **目的**：满足锚点的两个基本要求：非近义词（R1）和语义替代（R2）。
    - **流程**：
        1.  **自动候选生成**：利用大语言模型生成 `N` 个包含目标概念的句子。对每个句子，将目标概念替换为 `[MASK]` 标记，让大语言模型预测最可能的替代词，最终统计词频，选取前 `K` 个高频词作为候选集 `C0`。
        2.  **候选过滤与聚合**：将所有候选词和目标概念映射到连续的嵌入空间，使用聚类算法（如 K-Means）进行分组。**丢弃**与目标概念 `cu` 处于同一聚类的所有候选词，以确保语义区分度（避免近义词）。最后，对剩余的候选集 `C1` 中的概念嵌入进行简单平均，得到初始锚点嵌入 `e_a^0`。

- **第二阶段：通过交叉注意力代理进行流形优化**
    - **目的**：优化初始锚点，使其满足第三个关键要求：流形邻近性（R3）。
    - **流程**：
        1.  **交叉注意力一致性损失**：核心创新。该损失函数计算冻结的 U-Net 模型中，目标概念 `eu` 和待优化的锚点 `ea` 在同一潜在状态 `zt` 和时间步 `t` 下，**各层交叉注意力图**的 Frobenius 范数差异之和的期望。公式表达为：
            `L_{CA}(e_a) = \mathbb{E}_{t, z_t} [\sum_l \|A^l(z_t, t, e_u) - A^l(z_t, t, e_a)\|^2_F]`
            **原理**：交叉注意力图决定了生成图像的结构和布局。若锚点是语义替代品，对齐其与目标概念的注意力图，就能使其诱导出相似的结构模式，从而在生成结果上逼近目标概念，这被证明是流形邻近性的强代理。
        2.  **正则化约束**：为维持第一阶段满足的R1和R2特性，优化过程被限制在一个以目标概念嵌入 `eu` 为中心的球壳区域（一个有效语义子空间）内。通过一个惩罚项 `L_{reg}` 来强制执行半径 `r-` 和 `r+` 的边界。
        3.  **最终目标**：最小化 `L_{CA} + \lambda L_{reg}`，得到最终锚点嵌入 `e_a^*`。
        4.  **遗忘执行**：使用生成的最优锚点 `e_a^*` 替换现有遗忘损失函数（如 ESD 或 ConAbl 的损失）中的原始锚点或空嵌入，并进行标准的模型参数微调。

#### 2.3 理论支撑
论文进行了严格的理论分析，核心为：
- **定理 1（法向漂移的必然性）**：证明无锚点或使用有偏锚点的遗忘方法，其几乎所有的更新能量都不可避免地泄漏到法空间，导致模型偏离数据流形。
- **定理 2（邻近锚点抑制法向漂移）**：证明通过一个ζ-邻近锚点诱导的更新方向，其法空间能量可以被严格抑制在一个很小的范围内，从而实现稳定遗忘。

### 3. 实验设计

- **数据集与场景**：实验围绕三个典型的遗忘目标概念展开，覆盖了不同的内容类型：
    1.  **版权角色**：Mickey Mouse（米老鼠）。
    2.  **艺术风格**：Van Gogh Style（梵高风格）。
    3.  **不安全内容**：Nude（裸体）。
- **基准模型与方法（Baselines）**：与七种当前最先进的扩散遗忘方法进行全面比较：
    - **无锚点方法（4种）**：ESD, tuneSD-AF, ResAlign, CCRT。
    - **基于锚点方法（3种）**：ConAbl, FMN, tuneSD-AB。
- **评估指标**：
    - **CLIP (U)**：较低分数表示目标概念被成功移除。
    - **CLIP (R)**：较高分数表示模型对非目标概念的文本-图像对齐能力（模型效用）得到保留。
    - **FID**：较低分数表示生成的图像质量更高，适用于评估效用保留。

### 4. 资源与算力

- **硬件**：所有实验**均使用一块 NVIDIA A800 GPU（80GB 显存）**进行。
- **时间开销**：论文在第 6.3 节和附录 B.5 中提供了详细的端到端遗忘时间数据。标准方法遗忘一个概念耗时约 800 至 1300 秒，而集成 \mysysn 额外引入的计算开销很小，**平均仅增加 3.93% 至 8.06%**。例如，在 Mickey Mouse 任务上，ESD 方法原始耗时 872 秒，集成 AutoAnchor 后为 899 秒，增加约 3.14%。这表明该方法额外计算开销不大，具有较高的计算效率。

### 5. 实验数量与充分性

实验设计非常全面且充分，具有很强的说服力，主要包含以下几组：

- **主实验**：在 3 个目标概念上，对比了 1 种 \mysysn 独立方法和 7 种基线方法及其与 \mysysn 的集成版本（共 1 + 7*2 = 15 种配置），并在 CLIP(U), CLIP(R), FID 三个指标上进行评估。
- **集成增强实验**：通过“Orig”（原始基线） vs. “+AutoAnchor”（集成版）的对比，量化地展示了 \mysysn 对所有基线方法的**平均提升幅度**。
- **消融研究**：
    - **方法设计消融**：验证了两阶段设计的必要性。对比了仅使用第一阶段语义聚合锚点（`*` 变体）和完整两阶段（`+` 变体）的效果，证明交叉注意力优化阶段至关重要。
    - **案例分析**：对“Grumpy”概念，定量展示了不同手工选择锚点导致的有偏遗忘问题，以及 \mysysn 的改进。
    - **大语言模型影响**：测试了 Gemini、GPT-4、Claude 等多种大语言模型作为候选生成器，证明 \mysysn 对大语言模型选择具有很好的鲁棒性。
    - **超参数敏感度**：验证了超参数 `K`（候选词数量）的选择对性能影响不大，只要 `K` 不过小即可。
- **效率分析**：提供了所有方法在所有概念下的端到端运行时间对比。
- **定性分析**：提供了各类方法生成图像的视觉对比，直观展示了 \mysysn 在目标擦除和效用保留上的优势。
- **公平性**：所有实验结果为三次独立运行的平均值，且报告误差范围在 ±5% 以内，基线方法均采用标准设置，对比公平。

### 6. 论文的主要结论与发现

- **理论发现**：从理论上严格证明了缺乏流形邻近锚点会导致不可忽视的法空间漂移，这是现有扩散遗忘方法不稳定和有偏的**根本原因**。
- **方法有效性**：\mysysn 通过自动合成的流形邻近锚点，在概念移除和模型效用保持方面**均显著优于**所有基线方法。例如，在“Mickey Mouse”移除上，\mysysn 的 CLIP(U) 分数比最强基线低 31.04%。
- **即插即用性**：\mysysn 可以作为插件轻松集成到现有的无锚点和基于锚点的方法中，**一致地提升**它们的遗忘性能（平均提升 6.30% 的概念移除和 6.65% 的效用）和图像质量（FID）。
- **设计必要性**：验证了仅靠语义相似性不足以产生好的锚点，交叉注意力一致性损失作为流形代理是实现稳定遗忘的关键。

### 7. 优点

- **理论创新与深度**：首次从流形几何的视角理论形式化了扩散遗忘的不稳定性问题，并给出了严格的证明，为领域提供了新的理解维度。
- **方法论创新**：巧妙地利用交叉注意力图作为流形邻近性的计算高效代理，解决了直接在高维流形上优化的难题，实现了锚点的自动化、无偏合成。
- **性能优越且实用**：不仅独立性能最佳，还能作为一个轻量级、高效的即插即用组件，显著增强现有方法，且计算开销极小，具有很高的实用价值。
- **实验论证扎实**：实验覆盖了多种概念、多种基线，并进行了详尽的消融研究、鲁棒性测试和案例分析，结论可信度高。

### 8. 不足与局限

- **理论假设的理想化**：理论分析部分依赖于一些较强的假设，如数据流形的理想正则性、近似误差的严格各向同性，以及一个将 U-Net 注意力图差异与几何法向漂移直接关联的假设（注意力-几何同构）。现实中的复杂情况可能不完全满足这些假设。
- **大语言模型依赖性**：第一阶段锚点的初始化质量依赖于大语言模型对目标概念的语义理解和替代词预测能力，对于非常规或专有性极强的概念，大语言模型可能无法提供高质量的候选。
- **实验目标概念数量**：实验主要在三个典型概念上进行，虽然在附录中扩展了更多概念，但相比真实世界无限多样的有害或版权概念，覆盖面仍然有限。对于更抽象或组合概念的遗忘效果可能需要进一步探索。

（完）
