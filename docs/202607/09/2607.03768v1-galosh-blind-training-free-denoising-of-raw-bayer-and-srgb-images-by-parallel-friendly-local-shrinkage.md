---
title: "GALOSH: Blind, Training-Free Denoising of Raw Bayer and sRGB Images by Parallel-Friendly Local Shrinkage"
title_zh: GALOSH：通过并行友好的局部收缩实现盲、无需训练的Raw Bayer和sRGB图像去噪
authors: Yoshiro Sato
date: 2026-07-04
pdf: "https://arxiv.org/pdf/2607.03768v1"
tags: ["query:real-ir"]
score: 9.0
evidence: 盲、无训练的去噪方法，适用于原始和sRGB图像
tldr: 传统训练无关去噪器依赖块匹配搜索难以并行且延迟不固定，学习型方法需训练数据且域外性能下降。GALOSH首次移除搜索，提出全局部、数据无关的盲去噪流程：每图像噪声拟合、广义Anscombe变换、局部Walsh-Hadamard亮度收缩与亮度引导的局部色度回归，统一覆盖原始Bayer和sRGB域。在多个真实噪声基准上，GALOSH盲性能超越已知噪声水平的BM3D/NLM基线，原始域接近训练网络，速度比深度学习基线快7-650倍且能CPU实时运行。其固定结构适合定点数和流式硬件实现，为高效跨域去噪开辟了路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03768v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1761, \"height\": 1281, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03768v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1769, \"height\": 1281, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03768v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1774, \"height\": 693, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03768v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1775, \"height\": 691, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.03768v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 886, \"height\": 474, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.03768v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 886, \"height\": 484, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.03768v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 892, \"height\": 402, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.03768v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 889, \"height\": 395, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.03768v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 888, \"height\": 401, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.03768v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 891, \"height\": 352, \"label\": \"Table\"}]"
motivation: 训练无关去噪器因块匹配搜索并行性差，学习型方法受限于训练域和算力，亟需快速、通用、可硬化的盲去噪方案。
method: 提出全局部数据无关流程：每图像噪声拟合、广义Anscombe变换、局部Walsh-Hadamard亮度收缩与亮度引导的局部色度回归，统一处理原始Bayer和sRGB图像。
result: 在四个真实噪声基准上，GALOSH盲性能超越oracle噪声水平的BM3D/NLM基线，原始域接近训练网络，速度比DL基线快7-650倍，可CPU运行。
conclusion: 无搜索的全局部设计实现了快速、硬件友好的跨域盲去噪，为高效图像去噪开辟了新路径，并已验证定点实现可行性。
---

## 摘要
经典的无需训练的去噪器如BM3D和非局部均值，其优势很大程度上归功于搜索：内容相关的块匹配，其内存访问和数据依赖的控制流难以并行化，并排除了固定延迟的实现。基于学习的去噪器达到最高质量，但它们需要训练数据，在训练域之外性能下降（我们也观察到了这一点），并且每个像素的计算开销实际上需要GPU。我们提出了GALOSH（广义Anscombe局部收缩），一种重新设计的无需训练的去噪方法，完全移除了搜索，旨在同时实现多域覆盖、速度和质量：盲的每图像泊松-高斯噪声拟合、广义Anscombe变换、亮度的两次局部Walsh-Hadamard收缩，以及亮度引导的色度局部回归——针对两种感知上不同的噪声分量特意设计了两个不同的算子，每个都有自己的强度控制。每个阶段都是局部的、数据独立的且规则的——对于每张图像的每个像素，计算图相同。一个核心服务于两个域：原始Bayer马赛克和sRGB/YUV图像。在四个真实噪声基准测试（SIDD Medium和RawNIND，原始和sRGB）上，GALOSH在测试的盲、无需训练的方法中始终是最强的——超过了BM3D和NLM系列基线，即使给它们提供预言机噪声水平——并在原始数据上接近了训练过的网络，而在sRGB高ISO下仍低于域内训练的网络。无搜索使其速度很快：在相同GPU上以完整基准规模比深度学习基线快7到650倍，并且是比较中唯一也能在普通CPU上实际运行的强方法。固定的、数据独立的结构被设计为自然映射到定点和流式硬件，并通过操作计数分析和可工作的INT16定点实现来支持。

## Abstract
Classical training-free denoisers such as BM3D and non-local means owe much of their strength to search: content-dependent block matching whose memory traffic and data-dependent control flow parallelize poorly and preclude fixed-latency implementations. Learned denoisers reach the highest quality, but they need training data, degrade outside their training domain (which we also observe), and carry per-pixel compute budgets that effectively require a GPU. We present GALOSH (Generalized Anscombe LOcal SHrinkage), a redesign of training-free denoising that removes the search entirely and aims at multi-domain coverage, speed, and quality at once: a blind per-image Poisson-Gaussian noise fit, a generalized Anscombe transform, a two-pass local Walsh-Hadamard shrinkage of luminance, and a luminance-guided local regression of chrominance -- two deliberately different operators for the two perceptually different noise components, each with its own strength control. Every stage is local, data-independent, and regular -- the same computation graph for every pixel of every image. One core serves two domains: raw Bayer mosaics and sRGB/YUV images. On four real-noise benchmarks (SIDD Medium and RawNIND, raw and sRGB) GALOSH is consistently the strongest among the tested blind, training-free methods -- surpassing BM3D- and NLM-family baselines even when those are given an oracle noise level -- and approaches trained networks on raw data while remaining below in-domain trained networks at high ISO in sRGB. Being search-free makes it fast: 7x-650x faster than the DL baselines on the same GPU at full benchmark size, and the only strong method in the comparison that also runs practically on plain CPUs. The fixed, data-independent structure is designed to map naturally onto fixed-point and streaming hardware, supported by an operation-count analysis and a working INT16 fixed-point realization.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：传统无需训练的图像去噪方法（如BM3D、非局部均值）高度依赖基于内容相似性的块匹配搜索，这类搜索内存访问模式不规则、控制流数据依赖强，难以并行化且延迟不固定；基于深度学习的方法虽然质量高，但需要大量训练数据、在训练域外性能退化严重，且高计算消耗依赖GPU。
- **整体含义**：论文提出了一种彻底移除搜索、完全局部且数据独立的全新盲去噪范式，旨在以单一算法统一覆盖Raw Bayer马赛克和sRGB/YUV两个域，并在去噪质量、计算速度和硬件可实现性之间取得突破性平衡。

### 2. 论文提出的方法论
- **核心思想**：无需训练、无需块搜索，所有运算均为局部、固定、无数据依赖的规则操作，既可并行化也兼容流式硬件与定点整数实现。
- **关键技术流程**（分阶段处理）：
  1. **盲噪声拟合**：对每一张图像单独估计泊松-高斯混合噪声的参数，无需外部先验。
  2. **广义Anscombe变换**：将信号依赖的泊松-高斯噪声转换为近似加性、信号独立的稳定噪声，简化后续处理。
  3. **双阶段亮度局部收缩**：对亮度通道进行两次基于局部Walsh-Hadamard变换域的收缩（shrinkage），利用该变换在局部区域分离信号与噪声的能力。
  4. **亮度引导的色度局部回归**：利用已去噪的亮度信息，引导对色度通道进行局部回归去噪。亮度和色度使用不同算子，并各自独立控制强度，以适配两者不同的感知特性和噪声形态。
- **统一架构**：同一处理核心直接适用于原始Bayer马赛克数据以及sRGB/YUV图像，无需域适配。

### 3. 实验设计
- **数据集与场景**：在四个真实噪声基准上评测，覆盖两类域和两种数据格式：
  - SIDD Medium（智能手机真实噪声数据集）
  - RawNIND（原始域真实噪声）
  - 同时测试原始Bayer数据和sRGB数据。
- **对比方法**：
  - 传统训练无关方法：BM3D系列、非局部均值（NLM）系列，并在部分对比中为它们提供“预言机噪声水平”（理想参数）。
  - 基于学习的深度网络（域内训练）。
  - 深度学习基线参与速度对比。
- **评价维度**：去噪质量（盲条件下）、运行速度（相同GPU上全基准规模对比）、CPU可行性、定点实现可行性。

### 4. 资源与算力
- 论文未明确说明实验中使用的具体GPU型号和数量，仅提到速度对比“在相同GPU上”进行，且GALOSH比深度学习基线快7–650倍，并是唯一能在普通CPU上实际运行的强去噪方法。
- 所提供的文本中无训练时长、算力总量等细节。

### 5. 实验数量与充分性
- **实验范围**：
  - 至少在4个独立真实噪声基准上完成了质量评估（涵盖原始域与sRGB域）。
  - 与多类基线（传统训练无关、带预言机的传统方法、训练网络）进行了全面对比。
  - 进行了速度基准测试和定点整数实现验证。
- **充分性与公平性**：
  - 实验覆盖了关键的盲去噪场景，且对比基线包含了给予理想参数的强基线，较为公平。
  - 论文未详细披露消融实验（如各模块贡献分析），但整体评估角度多样（质量、速度、硬件映射），具备一定说服力。
  - 由于提取内容有限，无法判断是否存在更细粒度的参数敏感性或跨噪声模型实验。

### 6. 论文的主要结论与发现
- **质量**：GALOSH在盲设定下，去噪质量稳定超越获得理想噪声水平参数的BM3D和NLM系列基线；在原始域上接近专门训练的网络；在sRGB高ISO场景下仍不及域内训练网络。
- **速度**：搜索移除设计使其极快，在相同GPU上比深度学习基线快7–650倍，并可在普通CPU上实时运行。
- **硬件友好性**：固定数据独立计算图可自然映射到整数定点运算和流式硬件，并已实现可工作的INT16定点版本。
- **范式意义**：全局部、无搜索的跨域盲去噪路径是可行的，为高效、可硬化的通用图像去噪开辟了新方向。

### 7. 优点
- **并行友好**：彻底消除内容依赖的搜索，实现固定延迟和最高并行度，适合GPU/CPU及定制硬件加速。
- **双重域统一**：单一算法直接处理Raw Bayer和sRGB图像，避免域隔离设计。
- **完全盲去噪**：无需噪声水平先验或用户参数，通过每图像自动拟合完成适配。
- **速度与质量兼顾**：在极低计算开销下，质量大幅超越传统训练无关方法，并在原始域接近学习型方法。
- **硬件可部署性**：架构设计中考虑了定点化和流式处理，给出了实际的定点实现证据。

### 8. 不足与局限
- **极端噪声下的质量上限**：在sRGB高ISO等极困难场景，仍低于域内精心训练的深度学习模型。
- **色度与复杂纹理**：亮度和色度分开处理策略可能对强相关纹理或色度噪声极为复杂的情形存在局限（未在提供文本中深入讨论）。
- **实验覆盖细节未完全展开**：从给定片段看，消融研究、更多样化的噪声模型或传感器数据测试、极端失配条件下的鲁棒性等未被详细描述，难以评估泛化边界。
- **缺乏实时性具体指标**：虽提到“CPU实时运行”和定点实现，但未给出确切的吞吐量或延迟数值。

（完）
