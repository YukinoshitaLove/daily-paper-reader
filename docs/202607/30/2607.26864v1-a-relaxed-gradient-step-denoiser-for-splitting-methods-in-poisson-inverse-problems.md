---
title: A Relaxed Gradient Step Denoiser for Splitting Methods in Poisson Inverse Problems
title_zh: 一种用于泊松反问题分裂方法的松弛梯度步去噪器
authors: Alessandro Benfenati
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.26864v1"
tags: ["query:real-ir"]
score: 10.0
evidence: 即插即用方法…成像逆问题…盲去噪
tldr: 针对泊松图像逆问题的非二次KL保真度挑战，提出基于PnP-Split的rGSDSplit方法，用松弛梯度步降噪器替代通用去噪器，该降噪器由输入凸神经网络学习的凸势梯度定义，兼具盲去噪与光滑性。实验表明，rGSDSplit在经验FNE兼容范围内稳定重建，对ADMM参数敏感性低，且对不同模糊与泊松噪声水平有效，为泊松数据PnP方法提供了收敛保证与稳健性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.26864v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1482, \"height\": 401, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26864v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1489, \"height\": 325, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26864v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1465, \"height\": 574, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26864v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1482, \"height\": 363, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26864v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1467, \"height\": 637, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26864v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1463, \"height\": 1635, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.26864v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1260, \"height\": 604, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26864v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1243, \"height\": 270, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26864v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1282, \"height\": 263, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26864v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 911, \"height\": 273, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26864v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 902, \"height\": 265, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26864v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 821, \"height\": 313, \"label\": \"Table\"}]"
motivation: 针对泊松图像逆问题中非二次KL保真度导致PnP方法收敛困难，需要设计相容的降噪器。
method: 提出rGSDSplit，用输入凸神经网络学习凸势的梯度作为松弛步降噪器，结合PnP-Split框架与显式泊松更新。
result: 实验显示降噪器在FNE兼容范围内工作，对ADMM参数不敏感，对不同模糊和噪声水平重建稳定。
conclusion: rGSDSplit为泊松逆问题提供了有收敛保证的PnP框架，兼具盲去噪与光滑性，扩展了应用场景。
---

## 摘要
Plug-and-Play 方法将经典变分模型与学习的去噪器相结合，在成像反问题中取得了显著成果。针对高斯数据，其收敛性已得到广泛研究；而泊松模型由于非二次的 Kullback-Leibler 保真项，需要额外注意。本文提出了 \gsdsplit，这是一种基于 \pnpsplit 的分裂方法，其中通用去噪模块被替换为松弛的梯度步去噪器。所得方法保留了显式的泊松保真更新。该去噪器通过由输入凸神经网络参数化的学习凸势的梯度来定义。其架构和训练促进了盲去噪能力和足以满足牢固非膨胀性的光滑条件。利用经验光滑性估计来选择与 \pnpsplit 的收敛假设兼容的松弛参数。数值实验证明，在考虑的验证数据上，训练好的去噪器在一个 FNE 兼容的范围内运行。\gsdsplit 还在经验支持范围之外产生稳定的重建，并表现出对 ADMM 参数显著降低的敏感性，同时对不同的模糊算子和泊松噪声水平保持有效。

## Abstract
Plug\&Play methods combine classical variational models with learned denoisers and have achieved strong results in imaging inverse problems. Their convergence has been widely studied for Gaussian data, whereas Poisson models require additional care because of the nonquadratic Kullback--Leibler fidelity. This work introduces \gsdsplit, a splitting method built upon \pnpsplit, in which the generic denoising block is replaced by a relaxed Gradient Step Denoiser. The resulting method retains explicit Poisson fidelity updates. The denoiser is defined through the gradient of a learned convex potential parameterized by an Input Convex Neural Network. Its architecture and training promote both blind denoising capability and a smoothness condition sufficient for firm nonexpansiveness. Empirical smoothness estimates are used to select a relaxation parameter compatible with the convergence assumptions of \pnpsplit. Numerical experiments provide evidence that the trained denoiser operates in an FNE-compatible regime on the considered validation data. \gsdsplit also yields stable reconstructions outside the empirically supported range and shows substantially lower sensitivity to the ADMM parameter, while remaining effective for different blur operators and Poisson noise levels.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
*   **研究动机与背景**：图像恢复是科学成像（如显微镜、天文学）的核心问题，常用线性模型 `g = N(Hx_star + b)` 描述，其中 `x_star` 为干净图像，`H` 为模糊算子，`N` 为噪声。本文聚焦于**泊松噪声**，它普遍存在于光子受限的采集系统（如荧光显微镜、发射断层扫描）中，具有信号依赖性。
*   **核心问题**：基于最大后验估计（MAP），求解该逆问题变为：
    *   最小化 Kullback-Leibler 散度作为数据保真项 `KL(Hx+b, g)`。
    *   加上正则项 `R(x)` 和像素非负约束 `x>=0`。
*   **Plug-and-Play 方法的挑战**：PnP 方法（如 PnPSplit+）可将优化中的近端算子替换为高斯去噪器 `D`，但其收敛理论通常要求 `D` 是**牢固非膨胀（FNE）**的。面向高斯数据的 PnP 方法研究较成熟，但**泊松数据的非二次 KL 保真项**使传统的 PnP 收敛分析变得复杂，需要额外小心。
*   **整体含义**：本文旨在为泊松图像恢复设计一个**结构化、可解释性强且有收敛保证**的 PnP 算法，其核心是构建一个理论上满足 FNE 条件的“梯度步去噪器”。

### 2. 论文提出的方法论
*   **核心思想**：提出 **GSDSplit+** 方法，将 PnPSplit+ 框架中的通用黑盒去噪器，替换为一个由输入凸神经网络对凸势函数 `ψ_θ` 学习而来的**松弛梯度步去噪器**。
*   **去噪器定义**：`D_{θ,α}(x) = x - α∇ψ_θ(x)`，其中 `α` 为松弛参数。去噪残差 `(x - D(x))` 是一个保守向量场（梯度场），建立了与变分正则化、单调算子理论的天然联系。
*   **关键技术步骤**：
    1.  **继承 PnPSplit+ 框架**：采用交替方向乘子法分裂，保留了对 `x` 更新和 `w1` 更新中 KL 散度步骤的闭式解，避免了内部迭代。
    2.  **FNE 条件**：若势函数 `ψ_θ` 是凸且 L-光滑的，则当松弛参数满足 `0 < α ≤ 1/L` 时，去噪器 `D_{θ,α}` 被证明是**牢固非膨胀的**，从而保证 GSDSplit+ 算法收敛。
    3.  **网络架构**：`ψ_θ` 由改进的**输入凸神经网络**参数化，包含 5 层无偏置、3x3 滤波器的卷积层，并使用平滑的 LeakyReLU 变体 `ρ_{a,c}` 作为激活函数，以确保其凸性。
    4.  **训练策略**：分三步训练以赋予网络盲去噪和光滑性。
        *   **第一阶段**：在固定噪声水平（σ=0.05）的高斯噪声下预训练，损失函数为均方误差 `L(θ) = E[||D_{θ,1}(y) - x||^2]`。
        *   **第二阶段（盲去噪微调）**：在噪声水平（σ∈[0.01,0.05]）上训练，实现盲去噪。
        *   **第三阶段（光滑性微调）**：添加 Hessian 谱惩罚项 `R_{FNE}(θ) = μ ReLU(l_θ(y) - L_{target})^2`，其中 `l_θ(y)` 是 Hessian 最大特征值的估计，`L_{target}=1`。此举旨在经验上促进 `L ≤ 1` 的光滑性，使 `α=1` 的去噪器接近 FNE 条件。

### 3. 实验设计
*   **数据集**：
    *   **训练/验证**：从 DIV2K 数据集的 100 张图像中随机提取 64x64 像素块。
    *   **测试**：DIV2K 测试集（20 个 256x256 块）和 BSD500 数据集（10 张整图，或 256x256 块）。
*   **退化场景**：
    *   **模糊**：标准差为 1 的高斯模糊，线性运动模糊（长度 21，角度 35 度），以及半径为 5 的失焦模糊。
    *   **噪声**：泊松噪声级别 ν=20，高强度噪声 ν=5。
*   **对比基准与评估指标**：主要与 **PnPSplit+** 方法对比。用**峰值信噪比、结构相似性指数** 和**相对误差** 评估重建质量、对 ADMM 参数 γ 的敏感性，以及收敛残差。

### 4. 资源与算力
*   **训练**：论文提到，由于硬件限制，训练阶段是在 **Google Colab** 上进行的。
*   **推理与测试**：在配备 **Apple M4 处理器、16GB 统一内存**的 MacBook Pro 上进行，并使用金属性能着色器后端。
*   **训练时长**：文中仅报告了推理时间（如 400 次迭代下，256x256 块的推理需 15 秒），但**未明确说明训练的具体总时长**或 Colab 上使用的 GPU 型号和数量。

### 5. 实验数量与充分性
*   **实验数量与类型**：
    1.  **消融/参数分析**：对 ADMM 参数 γ 和松弛参数 α 进行了二维网格搜索，评估其对峰值信噪比的影响。
    2.  **FNE 条件实证检验**：在迭代过程中计算并可视化 `m_k(α)` 值，以检查迭代对间的 FNE 不等式是否满足。
    3.  **主要对比实验**：在 10 张 BSD500 图像上，与 PnPSplit+ 进行了两种场景的比较：两者均取最优 γ，以及两者均使用过估计的 γ。评估了峰值信噪比、结构相似性指数和相对误差随 γ 值变化全网格下的稳定性。
    4.  **泛化能力测试**：测试了不同模糊算子（运动、失焦）和高噪声水平下的性能。
    5.  **计算成本比较**：对比了不同图像块大小下两种方法的推理时间。
*   **充分性与客观性评估**：
    *   **优点**：实验设计全面且系统，不仅比较了最优性能，还**重点考察了参数稳健性**（如对 γ 的敏感性分析），这是一个重要的工程价值点。使用了多种图像质量评价指标，并进行了视觉质量展示。
    *   **局限**：对比的基准方法**仅有 PnPSplit+ 一种**，略显单一，缺少与其他泊松逆问题方法（如 RED 系列、Primal-Dual 方案）的比较。

### 6. 论文的主要结论与发现
*   **方法有效性**：训练的去噪器在其训练及更广的噪声水平范围内均表现出有效的盲去噪能力。
*   **收敛性实证**：通过功率迭代在多个数据集上估计得 L≈1.07。当松弛参数设为 α=0.9 时，αL<1，算法在理论和实证上均满足 FNE 兼容的稳定收敛，且残差能收敛到接近零。
*   **性能比较**：在各自最优的 ADMM 参数 γ 下，**GSDSplit+ 的峰值重建质量（峰值信噪比）低于 PnPSplit+**。
*   **核心优势 - 鲁棒性**：GSDSplit+ 的关键优势在于其对 ADMM 参数 γ 的敏感性**显著低于** PnPSplit+。当 γ 过估计时，PnPSplit+ 的性能严重下降，而 GSDSplit+ 的性能损失很小。
*   **泛化能力**：该方法能有效处理不同类型的模糊算子和高强度的泊松噪声。

### 7. 优点
*   **理论基础与设计的统一**：GC 步去噪器结构巧妙地将网络的凸性、光滑性与 FNE 的收敛条件联系起来，提供了明确的参数选择准则（α ≤ 1/L），增强了方法的可解释性。
*   **方法论创新**：将松弛参数 α 引入 GSDSplit+ 框架，不仅作为理论“补丁”，更提供了一个既满足收敛性保证又能保持良好重建效果的灵活控制手段。
*   **实用的鲁棒性**：相比基线方法，显著降低了对关键优化参数 γ 的敏感性，这使得方法在实际应用中更容易调参和部署。

### 8. 不足与局限
*   **峰值性能并非最优**：在精心调参后，GSDSplit+ 的重建质量仍不如 PnPSplit+，表明其探索的 ICNN 架构在表达力或去噪能力上可能存在上限。
*   **计算成本偏高**：由于每次迭代都需要用自动求导计算 ICNN 的梯度，GSDSplit+ 的推理时间显著慢于用简单前向传播的去噪 CNN 的 PnPSplit+。
*   **光滑性条件的局限性**：仅通过经验惩罚来促进 `L≤1`，并不能保证全局 Lipschitz 梯度条件，严格的理论收敛保证仅建立在局部或经验证据上。对 `α>1` 范围外仍能稳定的现象，缺乏理论解释。
*   **实验对比稀疏**：仅与 PnPSplit+ 进行了详细对比，缺乏与更广泛的 PnP、RED 或 Primal-Dual 求解器在同一泊松去模糊问题下的全面比较，说服力有所折扣。
*   **训练数据与规模**：训练仅使用了 DIV2K 的 100 张图像和受限的噪声范围（σ∈[0.01,0.05]），可能影响其在更复杂或域外数据上的泛化表现。

（完）
