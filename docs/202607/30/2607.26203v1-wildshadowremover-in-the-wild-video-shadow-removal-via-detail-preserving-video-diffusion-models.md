---
title: "WildShadowRemover: In-the-Wild Video Shadow Removal via Detail-Preserving Video Diffusion Models"
title_zh: WildShadowRemover：通过细节保持的视频扩散模型进行自然场景视频阴影去除
authors: "Jiamin Xu, Cong Wang, Zheng Dong, Chi Wang, Renshu Gu, Weiwei Xu, Gang Xu"
date: 2026-07-28
pdf: "https://arxiv.org/pdf/2607.26203v1"
tags: ["query:real-ir"]
score: 10.0
evidence: 通过细节保留调制将视频扩散模型应用于视频阴影去除
tldr: 面对真实场景视频阴影去除中复杂光照、多样阴影及训练数据的严重匮乏，现有方法难以有效处理。WildShadowRemover框架创新性地适配预训练视频扩散模型，通过LoRA高效微调，并嵌入细节注入模块与阴影掩码引导的频率分解调制模块，精准恢复高频纹理并消除阴影伪影。结合Depth Anything 3的单目深度先验以提供几何感知引导。在构建的WildShadow数据集上，本方法显著提升阴影去除质量与时间一致性，生成时序稳定的无阴影视频，且在不同场景下均表现出强泛化能力，推动了真实场景视频阴影去除技术的实用化并填补了该领域空白。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.26203v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 865, \"height\": 584, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26203v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1717, \"height\": 693, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26203v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 848, \"height\": 806, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26203v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1556, \"height\": 758, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26203v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 871, \"height\": 426, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.26203v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1822, \"height\": 547, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26203v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 883, \"height\": 311, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26203v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 755, \"height\": 281, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26203v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 878, \"height\": 210, \"label\": \"Table\"}]"
motivation: 自然场景视频阴影去除面临光照复杂、阴影形态多变及训练数据稀缺等挑战，亟需鲁棒高效的方法。
method: WildShadowRemover基于预训练视频扩散模型，通过LoRA微调并加入细节注入与频率分解调制模块，以保留纹理和抑制阴影，同时利用单目深度先验。
result: 在自建WildShadow数据集上，方法在阴影去除质量和时间一致性上优于现有技术，生成视觉质量优异且泛化性强的无阴影视频。
conclusion: WildShadowRemover通过扩散模型先验和细节保留机制，实现了真实场景视频阴影的有效去除，为该领域提供了新基准和数据集。
---

## 摘要
由于复杂的光照、多样的阴影外观和有限的训练数据，自然场景中的视频阴影去除仍然具有挑战性。尽管它对许多视觉和图形应用很重要，但在无约束的真实场景中，它仍然在很大程度上未被探索。为了解决这一差距，我们提出了 WildShadowRemover，一个通过 LoRA 微调将预训练视频扩散模型适配用于鲁棒视频阴影去除的框架。为了在保留模型强大生成先验的同时保持精细的图像细节，我们用细节注入模块增强冻结的 VAE 解码器，并引入阴影掩码引导的频率分解调制模块，以选择性地恢复高频纹理，同时抑制阴影伪影。来自 Depth Anything 3 的单目深度先验进一步在具有挑战性的光照条件下提供了几何感知的引导。我们还构建了 WildShadow，一个大规模成对的视频阴影去除数据集和基准，涵盖了多种合成场景。大量实验表明，我们的方法在阴影去除质量和时间一致性方面优于现有方法，产生时间连贯的无阴影视频，具有优越的视觉质量，并在具有挑战性的自然场景中具有强大的泛化能力。

## Abstract
Video shadow removal in the wild remains challenging due to complex illumination, diverse shadow appearances, and limited training data. Despite its importance to numerous vision and graphics applications, it remains largely unexplored in unconstrained real-world scenarios. To address this gap, we present WildShadowRemover, a framework that adapts a pretrained video diffusion model for robust video shadow removal via LoRA fine-tuning. To preserve fine image details while retaining the model's powerful generative prior, we augment the frozen VAE decoder with a detail injection module and introduce a shadow-mask-guided frequency-decomposed modulation module to selectively restore high-frequency textures while suppressing shadow artifacts. Monocular depth priors from Depth Anything 3 further provide geometry-aware guidance under challenging lighting conditions. We also construct WildShadow, a large-scale paired video shadow removal dataset and benchmark, covering diverse synthetic scenes. Extensive experiments demonstrate that our method outperforms existing approaches in shadow removal quality and temporal consistency, producing temporally coherent shadow-free videos with superior visual quality and strong generalization across challenging in-the-wild scenarios.

---

## 论文详细总结（自动生成）

好的，我将根据您提供的论文内容，以Markdown格式生成详细的中文总结。

### 1. 论文的核心问题与整体含义

论文聚焦于**自然场景下的视频阴影去除**这一具有挑战性的问题。
*   **研究动机**：阴影是视觉中的常见现象，会干扰目标跟踪、3D重建等下游任务。现有的阴影去除方法主要集中在单张图像上，难以保证视频帧间的时间一致性。同时，现有的阴影数据集多为室内受控环境，缺乏对街道、航拍等复杂自然场景的覆盖，导致模型泛化能力差。
*   **核心思路**：作者认为预训练的视频扩散模型已内在地学习了物体与阴影之间的时空关联，是一种极佳的生成先验。因此，本文旨在利用该先验，设计一个既能有效去除复杂阴影，又能保持视频帧间稳定性和图像细节的框架。

### 2. 论文提出的方法论

WildShadowRemover框架的核心是基于预训练视频扩散模型**Wan-Control**进行适配，并引入了一系列保真度和几何感知机制。
*   **基于视频扩散模型的主干架构与微调**：
    *   **潜空间流匹配**：模型在 VAE 的潜空间进行运算，给定输入阴影视频 \( X \)，目标无阴影视频 \( Y \) 和深度视频 \( D \)，通过流匹配范式学习从噪声到目标潜码的路径，目标函数为 \( \mathcal{L}_{FM} = \mathbb{E}_{t, \epsilon} \left\| \hat{V}_t - V_t \right\|_2^2 \)。
    *   **参数高效微调**：采用 **LoRA**技术对扩散模型的 DiT（扩散Transformer）层进行微调，固定预训练的大部分参数，既保留了原有生成能力又实现了高效的模型适配。
*   **细节保留与抗伪影机制**：
    *   **细节注入模块**：这是为了解决扩散模型输出中高频纹理丢失的问题。在 VAE 解码器的各层级，引入输入阴影视频编码器的同尺度特征来补充细节。为避免直接从带阴影视频注入特征导致阴影重新出现，引入以下调制机制。
    *   **阴影掩码引导的频率分解调制**：
        1.  **阴影掩码估计**：通过比较输入阴影视频 \( X \) 和模型输出的粗糙无阴影视频 \( \hat{Y}_0 \)，计算出软阴影掩码 \( M = 1 - X/(\hat{Y}_0 + \epsilon) \)。
        2.  **FDM模块**：将待注入的特征分解为低频分量 \( x_L \) 和高频分量 \( x_H \)。从阴影掩码 \( m_i \) 中预测出频率特异性的仿射变换参数,对 \( x_L \) 和 \( x_H \) 进行独立调制：\( F_{FDM}(x, m_i) = (\gamma_L \odot x_L + \beta_L) + (\gamma_H \odot x_H + \beta_H) \)。这能选择性地恢复非阴影区域的细节而抑制阴影区域的伪影。
*   **几何感知引导**：
    *   利用 **Depth Anything 3** 从输入视频中提取单目深度图,为光照复杂的阴影去除任务提供场景的几何信息，并将其特征融合进细节注入模块。

### 3. 实验设计

论文设计了全面的实验来验证模型在图像和视频层面的性能。

*   **主要数据集与Benchmark**:
    *   **WildShadow（视频）**：作者自建的大规模合成视频阴影数据集,包含来自 3D-FRONT（室内）、iCity（城市户外）和 Infinigen（自然户外）的成对视频，训练集约 6100 个片段，测试集约 650 个片段（含额外的 HSSD 室内场景）。
    *   **WildShadow-I（图像）**：从上述视频数据集构建的统一大规模图像数据集（36,000 张训练图片），包含室内、室外、自然等多域场景，用于评估模型泛化能力。
    *   **公开基准**：ISTD+、SRD、INS，用于与经典单图方法进行公平比较。
*   **对比方法**：
    *   与一系列现有的 SOTA 单图阴影去除方法对比，包括掩码依赖型（ShadowFormer, DMTN 等）和无掩码型（DeS3, OmniSR, StableSR, PhaSR 等）。
    *   视频评估方面，将提出的视频模型与单图方法的逐帧处理版本进行对比。

### 4. 资源与算力

*   **硬件配置**：实验使用**4块 NVIDIA RTX 4090 GPU** 进行训练。
*   **软件环境**：PyTorch 2.5.1, CUDA 12.1，并部署了 DeepSpeed 和分布式数据并行技术。
*   **训练细节**：采用两阶段训练策略（先训DiT+LoRA，再训细节注入模块）。第一阶段全局批大小为4，第二阶段全局有效批大小为8。**原文未提及具体的总训练时长**。

### 5. 实验数量与充分性

实验设计较为完备和充分，具备客观性和公平性。
*   **实验组数**：
    1.  **3个公开图像基准测试** (ISTD+, SRD, INS)。
    2.  **1个自建大规模图像基准测试** (WildShadow-I)。
    3.  **跨域泛化分析实验** (分别分析室内/室外数据域的性能)。
    4.  **视频基准测试** (定量重建指标+时间一致性指标)。
    5.  **组件消融实验** (验证深度条件、细节注入模块、DA3特征、掩码调制的作用)。
    6.  **真实场景定性评估** (在未见过的 DL3DV 和无人机视频上)。
*   **公平性**：对于所有自建基准，均使用统一的相同数据进行重新训练与评估，确保了对比的公平。

### 6. 论文的主要结论与发现

*   通过适配视频扩散模型，WildShadowRemover 在**视频阴影去除**任务上取得了当前最优的性能，尤其在确保**时间一致性**方面优势显著。
*   提出的**细节注入模块**和**频率分解调制**能够有效防止扩散模型解码时丢失高频细节，并精准抑制阴影伪影的再引入。
*   **几何深度先验**（DA3）的融入有助于模型在复杂光照下更好地理解场景结构，提升去阴影的效果。
*   构建的**WildShadow 数据集**有助于训练和评估模型在多样化场景（室内至户外）下的泛化能力。

### 7. 优点：方法或实验设计的亮点

*   **方法创新性**：首次将预训练视频扩散模型用于自然场景的视频阴影去除，将任务定义为保留细节的潜空间转换。
*   **精巧的保真机制**：提出的阴影掩码引导的**频率分解调制**模块非常具有洞察力，通过分频处理巧妙地解耦了细节恢复和阴影抑制。
*   **时空感知与泛化性**：利用视频扩散模型的先验知识使得生成的视频自然且时序一致，在跨域实验中表现出色。
*   **基准贡献**：发布的 WildShadow 多域视频数据集填补了这一领域缺乏大规模多样性数据的空白。

### 8. 不足与局限

*   **对复杂几何的局限性**：论文承认，对于密集植被、细薄结构等具有高度复杂几何的场景，方法依然存在挑战。
*   **数据依赖问题**：模型主要仍依赖**合成数据进行训练**，如何有效利用海量无配对的真实视频进行自监督或弱监督学习，是一个待解决的关键问题。
*   **算力与时间成本不明**：虽然说明了 GPU 型号和数量，但缺失**训练总时长**信息，评估其完整训练成本比较困难。

（完）
