---
title: "Small, Bias-Free, Blind and Convolutional Denoiser: A compact ConvNeXt U-Net for blind Gaussian color-image denoising"
title_zh: 小型无偏置盲卷积去噪器：用于盲高斯彩色图像去噪的紧凑ConvNeXt U-Net
authors: Nikolas Markou
date: 2026-07-24
pdf: "https://arxiv.org/pdf/2607.22793v1"
tags: ["query:real-ir"]
score: 9.0
evidence: 构建紧凑的无偏置ConvNeXt U-Net用于盲高斯彩色图像去噪
tldr: 针对盲高斯彩色图像去噪任务，提出紧凑的无偏置BF-ConvUNeXt，结合冻结深度Gabor茎、拉普拉斯金字塔跳连接及ConvNeXt U-Net，实现推理时一次齐次性，从而允许单一模型泛化至不同噪声水平。在CBSD68等多个数据集上，它一致优于DnCNN、FFDNet，平均提升约0.7dB，且仅需0.82M参数，性能接近大型模型；齐次性还支持随机采样及线性逆问题解算。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.22793v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1382, \"height\": 1583, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.22793v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1192, \"height\": 585, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.22793v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1189, \"height\": 587, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.22793v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1694, \"height\": 740, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.22793v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1299, \"height\": 621, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.22793v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1717, \"height\": 867, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.22793v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1082, \"height\": 450, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.22793v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1365, \"height\": 216, \"label\": \"Table\"}]"
motivation: 设计紧凑的无偏置盲高斯彩色图像去噪网络，通过一次齐次性实现跨噪声水平泛化与残差得分解释。
method: 结合冻结深度方向Gabor茎、拉普拉斯金字塔编码器、ConvNeXt-V1 U-Net主体和无偏置构造，使网络在推理时严格一次齐次，从而将残差解释为局部得分，并支持盲去噪。
result: "单一模型在噪声课程训练后，可外推至σ=200仍达20dB；在四个标准数据集σ={15,25,50}上平均比DnCNN高0.7dB，以0.82M参数接近大模型性能。"
conclusion: 提出的BF-ConvUNeXt以极小参数量实现了盲高斯去噪的一次齐次性，不仅性能优越，还可作为随机采样和线性逆问题的通用先验。
---

## 摘要
我们描述并评估了BF-ConvUNeXt，这是一种紧凑的无偏置ConvNeXt U-Net，用于盲加性白高斯噪声彩色图像去噪。它结合了四个现有组件，使得整个网络端到端只保留一个特性：一个冻结的深度可分离Gabor前端（方向带通，零可训练参数），一个将高频残差路由到每个跳跃连接的拉普拉斯金字塔编码器，一个ConvNeXt-V1 U-Net主体，以及全程无偏置构造（无加性偏置，线性头部，LeakyReLU，仅方差的批归一化）。这些共同使得这个82万参数的网络在推理时恰好具有一度齐次性，即 D(α y) = α D(y)，从而允许通过Miyasawa/Tweedie得分解释残差，并使单个模型能够泛化到各种噪声水平。我们在一个噪声标准差课程上（σ大约6.4到64，0-255尺度）训练了一个盲模型；它能够外推到该上限之外而不会性能骤降，在σ=150时平滑降至22.8 dB，在σ=200时降至20.0 dB。在四个标准彩色图像集（CBSD68、Kodak24、McMaster、Urban100）上以σ∈{15,25,50}进行不变的评估，它在每个数据集和噪声水平上都达到或超过了DnCNN和FFDNet，平均比DnCNN高出约0.7 dB。与重量级的CNN/transformer最新方法相比，它略有差距（根据不同数据集大约差0.3-1.7 dB），但参数量只有它们的1/15到1/39。齐次性仅限推理且特定于检查点，学习到的残差是一个局部得分而非全局得分（非保守雅可比矩阵），因此即插即用/RED的保证不能直接迁移；但它仍然可以驱动随机采样和线性逆问题（修复、超分辨率、去模糊、压缩感知）。

## Abstract
We describe and evaluate BF-ConvUNeXt, a compact bias-free ConvNeXt U-Net for blind additive-white-Gaussian-noise color image denoising, combining four existing ingredients so a single property survives end to end: a frozen depthwise Gabor stem (oriented band-pass, zero trainable parameters), a Laplacian-pyramid encoder routing the high-frequency residual into each skip connection, a ConvNeXt-V1 U-Net body, and bias-free construction throughout (no additive bias, linear head, LeakyReLU, variance-only batch norm). Together these make the 0.82M-parameter network exactly degree-1 homogeneous at inference, D(alpha y) = alpha D(y), licensing a Miyasawa/Tweedie score reading of the residual and blind generalization across noise levels from one model. We train a single blind model on a noise-sigma curriculum (sigma approximately 6.4 to 64, 0-255 scale); it extrapolates past that ceiling without a cliff, degrading smoothly to 22.8 dB at sigma=150 and 20.0 dB at sigma=200. Evaluated unchanged on four standard color sets (CBSD68, Kodak24, McMaster, Urban100) at sigma in {15,25,50}, it matches or beats DnCNN and FFDNet on every set and level, averaging about +0.7 dB over DnCNN. Against heavyweight CNN/transformer state of the art it trails by a small margin (roughly 0.3-1.7 dB depending on set) at 1/15 to 1/39 of their parameters. The homogeneity is inference-only and checkpoint-specific, and the learned residual is a local, not global, score (non-conservative Jacobian), so plug-and-play/RED guarantees do not transfer; it still drives stochastic sampling and linear inverse problems (inpainting, super-resolution, deblurring, compressive sensing).

---

## 论文详细总结（自动生成）

好的，请查阅以下对论文《Small, Bias-Free, Blind and Convolutional Denoiser: A compact ConvNeXt U-Net for blind Gaussian color-image denoising》的详细中文总结。

### 1. 论文的核心问题与整体含义

*   **核心问题**：研究如何在极低参数量下，构建一个“盲”高斯彩色图像去噪器。所谓“盲”，意指单一模型无需知道输入图像的噪声水平，即可处理任意强度的噪声，甚至能泛化至训练时未见过的噪声水平。
*   **研究动机与背景**：
    *   **现有方法的局限**：
        *   主流方法通常针对特定噪声水平训练专用模型，缺乏灵活性。
        *   许多高性能去噪模型参数量庞大（如CNN-Transformer混合模型），计算成本高昂。
        *   现有盲去噪模型往往结构复杂，未能保证严格的数学性质。
    *   **核心思想**：通过巧妙的架构设计，迫使网络在推理时满足**一次齐次性 (Degree-1 Homogeneity)**，即 `D(αy) = αD(y)`。这一性质为盲去噪提供了理论基础，使得模型输出残差可以被解释为对图像噪声得分函数的估计，从而单一模型就能泛化到不同噪声水平，并支持进一步的图像重建任务。

### 2. 论文提出的方法论

*   **核心思想**：构建一个完全无偏置（Bias-Free）的紧凑型U-Net，结合特定设计，确保网络在推理时严格满足一次齐次性。
*   **整体架构：BF-ConvUNeXt**
    该方法并非发明全新算子，而是精心组合了四个现有组件，使齐次性贯穿整个端到端网络：
    1.  **冻结的深度可分离Gabor茎 (Frozen Depthwise Gabor Stem)**:
        *   **技术细节**：使用预设的、零可训练参数的Gabor滤波器作为网络前端，进行方向带通滤波，提取多方向、多尺度的高频信息。
        *   **作用**：作为固定的线性变换，保持了齐次性，并显式提取对去噪任务至关重要的纹理信息。
    2.  **拉普拉斯金字塔跳连接编码器 (Laplacian Pyramid Skip-Connection Encoder)**:
        *   **技术细节**：在U-Net的编码器-解码器结构之间，不直接传递编码器特征图。而是通过拉普拉斯金字塔，将每一级的高频残差路由至解码器对应的跳连接。
        *   **作用**：替代了标准的跳连接，确保下采样和上采样路径间传递的信息在处理强度变化时也满足齐次性。
    3.  **ConvNeXt-V1 U-Net 主体**:
        *   **技术细节**：采用现代化的、以ConvNeXt-V1块为核心的U-Net作为去噪主网络。相比传统卷积，其设计更高效。
    4.  **全程无偏置构造 (Bias-Free Construction)**:
        *   **技术细节**：网络中**完全没有加性偏置项**、使用线性（无偏置）的预测头、LeakyReLU激活函数（不含偏置的可学习参数）、以及仅使用方差的批归一化（移除均值归一化带来的偏置影响）。
        *   **作用**：这是实现齐次性的关键。一个没有加性常数项的网络，其输出相对于输入必然是齐次的。
*   **数学性质与推论**：
    *   **一次齐次性**：以上设计共同保证了在推理时，对于任意正标量α，都有 `网络(α * 输入) = α * 网络(输入)`。
    *   **得分函数解释**：根据Miyasawa/Tweedie公式，在齐次性条件下，模型预测的残差（去噪结果 - 噪声图像）可与图像对数概率密度的局部得分函数建立联系。这使得该去噪器可作为一个通用的先验模型，用于解决其他逆问题。

### 3. 实验设计

*   **数据集**：
    *   用于盲模型训练。
    *   **测试集**：在四个标准彩色图像基准集上进行评估：`CBSD68`, `Kodak24`, `McMaster`, `Urban100`。
*   **评估场景**：
    *   **盲去噪**：在测试时，对固定噪声水平 (`σ ∈ {15, 25, 50}`) 的图像进行去噪。
    *   **外推能力**：测试模型在远超训练范围的极端噪声水平 (`σ` 最高至 200) 下的表现。
    *   **线性逆问题**：作为即插即用先验，驱动随机采样和解决修复、超分辨率、去模糊、压缩感知等任务。
*   **对比方法**：
    *   **直接对比的基准模型**：DnCNN、FFDNet（经典盲/非盲去噪方法）。
    *   **重量级最新方法 (SOTA)**：如结合CNN与Transformer的混合模型等，尽管参数量大得多。

### 4. 资源与算力

*   论文原文的摘要和提供的元数据中，**未明确说明**模型训练所使用的GPU型号、数量、显存占用或具体训练时长。仅提及模型自身参数量为 **0.82M (82万)**，并通过在噪声课程上训练单一模型来实现泛化。

### 5. 实验数量与充分性

*   **实验数量**：
    *   至少在**4个标准数据集**、**3种标准噪声水平** (`σ=15, 25, 50`) 上进行了定量对比。
    *   进行了噪声水平**外推实验**，测试了从低噪声到极高噪声 (`σ=200`)下的连续性能曲线。
    *   与**至少两个经典模型**（DnCNN, FFDNet）和**多个大型SOTA模型**进行了对比。
    *   测试了在多种线性逆问题中的应用，证明了方法的通用性。论文中很可能包含了消融实验以验证各组件的必要性（摘要中暗示了这是由四部分精确组合构成）。
*   **充分性与公平性**：
    *   **充分性**：评估覆盖了主流数据集、标准噪声水平和SOTA对比，实验设计全面。盲去噪+外推+逆问题应用的多维度验证，充分证明了方法的有效性和独特价值。
    *   **公平性**：在与经典模型（DnCNN, FFDNet）对比时，自身参数量极小（0.82M），且展示了性能优势。在与大型SOTA模型对比时，坦率承认了性能差距（0.3-1.7 dB），但同时强调了参数量上的巨大优势（仅为对方的1/15到1/39），这种对比是公平和富有洞察力的。

### 6. 论文的主要结论与发现

*   **高性能盲去噪**：提出的BF-ConvUNeXt在所有四个测试集和三个噪声水平上，一致地达到或超越了DnCNN和FFDNet，平均PSNR比DnCNN高出约 **0.7 dB**。
*   **极强泛化能力**：单一模型在训练课程上限 (`σ≈64`) 之外表现稳健，性能平滑下降，在极高噪声水平 (`σ=150`) 时仍可达22.8 dB，在 `σ=200` 时达20.0 dB，无性能悬崖。
*   **极大的参数效率**：该模型仅用 **0.82M 参数**就实现了强大性能，相比重量级SOTA方法，参数量减少了15到39倍，性能差距不大。
*   **齐次性的价值**：严格一次齐次性的实现，不但使盲去噪成为可能，还赋予了残差以得分函数的解释，使模型能被用作其他图像逆问题的通用“先验”。

### 7. 优点

*   **极简与高效**：以82万的极小参数量，实现了高性能盲去噪，参数效率惊人。
*   **数学优雅性**：通过简单、明确的架构组合，实现了精确的数学性质（一次齐次性），为模型行为提供了理论保证和解释。
*   **实用性强**：单一模型能直接用于处理任意未知强度的高斯噪声，无需像传统模型那样切换或重新训练，极大提升了应用便利性。
*   **多功能性**：不仅是一个去噪器，还可以作为通用先验驱动随机采样和解决多种线性逆问题，应用范围广。

### 8. 不足与局限

*   **性能与SOTA的差距**：尽管参数效率极高，但在绝对性能（PSNR）上，与最先进的重量级模型相比仍存在 **0.3 到 1.7 dB** 的清晰差距。
*   **理论保证的局限性**：
    *   **推理时专属性质**：齐次性是在推理时成立的，而非训练时强制约束，且它**依赖特定的模型检查点**。
    *   **局部得分 vs 全局得分**：学习到的残差为局部得分，其雅可比矩阵是非保守的。这意味着它不是任何全局概率分布的精确梯度场，因此经典的即插即用（PnP）或正则化去噪（RED）的收敛保证不能直接迁移。

（完）
