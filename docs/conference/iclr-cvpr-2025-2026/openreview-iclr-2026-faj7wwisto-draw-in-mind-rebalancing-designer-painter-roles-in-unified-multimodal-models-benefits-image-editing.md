---
title: "Draw-In-Mind: Rebalancing Designer-Painter Roles in Unified Multimodal Models Benefits Image Editing"
title_zh: Draw-In-Mind：统一多模态模型中重新平衡设计师-画家角色以改进图像编辑
authors: "Ziyun Zeng, David Junhao Zhang, Wei Li, Mike Zheng Shou"
date: 2026-01-26
pdf: "https://openreview.net/pdf?id=FaJ7WwIstO"
tags: ["query:real-ir"]
score: 10.0
evidence: 重新平衡统一多模态模型中的设计师-画家角色以实现精确图像编辑
tldr: 发现统一多模态模型中理解与生成模块职责失衡，提出重新平衡“设计师”与“画家”角色的方法，让理解模块更多地参与编辑决策，显著提升指令图像编辑的精度。
source: ICLR-2026-Accepted
selection_source: conference_retrieval
motivation: 统一模型在文生图任务上表现很好，但对精确图像编辑仍困难，源于模块职责分配不当。
method: 通过分析并重新分配理解与生成模块的职责，让理解模块更多地参与编辑决策。
result: 重新平衡后，图像编辑在区域定位和内容修改上更加准确。
conclusion: 揭示了统一模型中角色分配的重要性，为多模态编辑模型设计提供新思路。
---

## Abstract
In recent years, integrating multimodal understanding and generation into a single unified model has emerged as a promising paradigm. While this approach achieves strong results in text-to-image (T2I) generation, it still struggles with precise image editing. We attribute this limitation to an imbalanced division of responsibilities. The understanding module primarily functions as a translator that encodes user instructions into semantic conditions, while the generation module must simultaneously act as designer and painter, inferring the original layout, identifying the target editing region, and rendering the new content. This imbalance is counterintuitive because the understanding module is typically trained with several times more data on complex reasoning tasks than the generation module. To address this issue, we introduce *Draw-In-Mind* (DIM), a dataset comprising two complementary subsets: (**i**) DIM-T2I, containing 14M long-context image-text pairs to enhance complex instruction comprehension; and (**ii**) DIM-Edit, consisting of 233K chain-of-thought imaginations generated by GPT-4o, serving as explicit design blueprints for image edits. We connect a frozen Qwen2.5-VL-3B with a trainable SANA1.5-1.6B via a lightweight two-layer MLP, and train it on the proposed DIM dataset, resulting in DIM-4.6B-T2I/Edit. Despite its modest parameter scale, DIM-4.6B-Edit achieves SOTA or competitive performance on the ImgEdit and GEdit-Bench benchmarks, outperforming much larger models such as UniWorld-V1 and Step1X-Edit. These findings demonstrate that explicitly assigning the design responsibility to the understanding module provides significant benefits for image editing. Our dataset and models are available at https://github.com/showlab/DIM.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：当前统一多模态模型（将理解与生成集成于单一模型）在文生图（T2I）任务上表现优异，但在精确图像编辑（例如按指令修改局部区域、替换物体等）时仍存在困难。
- **原因分析**：作者将其归因于模型内部职责划分失衡——理解模块仅充当“翻译器”，将用户指令编码为语义条件；而生成模块既要承担“设计师”角色（推断原图布局、定位编辑区域），又要充当“画家”（渲染新内容）。这种分工违背了直觉，因为理解模块通常在更复杂推理任务上接受数倍于生成模块的数据训练。
- **整体含义**：重新平衡设计师与画家的职责，让理解模块更多地参与“设计”决策，有望大幅提升指令图像编辑的精度，并为统一模型的设计提供新思路。

### 2. 论文提出的方法论
- **核心思想**：显式地将“设计”职责（如定位编辑区域、规划修改方案）分配给理解模块，生成模块专注于执行（渲染），从而缓解原有不平衡。
- **关键技术手段**：
  - 构建 **Draw-In-Mind (DIM)** 数据集，包含两个互补子集：
    - **DIM-T2I**：1400 万对长上下文图像-文本对，用于增强模型对复杂指令的理解能力。
    - **DIM-Edit**：23.3 万条由 GPT-4o 生成的思维链式“设计蓝图”，为图像编辑提供显式的、分步的编辑决策规划。
  - 模型架构：连接**冻结的 Qwen2.5-VL-3B**（理解模块）与**可训练的 SANA1.5-1.6B**（生成模块），中间通过一个轻量级两层 MLP 进行桥接。
  - 训练策略：在 DIM 数据集上联合训练，最终得到 DIM-4.6B 模型，可分别用于文生图（T2I）和图像编辑（Edit）。
- **工作流程**：给定编辑指令，理解模块先生成包含推理步骤的“设计蓝图”，再交由生成模块实现像素级修改。

### 3. 实验设计
- **数据集/场景**：
  - 训练：自建的 DIM-T2I 与 DIM-Edit。
  - 评估基准：**ImgEdit** 和 **GEdit-Bench**，覆盖基于指令的图像编辑任务。
- **对比方法**：包括大规模模型如 **UniWorld-V1** 和 **Step1X-Edit** 等。对比指标未在摘要中详列，但提及 DIM-4.6B-Edit 在这两个基准上达到 SOTA 或具备竞争力，且参数量远小于对手。
- **模型尺度**：DIM-4.6B 为 46 亿参数（理解模块 3B + 生成模块 1.6B），属于相对轻量的统一模型。

### 4. 资源与算力
- **本摘要未明确提及**训练所使用的 GPU 型号、数量、训练时长或总计算量（如 GPU-hours）。需从正文中进一步获取。基于提供的信息，无法给出具体算力数据。

### 5. 实验数量与充分性
- 摘要仅提到在 **ImgEdit** 和 **GEdit-Bench** 两个基准上进行了评估，并与至少两个大型模型对比。详细实验数量（如消融研究、不同子集的效果、不同模块连接方式等）未在摘要中展开，但论文可能包含更充分的实验。从已报告的结果看，该方法在主流编辑基准上表现突出，且与更大模型对比证明效率，初步判断实验具有客观性和公平性，但需全文验证其消融实验的全面性。

### 6. 论文的主要结论与发现
- 显式地将设计职责分配给理解模块，对图像编辑任务有显著益处。
- 所提出的 **Draw-In-Mind** 数据集与对应训练方案，使得小型统一模型（4.6B）在图像编辑上达到甚至超越更大规模模型的性能。
- 该发现揭示了统一多模态模型中角色分配的重要性，为未来多模态编辑模型的设计提供了明确方向。

### 7. 优点
- **问题洞察深刻**：从职责失衡角度剖析统一模型编辑难，观点新颖。
- **方法论针对性明确**：通过构建“设计蓝图”数据集强制理解模块参与决策，直接针对不平衡问题。
- **模型轻量高效**：以 4.6B 参数达到 SOTA 效果，相比竞争对手大幅节省资源，实用性高。
- **模型可用性高**：公开了数据集和模型代码（提供 GitHub 链接），便于复现与进一步研究。

### 8. 不足与局限
- **算力信息缺失**：摘要未提及训练成本，无法评估资源效率的实际门槛。
- **数据集依赖外部模型**：DIM-Edit 的设计蓝图由 GPT-4o 生成，可能继承其偏差与错误，影响编辑指令的多样性与边界。
- **评估范围有限**：仅提及两个编辑基准，未涉及更复杂的真实编辑场景（如多轮编辑、指令模糊下的表现），泛化性有待进一步检验。
- **单架构验证**：仅使用 Qwen2.5-VL + SANA1.5 组合，该“设计师-画家”平衡策略是否具有跨架构普适性尚不清楚。

（完）
