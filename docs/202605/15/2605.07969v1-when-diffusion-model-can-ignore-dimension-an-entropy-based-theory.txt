Title: When Diffusion Model Can Ignore Dimension: An Entropy-Based Theory

URL Source: https://arxiv.org/pdf/2605.07969v1

Published Time: Mon, 11 May 2026 01:31:22 GMT

Number of Pages: 23

Markdown Content:
# When Diffusion Model Can Ignore Dimension: An Entropy-Based Theory 

Ahmad Aghapour ∗ Erhan Bayraktar †

Abstract 

Diffusion models perform remarkably well on high-dimensional data such as images, often using only a modest number of reverse-time steps. Despite this practical success, existing convergence theory does not fully explain why such samplers remain efficient in high dimensions. Many prior KL guarantees bound the discretization error in terms of the ambient dimension, while other improved results replace this dependence using intrinsic-dimensional or geometric structure assumptions. In this work, we develop an alternative information-theoretic perspective on diffusion sampler convergence. We prove that, for Gaussian mixture targets, the discretization error is controlled by the Shannon entropy of the latent mixture component rather than by the ambient dimension. Consequently, the leading step complexity scales linearly with latent entropy and depends only logarithmically on the second moment of the data. Our analysis also extends to discrete target distributions, where the relevant complexity is the entropy of the target rather than the dimension of the embedding space. These results suggest that diffusion sampling can remain efficient in high-dimensional spaces when the data distribution admits a compact latent representation, as is widely believed to be the case for natural images. 

# 1 Introduction 

Diffusion models have become one of the most successful approaches to generative modeling, achiev-ing striking performance on high-dimensional data such as images, audio, video, and molecular structures [19, 20, 21, 6, 18]. These models generate samples by starting from a simple noise dis-tribution and numerically simulating a reverse-time denoising process driven by a learned score. In practice, high-quality samples are often produced with a moderate number of reverse steps, even when the ambient dimension of the data is extremely large. This empirical success raises a basic theoretical question: why do diffusion samplers work so efficiently in high dimension? Several recent works have attempted to explain this phenomenon by identifying notions of effective dimension that are smaller than the ambient dimension. One perspective is geometric: high-dimensional data may concentrate near a low-dimensional manifold or admit small covering number, allowing diffusion samplers to adapt to intrinsic rather than ambient dimension [15, 17, 10]. Another perspective is distributional: structured classes such as Gaussian mixtures can admit sampling guarantees whose dependence on ambient dimension is substantially reduced or removed under appropriate assump-tions [11]. These results suggest that the apparent high-dimensionality of natural data may not be the right complexity measure for diffusion sampling. 

> ∗Department of Mathematics, University of Michigan, Ann Arbor, 48109; email: aghapour@umich.edu.
> †Department of Mathematics, University of Michigan, Ann Arbor, 48109; email: erhan@umich.edu.

1

> arXiv:2605.07969v1 [cs.LG] 8 May 2026

In this work, we take a different information-theoretic viewpoint. Rather than measuring com-plexity by ambient dimension, covering number, or geometric intrinsic dimension, we measure the amount of latent uncertainty that the reverse process must resolve. This viewpoint is motivated by modern image representations. High-dimensional images are often encoded by compact latent variables before being decoded back to pixel space. Vector-quantized and token-based image mod-els, for example, represent images using a sequence of discrete codebook indices rather than raw pixels [22, 7, 23]. Recent tokenizers such as TiTok show that a 256 × 256 image can be represented using as few as a small number of discrete latent tokens [23]. Such models suggest that, although images live in a very high-dimensional Euclidean space, much of their generative uncertainty may be carried by a compact discrete latent representation. This motivates the following question: can diffusion sampler convergence be controlled by the entropy of a latent representation, rather than by the dimension of the ambient space? We answer this question affirmatively for Gaussian mixture targets. We consider data of the form 

Z = U + ϵG 0, U = μJ ,

where J is a discrete latent variable indexing a mixture component and G0 is Gaussian noise. The sample Z lies in Rd, but the dominant uncertainty is represented by the latent index J. Our main result shows that, for a suitable score-based reverse sampler, the discretization error is governed by the Shannon entropy H(J) rather than by the ambient dimension d.More precisely, Theorem 1 proves that there exists a K-step sampling grid such that KL( Pδ ∥ bPT −δ) ≤ R

2T + 4H(J)

K



2 + log +

R η max 

2H(J)

2

+ 12 eapx ,

where R = E∥Z∥2, ηmax = ( δ + ϵ2)−1, and eapx is the score-approximation contribution. Thus, apart from initialization and score-estimation errors, the leading discretization contribution scales as 

O H(J)

K



1 + log +

Rη max 

H(J)

2!

.

In particular, the leading term is linear in the entropy of the latent component and has only logarithmic dependence on scale quantities such as the second moment and endpoint signal-to-noise level. The key technical idea is to rewrite the discretization error as an area functional involving the minimum mean-square error of the Gaussian channel associated with the latent variable. This con-verts a numerical analysis problem into an information-theoretic one. Using the I-MMSE identity of Guo, Shamai, and Verd´ u [8] together with the elementary bound that mutual information is at most the entropy of the discrete latent variable, we show that the MMSE area is controlled by H(J). The resulting bound gives an entropy-based explanation for why diffusion sampling can remain efficient in high-dimensional spaces when the data admit a compact latent description. Our analysis also covers discrete target distributions as the zero-variance limit ϵ → 0. In this case, the Gaussian mixture collapses to a discrete distribution supported on embedded codewords, and the relevant complexity becomes the entropy of the target distribution itself. This setting captures an idealized version of diffusion over vector-quantized latent codes or embedded discrete sequences. Such quantized representations are used in practice in VQ-based generative models, in-cluding VQ-VAE and VQGAN-style image tokenizers [22, 7]. They are also used in latent diffusion 2models: [18] train diffusion models in the latent space of a VQ-regularized first-stage autoencoder, where the first stage uses a vector-quantization layer and is interpreted as a VQGAN-style com-pression model. In this view, the embedding or decoded image dimension may be large, but the sampling complexity is controlled by the information content of the latent code rather than by the latent dimension. This Shannon-entropy dependence is important for two reasons. First, Shannon entropy mea-sures the effective uncertainty of the latent representation rather than the size of the latent space. Thus it can be much smaller than log |J | when the distribution over latent codes is nonuniform, as is often expected in learned discrete representations. Second, the bound does not require the latent space to be finite: it applies to countably infinite collections of mixture components whenever 

H(J) < ∞. In this sense, the result depends on the information content of the latent variable, not on the number of possible codes or on the ambient embedding dimension. The remaining depen-dence on the second moment and endpoint signal-to-noise scale is only logarithmic, so these scale parameters affect the discretization complexity much more mildly than an ambient-dimensional term would. 

# 2 Related Work 

We now situate our result within the growing literature on non-asymptotic convergence guarantees for diffusion samplers. A common goal in this line of work is to determine whether the number of reverse steps must scale with the ambient dimension, or whether sharper guarantees can exploit additional structure in the target distribution. Recent theory has made substantial progress under broad data assumptions, without requiring log-concavity or restrictive functional inequalities [4, 3, 2, 12]. These analyses typically decompose the sampling error into initialization, discretization, and score-approximation terms. The resulting guarantees can be roughly divided according to the metric in which the final sampling error is measured, most commonly total variation distance or Kullback–Leibler divergence. 

Total-variation convergence. One line of work studies convergence in total variation distance. For general target distributions, sharp recent results show that DDPM-type samplers can achieve total-variation error with nearly linear dependence on the ambient dimension, for example through 

O(d/T )-type rates under minimal assumptions [12]. Related results for probability-flow ODE sam-plers obtain nearly d/ε iteration complexity in total variation under minimal smoothness assump-tions on the data distribution [13]. More refined TV analyses show that this ambient-dimensional dependence can improve when the target has additional structure: DDPM and DDIM can adapt to an intrinsic dimension k for broad low-dimensional target classes [15], and approximate Gaussian mixture targets admit dimension-free TV guarantees, independent of both the ambient dimension and the number of mixture components up to logarithmic factors [11]. These results give important evidence that diffusion samplers can exploit structure beyond ambient dimension. 

Gaussian mixtures. The Gaussian-mixture result of [11] is particularly close to our setting. That work studies targets that are close in total variation to finite isotropic Gaussian mixtures, with bounded component means, and proves TV guarantees whose leading complexity is independent of the ambient dimension and the number of components up to logarithmic factors. Our work is complementary but different in both metric and complexity measure. We work directly in KL 3divergence and obtain a discretization bound controlled by the Shannon entropy H(J) of the latent mixture component. Thus the leading quantity in our bound is the effective uncertainty of the component index, rather than the worst-case number of mixture components. This distinction is important when the mixture weights are highly nonuniform, since H(J) can be much smaller than log K. Moreover, our formulation allows countably many latent components, provided the latent entropy and second moment are finite. 

Why total variation may not fully explain practice. The goal of convergence theory is not only to prove that diffusion samplers converge, but also to explain the scale at which approximation errors arise in practice. From this perspective, total variation distance may be too coarse for some high-dimensional generative modeling questions. In many diffusion analyses, the score-estimation contribution is naturally expressed as an integrated squared score, noise-prediction, or denoising error. When this quantity is converted into total variation, the resulting bound can saturate. For example, if an image has D ≈ 216 coordinates, then an ℓ22 prediction error of order one corresponds to a root-mean-square coordinate error of order D−1/2 ≈ 2−8. Such an error can be small from the perspective of coordinatewise neural prediction, while the corresponding TV upper bound may still be O(1) and therefore close to vacuous. This motivates working directly with KL divergence, where the pathwise decomposition retains the natural squared-error approximation term. 

KL convergence guarantees. A second line of work therefore studies KL convergence more directly. Early and recent KL analyses prove convergence for score-based samplers under finite-moment, smoothing, or score-accuracy assumptions, with rates that are often nearly linear in the ambient dimension [3, 2]. Other work obtains KL guarantees under regularity conditions such as finite relative Fisher information with respect to a Gaussian reference measure [5]. More recent results show that KL convergence can also adapt to geometric low-dimensional structure: under manifold or intrinsic-dimensional assumptions, the number of sampling steps can scale linearly, up to logarithmic factors, with an intrinsic dimension rather than with the ambient dimension [17, 10]. These results provide a geometric explanation for dimension reduction in KL: the target distribution may concentrate near a low-dimensional manifold or satisfy an intrinsic dimension condition. 

Entropy-based KL analyses. Closest in spirit to our work are entropy-based KL analyses, which seek dimension-free discretization bounds in terms of information-theoretic quantities rather than geometric dimension. [1] derive dimension-free KL discretization guarantees using an MMSE representation and information-theoretic control [1]. Their analysis is fully independent of second-moment scale quantities, but the resulting entropy dependence is quadratic: under weaker assump-tions it is controlled by the square of the order-1 /2 R´ enyi entropy, H21/2, and under an additional concentration condition on the information content it yields a Shannon-entropy-squared bound. The rest of the paper develops this latent-entropy viewpoint. We first define the Gaussian-mixture target model and the corresponding score-based reverse sampler. We then decompose the pathwise KL error into initialization, discretization, and score-approximation terms. Finally, we prove the main entropy-based discretization bound by expressing the discretization error as an MMSE area functional for the latent Gaussian channel and controlling this area using the I-MMSE identity. 43 Problem Setup and Sampling Procedure 

Let p be a target distribution on Rd. We begin with the standard Brownian noising process 

dX t = dW t, X0 ∼ p, t ∈ [0 , T ],

where ( Wt)t∈[0 ,T ] is a Brownian motion independent of X0. We write Z := X0 and denote by pt

the law of Xt. For t > 0, let 

mt(x) := E[Z | Xt = x]be the Bayes denoiser. Since Xt = Z + √t G with G ∼ N (0 , I d), Tweedie’s formula gives 

∇ log pt(x) = mt(x) − xt .

Thus the reverse-time process Ys := XT −s satisfies, under the usual time-reversal conditions [9], 

dY s = mT −s(Ys) − Ys

T − s ds + dB s, Y0 = XT .

We analyze the reverse dynamics up to time T − δ, for a fixed δ > 0, in order to avoid the singular terminal endpoint. Let 0 = s0 < s 1 < · · · < s K = T − δ, tk := T − sk.

A useful way to view the stochastic DDIM sampler with η = 1 in this Brownian setting is as follows. On each interval ( sk−1, s k], the posterior mean is frozen at the previous grid point, while the linear part of the reverse drift is integrated exactly. If the exact denoiser were available, this would correspond to 

d eYs = mtk−1 ( eYsk−1 ) − eYs

T − s ds + dB s, s ∈ (sk−1, s k].

Solving this linear SDE over one interval gives 

eYsk = tk

tk−1

eYsk−1 +



1 − tk

tk−1



mtk−1 ( eYsk−1 ) + 

s

tk



1 − tk

tk−1



ξk,

where ξk ∼ N (0 , I d) are independent. In this form, the discretization error is precisely the error caused by holding the posterior mean fixed during each reverse-time step. We now specialize to the structured targets considered in this paper. Assume that the data are generated from a latent Gaussian mixture: 

Z = U + ϵG 0, U = μJ , G0 ∼ N (0 , I d),

where J is a discrete latent variable with probabilities ( πj ), the vectors μj ∈ Rd are mixture centers, and G0 is independent of J. For ϵ > 0, the target distribution is the continuous Gaussian mixture 

p(z) = X

> j

πj φϵ2Id (z − μj ).

5Under this model, the forward process admits the representation 

Xt = U + pt + ϵ2 G, G ∼ N (0 , I d).

It is therefore natural to track the posterior mean of the latent center, 

Mt(x) := E[U | Xt = x].

For Gaussian mixtures, the score can be written as 

∇ log pt(x) = Mt(x) − xt + ϵ2 , Mt(x) = x + ( t + ϵ2)∇ log pt(x).

Thus the latent posterior mean is not an extra oracle: it can be recovered directly from the score. The reverse SDE can therefore be written in latent-posterior form, 

dY s = MT −s(Ys) − Ys

T − s + ϵ2 ds + dB s, Y0 = XT .

This suggests a modification of the usual posterior-mean freezing rule. Instead of freezing the posterior mean of the full data variable, we freeze the posterior mean of the latent center. Given a learned score bst, define cMt(x) := x + ( t + ϵ2)bst(x).

The proposed sampler is the continuous-time interpolation 

d eYs = cMtk−1 ( eYsk−1 ) − eYs

T − s + ϵ2 ds + dB s, s ∈ (sk−1, s k]. (1) Equivalently, setting 

ak := tk + ϵ2

tk−1 + ϵ2 ,

one obtains the one-step update 

eYsk = ak eYsk−1 + (1 − ak) cMtk−1 ( eYsk−1 ) + p(tk + ϵ2)(1 − ak) ξk,

where ξk ∼ N (0 , I d) are independent. Substituting the definition of cMt shows that the sampler only requires evaluations of the learned score at the grid points. The latent posterior mean is used to reveal the structure of the dynamics, but the implemented algorithm is fully score-based. 

# 4 Analysis of the Proposed Sampler 

We now analyze the sampler defined in (1). The purpose of this section is to separate the sampling error into the error coming from time discretization and the error coming from score approximation. The entropy-based control of the discretization term is developed in the next section. 

Assumption 1 (Gaussian mixture target) . The target distribution is generated as 

Z = U + ϵG 0, U = μJ , G0 ∼ N (0 , I d),

6where J is a discrete latent variable taking values in an index set J with probabilities (πj )j∈J , the vectors {μj }j∈J ⊂ Rd are the mixture centers, and G0 is independent of J. We assume that the latent entropy is finite, 

H(J) := − X

> j∈J

πj log πj < ∞,

and that the target has finite second moment, 

E ∥Z∥2 = R < ∞.

Remark 1. When ϵ = 0 , the model reduces to a discrete distribution supported on the set of centers {μj }. This includes latent diffusion models with vector-quantized latent representations, where data are represented by discrete codebook indices, mapped to Euclidean latent embeddings, and the diffusion process is performed in this latent space before decoding back to data space [18]. It also connects to continuous diffusion language models, where discrete tokens are embedded into Euclidean space and the Gaussian diffusion process is performed on these continuous embeddings. For example, Diffusion-LM denoises a sequence of Gaussian vectors into word vectors [14]. 

Remark 2. When ϵ > 0, we model the data distribution in pixel space as the decoded image associated with a discrete latent code, plus a small continuous residual. Concretely, the center μj

may be interpreted as the pixel-space image obtained by decoding the discrete latent code j, while the Gaussian term ϵG 0 represents reconstruction error, decoder variability, or additional observation noise around that decoded image. Thus the model 

Z = μJ + ϵG 0

can be viewed as a pixel-space Gaussian mixture induced by a discrete latent representation. This perspective is motivated by modern image tokenizers, which represent high-dimensional images using compact discrete latent codes before decoding them back to pixel space. For example, TiTok represents a 256 × 256 image using as few as 32 discrete tokens [23]. In this setting, the sample Z lies in the ambient pixel space Rd, but the dominant generative uncertainty is carried by the discrete latent index J, while ϵ controls the size of the reconstruction or residual error around the decoded center. 

Let P and eP denote the path laws on C([0 , T − δ], Rd) of the exact latent-posterior reverse process and the approximate process in (1), initialized from the same law at time zero. On each interval ( sk−1, s k], define the drift mismatch along the exact path by ∆s := MT −s(Ys) − cMtk−1 (Ysk−1 )

T − s + ϵ2 . (2) The remaining source of error comes from replacing the exact score by the learned score along the sampling grid. To isolate this contribution, we measure the error of the learned model through the induced x0-prediction loss at the grid points. This quantity is the part of the final sampling error that depends on the learned score, rather than on the discretization of the reverse dynamics. We therefore record an assumption that controls the total score-approximation error along the chosen sampling schedule. As in the schedule-dependent score-error assumption of [2], the approximation assumption below is understood to be imposed on the same discretization grid used by the sam-pler. In particular, when applying the assumption in Theorem 1, the grid is the final K-step grid constructed in that theorem. 7Assumption 2. For the chosen SNR grid γk = 1 /t k, the learned score satisfies 

Eapx =

> K

X

> k=1

(γk − γk−1)Lx0 (γk−1) ≤ eapx ,

where 

Lx0 (γk) := E

h

∥mtk (Xtk ) − bmtk (Xtk )∥2i

, tk = γ−1 

> k

.

Here eapx ≥ 0 denotes the total x0-prediction error of the learned model along the sampling schedule. 

Theorem 1. Assume Assumption 1, and suppose that H(J) > 0 and R > 0. Let the sampler (1) 

be initialized from the Gaussian prior N (0 , T I d), and let bPT −δ denote the law of its output at time 

T − δ. Let Pδ denote the law of the forward process Xδ. Define 

ηmax := 1

δ + ϵ2 , α := 2H(J)

R , ℓ := log +

 ηmax 

α



= log +

 R η max 

2H(J)



,

where log +(x) := max {log x, 0}. Then, for every integer 

K ≥ 4(2 + ℓ),

there exists a K-step grid ΓK for the sampler (1) such that, if Assumption 2 holds on this grid with approximation bound eapx , then 

KL( Pδ ∥ bPT −δ) ≤ R

2T + 4H(J)

K (2 + ℓ)2 + 12 eapx .

Equivalently, 

KL( Pδ ∥ bPT −δ) ≤ R

2T + 4H(J)

K



2 + log +

R η max 

2H(J)

2

+ 12 eapx .

In particular, the discretization contribution to the KL error is of order 

H(J)

K



1 + log +

R η max 

H(J)

2

.

Consequently, up to initialization and score-approximation errors, it is sufficient to take 

K = O H(J)

ξ



1 + log +

R η max 

H(J)

2!

steps to make the discretization contribution at most ξ.

Proof sketch. The full proof of Theorem 1 is deferred to Appendix A. By the pathwise-KL argu-ment in Appendix B, the total error is reduced to an initialization term, a score-approximation term, and a discretization term. The initialization term is handled directly, and the score-approximation term is controlled by Assumption 2. The main task is therefore to bound the discretization term. For this, we first rewrite the discretization error as an MMSE area functional in the regularized SNR variable 

η := 1

t + ϵ2 ,

8as stated in Lemma 1. To control this area, we need a pointwise upper bound on the latent MMSE. Lemma 2 provides exactly this bound: mmse( η) ≤ min 



R, 2H(J)

η



.

where mmse( η) = E



U − E[U | U + η−1/2G] 2

This is the key structural input in the proof. It shows that the MMSE is bounded by the second moment R in the low-SNR regime and by the entropy term 2 H(J)/η in the high-SNR regime. This two-regime behavior is what motivates the hybrid grid construction used in the appendix. 

Lemma 1. For any grid written in the regularized SNR variable as 

ηk := 1

tk + ϵ2 ,

the discretization term satisfies 

Edisc =

> K

X

> k=1

Z ηk

> ηk−1

(mmse( ηk−1) − mmse( η)) dη. 

Proof. The proof is given in Appendix C. This MMSE-functional representation is closely related to the entropy-based viewpoint developed in [1]. 

Lemma 2. Under Assumption 1, for every η > 0,

mmse( η) ≤ min 



R, 2H(J)

η



.

Proof. Since Z = U + ϵG 0 and G0 is independent of U , Assumption 1 implies 

E ∥U ∥2 ≤ E ∥Z∥2 = R. 

Therefore mmse( η) ≤ E ∥U ∥2 ≤ R. 

For the second bound, we use the I-MMSE identity for the Gaussian channel: 

I(U ; U + η−1/2G) = 12

Z η

> 0

mmse( λ) dλ. 

Since mmse( λ) is nonincreasing in λ,

η

2 mmse( η) ≤ I(U ; U + η−1/2G).

Moreover, U is a measurable function of the discrete latent variable J, so by data processing, 

I(U ; U + η−1/2G) ≤ H(J).

Combining the last two inequalities yields mmse( η) ≤ 2H(J)

η ,

which proves the claim. 9Lemma 2 is used together with Lemma 1 to design the sampling grid. Indeed, the two bounds meet at 

α := 2H(J)

R .

Accordingly, in the proof of Theorem 1 we choose a hybrid grid in η: uniform on [ ηmin , α ], where the MMSE is bounded by R, and geometric on [ α, η max ], where the MMSE is bounded by 2 H(J)/η .Summing the corresponding MMSE areas then yields the stated discretization bound. 

Remark 3. If H(J) = 0 , then the latent variable J is deterministic. Hence U is deterministic and 

mmse( η) = E



U − E[U | U + η−1/2G] 2

= 0 

for every η > 0. Therefore the MMSE-area discretization term vanishes identically. In this case, the discretization contribution in Theorem 1 should be interpreted as zero. 

Remark 4. When ϵ = 0 , the Gaussian mixture model reduces to a discrete target distribution supported on the centers {μj }. Then Z = U , so the latent posterior mean Mt coincides with the usual data posterior mean mt. Hence the proposed sampler reduces to the usual posterior-mean freezing discretization, or equivalently the stochastic DDIM-type sampler with η = 1 in the Gaussian noising formulation. Theorem 1 therefore shows that the discretization error is controlled by the entropy of the discrete target. For vector-quantized latent representations with n tokens and alphabet size S, the latent index satisfies J ∈ [S]n, and hence 

H(J) ≤ n log S. 

Thus the discretization contribution is 

O

 H(J)

K (1 + log R)2



,

up to terminal-SNR logarithmic factors. In the worst case this gives 

O

 n log SK (1 + log R)2



.

The same interpretation applies to embedded language models: for a length-d sequence over a vo-cabulary of size S, one has J ∈ [S]d and 

H(J) ≤ d log S, 

so the worst-case discretization contribution is 

O

 d log SK (1 + log R)2



,

again up to terminal-SNR logarithmic factors. This differs from discrete diffusion samplers such as τ -leaping, whose KL step-complexity bounds are stated directly in terms of the vocabulary size. For example, [16] gives a standard τ -leaping rate 

eO

 d2SεKL 



,

10 improving the previous 

eO

 d2S2

εKL 



bound. By contrast, in the Gaussian diffusion setting considered here, the vocabulary-size depen-dence enters only through the entropy bound H(J) ≤ d log S. Thus, for Gaussian noising of em-bedded discrete data, the present bound gives a sharper vocabulary-size dependence than existing 

τ -leaping-type discrete diffusion guarantees. 

Remark 5. The Gaussian mixture model can be viewed as an abstraction of image distributions generated from compact discrete latent codes. For example, TiTok represents a 256 × 256 image using as few as 32 discrete tokens, with codebook size 4096 in the TiTok-L-32 configuration [23]. Hence the worst-case entropy of the latent code satisfies 

H(J) ≤ 32 log 4096 ≈ 266 .2

nats, or equivalently 384 bits. This is only a worst-case bound; the actual entropy may be much smaller if the token distribution is nonuniform. If a pixel-space image is modeled as the decoding of such a discrete latent code plus small con-tinuous reconstruction noise, then the resulting pixel-space law is naturally idealized as a Gaussian mixture. Each mixture component corresponds to one latent code, while the Gaussian perturbation captures local variability around the decoded image. In the TiTok-L-32 worst case, the discretization error is coarsely bounded by 

O

 266 

K (1 + log R)2



.

Thus the leading sampling complexity is governed by the entropy of the latent image code, rather than by the ambient pixel dimension. 

Remark 6. A related result is the dimension-free DDPM analysis of [11] for approximate Gaussian mixtures. In the exact finite GMM case, let Kmix denote the number of mixture components. Their TV discretization bound scales as 

eO

 log 2 Kmix 

K



,

up to additional logarithmic factors and score-estimation error. Their result is dimension-free and does not require component separation, but it is proved for finite isotropic GMMs under a component-mean growth condition, a specific DDPM schedule, and a clipped-score analysis. Our result is complementary: it replaces dependence on the number of mixture components by dependence on the entropy of the latent component. This can be strictly sharper for highly nonuni-form mixtures, since H(J) ≤ log Kmix and the inequality can be loose. It also allows countably many components whenever H(J) < ∞ and the second-moment assumption holds. Finally, although Theorem 1 is stated in KL divergence, Pinsker’s inequality gives the corre-sponding TV control 

T V (Pδ, bPT −δ) ≤

r 12 KL( Pδ∥ bPT −δ).

Thus, in low-entropy latent-mixture settings, the present bound can give a sharper dependence on mixture complexity. 

11 Limitations 

Our analysis is specific to Gaussian-mixture targets and to the latent-posterior freezing sampler studied here. The bound assumes finite latent entropy and a finite second moment, and the score-approximation term is imposed on the same grid used by the sampler. The result explains settings with compact discrete or countable latent structure, but it does not by itself show that arbitrary high-dimensional data distributions admit such a representation. 

# References 

[1] Ahmad Aghapour, Erhan Bayraktar, and Ziqing Zhang. “Entropy-Based Dimension-Free Convergence and Loss-Adaptive Schedules for Diffusion Models”. In: arXiv preprint arXiv:2601.21943 

(2026). [2] Joe Benton et al. “Nearly $d$-Linear Convergence Bounds for Diffusion Models via Stochastic Localization”. In: The Twelfth International Conference on Learning Representations . 2024. [3] Hongrui Chen, Holden Lee, and Jianfeng Lu. “Improved analysis of score-based generative modeling: User-friendly bounds under minimal smoothness assumptions”. In: International Conference on Machine Learning . PMLR. 2023, pp. 4735–4763. [4] Sitan Chen et al. “Sampling is as easy as learning the score: theory for diffusion models with minimal data assumptions”. In: The Eleventh International Conference on Learning Representations . 2023. [5] Giovanni Conforti, Alain Durmus, and Marta Gentiloni Silveri. “KL convergence guarantees for score diffusion models under minimal data assumptions”. In: SIAM Journal on Mathe-matics of Data Science 7.1 (2025), pp. 86–109. [6] Prafulla Dhariwal and Alexander Nichol. “Diffusion models beat gans on image synthesis”. In: Advances in neural information processing systems 34 (2021), pp. 8780–8794. [7] Patrick Esser, Robin Rombach, and Bjorn Ommer. “Taming transformers for high-resolution image synthesis”. In: Proceedings of the IEEE/CVF conference on computer vision and pat-tern recognition . 2021, pp. 12873–12883. [8] Dongning Guo, Shlomo Shamai, and Sergio Verd´ u. “Mutual Information and Minimum Mean-Square Error in Gaussian Channels”. In: IEEE Transactions on Information Theory 51.4 (2005), pp. 1261–1282. doi : 10.1109/TIT.2005.844072 .[9] Ulrich G Haussmann and Etienne Pardoux. “Time reversal of diffusions”. In: The Annals of Probability (1986), pp. 1188–1205. [10] Zhihan Huang, Yuting Wei, and Yuxin Chen. “Denoising diffusion probabilistic models are optimally adaptive to unknown low dimensionality”. In: Mathematics of Operations Research 

(2026). [11] Gen Li, Changxiao Cai, and Yuting Wei. “Dimension-free convergence of diffusion models for approximate Gaussian mixtures”. In: arXiv preprint arXiv:2504.05300 (2025). [12] Gen Li and Yuling Yan. “O(d/T) Convergence Theory for Diffusion Probabilistic Models under Minimal Assumptions”. In: The Thirteenth International Conference on Learning Rep-resentations . 2025. 12 [13] Gen Li et al. “A sharp convergence theory for the probability flow odes of diffusion models”. In: arXiv preprint arXiv:2408.02320 (2024). [14] Xiang Li et al. “Diffusion-lm improves controllable text generation”. In: Advances in neural information processing systems 35 (2022), pp. 4328–4343. [15] Jiadong Liang, Zhihan Huang, and Yuxin Chen. “Low-dimensional adaptation of diffusion models: Convergence in total variation”. In: arXiv preprint arXiv:2501.12982 (2025). [16] Yuchen Liang et al. “Discrete diffusion models: Novel analysis and new sampler guarantees”. In: arXiv preprint arXiv:2509.16756 (2025). [17] Peter Potaptchik, Iskander Azangulov, and George Deligiannidis. “Linear Convergence of Dif-fusion Models Under the Manifold Hypothesis”. In: Proceedings of Thirty Eighth Conference on Learning Theory . Vol. 291. Proceedings of Machine Learning Research. PMLR, 30 Jun–04 Jul 2025, pp. 4668–4685. [18] Robin Rombach et al. “High-resolution image synthesis with latent diffusion models”. In: 

Proceedings of the IEEE/CVF conference on computer vision and pattern recognition . 2022, pp. 10684–10695. [19] Jascha Sohl-Dickstein et al. “Deep unsupervised learning using nonequilibrium thermody-namics”. In: International conference on machine learning . pmlr. 2015, pp. 2256–2265. [20] Yang Song and Stefano Ermon. “Generative modeling by estimating gradients of the data distribution”. In: Advances in neural information processing systems 32 (2019). [21] Yang Song et al. “Score-Based Generative Modeling through Stochastic Differential Equa-tions”. In: International Conference on Learning Representations . 2021. [22] Aaron Van Den Oord, Oriol Vinyals, et al. “Neural discrete representation learning”. In: 

Advances in neural information processing systems 30 (2017). [23] Qihang Yu et al. “An image is worth 32 tokens for reconstruction and generation”. In: Ad-vances in Neural Information Processing Systems 37 (2024), pp. 128940–128966. 

# A Proof of Theorem 1 

The proof has three parts. First, the mismatch between the true initial law pT and the Gaussian prior contributes the initialization error R/ (2 T ). Second, Girsanov-type theorem decomposes the remaining pathwise KL into the discretization term and the approximation term, as in the standard diffusion analysis of [4]. Third, the discretization term is rewritten as an MMSE area functional in the regularized SNR variable and then bounded using the entropy of the latent mixture variable. We begin with the initialization error. The exact reverse process starts from the law of XT =

Z + √T G , while the implemented sampler starts from N (0 , T I d). By convexity of KL divergence, KL( pT ∥ N (0 , T I d)) ≤ EZ KL( N (Z, T I d) ∥ N (0 , T I d)) = E ∥Z∥2

2T = R

2T .

Therefore, by the chain rule for pathwise KL, the initialization mismatch contributes at most 

R/ (2 T ). Next, we compare the exact and approximate reverse path measures under the correct initial law. A direct application of Girsanov’s theorem is not automatic under our minimal assumptions. 13 Instead, we use the pathwise-KL statement appearing as Proposition 1 in [1] and as Proposition 3 in [2]: if 

EP

Z T −δ

> 0

∥∆s∥2 ds < ∞,

then KL( P ∥ eP ) ≤ 12 EP

Z T −δ

> 0

∥∆s∥2 ds. 

It therefore suffices to verify the required square-integrability. Since ∆s = As + Bs,

where, for s ∈ (sk−1, s k], 

As := MT −s(Ys) − Mtk−1 (Ysk−1 )

T − s + ϵ2 , Bs := Mtk−1 (Ysk−1 ) − cMtk−1 (Ysk−1 )

T − s + ϵ2 .

we have 

∥∆s∥2 ≤ 2 ∥As∥2 + 2 ∥Bs∥2 ,

and hence 

EP

Z T −δ

> 0

∥∆s∥2 ds ≤ 2Edisc + 2 EM

> apx

.

Appendix B shows that 

EM 

> apx

≤ E apx =

> K

X

> k=1

(γk − γk−1)Lx0 (γk−1) ≤ eapx .

and the bound proved below shows that Edisc < ∞. Therefore 

EP

Z T −δ

> 0

∥∆s∥2 ds < ∞,

so the above pathwise-KL bound applies. Finally, Appendix B shows that the energy decomposes as 

EP

Z T −δ

> 0

∥∆s∥2 ds = Edisc + EM

> apx

,

and that 

EM 

> apx

≤ E apx =

> K

X

> k=1

(γk − γk−1)Lx0 (γk−1) ≤ eapx .

Thus KL( P ∥ eP ) ≤ 12

 Edisc + EM

> apx

 ≤ 12 Edisc + 12 eapx .

It remains to control Edisc .Define the regularized SNR variable 

η := 1

t + ϵ2 .

14 For the latent channel Xt = U + √t + ϵ2G, define mmse( η) := E



U − E[U | U + η−1/2G] 2

.

We now choose a grid adapted to the two regimes of the MMSE envelope. Work in the regu-larized SNR variable 

η := 1

t + ϵ2 , ηmin := 1

T + ϵ2 , ηmax := 1

δ + ϵ2 .

By Lemma 2, mmse( η) ≤ min 



R, 2H(J)

η



.

The two bounds meet at 

α := 2H(J)

R .

Accordingly, the grid is chosen to be uniform in η below α and geometric in η above α. More precisely, for a parameter h ∈ (0 , α ], we use increments at most h on [ ηmin , α ] and, on [ α, η max ], a geometric ratio 

r := 1 + hα .

This choice makes the first geometric increment equal to h, matching the scale of the uniform part. 

Lemma 3. Fix h ∈ (0 , α ]. There exists a grid 

ηmin = η0 < η 1 < · · · < η N = ηmax 

such that 

N ≤

 (α − ηmin )+

h



+

 log +(ηmax /α )log(1 + h/α )



+ 2 

and 

Edisc ≤ hR 



2 + log +

ηmax 

α



.

Proof. We give a precise construction of the grid and then bound the discretization error by sum-mation by parts. To avoid notational distractions, we first treat the main case 

ηmin < α < η max .

The cases α ≤ ηmin and ηmax ≤ α are obtained by removing, respectively, the uniform or the geometric part of the construction. Fix h ∈ (0 , α ] and set 

r := 1 + hα .

Let 

M := 

 α − ηmin 

h



.

We define the uniform part of the grid by 

ηi := ηmin + ih, 0 ≤ i ≤ M − 1,

15 and then set 

ηM := α. 

Thus the first M − 1 increments are equal to h, while the last increment in the uniform part is 

ηM − ηM −1 = α − ηmin − (M − 1) h ∈ (0 , h ].

This last interval may be shorter than h, and we will account for this explicitly below. For the geometric part, let 

Q := 

 log( ηmax /α )log r



.

Starting from ηM = α, define 

ηM +j := αr j , 1 ≤ j ≤ Q − 1,

and finally set 

ηM +Q := ηmax .

Since Q is the first integer for which αr Q ≥ ηmax , the final geometric interval is possibly shortened, and all previous geometric intervals have the exact multiplicative ratio r. The total number of intervals is 

N = M + Q. 

Moreover, 

M ≤ α − ηmin 

h + 1 ≤ αh + 1 and 

Q ≤ log( ηmax /α )log r + 1 .

Using log(1 + x) ≥ x/ (1 + x) with x = h/α , and using h ≤ α, we obtain log r = log 



1 + hα



≥ hα + h ≥ h

2α .

Hence 

Q ≤ 2αh log ηmax 

α + 1 .

Therefore 

N ≤ αh



1 + 2 log ηmax 

α



+ 2 .

We now bound the discretization error. Write 

mk := mmse( ηk), ∆k := ηk − ηk−1.

By the MMSE representation, 

Edisc =

> N

X

> k=1

Z ηk

> ηk−1

 mk−1 − mmse( η) dη. 

16 Since mmse is nonincreasing, mmse( η) ≥ mk, η ∈ [ηk−1, η k],

and therefore 

Edisc ≤

> N

X

> k=1

∆k(mk−1 − mk).

The summation-by-parts identity gives 

> N

X

> k=1

∆k(mk−1 − mk) = ∆ 1m0 + 

> N−1

X

> k=1

(∆ k+1 − ∆k)mk − ∆N mN .

Since the last term is nonpositive, we discard it. We now control the remaining terms carefully. On the uniform part, the increments are equal to h except possibly for the last interval ending at α. Hence the interior differences are zero until the shortened interval. The initial contribution is bounded by ∆1m0 ≤ hR, 

because ∆ 1 ≤ h and m0 ≤ R.There is one possible positive jump at the transition from the last uniform interval to the first geometric interval. Indeed, the first geometric interval has length 

αr − α = α(r − 1) = h, 

while the last uniform interval has length at most h. Therefore this transition contributes at most 

h mmse( α) ≤ hR. 

This is the term that must be included explicitly. It remains to control the increases inside the geometric part. For the nontruncated geometric intervals, 

ηM +j = αr j , j ≥ 0,

and the corresponding increments are ∆M +j+1 = ηM +j+1 − ηM +j = αr j (r − 1) .

Thus, for consecutive full geometric intervals, ∆M +j+2 − ∆M +j+1 = αr j (r − 1) 2.

At the endpoint ηM +j+1 = αr j+1 , the entropy bound on the MMSE gives 

mM +j+1 ≤ 2H(J)

αr j+1 .

Therefore each positive increase inside the geometric region is bounded by 

 ∆M +j+2 − ∆M +j+1 

mM +j+1 ≤ αr j (r − 1) 2 2H(J)

αr j+1 

= 2H(J)( r − 1) 2

r .

17 The final geometric interval may be shortened. A shortened final interval cannot create a larger positive increase than the corresponding full geometric interval, so the same bound applies to the last possible increase as well. The number of positive increases inside the geometric part is at most Q − 1. Since 

Q − 1 ≤ log( ηmax /α )log r ,

the total geometric contribution is at most 2H(J)( r − 1) 2

r

log( ηmax /α )log r .

Using log r ≥ r − 1

r ,

we get (r − 1) 2

r log r ≤ r − 1.

Hence the geometric contribution is bounded by 2H(J)( r − 1) log ηmax 

α .

Since r − 1 = h/α and α = 2 H(J)/R , this becomes 

hR log ηmax 

α .

Combining the initial uniform contribution, the transition contribution, and the geometric con-tribution yields 

Edisc ≤ hR 



2 + log ηmax 

α



.

This proves the desired bound in the case ηmin < α < η max .If ηmax ≤ α, the geometric part is absent. The same argument gives 

Edisc ≤ hR, 

which is stronger than the displayed bound with the logarithmic term removed. If α ≤ ηmin , the uniform part is absent and the same geometric argument gives 

Edisc ≤ hR log ηmax 

ηmin 

≤ hR log ηmax 

α ,

using ηmin ≥ α. Thus, in all cases, the hybrid-grid estimate holds after replacing the logarithm by log +(ηmax /α ) and allowing the universal constant in front of the nonlogarithmic term. We now convert the h-dependent estimate into a K-step estimate. Set 

ℓ := log +

ηmax 

α , L := 2 + ℓ. 

18 The proof of Lemma 3 gives, for every h ∈ (0 , α ], a hybrid grid with 

Edisc ≤ hRL 

and with number of intervals bounded by 

N ≤ αh (1 + 2 ℓ) + 2 .

Since 1 + 2 ℓ ≤ 2(2 + ℓ) = 2 L, we have 

N ≤ 2αL h + 2 .

Assume now that 

K ≥ 4L. 

Choose 

h := 4αL K .

Then h ≤ α, so the construction in Lemma 3 applies. Moreover, 

N ≤ 2αL h + 2 = K

2 + 2 ≤ K, 

where the last inequality follows from K ≥ 4. Thus the constructed grid uses at most K intervals. If it uses fewer than K intervals, we refine the grid by adding arbitrary intermediate points. This cannot increase Edisc , since splitting an interval only replaces one left-endpoint MMSE area by the sum of two smaller such areas. Hence there exists an exact K-step grid with the same upper bound. Substituting the chosen value of h into the discretization estimate gives 

Edisc ≤ hRL = 4αR K L2.

Since 

α = 2H(J)

R ,

we obtain 

Edisc ≤ 8H(J)

K



2 + log +

ηmax 

α

2

.

Equivalently, 

Edisc ≤ 8H(J)

K



2 + log +

R η max 

2H(J)

2

.

Combining this estimate with the KL decomposition and Assumption 2 yields KL( Pδ ∥ bPT −δ) ≤ R

2T + 4H(J)

K



2 + log +

R η max 

2H(J)

2

+ 12 eapx .

This is the finite-step form of the almost dimension-free bound. In particular, the discretization contribution to the KL error scales as 

H(J)

K



2 + log +

R η max 

H(J)

2

up to a universal numerical constant. 19 B Deferred proof of the pathwise decomposition 

It remains only to justify the orthogonality of the two error terms and the x0-prediction represen-tation of Eapx . We write the reverse-time variable as 

s := T − t, 

and define the increasing filtration 

Gs := FT −s = σ(Xu : T − s ≤ u ≤ T ).

Thus ( Gs)0≤s≤T −δ records the information revealed as the reverse process moves from time T down to time δ.Since 

Mt(Xt) = E[U | F t],

the process 

Ns := MT −s(XT −s)is a Doob martingale with respect to the increasing filtration ( Gs)s≥0. Let sk := T − tk. For 

s ∈ (sk−1, s k], the discretization error is the martingale increment 

As = MT −s(XT −s) − Mtk−1 (Xtk−1 ) = Ns − Nsk−1 .

Hence 

E[As | G sk−1 ] = 0 .

On the other hand, the approximation error term Bs is evaluated at the left endpoint tk−1, and is therefore 

Gsk−1 -measurable .

Consequently, 

E⟨As, B s⟩ = E  E[As | G sk−1 ], B s

 = 0 .

Integrating over s yields 

E

Z T −δ

> 0

⟨As, B s⟩ ds = 0 .

and thus 

EP

Z T −δ

> 0

∥∆s∥2 ds = Edisc + EM

> apx

.

For the approximation term, the pathwise decomposition first gives 

EM 

> apx

:= 

> K

X

> k=1

Z sk

> sk−1

E



Mtk−1 (Ysk−1 ) − cMtk−1 (Ysk−1 ) 2

(T − s + ϵ2)2 ds. 

We now relate this quantity to the x0-prediction loss. Since 

Mt(x) = x + ( t + ϵ2)∇ log pt(x), mt(x) = x + t∇ log pt(x),

20 and cMt(x) = x + ( t + ϵ2)bst(x), bmt(x) = x + tbst(x),

we have, for every t > 0, 

Mt(x) − cMt(x) = t + ϵ2

t

 mt(x) − bmt(x).

Using Ysk−1 ∼ Xtk−1 under the exact reverse process, 

EM 

> apx

=

> K

X

> k=1

 tk−1 + ϵ2

tk−1

2

Lx0 (γk−1)

 1

tk + ϵ2 − 1

tk−1 + ϵ2



.

Since tk ≤ tk−1, we claim that 

 tk−1 + ϵ2

tk−1

2  1

tk + ϵ2 − 1

tk−1 + ϵ2



≤ 1

tk

− 1

tk−1

.

Indeed, the left-hand side can be rewritten as 

 tk−1 + ϵ2

tk−1

2  1

tk + ϵ2 − 1

tk−1 + ϵ2



=

 tk−1 + ϵ2

tk−1

2 tk−1 − tk

(tk + ϵ2)( tk−1 + ϵ2)= (tk−1 − tk)( tk−1 + ϵ2)

t2

> k−1

(tk + ϵ2) .

On the other hand, 1

tk

− 1

tk−1

= tk−1 − tk

tktk−1

.

Therefore the desired inequality is equivalent, after cancelling the nonnegative factor tk−1 − tk, to 

tk−1 + ϵ2

t2

> k−1

(tk + ϵ2) ≤ 1

tktk−1

.

Multiplying by the positive quantity tkt2

> k−1

(tk + ϵ2), this is equivalent to 

tk(tk−1 + ϵ2) ≤ tk−1(tk + ϵ2).

After expanding and cancelling tktk−1, this becomes 

tkϵ2 ≤ tk−1ϵ2,

which follows from tk ≤ tk−1 and ϵ2 ≥ 0. Hence 

 tk−1 + ϵ2

tk−1

2  1

tk + ϵ2 − 1

tk−1 + ϵ2



≤ 1

tk

− 1

tk−1

= γk − γk−1.

Therefore, 

EM 

> apx

≤

> K

X

> k=1

(γk − γk−1)Lx0 (γk−1) = Eapx ≤ eapx ,

where the last inequality follows from Assumption 2. 21 C Proof of Lemma 1 

Proof. We give a slightly more explicit construction of the Gaussian channel in the regularized SNR variable. Write 

η = 1

t + ϵ2 .

Equivalently, the forward observation 

Xt = U + pt + ϵ2 G

has the same conditional law, given U , as 

U + η−1/2G. 

Thus define 

Mη := E

h

U | U + η−1/2G

i

, mmse( η) := E

h

∥U − Mη∥2i

.

With this notation, 

Mt(Xt) = Mη, η = ( t + ϵ2)−1.

To justify the martingale identity used below, it is convenient to realize the Gaussian channel on a common probability space. Let ( Wη)η≥0 be a standard Brownian motion in Rd, independent of U , and define 

Yη := ηU + Wη.

Then Yη

η = U + η−1 Wη d

= U + η−1/2G. 

Moreover, for fixed η, Yη is a sufficient statistic for U among the observations {Yλ : 0 ≤ λ ≤ η}.Indeed, conditional on U = u, the likelihood of the path up to time η depends on u only through exp 



⟨u, Y η⟩ − η

2 ∥u∥2

.

Hence, if 

Fη := σ(Yλ : 0 ≤ λ ≤ η),

then 

E[U | F η] = E[U | Yη] = E

h

U | U + η−1/2G

i

= Mη.

Therefore ( Mη)η≥0 is a square-integrable martingale with respect to the increasing filtration ( Fη)η≥0.Now fix η ≥ ηk−1. Since Mηk−1 = E[U | F ηk−1 ] and Mη = E[U | F η], the martingale projection identity gives 

E

h

Mη − Mηk−1

> 2

i

= E ∥Mη∥2 − E Mηk−1 

> 2

.

On the other hand, by the orthogonality principle for conditional expectation, mmse( η) = E ∥U − Mη∥2 = E ∥U ∥2 − E ∥Mη∥2 .

Therefore 

E

h

Mη − Mηk−1

> 2

i

= E ∥Mη∥2 − E Mηk−1

> 2

= mmse( ηk−1) − mmse( η).

22 We now apply this identity to the discretization term. On the interval s ∈ (sk−1, s k], set 

t = T − s, η = 1

t + ϵ2 .

Since tk = T − sk, we have 

ηk = 1

tk + ϵ2 .

Also, 

dη = ds 

(T − s + ϵ2)2 .

Thus the contribution of the k-th interval is 

Z sk

> sk−1

E

h

MT −s(Ys) − Mtk−1 (Ysk−1 ) 2i

(T − s + ϵ2)2 ds 

=

Z ηk

> ηk−1

E

h

Mη − Mηk−1

> 2

i

dη 

=

Z ηk

> ηk−1

(mmse( ηk−1) − mmse( η)) dη. 

Summing over k = 1 , . . . , K gives 

Edisc =

> K

X

> k=1

Z ηk

> ηk−1

(mmse( ηk−1) − mmse( η)) dη, 

which proves the lemma. 23