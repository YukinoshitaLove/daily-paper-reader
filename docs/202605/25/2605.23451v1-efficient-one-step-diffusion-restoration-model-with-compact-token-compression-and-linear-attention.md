---
title: Efficient One-Step Diffusion Restoration Model with Compact Token Compression and Linear Attention
title_zh: 基于紧凑标记压缩与线性注意力的高效一步扩散复原模型
authors: "Bingtian Qiao, Yue Shi, Yingjie Zhou, Yong Guo, Guangtao Zhai, Jiezhang Cao"
date: 2026-05-22
pdf: "https://arxiv.org/pdf/2605.23451v1"
tags: ["query:real-ir"]
score: 10.0
evidence: 用于真实世界超分辨率的一步扩散恢复模型与令牌压缩
tldr: 真实世界图像超分辨率任务中，现有生成式方法继承高分辨率合成的稠密表示和平方成本全局建模，导致计算、内存和延迟随分辨率增加而不可控。本文提出一步扩散恢复模型，通过紧凑令牌压缩减少冗余，并结合线性注意力替代平方复杂度全局建模，在保持恢复先验的同时大幅提升效率。实验在多个真实超分辨率基准上取得优越的性能与速度平衡，为实用化部署提供了可能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.23451v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1438, \"height\": 542, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.23451v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1424, \"height\": 788, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.23451v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1431, \"height\": 1076, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.23451v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1433, \"height\": 440, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.23451v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1306, \"height\": 1940, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.23451v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1450, \"height\": 283, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.23451v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1439, \"height\": 713, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.23451v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1445, \"height\": 722, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.23451v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1448, \"height\": 720, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.23451v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1448, \"height\": 295, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.23451v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 788, \"height\": 217, \"label\": \"Table\"}]"
motivation: 用于真实世界超分辨率的一步扩散恢复模型与令牌压缩。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
真实世界图像超分辨率旨在从复杂且未知的真实退化中恢复高质量图像。然而，现有的生成式真实图像超分辨率方法大多沿用为高分辨率图像合成开发的稠密潜在表示和二次代价的全局建模范式，导致计算量、内存占用和推理延迟随分辨率增加而急剧增长，从而限制了实际部署。我们认为关键瓶颈不在于复原先验不足，而在于高分辨率复原过程中标记冗余过多和标记交互代价高昂。基于这一观察，我们从紧凑潜在表示和线性复杂度建模的角度重新审视真实图像超分辨率，并提出 SANA-SR，一种高效的一步复原框架。具体来说，SANA-SR 采用压缩比为 32 倍的深度压缩自编码器，在保留复原相关结构和纹理的同时大幅减少潜在标记。在此紧凑潜在空间之上，我们引入带 LoRA 微调的线性注意力 DiT，实现具有线性复杂度标记混合的高效高分辨率复原。在所有基准数据集上的大量实验表明，SANA-SR 在定量性能上与现有方法相比极具竞争力，且往往更优，同时能恢复出更清晰、更逼真的纹理。此外，经过剪枝后，部署的模型运行时间仅为 0.019 秒，计算量为 407.95G MACs，参数量为 344M，凸显了其在实际移动端部署的巨大潜力。

## Abstract
Real-world image super-resolution aims to recover high-quality images from complex and unknown real-world degradations. However, existing generative Real-ISR methods largely inherit the dense latent representations and quadratic-cost global modeling paradigm developed for high-resolution image synthesis, causing computation, memory usage, and inference latency to scale unfavorably with resolution and thus limiting practical deployment. We argue that the key bottleneck lies not in insufficient restoration priors, but in excessive token redundancy and costly token interactions during high-resolution restoration. Motivated by this observation, we revisit Real-ISR from the perspectives of compact latent representation and linear-complexity modeling, and propose SANA-SR, an efficient one-step restoration framework. Specifically, SANA-SR employs a deep compression autoencoder with a 32x compression ratio to drastically reduce latent tokens while preserving restoration-relevant structures and textures. On top of this compact latent space, we introduce a linear-attention DiT with LoRA fine-tuning, enabling efficient high-resolution restoration with linear-complexity token mixing. Extensive experiments on all benchmark datasets demonstrate that SANA-SR achieves highly competitive and often superior quantitative performance against existing methods, while restoring clearer and more realistic textures. Moreover, after pruning, the deployed model runs in 0.019s with 407.95G MACs and 344M parameters, highlighting its strong potential for practical mobile deployment.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：真实世界图像超分辨率（Real‑ISR）的生成式方法普遍继承高分辨率图像合成的稠密潜在表示与二次代价的全局建模（如扩散模型的平方复杂度注意力），导致计算、显存和推理时间随分辨率急剧增长，难以在移动端等资源受限场景部署。
- **关键洞察**：瓶颈不在于复原先验不足，而在于高分辨率复原过程中**标记冗余过大**和**标记交互代价过高**。因此需要从紧凑潜在表示和线性复杂度建模两方面重新设计 Real‑ISR 框架。
- **目标**：在保持甚至提升复原质量的前提下，构建**高效的一步推理框架**，实现低延迟、低计算量和合适的参数量，便于实际部署。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 2.1 退化稳定的紧凑潜在表示
- 使用预训练的 SANA 深度压缩自编码器（DC‑AE），压缩比 **32 倍**，对低质量（LQ）输入和高质量（HQ）目标分别得到潜在表示 zL 和 zH。
- 编码器冻结，在紧凑空间中进行复原，大幅减少后续 transformer 处理的空间标记数（例如 512×512 图像仅产生 **256 个潜在标记**），相比现有 4×/8× 压缩的方法显著降低标记数。

### 2.2 退化感知提示驱动的一步线性 DiT
- **提示构建**：从 LQ 图像提取语义标签（如 `tree`, `leaf`），与固定质量后缀（“clean, sharp, best quality…”）拼接，并屏蔽残余退化词语，通过冻结的文本编码器得到提示嵌入 `c` 和注意力掩码 `m`。
- **一步更新**：以冻结的预训练 SANA LinearDiT（线性注意力 DiT）为基础，通过 **LoRA** 在自注意力、交叉注意力和 FFN 层注入可训练低秩矩阵。
- **线性注意力**：采用非负特征映射 ϕ(·) 将查询 Q、键 K、值 V 映射后，计算 `LA(Q,K,V) = ϕ(Q)(ϕ(K)⊤V) / (ϕ(Q)(ϕ(K)⊤1) + ε)`，复杂度 O(N) 而非 O(N²)。
- **推理形式**：固定生成时间步 τg，预测潜在残差 `ẑ = zL – σ_{τg} fθ(zL, τg, c, m)`，然后由冻结解码器 D 输出超分图像。整个过程仅需一次前传，无需迭代扩散。

### 2.3 冻结先验对齐与适配器一致性
- **对齐损失 L_align**：将恢复后的潜在 ẑ 与真实潜在 zH 用同高斯噪声扰动，送入冻结骨干 f0，匹配两者响应的逐通道高斯统计量（均值与方差），使更新步靠近预训练先验。
- **一致性损失 L_cons**：比较 f0 与 LoRA 版 fθ 在扰动恢复潜在上的输出均方误差，防止适配器偏离冻结先验过远。
- **图像重建损失 L_rec**：结合像素 L2 损失和 LPIPS 感知损失。
- 最终总损失：`L = L_rec + λ_a L_align + λ_c L_cons`。仅优化 LoRA 参数。

### 2.4 提示感知的结构化剪枝
- 训练后合并 LoRA 回骨干，获得稠密模型。
- 使用与训练任务一致的损失（不含 L_cons）进行 K 步校准，对每个参数计算对角 Fisher 代理曲率 `F_i = 1/K Σ ω(t_k) (∂L_cal/∂w_i)²`。
- 定义块级重要性 `S_ℓ = Σ_{w_i∈B_ℓ} w_i² / (F_i + ε_p)`。
- 在参数总量不超过部署预算的前提下，按 S_ℓ 降序选择保留的 transformer 块，并强制保留首尾块。
- 剪枝后获得压缩的一步推理模型，保持与剪枝前相同的推理路径。

## 3. 实验设计：使用了哪些数据集/场景，它的 benchmark 是什么，对比了哪些方法

### 3.1 训练与评估数据集
- **训练**：混合 DIV2K（800 张）、Flickr2K（~2.6K）、LSDIR（~85K）、FFHQ（10K）的高清图，通过 Real‑ESRGAN 退化管道在线合成 LQ-HQ 对；也可选加入 RealSR 训练图像。
- **测试**：
  - **DIV2K‑Val**（合成退化场景）
  - **RealSR**（真实世界退化）
  - **DRealSR**（多摄像机真实退化，93 对 LR-HR）

### 3.2 评价指标
- 像素保真度：PSNR、SSIM（Y 通道）
- 无参考感知质量：MANIQA、MUSIQ、CLIPIQA、NIQE
- 感知相似性：LPIPS、DISTS
- 效率指标：推理时间、MACs（乘加运算数）、参数量

### 3.3 对比方法
分为两大类，涵盖广泛：
- **先验驱动的多步/灵活步方法**：StableSR、DiffBIR、SeeSR、PASD、ResShift、SUPIR、DreamClear、InvSR、LinearSR。
- **高效/一步方法**：SinSR、OSEDiff、S3Diff、AddSR、D3SR、FLUX‑SR、AdcSR、TSD‑SR。
- 所有对比采用官方检查点和默认推理设置，保持测试集、放大倍数、图像尺寸处理和评价脚本一致。

## 4. 资源与算力：如果文中有提到，请总结使用了多少算力

- **训练硬件**：8 块 NVIDIA RTX 4090 GPU，FP16 混合精度。
- **训练步数**：100K 步 AdamW，批次大小 4，输入裁剪 512×512 随机块。
- **其他细节**：未具体说明训练总时长，但给出了主要超参数与优化配置。推理速度在单块 RTX 4090 上测得，剪枝后模型 **0.019 秒/** 张。

## 5. 实验数量与充分性：大概做了多少组实验，这些实验是否充分、是否客观、公平

- **主要定量对比**：在 DIV2K‑Val、RealSR 和 DRealSR 三个数据集上，与 **20+ 种** 最近代表方法进行了全面比较，指标多维（保真、感知、效率）。
- **消融实验**：对提示设计（LQ vs. HQ vs. HQ+质量后缀 vs. DAPE）、训练目标各组份（L_align、L_cons、LPIPS）、生成时间步 τg 和分辨率先验（512 vs. 1024）均进行了控制变量分析，可视化清晰。
- **压缩/剪枝实验**：对比了全模型、尾部剪枝、随机剪枝、非结构化剪枝与本文提示感知剪枝的参数与性能。
- **效率对比**：单独表格列出不同方法的步数、推理时间、MACs 和参数量。
- 实验设计**公平**：统一评价协议，图像尺寸处理相同，公平比较了不同类别方法。消融充分，论证了每个模块的有效性。

## 6. 论文的主要结论与发现

- SANA‑SR 通过 **32 倍潜在压缩** 和 **线性注意力 DiT**，将 Real‑ISR 的计算瓶颈从高分辨率标记交互转移到紧凑空间的 O(N) 建模。
- 在合成和真实基准上，SANA‑SR 实现了**极具竞争力的定量性能**，尤其在感知质量（MUSIQ、CLIPIQA）和纹理清晰度上表现出色，同时保真度（PSNR/SSIM）领先多数方法。
- 剪枝后模型推理时间 **0.019 秒**、407.95G MACs，在相同或更低计算量和参数量下超越其他一步方法（如 AdcSR），展现出对移动端部署的强适应性。
- 消融证明：退化稳定提示、冻结先验对齐和适配器一致性是维持一步更新稳定性的关键；提示感知剪枝优于朴素剪枝策略。

## 7. 优点：方法或实验设计上有哪些亮点

- **问题视角新颖**：从标记冗余和交互成本切入，而非仅减少扩散步数，为 Real‑ISR 效率优化提供了新方向。
- **方法简洁且系统**：将紧凑自编码器、线性注意力、LoRA、先验对齐/一致性和提示感知剪枝有机结合，形成端到端的高效框架，各部分作用清晰。
- **效率与质量的突出平衡**：在比许多多步方法快几个数量级的同时，保持甚至超越其感知质量，并在参数和计算量上优势明显。
- **实验全面扎实**：覆盖三个基准、20+ 对比方法、多个维度的指标，消融和剪枝对比充分，公平性好。
- **部署友好**：给出剪枝后的实际 MACs、时延和参数量，验证了移动端潜力。

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **退化场景局限**：训练和合成测试主要基于 Real‑ESRGAN 退化管道；对更广泛、更极端的真实世界退化（如超低光、运动模糊极端情形）未单独验证，可能存在偏差。
- **推理平台差异**：0.019 s 是 RTX 4090 上的结果，移动端 NPU 等边缘硬件性能未实际测试，且未讨论量化加速。
- **放大倍数单一**：仅专注于 4 倍超分，更高倍（8 倍、16 倍）可能需要架构调整，文中未涉及。
- **依赖预训练模型**：方法基于冻结的 SANA DC‑AE 和 LinearDiT 预训练先验，若替换先验需重新评估其压缩器的退化稳定性。

（完）
