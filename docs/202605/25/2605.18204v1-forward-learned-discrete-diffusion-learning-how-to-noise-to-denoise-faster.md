---
title: "Forward-Learned Discrete Diffusion: Learning how to noise to denoise faster"
title_zh: 前向学习的离散扩散：学习如何加噪以实现更快去噪
authors: "Grigory Bartosh, Teodora Pandeva, Sushrut Karmalkar, Javier Zazo"
date: 2026-05-18
pdf: "https://arxiv.org/pdf/2605.18204v1"
tags: ["query:real-ir"]
score: 9.0
evidence: 提出前向学习离散扩散以实现高效少步生成
tldr: 离散扩散模型因反向过程常用因子化分布，需大量采样步骤才逼近目标，计算效率低。FLDD创新地让前向加噪过程可学习，采用非马尔可夫设计并联合优化边际与后验分布，使因子化生成模型仍能精确匹配目标。在文本、图像等基准上，相同步数下FLDD生成样本质量显著优于传统方法，为少步高效离散扩散提供直接方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.18204v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 853, \"height\": 328, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.18204v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1427, \"height\": 702, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.18204v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1461, \"height\": 546, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.18204v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1456, \"height\": 337, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.18204v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 997, \"height\": 518, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.18204v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1416, \"height\": 570, \"label\": \"Table\"}]"
motivation: 离散扩散模型需长采样链，计算昂贵，且因子化反向过程难以用少量步数学习目标分布。
method: 提出FLDD，学习非马尔可夫前向过程的边际与后验分布，端到端训练，使因子化生成匹配目标。
result: 在多个基准上，给定相同采样步数，FLDD生成质量显著超越传统离散扩散模型。
conclusion: 可学习的前向过程有效缩小分布差距，实现更高效的少步离散扩散生成。
---

## 摘要
离散扩散模型是一类强大的生成模型，在多个领域展现出卓越性能。然而，为了提高效率，离散扩散通常用因式分布来参数化生成（逆向）过程，这使得模型难以在少量步骤内学习目标过程，需要冗长且计算昂贵的采样过程。为缩小目标分布与模型分布之间的差距并实现少步生成，我们提出了前向学习的离散扩散（FLDD），它引入了一种具有可学习前向（加噪）过程的离散扩散。我们不再固定一条马尔可夫前向链，而是采用一种具有可学习边缘分布和后验分布的非马尔可夫公式。这使得生成过程保持因式化，同时能够匹配由加噪过程所定义的目标。我们在标准变分目标下进行端到端的参数训练。在多个基准测试上的实验表明，对于给定的采样步数，我们的方法在使用相同逆向参数化的情况下，能生成比传统离散扩散模型质量更高的样本。

## Abstract
Discrete diffusion models are a powerful class of generative models with strong performance across many domains. For efficiency, however, discrete diffusion typically parameterizes the generative (reverse) process with factorized distributions, which makes it difficult for the model to learn the target process in a small number of steps and necessitates a long, computationally expensive sampling procedure. To reduce the gap between the target and model distributions and enable few-step generation, we propose Forward-Learned Discrete Diffusion (FLDD), which introduces discrete diffusion with a learnable forward (noising) process. Rather than fixing a Markovian forward chain, we adopt a non-Markovian formulation with learnable marginal and posterior distributions. This allows the generative process to remain factorized while matching the target defined by the noising process. We train all parameters end-to-end under the standard variational objective. Experiments on various benchmarks show that, for a given number of sampling steps, our approach produces a higher quality samples than conventional discrete diffusion models using the same reverse parameterization.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
离散扩散模型虽已在多个领域取得良好性能，但其生成（逆向）过程通常采用因子化分布（各维度独立采样），导致模型的表达能力受限，难以在少量步骤内逼近真实扩散后验分布。这迫使离散扩散需要较长的采样链（步数常接近数据维度），导致推理计算昂贵、速度慢。本工作旨在缩小目标分布与模型参数化分布之间的差距，**让扩散模型在极少的采样步数下也能生成高质量样本**。为此，论文提出**前向学习的离散扩散（FLDD）**，核心思想是反过来学习前向加噪过程，使加噪过程自身所产生的中间分布恰好能被简单的因子化生成模型精确匹配，从而无需改变生成架构即可实现高效少步采样。

### 2. 方法论：核心思想、关键技术细节与流程
FLDD 的关键创新在于将离散扩散的前向过程从固定的马尔可夫链改为**可学习的非马尔可夫过程**，并端到端地与生成模型联合优化。

- **前向过程参数化**
  - 前向过程的边缘分布 \( q_\phi(z_t | x) \) 被设计为与生成模型一样的**因子化分布**：每个坐标独立从分类分布采样，但该分类分布的参数依赖于完整的数据样本 \( x \) 和时间步 \( t \)。
  - 前向后验分布 \( q_\phi(z_s | z_t, x) \) 采用一种非参数方法——**最大耦合（Maximum Coupling）** 进行构造，确保其与边缘分布一致且可高效计算。该做法使每个坐标的前向轨迹在给定 \( x \) 时条件独立，但总体上仍具有丰富依赖。
  - 通过适当的重参数化，强制满足边界条件 \( q_\phi(z_0 | x) = \delta(z_0 - x) \) 和 \( q_\phi(z_T | x) = p(z_T) \)。

- **训练优化**
  - 沿用标准扩散变分下界目标，即最小化逆向过程与前向真实后验间的 KL 散度。
  - 由于前向过程涉及离散采样，无法使用重参数化技巧，初始训练阶段采用 **Concrete 分布（Gumbel-Softmax）** 进行连续松弛预热（温度从 1 指数衰减至 10⁻³）。
  - 预热结束后，改用 **REINFORCE** 方法得到无偏梯度估计，继续优化前向参数 \( \phi \) 与逆向参数 \( \theta \)。
  - 算法流程：每轮迭代采样数据 \( x \)、时间步 \( t \) 和 \( s = t-1 \)，计算前向参数 \( u_s, u_t \)，采样 \( z_t \)，构造后验参数 \( u_{s|t} \)，计算逆向输出 \( v_{s|t} \)，最后基于 KL 散度利用 REINFORCE 更新模型。

- **生成过程**
  - 生成（逆向）过程与普通扩散完全相同，保持因子化分布 \( p_\theta(z_s | z_t) = \prod_i \text{Cat}(z_i^s; v_i^\theta(z_t, t)) \)，不需要前向网络参与，因此**推理时无额外计算开销**。

### 3. 实验设计：数据集、基准与对比方法
实验覆盖了合成数据、图像、文本和分子图等多个领域，以验证 FLDD 在不同场景下的少步生成能力。

- **合成数据**
  - 二维坐标分类（50×50）上的**两簇高斯混合**，展示两步骤内模型如何学习生成结构。
- **文本生成**
  - 数据集：**ROCStories**。
  - 对比方法：GPT-2、GPT-Neo、AR-Diffusion、DiffuSeq、SeqDiffuSeq、TESS、SEDD、LD4LG、COSMOS 等。
  - 指标：MAUVE、PPL、Diversity。
  - FLDD 测试了 \( T=100 \) 和 \( T=10 \) 两种步数，并与上述基线比较。
- **分子生成**
  - 数据集：**QM9** 和 **ZINC250k**。
  - 对比方法：MoFlow、EDP-GNN、GraphEBM、GDSS、Digress、Flow Matching、Dirichlet FM、CatFlow、LO-ARM 等。
  - 指标：Validity、Uniqueness、FCD。
  - 同样测试 \( T=100 \) 和 \( T=10 \)。
- **可学习掩码扩散**
  - 数据集：**二值化 MNIST**。
  - 比较了传统掩码扩散与 FLDD（前向仅学掩码概率）在 4 步生成下的图像质量差异。
- **对比基准的共同点**：所有实验均保证逆向过程使用**相同的因子化参数化架构**，仅前向过程有差异，确保公平性。

### 4. 资源与算力
论文**未明确说明**所使用的 GPU 型号、数量及具体训练时长。仅提及训练细节：使用 AdamW 优化器，学习率 \( 2 \times 10^{-4} \)，Concrete 分布预热 \( 10^5 \) 步，随后 REINFORCE 优化 \( 10^5 \) 步（对于分子等任务可能有所调整）。因此，无法从现有信息中获取算力消耗的精确数据。

### 5. 实验数量与充分性评估
- **实验组数**：论文至少进行了 4 类不同数据模态的实验（合成、文本、分子×2、图像），并在每个任务上均设置了 \( T=100 \) 和 \( T=10 \) 两步数实验，总计超过 10 组直接比较实验。
- **充分性与公平性**：
  - 选用了多个领域的常用基准，对比方法涵盖经典自回归模型以及主流的离散扩散模型，内容较全面。
  - 逆向过程参数化与网络结构保持一致，实验对照设计**公平**。
  - 论文指出自身参数化与超参可能未调至最优，部分基线在某些指标上略优于 FLDD，这一点诚实地反映了方法当前的局限，而非实验不充分。
  - 总体看，实验设计合理，能够有效证明 FLDD 在少步生成上的优势及广泛适用性，但缺少对更少步数（如 \( T=2,4 \)）的系统消融，或对 REINFORCE 方差影响的分析实验，略微降低了彻底性。

### 6. 论文的主要结论与发现
- FLDD 成功实现了**可学习的前向加噪过程**，能够在保持生成模型高效率因子化参数化不变的情况下，显著提升在少量步骤（如 \( T=10 \)）下的生成质量。
- 在 \( T=100 \) 的常规设置下，FLDD 能达到与强基线（如 LD4LG、CatFlow）相当的水平；当步数降至 \( T=10 \) 时，传统扩散模型几乎无法生成合理样本，而 FLDD 的质量仅轻微下降，展现了极佳的少步生成能力。
- 在掩码扩散实验中，FLDD 能学习到**数据感知的非均匀掩码调度**，生成更自然的中间状态，而传统 MDM 因独立去掩码而失败。
- 整个方法不改变采样过程，计算开销与传统扩散相同，却带来了更好的质量-延迟权衡。

### 7. 优点（亮点）
- **问题切入精准**：直指离散扩散少步生成困难的本质（因子化逆向与复杂目标分布失配），并通过使前向过程去适应模型这一反向思路来解决。
- **设计巧妙且高效**：非马尔可夫前向配合因子化边缘与最大耦合后验，使端到端学习成为可能，且推理无开销。
- **兼容性强**：方法作为通用框架，可直接套用任何现有的因子化离散扩散逆向架构。
- **实验多样性强**：覆盖图像、文本、分子等多个差异化领域，证明了方法的通用性。
- **训练策略务实**：利用 Concrete 预热缓解 REINFORCE 初始方差过大问题，确保了端到端训练的稳定性。

### 8. 不足与局限
- **优化难度**：需引入 REINFORCE 进行梯度估计，该方法本身方差较高，可能限制模型在更复杂任务上的扩展性，论文也承认寻找更低方差估计器是未来方向。
- **参数数量翻倍**：前向网络与逆向网络通常使用相同架构，训练时参数量约为原来的两倍，虽然不影响推理速度，但增加了训练成本。
- **生成参数化次优**：论文明确提到，为保证因子化目标被匹配，逆向过程需直接预测 \( p_\theta(z_s|z_t) \) 而非先预测 \( \hat{x} \) 再套用后验，而这种直接预测在传统扩散中已知效果次优，可能限制了性能上限。
- **前向过程设计空间未充分探索**：当前仅给出了一种可行的前向参数化形式，或许存在更优的设计（如带部分自回归依赖、坐标间耦合等），论文将此留作开放问题。
- **理论分析薄弱**：缺少对为何可学习前向能导致因子化目标的更深入理论解释，或收敛性、最优性等分析。

（完）
