---
title: One-Step Distillation of Discrete Diffusion Image Generators via Fixed-Point Iteration
title_zh: 通过不动点迭代对离散扩散图像生成器进行一步蒸馏
authors: "Chaoyang Wang, Yunhai Tong"
date: 2026-05-20
pdf: "https://arxiv.org/pdf/2605.21484v1"
tags: ["query:real-ir"]
score: 9.0
evidence: 基于不动点迭代的离散扩散图像生成器一步蒸馏
tldr: 离散扩散模型虽图像合成效果出色，但推理需多步解码，速度慢。现有单步蒸馏方法或需额外训练辅助网络使计算加倍，或依赖复杂参数化导致优化割裂。本文提出固定点蒸馏（FPD），通过部分破坏学生草案并用教师一步修正构建局部目标，在连续特征空间使用多带宽漂移损失与直通估计器实现端到端优化。FPD在单步推理中取得接近多步教师的视觉保真度与结构对齐，显著超越现有离散蒸馏基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.21484v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1425, \"height\": 720, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.21484v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1439, \"height\": 370, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.21484v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1438, \"height\": 802, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.21484v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1455, \"height\": 821, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.21484v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1107, \"height\": 704, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.21484v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 571, \"height\": 282, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.21484v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 448, \"height\": 196, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.21484v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 665, \"height\": 238, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.21484v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 619, \"height\": 279, \"label\": \"Table\"}]"
motivation: 离散扩散模型推理需缓慢迭代解码，现有单步蒸馏方法引入双重计算或碎片化优化流程，亟需高效统一方案。
method: 提出FPD，构建部分破坏学生输出并教师单步精炼的修正路径，结合连续特征多带宽漂移损失和直通估计器，支持可选对抗损失。
result: 在类及文本条件生成任务上，FPD单步推理的视觉质量与结构一致性接近多步教师，明显优于现有离散蒸馏方法。
conclusion: FPD为离散扩散模型提供端到端的一步蒸馏框架，在保持生成质量的同时大幅提升效率，优于已有基线。
---

## 摘要
离散扩散模型在视觉合成方面表现出色，但依赖于缓慢的迭代解码。现有的一步蒸馏方法试图绕过这一瓶颈，要么通过训练辅助得分网络，其计算量实际上翻倍，要么通过引入专门的参数化和多阶段流水线，这会使优化变得零散。在本文中，我们引入了不动点蒸馏（FPD），这是一个端到端框架，通过部分破坏学生模型的一步草稿并使用单步教师模型进行精炼来构建局部校正目标。为了在语义有意义的空间中计算训练目标，我们将离散标记提升为连续特征，并应用多带宽漂移损失以迭代累积这些校正。为了通过离散瓶颈进行反向传播，我们采用直通估计器，在前向传播过程中将精确的硬采样标记馈送给教师模型和解码器，确保训练和推理在相同的码本流形上运行，同时将连续梯度路由回学生logits。这一完全可微的路径还容纳了一个可选的无条件对抗目标，以增强感知真实感。在类别和文本条件生成上的评估验证了我们框架的有效性。FPD在单步推理中实现了有竞争力的视觉保真度和结构对齐，缩小了与多步教师模型的差距，同时优于现有的离散蒸馏基线。

## Abstract
Discrete diffusion models excel at visual synthesis but rely on slow, iterative decoding. Existing single-step distillation methods attempt to bypass this bottleneck, either by training auxiliary score networks that effectively double compute, or by introducing specialized parameterizations and multi-stage pipelines that fragment optimization. In this paper, we introduce Fixed-Point Distillation (FPD), an end-to-end framework that constructs local correction targets by partially corrupting the student's one-step draft and refining it with a single teacher step. To compute the training objective in a semantically meaningful space, we lift discrete tokens into continuous features and apply a multi-bandwidth drift loss that iteratively accumulates these corrections. To backpropagate through the discrete bottleneck, we employ a straight-through estimator that feeds exact hard-sampled tokens to the teacher and decoder during the forward pass, ensuring that training and inference operate on the same codebook manifold, while routing continuous gradients back to the student logits. This fully differentiable pathway additionally accommodates an optional unconditional adversarial objective to enhance perceptual realism. Evaluations on both class- and text-conditional generation validate the effectiveness of our framework. FPD achieves competitive visual fidelity and structural alignment within a single inference step, narrowing the gap to multi-step teachers while outperforming existing discrete distillation baselines.

---

## 论文详细总结（自动生成）

## 论文核心问题与整体含义
- **研究动机**：离散扩散模型（如 MaskGIT, MaskGen）在图像合成上质量高，但推理依赖数十甚至上百步的迭代去噪，速度极慢，难以部署到低延迟场景。
- **现有蒸馏方法痛点**：
  - 基于辅助得分网络的方案：需额外在线学习一个与教师等大的网络，导致训练计算量翻倍。
  - 基于特殊参数化或多阶段管线的方案：要么增加单步计算量，要么将优化拆成多个离线阶段，流程割裂且通用性差。
  - 缺少一个**高效训练、通用且能一步生成**的离散蒸馏框架。
- **本文目标**：提出**不动点蒸馏 (FPD)**，将单步生成建模为固定点匹配问题，在不引入辅助网络、不拆分训练的前提下，实现离散扩散模型的端到端一步蒸馏。

## 论文提出的方法论

### 核心思想
把预训练多步教师视为状态依赖的局部精炼算子，训练学生时**只使用学生自己生成的草稿**：将学生一步输出部分掩码，再让教师执行单步校正，由此构造出与学生输出紧密耦合的修正目标。当学生的输出对该“破坏-精炼”循环近似不变时，即达到不动点，单步采样就等价于教师的多步生成。

### 关键技术细节

1. **生成流程与目标构造**
   - 学生接收高掩码率且混入随机码的初始化序列 `z_init`，一步推理输出完整离散标记草稿 `ẑ`。
   - 对 `ẑ` 以随机中间掩码率 `r` 进行部分掩码，送入冻结的教师模型进行**单步**去噪，得到精炼标记 `ẑ_T`。
   - 训练目标：最小化 `ẑ` 与 `ẑ_T` 在连续语义空间中的差异。

2. **连续空间漂移损失 (Lifted Drift Loss)**
   - 将离散标记通过冻结的 VQ 解码器和预训练视觉骨干（DINOv3）映射为连续空间特征 `X` (学生) 与 `Y` (教师)。
   - 在该空间内构造多带宽漂移向量 `V_h(X)`：同时包含向 `Y` 的吸引力，以及向批次内循环移位的负样本（表征学生边际分布）的排斥力。
   - 最终损失为学生特征与 `stop‑gradient(X + V_h(X))` 之间的均方误差，多个带宽取加权和（RMS归一化）。  
     公式示意：  
     `L_drift = Σ_{h∈H} (1/Z_h) E[ ‖ X − sg(X + V_h(X)) ‖² ]`

3. **直通估计器 (STE) 处理离散瓶颈**
   - **前向**：直接使用硬采样标记的码本嵌入 `E[ẑ]`，确保教师和解码器始终接收合法的离散输入，消除软松弛引起的分布外偏置。
   - **反向**：梯度通过软嵌入 `ẽ = Σ_v p_{θ,iv} E_v` 流回学生 logits，实现端到端可微。  
     具体实现：`e_STE = E[ẑ] + ẽ − sg(ẽ)`。

4. **可选对抗监督**
   - 利用上述可微通路，在解码后的学生图像上施加**无条件** GAN 损失 `L_gan`，增强高频细节与感知质量，总损失为 `L_total = L_drift + λ L_gan`。

5. **训练流程**
   - 教师、VQ编解码器、特征骨干均冻结，仅更新学生参数。
   - 学生每次迭代直接生成草稿，教师就地精炼，无需预收集多步轨迹。

## 实验设计

### 评估场景与数据集
- **类条件生成**：ImageNet 256×256，生成 50k 样本，使用 FID、IS、Precision、Recall。
- **文本到图像生成**：LAION 400K 子集，在 GenEval 基准上评估对象计数、颜色、位置、属性等对齐指标。

### 基准模型与对比方法
- **教师模型**：类条件用 MaskGIT，文本到图像用 MaskGen‑L（0.6B 参数）。
- **离散蒸馏基线**：SDTT、di4c、ReDi、DiMO。
- **连续蒸馏基线**：InstaFlow、SiD‑LSG、LCM、TDM、ADD、DMD2。
- 对比时标注了各方法是否依赖**辅助得分网络 (Aux. Score Net)**。

## 资源与算力
- 文中未明确交代所用 GPU 型号、数量及具体训练时长。
- 给出部分训练设置：优化器 AdamW，学习率 1e‑5；MaskGIT 批大小 32，MaskGen‑L 批大小 64。
- 所有蒸馏训练均冻结教师与 VQ 编解码器，仅更新学生。

## 实验数量与充分性
- **主要实验**：两组核心任务（类条件、文生图），各含多步教师、多种蒸馏方法对比（2 张表格）。
- **消融实验**：
  - 梯度路由策略 (STE vs. 软嵌入) 与对抗损失的影响 (1 表)。
  - 精炼草稿来源（学生自身 vs. 随机）(1 表)。
  - 漂移空间（像素 vs. 特征）与带宽选择 (1 表)。
  - 特征骨干的层选择与空间粒度 (全局池化 vs. 空间网格) (1 表)。
- **总计约 5‑6 组消融**，覆盖方法关键部件，对比公平，消融充分验证了每个设计选择的必要性。
- 客观性较好：使用标准公开基准和常见指标，对比涵盖离散与连续域方法。

## 主要结论与发现
1. FPD 无需辅助得分网络，即可在**单步推理**下取得极具竞争力的结果：
   - 类条件 ImageNet：FID 6.90 / IS 215，接近 16 步教师 (6.60 / 224)，超越多数多步离散蒸馏方案。
   - 文本到图像 GenEval 总分 0.45，优于同阶离散蒸馏 DiMO (0.42)，且大幅超越教师一步生成（直接崩溃）。
2. 消融实验证明：
   - **直通估计器 (STE)** 对保持教师评估分布一致性至关重要，软嵌入会导致伪影和性能下降。
   - **学生自产草稿**的耦合精炼是关键，若替换为随机序列则性能崩塌。
   - 在语义特征空间做多带宽漂移，配合多层空间网格，远优于像素空间或全局池化。

## 优点
- **方法简洁高效**：以不动点迭代代替复杂轨迹模仿或分布匹配，全程无需额外可学习网络，训练端到端。
- **训练‑推理一致性**：STE 保证前向使用硬令牌，避免训练时的分布偏移，也无需推理时后处理。
- **模块化设计**：特征空间漂移损失与对抗损失解耦，既可独立工作，也可灵活组合。
- **实验扎实**：在两类任务上验证，覆盖多基线，消融设计全面，包括路由、草稿源、表征空间等核心组件。

## 不足与局限
- **计算资源未报告**：没有提供 GPU 型号、数量及训练耗时，难以精确评估其实际训练效率。
- **模型与任务泛化性有限**：仅在 MaskGIT 和 MaskGen 两个特定架构上验证，未探索其他离散扩散骨干（如自回归类模型、语言域生成模型等）。
- **对比公平性细节**：文生图表格中包含参数规模差异较大的模型（如 SDXL‑Turbo 等），虽主要同参数对比是公平的，但整体最优仍相差较多，未充分讨论上限差距。
- **对抗训练的依赖**：消融显示加入 GAN 损失带来明显提升，但对抗训练的稳定性和对伪影的引入风险未深入讨论。
- **超参数敏感性**：多带宽、骨干层选择等均经消敛确定，但未报告对其它种子或稍大范围超参数的鲁棒性。

（完）
