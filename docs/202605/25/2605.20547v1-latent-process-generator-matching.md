---
title: Latent Process Generator Matching
title_zh: 隐过程生成器匹配
authors: "Lukas Billera, Hedwig Nora Nordlinder, Ben Murrell"
date: 2026-05-19
pdf: "https://arxiv.org/pdf/2605.20547v1"
tags: ["query:real-ir"]
score: 9.0
evidence: 提出具有潜在过程的扩散类生成模型的通用框架
tldr: 许多流匹配和扩散模型在训练时借助辅助随机动态定义条件目标，但辅助状态在生成时不可用或难以采样。现有Generator Matching理论仅处理静态潜变量，而近期工作仅针对特例。本文提出潜过程生成器匹配框架，将生成状态X_t建模为易处理马尔可夫潜过程Y_t的确定性映射Φ(Y_t)。证明可学习图像空间上的随机过程生成器，使其单时间边际分布与投影过程一致。该框架统一并推广了离散潜过程结果，将生成器匹配扩展至时间依赖潜条件过程族，为更丰富的动态生成模型奠基。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.20547v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1625, \"height\": 1119, \"label\": \"Figure\"}]"
motivation: 现有流匹配和扩散模型训练中依赖辅助动态但生成时难以使用，且Generator Matching仅支持静态潜变量，亟需扩展到时间相关潜过程。
method: 提出潜过程生成器匹配，将观测状态视为易处理马尔可夫潜过程的确定性映射，学习图像空间上的随机过程生成器。
result: 证明了所学生成器与投影过程具有相同的单时间边际分布，并推广了文献中离散潜过程的特例结果。
conclusion: 该框架将Generator Matching从静态潜变量推广至丰富的时间依赖条件过程族，统一并扩展了先前离散结果。
---

## 摘要
近年来，许多基于流匹配和扩散式的生成模型在训练时依赖辅助随机动态：模拟一个更丰富的过程来定义条件目标，但辅助状态要么在生成过程中难以采样，要么根本不是期望输出的一部分。现有的生成器匹配理论形式化了以静态隐随机变量为条件的设定，并且近期有几篇论文证明了特定增广状态构造下的投影结果的特例。我们提出了隐过程生成器匹配，一个通用框架，将观测到的生成状态视为可处理的马尔可夫过程Y_t的确定性映射X_t=Φ(Y_t)。我们证明，在此设定下，可以学习一个图像空间上的随机过程的生成器，该过程与投影过程具有相同的单时边际分布。这一框架推广并包含了文献中离散隐过程的结果，并将生成器匹配从静态隐变量扩展到一大家族时变隐条件过程。

## Abstract
Many recent flow-matching and diffusion-style generative models rely on auxiliary stochastic dynamics during training: a richer process is simulated to define conditional targets, but the auxiliary state is either intractable to sample at generation time or simply not part of the desired output. Existing Generator Matching theory formalises conditioning on static latent random variables, and several recent papers prove special cases of projection results for particular augmented-state constructions. We introduce latent process generator matching, a general framework that treats the observed generative state as a deterministic image $X_t=Φ(Y_t)$ of a tractable Markov process $Y_t$. We show that in this setting one may learn the generator of a stochastic process on the image space which has the same one-time marginal distributions as the projected process. This generalizes and subsumes the discrete latent process results from the literature, and extends Generator Matching from static latent variables to a rich family of time-dependent latent conditional processes.

---

## 论文详细总结（自动生成）

# 论文总结：Latent Process Generator Matching

## 1. 核心问题与整体含义
- **研究动机**：许多流匹配和扩散式生成模型在训练时引入辅助随机动态（如增广状态、编辑操作、分支结构等），但生成时这些辅助变量不可用或难以采样，需要将联合过程投影到输出空间。现有理论（如Generator Matching）仅处理静态潜变量条件，而最近的工作（如Edit Flows、Branching Flows等）分别针对特定离散潜变量构造证明了投影结果，缺乏统一框架。
- **整体含义**：本文提出一种通用框架，将生成过程建模为可处理的马尔可夫潜过程 \(Y_t\) 经过确定性映射 \(\Phi\) 后的投影 \(X_t = \Phi(Y_t)\)，并证明可以在投影空间上学习一个随机过程的生成器，使其单时边际分布与投影过程一致。该框架推广了所有已有的离散潜过程特例，并将Generator Matching从静态潜变量扩展到时间依赖的潜过程条件设定。

## 2. 方法论
- **核心思想**：对任意Polish空间上的Feller过程 \(Y_t\)（具有生成器 \(W_t\)）和可测映射 \(\Phi: Y \to X\)，定义投影后过程 \(X_t = \Phi(Y_t)\) 的边际生成器 \(L_t\) 为条件期望：
  \[
  L_t f(x_t) = \mathbb{E}[W_t(f \circ \Phi)(Y_t) \mid \Phi(Y_t) = x_t]
  \]
  证明该生成器满足Kolmogorov前向方程，使得投影过程的边际分布由 \(L_t\) 生成。
- **关键技术细节**：
  - 基于Generator Matching框架（Holderrieth et al., 2025），利用线性参数化表示生成器：\(L_t f(x) = \langle K_{t,x} f, F_t(x) \rangle\)，其中 \(K_{t,x}\) 编码结构（如差分或微分算子），\(F_t(x)\) 是可学习的参数（如速率或漂移）。
  - 定义**潜过程条件生成器**：对于每个 \(Y_t = y_t\)，定义点级条件生成器 \(L_t^{y_t} f(x_t) = W_t(f \circ \Phi)(y_t)\)，并假设其具有线性参数化形式，其中目标 \(F_t^{y_t}(x_t)\) 仅通过 \(x_t = \Phi(y_t)\) 依赖于 \(y_t\)。
  - **训练损失**：使用潜过程条件生成器匹配损失（Bregman散度），并证明其梯度等价于边际生成器匹配损失的梯度（定理3.12）。因此，可以仅用从 \(Y_t\) 采样获得的潜变量计算目标，训练只接收 \(X_t\) 的网络。
  - 进一步给出**双条件**视角：可同时以静态端点潜变量 \(z\) 和过程潜变量 \(y_t\) 为条件，提供实用的采样-训练流程。
  - **特例统一**：说明Edit Flows、Branching Flows等均为取 \(Y = X \times Z\) 且 \(\Phi\) 为投影的特例，其速率投影、条件目标构造等均可由统一框架直接导出。

## 3. 实验设计
- **本文主要为理论框架，未进行充分的实证验证**。
- 实验验证仅限于**一个一维示例**（3.7.1节），展示一个带有隐状态切换的扩散过程，其条件路径包含非连续的漂移方向切换，而学习的边际过程恢复平滑路径并使最终分布与目标一致。
- **没有使用标准数据集，也没有对比其他方法的实验**。论文仅证实了框架的可行性，并未评估其在复杂生成任务上的性能。

## 4. 资源与算力
- 文中**未提及任何GPU型号、数量、训练时长或计算资源**。一维示例的计算量微乎其微，无需特殊硬件。

## 5. 实验数量与充分性
- **实验极其有限**：仅包含一个一维玩具示例。没有消融实验，没有与其他潜变量生成模型（如Edit Flows、Branching Flows等）的定量比较，也没有在图像、蛋白质等高维数据上的验证。
- 因此，**实验的充分性和普遍性严重不足**，无法评估该方法在实际问题中的效果、效率或相对于特例方法的改进。

## 6. 主要结论与发现
- 提出并证明了**潜过程生成器匹配**框架，将Generator Matching理论从静态潜变量扩展到时间依赖的潜过程，涵盖连续、离散、流形等任意状态空间。
- 统一了多个近期工作（Edit Flows、OneFlow、Flowception、Branching Flows、Any-order masked diffusion）中关于投影和边际化的特例结果。
- 通过梯度等价定理保证：即使训练时依赖不可见的潜过程变量，仍能正确学习仅依赖观测状态 \(X_t\) 的生成器。
- 在一个一维示例中验证了框架的有效性。

## 7. 优点
- **理论贡献显著**：提供了一个严谨、通用的数学框架，填补了Generator Matching与时间依赖潜过程之间的理论空缺。
- **统一性强**：清晰地将多个独立工作归入同一理论体系，并给出简洁的推导。
- **适用性广**：允许多种状态空间（连续、离散、流形），支持非投影映射（如蛋白质骨架中的刚体运动），为未来灵活设计潜空间提供了理论基础。
- **证明清晰**：附录详细给出了测度论层面的严格证明，可重复性强。

## 8. 不足与局限
- **实验严重匮乏**：仅有一个一维演示，无法展示方法的实际价值，尤其是在高维、复杂数据上的表现。未与基线方法比较，无法判断是否有经验优势。
- **正则性条件可能难以满足**：文中指出KFE唯一性要求（Assumption A.19）可能被违反，并给出反例（光滑映射加布朗运动导致非唯一边际路径），但未讨论实际应用中如何检测或避免。
- **计算成本未知**：未讨论潜过程模拟和条件目标计算的额外开销，对于实际应用可能重要。
- **应用前景未落地**：虽然提出了蛋白质骨架生成的潜在应用，但并未实现，留作未来工作。
- **局限在理论阶段**：从当前版本来看，本文更像一篇理论纯论文，而非提供可即用的方法或模型。

（完）
