---
title: Rethinking Token Reduction for Diffusion Models via Output-Similarity-Awareness
title_zh: 通过输出相似性感知重新思考扩散模型的标记减少
authors: "Hangyeol Lee, Hyojeong Lee, Joo-Young Kim"
date: 2026-05-21
pdf: "https://arxiv.org/pdf/2605.22011v1"
tags: ["query:real-ir"]
score: 9.0
evidence: 提出面向扩散Transformer的输出相似性感知令牌缩减
tldr: 扩散变换器（DiTs）虽生成质量优异，但计算开销随令牌数二次增长，亟需高效令牌减少。现有方法仅依赖输入令牌相似性，忽略了生成任务的核心目标——最小化输出恢复误差，导致性能次优。本文重新思考令牌减少，提出输出相似性感知的DiTo范式，通过前步相似性引导匹配与多步重用，并设计PMR调度与频率感知匹配，大幅提升加速下的生成质量。实验表明DiTo在相似加速下PSNR提升1.6-3.9 dB，实现了更优的帕累托前沿，为生成模型效率优化提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.22011v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1198, \"height\": 634, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22011v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1256, \"height\": 439, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22011v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1268, \"height\": 315, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22011v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1251, \"height\": 527, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22011v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1213, \"height\": 865, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22011v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1264, \"height\": 542, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.22011v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1261, \"height\": 852, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.22011v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1272, \"height\": 672, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.22011v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1273, \"height\": 673, \"label\": \"Table\"}]"
motivation: 现有令牌减少方法仅关注输入相似性，与生成任务最小化输出恢复误差的目标错位，导致加速时质量下降。
method: 提出DiTo，利用前一时间步输出相似性代理当前步，在匹配步建立令牌对应并多步重用，结合PMR引导调度和频率感知匹配减少误差。
result: DiTo在相似加速下PSNR提升1.6-3.9 dB，超越现有方法，实现更优的质量-效率帕累托前沿。
conclusion: 输出相似性驱动的令牌减少有效弥合生成目标与效率优化的鸿沟，为扩散模型加速提供了新范式。
---

## 摘要
扩散Transformer（DiTs）实现了卓越的图像生成质量，但遭受与标记数量相关的二次计算复杂度。虽然已经提出了各种标记减少（TR）方法来减轻这一成本，但它们忽略了生成模型的主要目标：最小化恢复误差，这需要反映输出标记的相似性。它们仅依赖于从仅减少的ViT范式继承的输入标记相似性，导致与该目标根本上的不一致。为了弥补这一差距，我们提出了DiTo，这是一种新颖的TR范式，将重点转向以输出为中心的标记减少。基于观察到相邻时间步之间输出标记相似性始终保留的观察，DiTo利用先前步的相似性作为有效代理，在匹配时间步建立标记对应关系，然后在多个后续减少时间步中重复使用。为了优化这种交错调度，我们提出了基于成对匹配率（PMR）引导的间隔调度，以确定最佳匹配频率。此外，为了减轻重复重用导致的局部近似误差和由此产生的块状伪影，我们通过引入选择频率惩罚提出了频率感知的标记匹配。大量实验表明，DiTo始终优于现有的TR方法，在可比加速比下，PSNR高出1.6-3.9 dB，实现了优越的帕累托前沿。

## Abstract
Diffusion Transformers (DiTs) achieve superior image generation quality but suffer from quadratic computational complexity relative to token count. While various token reduction (TR) methods have been proposed to mitigate this cost, they overlook the primary objective of generative models: minimizing recovery error, which requires reflecting output token similarity. They rely solely on input token similarity inherited from reduction-only ViT paradigms, leading to a fundamental misalignment with this objective.   To bridge this gap, we propose DiTo, a novel TR paradigm that shifts the focus toward output-centric token reduction. Based on the observation that output token similarity is consistently preserved across adjacent timesteps, DiTo utilizes prior-step similarities as an effective proxy to establish token correspondences at a Matching timestep, which are then reused across multiple subsequent Reduction timesteps. To optimize this interleaved scheduling, we propose Pair Match Ratio (PMR)-guided Interval Scheduling to determine the optimal matching frequency. Furthermore, to mitigate localized approximation errors and resulting blocking artifacts caused by repeated reuse, we propose Frequency-aware Token Matching by incorporating a selection-frequency penalty. Extensive experiments demonstrate that DiTo consistently outperforms existing TR methods with 1.6-3.9 dB higher PSNR at comparable speedups, achieving a superior Pareto frontier.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：扩散变换器（DiTs）虽然生成质量高，但自注意力机制的计算复杂度随令牌数量二次增长，导致推理成本高昂。现有的令牌缩减（Token Reduction, TR）方法通过剪枝或合并冗余令牌来加速，但它们的设计照搬了面向判别式任务的视觉Transformer（ViT）范式，仅依赖**输入令牌相似性**。
- **根本矛盾**：生成式模型（扩散模型）需要将缩减后的令牌恢复到原始数量，其核心目标是最小化**恢复误差**（即恢复后的输出与密集原始输出的差异）。由于恢复阶段通过拷贝匹配令牌的输出完成，误差大小根本上取决于被缩减令牌与其匹配令牌的**输出相似性**。因此，基于输入相似性的现有方法在目标上存在根本错位，导致生成质量下降。
- **整体含义**：论文提出需要重新思考面向扩散模型的令牌缩减，从输出相似性的角度出发，才能实现高质量与高效率的帕累托最优。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想（DiTo范式）**：将令牌缩减的焦点从输入转向**输出相似性**。利用扩散模型相邻时间步之间输出令牌相似性高度保持的特性，使用**前一步的输出**作为代理来指导当前步的令牌匹配，并将匹配结果跨多个步骤重用。
- **关键技术细节**：
  - **DiTo流水线**：将去噪步骤划分为**匹配步（Matching Step）**与**缩减步（Reduction Step）**。匹配步执行完整计算，用输出令牌相似性建立令牌对应关系；随后的多个缩减步复用该对应关系进行缩减与恢复，大幅降低匹配频率。
  - **PMR引导的间隔调度**：提出**成对匹配率（Pair Match Ratio, PMR）**指标，量化先前步匹配结果与当前步黄金结果（基于当前输出）的对齐程度。通过设置PMR阈值 τ，确定每一步允许的最大复用间隔 Δ_max。然后采用超前一步的离线调度算法，动态决定每一步是匹配步还是缩减步，在保证PMR不低于阈值的前提下最大化效率。
  - **频率感知的令牌匹配**：多次重复使用匹配结果会导致某些令牌持续被选为缩减目标，造成局部近似误差积累和块状伪影。为此，在匹配阶段引入基于**令牌选择频率的惩罚项** `p = λ·r·Δs·C[S]`，其中 C 记录历史选择次数，r 为缩减比例，Δs 为相似度得分范围，λ 控制惩罚强度。从候选相似度得分中减去该惩罚，均衡选择频率，抑制伪影。
- **算法流程概述**：整体流程为离线计算PMR并获取 Δ_max 表；推理时按调度算法交替执行匹配步（完整计算+令牌匹配存储索引）和缩减步（利用存储的索引直接进行缩减和恢复）。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法
- **数据集与场景**：文本到图像生成任务。使用 ImageNet-1k 的 1000 个类别名称作为提示，每个提示用 3 个随机种子各生成 1 张图像，总计 3000 张评估图像（消融实验用 300 张）。图像固定为 1024×1024 分辨率。
- **基准模型**：两个有代表性的 DiT 模型——**FLUX.1-dev (Flux)**（35 采样步）和 **stable-diffusion-3-medium (SD3)**（50 采样步）。均使用 Diffusers 框架和 FlashAttention-2。
- **对比方法**：现有令牌缩减方法 **ToMeSD**、**ToFu**、**SiTo**、**ToMA**（原用于 UNet，通过提取核心组件适配到 DiT）。对比两种缩减比例（0.25 和 0.50）。
- **评估指标**：FID（分布质量）、CLIP score（文本-图像对齐）、PSNR、SSIM、LPIPS（重建保真度），以及推理延迟。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU 型号、数量、训练时长等）。若未明确说明，也请指出这一点
- 论文方法 **DiTo 属于训练无关（training-free）的加速技术**，无需额外训练算力。
- 推理性能评测在 **NVIDIA RTX 6000 Ada GPU** 上进行，延迟取 100 张图像的平均值。
- 文中未提及训练任何模型，也未报告训练时长或 GPU 数量。

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平
- **主实验**：在 Flux 和 SD3 两个模型上，设定 0.25 和 0.50 两种缩减比，对比 DiTo 与 4 种现有方法，同时报告 FID、PSNR、SSIM、LPIPS、CLIP 和延迟，数据量充分（3000 张图像）。
- **可视化比较**：提供了跨模型的视觉对比图、不同缩减比下的生成效果图，以及频率惩罚效果的局部热力图，直观且全面。
- **消融实验**（正文提及详见附录）：评估了 PMR 阈值 τ、惩罚强度 λ、不同缩减策略（剪枝/合并）和相似度度量等关键设计选择，验证了各部件的有效性。
- **补充分析**：进行了 FLOPs 和内存消耗的分析，确认了匹配结果存储的开销可忽略。
- **公平性**：所有对比方法在相同的硬件环境、提示集和种子下运行，FLOPs 缩减保持相同水平，对比公平客观。

### 6. 论文的主要结论与发现
- DiTo 通过直接利用输出相似性最小化恢复误差，在同等或略优的加速比下，PSNR 一致高出 1.6–3.9 dB，显著提升了重建保真度。
- 方法在多个指标（FID、SSIM、LPIPS）上均优于传统输入基方法，尤其在高缩减比（0.50）下质量衰退极小，展现出极强的鲁棒性。
- 提出的 PMR 引导调度和频率感知匹配有效平衡了效率与质量，抑制了因重复重用产生的块状伪影。
- DiTo 成功突破了现有令牌缩减方法的质量-效率权衡，实现了更优的帕累托前沿。

### 7. 优点：方法或实验设计上有哪些亮点
- **视角新颖**：首次明确将输出相似性作为令牌缩减的核心，纠正了从 ViT 继承而来的目标错位，是对扩散模型加速问题的重要反思。
- **巧妙利用时间一致性**：通过前步输出作为代理，无需当前步输出即可预测最佳匹配，并设计了复用机制与调度策略，极大增强了实用性。
- **系统性的优化设计**：不仅提出流水线，还配套了自适应间隔调度（PMR）和频率感知惩罚，解决了复用带来的品质下降，设计周密。
- **训练无关，即插即用**：方法无需任何微调，可直接应用于预训练模型，便于推广。
- **实验论证扎实**：在两个主流 DiT 模型、全面指标和公平对比下验证，并且消融和可视化支撑充分。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
- **模型泛化性验证有限**：仅在 Flux 和 SD3 两种 DiT 模型上测试，未验证在更早期 DiT（如 DiT-XL/2）或其它架构（如 U-ViT）上的效果。
- **对时序步长敏感**：方法依赖相邻步输出相似性，当去噪步数极少或采样步长过大时（如步数降至 1–5 步的蒸馏模型），输出变化可能加剧，导致匹配重用失效。
- **超参数依赖**：PMR 阈值 τ 和惩罚强度 λ 需要离线调整，对不同的模型或任务配置可能需要重新调参优化。
- **存储与计算开销**：虽然匹配索引存储开销小，但在匹配步需额外执行一次完整的令牌匹配计算，且需维护选择历史向量 C，对极低延迟场景可能有轻微影响。
- **未探索与其他加速方法的联合**：论文未与采样步缩减、蒸馏、量化等方法结合，实际应用中多方法结合时的性能与兼容性尚待验证。

（完）
