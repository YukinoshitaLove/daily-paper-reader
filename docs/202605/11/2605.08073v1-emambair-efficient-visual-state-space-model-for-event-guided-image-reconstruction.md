---
title: "EmambaIR: Efficient Visual State Space Model for Event-guided Image Reconstruction"
title_zh: EmambaIR：一种用于事件引导图像重建的高效视觉状态空间模型
authors: "Wei Yu, Yunhang Qian"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08073v1"
tags: ["query:real-ir"]
score: 9.5
evidence: 用于图像重建的高效视觉状态空间模型
tldr: 该研究针对事件驱动图像重建中CNN无法捕捉全局特征、ViT计算开销过大等问题，提出了高效视觉状态空间模型EmambaIR，通过Top-k稀疏注意模块和门控状态空间模块实现跨模态融合与低复杂度全局建模，在多任务测试中显著提升重建质量并降低算力需求。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.08073v1/fig-001.webp\", \"caption\": \"Figure 5: Qualitative comparison results of different image deblurring methods on the GoPro dataset.\", \"page\": 6, \"index\": 1, \"width\": 959, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2605.08073v1/fig-002.webp\", \"caption\": \"Figure 6: Qualitative comparison results of different image deraining methods on the Adobe240 dataset.\", \"page\": 6, \"index\": 2, \"width\": 476, \"height\": 261}, {\"url\": \"assets/figures/arxiv/2605.08073v1/fig-003.webp\", \"caption\": \"Figure 7: Qualitative comparisons of different image HDR methods on the SDSD dataset.\", \"page\": 6, \"index\": 3, \"width\": 477, \"height\": 266}, {\"url\": \"assets/figures/arxiv/2605.08073v1/fig-004.webp\", \"caption\": \"Figure 8: Ablation studies of the proposed TSAM and GSSM under challenging scenes with high-speed motion blur and high-dynamic rain streaks.\", \"page\": 7, \"index\": 4, \"width\": 474, \"height\": 424}, {\"url\": \"assets/figures/arxiv/2605.08073v1/fig-005.webp\", \"caption\": \"Figure 9: Ablation studies on the impact of varying k values in TSAM.\", \"page\": 7, \"index\": 5, \"width\": 440, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2605.08073v1/fig-006.webp\", \"caption\": \"Figure 2: Overall architecture of (a) our EmambaIR for event-guided image reconstruction, which consists of a UNet-based backbone built upon the proposed (b) top-k sparse attention module and (c) gated state space module.\", \"page\": 3, \"index\": 6, \"width\": 964, \"height\": 442}, {\"url\": \"assets/figures/arxiv/2605.08073v1/fig-007.webp\", \"caption\": \"Figure 3: Illustration of our top-k selection sparse attention and vision transformer with global attention mechanisms.\", \"page\": 4, \"index\": 7, \"width\": 475, \"height\": 324}, {\"url\": \"assets/figures/arxiv/2605.08073v1/fig-008.webp\", \"caption\": \"Table 1: Quantitative comparison results of our method and other state-of-the-art methods on three reconstruction tasks.\", \"page\": 5, \"index\": 8, \"width\": 968, \"height\": 190}]"
motivation: 现有CNN和ViT在事件驱动图像重建中分别存在全局关联捕捉不足和高算力消耗的问题。
method: 论文提出了包含Top-k稀疏注意模块TSAM和门控状态空间模块GSSM的高效状态空间模型EmambaIR。
result: 在六个数据集的三类任务上，EmambaIR在性能、内存和计算效率方面均优于现有方法。
conclusion: EmambaIR有效缓解了现有方法在事件引导图像重建中的算力与记忆瓶颈，为高分辨率、高质量重建提供了新思路。
---

## 摘要
近年来，事件驱动的图像重建方法主要依赖卷积神经网络（CNN）和视觉 Transformer（ViT）来处理互补的事件信息。然而，这些架构存在根本性局限：CNN 往往难以捕捉全局特征关联，而 ViT 由于其二次计算复杂度（例如 $O(n^2)$），在高分辨率场景中的应用受到限制。为解决这些瓶颈，我们提出 EmambaIR——一种高效的视觉状态空间模型（Efficient visual State Space Model），用于利用空间稀疏且时间连续的事件流进行图像重建。该框架包含两个关键组件：跨模态 Top-k 稀疏注意模块（Top-k Sparse Attention Module, TSAM）和门控状态空间模块（Gated State-Space Module, GSSM）。TSAM 能够高效地执行像素级 Top-k 稀疏注意，以引导跨模态交互，从而生成丰富而稀疏的融合特征。随后，GSSM 通过非线性门控单元增强线性复杂度（$O(n)$）的基础状态空间模型（SSM）的时序表示，有效捕获全局上下文依赖关系，而不增加典型的计算开销。在三个不同的图像重建任务——运动去模糊、去雨以及高动态范围（HDR）增强——的六个数据集上进行的大量实验表明，EmambaIR 在显著降低内存消耗和计算成本的同时，大幅优于当前最先进的方法。源代码和数据可在以下地址公开获取：https://github.com/YunhangWickert/EmambaIR

## Abstract
Recent event-based image reconstruction methods predominantly rely on Convolutional Neural Networks (CNNs) and Vision Transformers (ViTs) to process complementary event information. However, these architectures face fundamental limitations: CNNs often fail to capture global feature correlations, whereas ViTs incur quadratic computational complexity (e.g., $O(n^2)$), hindering their application in high-resolution scenarios. To address these bottlenecks, we introduce EmambaIR, an Efficient visual State Space Model designed for image reconstruction using spatially sparse and temporally continuous event streams. Our framework introduces two key components: the cross-modal Top-k Sparse Attention Module (TSAM) and the Gated State-Space Module (GSSM). TSAM efficiently performs pixel-level top-k sparse attention to guide cross-modal interactions, yielding rich yet sparse fusion features. Subsequently, GSSM utilizes a nonlinear gated unit to enhance the temporal representation of vanilla linear-complexity ($O(n)$) SSMs, effectively capturing global contextual dependencies without the typical computational overhead. Extensive experiments on six datasets across three diverse image reconstruction tasks - motion deblurring, deraining, and High Dynamic Range (HDR) enhancement - demonstrate that EmambaIR significantly outperforms state-of-the-art methods while offering substantial reductions in memory consumption and computational cost. The source code and data are publicly available at: https://github.com/YunhangWickert/EmambaIR

---

## 论文详细总结（自动生成）

# EmambaIR：一种用于事件引导图像重建的高效视觉状态空间模型 — 深度总结

---

## 一、研究问题与背景

### 1. 背景动机
- **事件相机（Event Camera）**能够以极高的时间分辨率、低延迟和高动态范围捕获场景变化，为图像重建、运动去模糊、HDR增强等任务提供了新契机。  
- 传统的 **卷积神经网络（CNN）** 在处理事件数据时擅长局部特征提取，但 **缺乏全局特征关联能力**。  
- **视觉 Transformer（ViT）** 虽然能捕捉全局依赖，但其 **计算复杂度呈二次增长（O(n²)）**，无法适用于高分辨率场景。  
- 本论文针对这一矛盾，提出一种高效的跨模态建模架构，旨在兼顾 **全局建模能力与计算效率**。

### 2. 核心问题
如何在事件驱动的图像重建中同时：
- 捕捉跨模态（事件与图像）全局依赖；
- 保持线性级（O(n)）计算复杂度；
- 降低显存占用并提升重建质量。

---

## 二、方法论与架构

### 1. 整体架构
- 模型名称：**EmambaIR（Efficient visual State Space Model for Event-guided Image Reconstruction）**
- 基础结构：整体采用 **U-Net** 框架，融合两个设计模块：
  1. **Top-k Sparse Attention Module (TSAM)**
  2. **Gated State Space Module (GSSM)**

### 2. 核心思想
通过稀疏注意与门控状态空间机制相结合，在跨模态（事件+图像）特征融合时实现：
- **稀疏化注意力计算**以降低复杂度；
- **门控状态空间更新**以增强长期依赖建模。

### 3. 技术细节概述
#### （1）Top-k Sparse Attention Module (TSAM)
- 思路：在标准多头注意力的基础上，执行 **像素级 Top-k 选择**，只保留最相关的特征通道。
- 功能：高效引导事件流与图像帧之间的跨模态交互。
- 效果：减少了无效连接，使注意力分布更加聚焦且稀疏，从而节省算力。

#### （2）Gated State Space Module (GSSM)
- 建立在线性状态空间模型（SSM）基础之上，通过 **门控单元（gating unit）** 实现非线性增强。
- 时间复杂度为 $O(n)$，显著优于 Transformer。
- 实现：状态转移通过门控机制控制输入与更新路径，保留长期上下文依赖。
- 特点：能在不增加显著计算量的情况下，实现全局上下文捕获。

### 4. 算法流程（文字版）
1. 输入事件流与低质量帧图像；
2. TSAM 执行跨模态 Top-k 稀疏注意；
3. 将融合后的特征输入 GSSM，捕捉时间序列依赖；
4. 通过 U-Net 解码重建高清图像；
5. 计算多任务损失函数（去模糊、去雨、HDR）并进行端到端训练。

---

## 三、实验设计

### 1. 数据集与任务
论文在 **三类任务、六个数据集** 上进行评估：

| 任务类别 | 数据集示例 | 场景描述 |
|-----------|-------------|----------|
| 去模糊（Motion Deblurring） | GoPro | 高速运动场景下的模糊恢复 |
| 去雨（Deraining） | Adobe240 | 动态雨条纹干扰场景 |
| HDR增强（High Dynamic Range Enhancement） | SDSD | 高亮度与低曝光图像融合重建 |

### 2. Benchmark 与对比方法
- 与当前 **最先进（SoTA）方法** 对比，包括：
  - CNN为主的模型（例如 EVDNet、LEDVDI）
  - Transformer类模型（例如 Restormer、ESRT）
- 评测指标主要包括：
  - PSNR（峰值信噪比）
  - SSIM（结构相似度）
  - 计算成本（时间、显存）

---

## 四、资源与算力

- 文中未明确说明具体的硬件配置。
- 未提及 GPU 型号、训练时间或具体算力规模。
- 但论文强调 EmambaIR 相较于 Transformer 型方法在“内存消耗与计算成本上显著降低”，表明其 **资源效率高，适合中等硬件条件训练和推理**。

---

## 五、实验数量与充分性

### 1. 实验类型
- **跨任务综合评估**（三类任务 × 六个数据集）
- **消融实验（Ablation Study）**
  - 探讨 TSAM、GSSM 各模块的独立作用。
  - 研究 Top-k 参数变化对性能的影响（见图 8、图 9）。
- **定性与定量分析并举**（多组视觉对比图 + 表格结果）

### 2. 实验充分性评估
- 任务覆盖面广，包含不同模态与场景；
- 消融设计完善，验证各模块独立有效性；
- 对比方法选择全面，客观性较好；
- 未见过度调参或不公平对比情况，实验设置较为稳健。

---

## 六、主要结论与发现

- EmambaIR 在三个任务上均取得 **显著性能提升**，同时 **计算效率与内存占用均显著优于现有方法**；
- 顶层发现：
  - TSAM 提升跨模态融合能力；
  - GSSM 提供线性复杂度下的强全局建模；
  - 整体架构能在高分辨率场景中保持高效推理。

---

## 七、优点与亮点

- **算法创新性高**：首次在事件驱动重建中引入视觉状态空间模型思想；
- **跨模态高效融合**：Top-k 稀疏注意力有效解决了事件与图像信息的不平衡问题；
- **计算效率优化**：实现线性复杂度全局建模；
- **任务普适性强**：在去模糊、去雨、HDR 三类任务中均表现优异；
- **实验设计完整**：包含消融和参数敏感性分析，验证充分。

---

## 八、不足与局限

- **资源信息缺失**：论文未提供训练算力规格，难以评估真实运行成本；
- **泛化分析有限**：未在真实室外事件数据中验证鲁棒性；
- **解释性不足**：模型内部的门控机制与注意力对具体视觉语义的影响仍缺乏可解释性探索；
- **潜在扩展风险**：在极端场景（如超高分辨率）下性能稳定性尚未验证。

---

（完）
