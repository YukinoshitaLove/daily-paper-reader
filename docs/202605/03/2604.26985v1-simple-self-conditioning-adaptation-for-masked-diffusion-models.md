---
title: Simple Self-Conditioning Adaptation for Masked Diffusion Models
title_zh: 掩码扩散模型的简单自条件适配
authors: "Michael Cardei, Huu Binh Ta, Ferdinando Fioretto"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.26985v1"
tags: ["query:real-ir"]
score: 8.0
evidence: 掩码扩散模型的训练后适配
tldr: 本文提出了一种针对掩码扩散模型的后训练自条件适配方法SCMDM，通过在每一步去噪中利用模型先前的预测结果实现跨步信息保留，无需额外训练或架构改动，即可在文本、图像、分子及基因序列生成任务中显著提升性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26985v1/fig-001.webp\", \"caption\": \"Figure 1: Relative performance improvement of SCMDM compared to the vanilla MDM.\", \"page\": 1, \"index\": 1, \"width\": 460, \"height\": 358}, {\"url\": \"assets/figures/arxiv/2604.26985v1/fig-002.webp\", \"caption\": \"Figure 2: Limitation of standard masked diffusion models.\", \"page\": 2, \"index\": 2, \"width\": 519, \"height\": 495}, {\"url\": \"assets/figures/arxiv/2604.26985v1/fig-003.webp\", \"caption\": \"Figure 4: Distribution of LLM-judge (gemma-4-31b) scores.\", \"page\": 7, \"index\": 3, \"width\": 542, \"height\": 331}, {\"url\": \"assets/figures/arxiv/2604.26985v1/fig-004.webp\", \"caption\": \"Table 3: Entropy and autoregressiveness statistics for unconditioned sampling at different denoising budgets. Entropy denotes token-level entropy of the generated sequence. L-AR@1 and G-AR@4 denote local and global autoregressiveness metrics. Higher AR values indicate more autoregressive-like behavior.\", \"page\": 7, \"index\": 4, \"width\": 881, \"height\": 196}, {\"url\": \"assets/figures/arxiv/2604.26985v1/fig-005.webp\", \"caption\": \"Table 8: Performance of SCMDM vs baselines on small molecule generation. Training tokens denotes the number of tokens used in base training or post-training. Valid, Unique, and Novel report the number of chemically valid molecules, the number of distinct valid molecules, and the number of unique molecules not appearing in the training set, respectively. Entries are reported as mean ± standard deviation, and higher is better for all three metrics.\", \"page\": 10, \"index\": 5, \"width\": 925, \"height\": 242}, {\"url\": \"assets/figures/arxiv/2604.26985v1/fig-006.webp\", \"caption\": \"Figure 3: Comparison of reverse denoising steps. (a) Vanilla method discards the clean state prediction after applying the reverse kernel to obtain the next latent state. (b) SCMDM conditions the next steps denoiser on both the current latent state and the previous clean state prediction.\", \"page\": 5, \"index\": 6, \"width\": 792, \"height\": 353}]"
motivation: 标准的掩码扩散模型在反向更新后仍保持掩码的token会丢弃前一步的预测，限制了跨步的精细化推理。
method: 提出在扩散过程每一步中引入自条件机制，利用模型先前的预测作为当前步骤的额外输入。
result: 在多个任务上，SCMDM相较于基线MDM显著提升生成质量，困惑度几乎减半。
conclusion: SCMDM在无需重训练和额外计算的情况下有效增强了MDM的自我精炼能力。
---

## 摘要
掩码扩散模型（Masked Diffusion Models, MDMs）通过在吸收型掩码过程下的迭代去噪来生成离散序列。在标准的掩码扩散过程中，如果某个token在反向更新后仍然保持掩码状态，模型会丢弃该位置的干净状态预测。因此，仍被掩盖的位置必须仅根据掩码token反复推断。这种设计选择限制了跨步的细化能力。为了解决这一局限，本文提出了一种简单而有效的MDM训练后适配方法，使每个去噪步骤都以模型自身先前的干净状态预测为条件。该方法称为自条件掩码扩散模型（Self-Conditioned Masked Diffusion Models, SCMDM），仅需极少的结构改动，不引入递归的潜在状态通道，不依赖辅助参考模型，且在采样过程中不增加额外的去噪器评估次数。这与以往需要从头进行昂贵训练的部分自条件方法有显著区别。特别是，本文表明，在训练后适配阶段，部分自条件方法（包括常用的50% dropout策略）并非最优。相反，当模型自生成的干净状态估计变得具有信息量后，专注于细化比混合条件与无条件目标更为优越。SCMDM在多个领域进行了评估，相较于原始MDM基线表现出一致的改进：在OWT训练的模型上将生成困惑度降低近50%（从42.89降至23.72），同时在离散化图像合成质量、小分子生成以及基因组分布建模的保真度上均取得显著提升。

## Abstract
Masked diffusion models (MDMs) generate discrete sequences by iterative denoising under an absorbing masking process. In standard masked diffusion, if a token remains masked after a reverse update, the model discards its clean-state prediction for that position. Thus, still-masked positions must be repeatedly inferred from the mask token alone. This design choice limits cross-step refinement. To address this limitation, this paper proposes a simple, yet effective, post-training adaptation for MDMs that conditions each denoising step on the model's own previous clean-state predictions. The resulting method, called Self-Conditioned Masked Diffusion Models (SCMDM), requires minimal architectural change, does not introduce a recurrent latent-state pathway, does not rely on an auxiliary reference model, and adds no extra denoiser evaluations during sampling. This is an important departure from partial self-conditioning approaches which requires expensive model training from scratch. In particular, the paper shows that partial self-conditioning, including the commonly used 50% dropout strategy for training self-conditioned models from scratch, is suboptimal in the post-training regime. Instead, once the model's self-generated clean-state estimates become informative, the specialization to refinement is preferable to mixing conditional and unconditional objectives. SCMDM is evaluated across multiple domains, demonstrating consistent improvement over vanilla MDM baselines, achieving nearly a 50% reduction in generative perplexity on OWT-trained models (42.89 to 23.72), alongside strong improvements in discretized image synthesis quality, small molecular generation, and enhanced fidelity in genomic distribution modeling.