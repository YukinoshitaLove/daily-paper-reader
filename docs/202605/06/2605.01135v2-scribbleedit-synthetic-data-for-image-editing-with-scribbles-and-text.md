---
title: "ScribbleEdit: Synthetic Data for Image Editing with Scribbles and Text"
title_zh: ScribbleEdit：结合涂鸦与文本的图像编辑合成数据集
authors: "Anya Ji, George Ma, Téa Wright, Yiming Zhang, David M. Chan, Alane Suhr, Somayeh Sojoudi"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.01135v2"
tags: ["query:real-ir"]
score: 10.0
evidence: 结合涂鸦和文本的图像编辑
tldr: 本文提出ScribbleEdit大型合成数据集，用以结合自然语言和手绘涂鸦实现更精确的图像编辑。通过自动生成图像、涂鸦和文本指令，微调扩散和自回归模型后，其空间对齐与语义一致性均有显著提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.01135v2/fig-001.webp\", \"caption\": \"Figure 1. Overview of the ScribbleEdit pipeline for dataset construction and model finetuning.\", \"page\": 1, \"index\": 1, \"width\": 487, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2605.01135v2/fig-002.webp\", \"caption\": \"Table 1. Quantitative results on our dataset. Inputs denote scribble (S), text instruction (T), and mask (M). Bold indicates the best result in each column, and underline indicates the second best.\", \"page\": 3, \"index\": 2, \"width\": 1025, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2605.01135v2/fig-003.webp\", \"caption\": \"Figure 2. Comparison of editing results across methods. Each row shows one dataset example (Edit Instruction, Original, Scribble, Target) followed by results from four models.\", \"page\": 4, \"index\": 3, \"width\": 1036, \"height\": 494}]"
motivation: 现有图像编辑模型难以同时理解文本的语义和涂鸦的空间信息，导致控制不精确。
method: 构建了一个自动生成图像对、涂鸦和文本指令的合成数据管线，并用该数据微调生成模型。
result: 微调后的模型在空间对齐和语义一致性上显著优于未微调模型。
conclusion: ScribbleEdit证明合成数据能显著提升模型在结合文本与涂鸦输入时的编辑精度与控制力。
---

## 摘要
近年来，生成模型的进步极大地提升了图像编辑能力，但实现精确且直观的用户控制仍然具有挑战。具体而言，用户往往难以同时表达准确的空间布局和具体的语义细节。自然语言指令能够有效传达纹理、颜色等高层语义信息，但缺乏空间特异性；相反，自由手绘涂鸦可以提供大致的空间边界，但难以表达细节化的视觉属性。因此，要实现精确控制需要结合这两种模态。然而，现有模型由于缺乏专门的训练数据，难以同时解释抽象涂鸦与文本指令。为解决这一问题，本研究提出了一个名为 ScribbleEdit 的大规模合成数据集，旨在通过结合自然语言指令和自由手绘涂鸦输入，实现更精确、可控的图像编辑。我们通过一个自动化的合成管线构建了该数据集：利用修复（inpainting）生成源图像与目标图像对，并配以人工绘制的涂鸦及由视觉语言模型（VLM）生成的文本指令。基于 ScribbleEdit，我们评估并微调了扩散式和自回归式统一多模态图像编辑模型。实验结果表明，通用模型在处理抽象涂鸦输入时表现欠佳，而在我们合成数据集上微调后，其生成的编辑结果在空间对齐性与语义一致性方面均显著提升。

## Abstract
Recent progress in generative models has significantly advanced image editing capabilities, yet precise and intuitive user control remains difficult. Specifically, users often struggle to communicate both exact spatial layouts and specific semantic details simultaneously. While natural language instructions effectively convey high-level semantics like texture and color, they lack spatial specificity. Conversely, freehand scribbles provide rough spatial boundaries but cannot express detailed visual attributes. Consequently, achieving precise control requires combining both modalities. However, existing models struggle to jointly interpret abstract scribbles alongside text due to a lack of specialized training data.   In this work, we introduce ScribbleEdit, a large-scale synthetic dataset designed to bridge this gap by combining natural language instructions with freehand scribble inputs for more accurate, controllable edits. We construct this dataset through a synthetic pipeline that automatically generates source-target image pairs via inpainting, which are then paired with human-drawn scribbles and VLM-generated text instructions. Using ScribbleEdit, we evaluate and finetune both diffusion-based and autoregressive unified multimodal image editing models. Our experiments reveal that while off-the-shelf models struggle with abstract scribble inputs, finetuning on our synthetic dataset significantly improves their ability to generate spatially aligned and semantically consistent edits.

---

## 论文详细总结（自动生成）

# ScribbleEdit：结合涂鸦与文本的图像编辑合成数据集 — 深度总结

---

## 一、研究问题与整体意义

- **研究动机**  
  现有图像编辑生成模型（如扩散模型、LLM-based multimodal models）虽然在视觉生成质量上已取得显著进展，但**精准控制编辑位置与语义内容**仍存在困难。  
  - 文本指令能表达语义（如颜色、类别、风格），但无法准确描述**空间位置与形状**。  
  - 手绘涂鸦（scribble）虽然能提供大致空间布局，却难以传达外观细节。  
- **核心问题**  
  缺乏同时包含文本与涂鸦的专用训练数据，使模型难以学会融合两种信号。  
- **总体目标**  
  作者提出 **ScribbleEdit**，一个大规模合成数据集，旨在提升模型在“文本 + 涂鸦”双模态输入下的图像编辑能力，使其能生成**空间对齐、语义一致**的编辑结果。

---

## 二、方法论：核心思路与技术流程

### 2.1 总体架构
- 论文提出 **自动化合成数据生成管线**，涵盖：
  1. **数据源选取** – 从 *Sketchy Database* 中抽取图像与人工绘制涂鸦；
  2. **对象分割与移除** – 利用 Grounding DINO + SAM 获取目标对象掩码；
  3. **背景修复（inpainting）** – 通过 LaMa 模型填补被移除的区域；
  4. **文本指令生成** – 使用视觉语言模型 *Gemma 3 (4B)*，基于输入与目标生成自然语言编辑指令；
  5. **数据组合** – 形成包含四部分的样本：
     - 原图（目标图像）  
     - 掩码修复后图（输入图像）  
     - 涂鸦（空间引导）  
     - 文本指令（语义引导）

### 2.2 关键技术细节
- **对象检测与分割**
  - Grounding DINO 以目标类别名为文本提示，输出边界框；
  - SAM（ViT-H）生成多个掩码候选，选最高 IoU 的一个；
  - 掩码膨胀 15 像素确保覆盖完整目标。
  
- **文本生成策略**
  - 给定“输入图 + 目标图”，Gemma 3 自动生成描述性指令（如“在图像中央放置一个红色苹果”），实现语义与位置匹配。

- **数据划分**
  - 共生成 11,775 样本；划分为训练 9,438、验证 1,164、测试 1,173 图片；结合多涂鸦样本最终获得约 23,550 条训练实例。

### 2.3 模型微调流程
作者针对两类模型进行了微调：

1. **Image-conditioned ControlNet-Scribble（基于扩散模型）**
   - 改进：新增图像条件，将原图的 VAE latent 与噪声 latent 拼接输入 U-Net，扩展输入通道由 4 改为 8。
   - 训练策略：  
     - 前 3 个 epoch 更新新增层（学习率 1e-4）；  
     - 后 7 个 epoch 微调全 U-Net（学习率 1e-5）；  
     - 优化器 AdamW，权重衰减 1e-2，梯度裁剪。  
     - 使用 classifier-free guidance（CFG）中对文本/涂鸦 dropout 概率 0.1，推理时采用 DDIM 50 步，guidance scale=7.5。

2. **BAGEL-7B-MoT（多模态自回归模型）**
   - 采用 LoRA 微调（rank=32，α=64，lr=1e-4，cosine decay，5 epoch，batch=64）。  
   - 加权 MSE 损失：编辑区权重 5× 背景权重，鼓励模型只修改目标区域。  
   - 同样应用 CFG dropout（0.1）。

---

## 三、实验设计

### 3.1 数据集与任务
- 使用 **ScribbleEdit** 合成数据集，任务为“对象添加（object addition）”。  
- 每样本包含：输入图 + 目标图 + 涂鸦 + 文本指令。

### 3.2 对比模型（Benchmark）
包括：
1. **SD Inpaint + ControlNet-Scribble**：扩散式基线，文本 + 涂鸦 + 掩码。
2. **BAGEL-7B-MoT**：多模态统一模型（零样本与微调版）。
3. **Image-conditioned ControlNet-Scribble（微调版）**。

### 3.3 评价指标
- **Histogram Intersection（色彩分布相似度）**
- **RMSE（像素误差）**
- **CLIP Similarity（语义一致性）**
- **FID（图像真实度）**
  - 部分指标在全图与 BBOX 内都计算。

---

## 四、资源与算力

- **论文未明确说明 GPU 型号、数量、训练时长**。  
  仅推测：开源模型（SD Inpaint, BAGEL, ControlNet）在 512×512 分辨率下微调，batch size 分别为 16 与 64，训练 epoch 约 5–10。  
  无更详细硬件或算力数据。

---

## 五、实验数量与充分性

- **实验规模**
  - 主实验在单一任务（对象添加）上，但使用近 12K 样本。  
  - 包含多模态输入组合评估（S、T、S+T、S+T+M）。  
  - 对比多个模型基线，同时提供定量与定性结果。
- **充分性**
  - 定量表现覆盖四个维度指标，样本量中等；
  - 还进行了误差分析（随机抽取 50 张图像，分析指导信号错误率与模型遵循度）。  
  整体设计较为系统，但未见跨任务或跨数据集验证。

---

## 六、主要结论与发现

- 微调后模型显著提升空间对齐与语义一致性：  
  - ScribbleEdit-finetuned **BAGEL-7B-MoT** 在所有指标上表现最佳（CLIP=0.88，FID=33.36）。  
  - **ControlNet-Scribble** 微调后生成背景更真实（最低 FID），但编辑精度略低。  
- 未微调模型对抽象涂鸦理解力弱，常出现“不编辑或误编辑”现象。  
- 误差分析表明：
  - 涂鸦指令存在约 16% 不清晰比例；
  - 文本指令约 34% 不完整或错误；
  - 在指导正确样本上，模型能较好遵循涂鸦（82%），但语义遵循（颜色/物体种类）仍仅 57%。

---

## 七、优点与创新亮点

- **方法创新**
  - 提出首个结合文本与自由手绘涂鸦的合成数据生成管线；
  - 数据构建过程无人工标注依赖，完全自动化（通过 DINO、SAM、LaMa、Gemma）；
  - 数据集既保留人类绘制的自然涂鸦，又生成高质量目标图。
  
- **模型优化**
  - 通过添加图像条件，修复 ControlNet 的“背景破坏”问题；
  - 在 BAGEL 上引入空间权重损失，使模型更聚焦于目标区域。

- **综合实验**
  - 同时涵盖扩散式与自回归模型两类主流架构，评估全面。
  - 提供多模态基准（S、T、S+T）对比，展现 ScribbleEdit 的实用性。

---

## 八、不足与局限

- **数据局限**
  - 涂鸦来源单一（来自 Sketchy Database），限制多样性与规模；
  - 文本指令为机器生成，存在语义噪声（约三分之一错误）。
- **任务覆盖有限**
  - 仅研究“对象添加”，未涉及替换、局部修改、风格转换等编辑类型。
- **模型评估范围**
  - 未说明跨域测试或泛化至真实用户涂鸦场景；
  - 缺少大模型对比（如最新 VLM 或强视觉-文本融合模型）。
- **算力与训练质量不透明**
  - 无 GPU 规格与模型参数规模细节，训练可复现性受限。

---

**（完）**
