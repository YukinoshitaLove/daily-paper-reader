---
title: Stop Denoising Your Blurs
title_zh: 停止对你的模糊图像进行去噪
authors: "Sasidhar Parvathireddy, Vamsidhar Saraswathula, Rama Krishna Gorthi"
date: 2026-05-24
pdf: "https://arxiv.org/pdf/2605.25014v1"
tags: ["query:real-ir"]
score: 10.0
evidence: 基于扩散的图像去模糊，采用卷积前向过程
tldr: 扩散模型在图像复原中通常假设退化先于加性噪声，但模糊是卷积操作，不符合该假设。本文提出ConvDiff，一种新颖的卷积扩散框架，用卷积替代加性噪声来构建从清晰图像到模糊图像的有意义前向轨迹，利用频域特性实现图像去模糊。该方法突破了传统扩散模型对退化类型的限制，在去模糊任务上表现出色，为扩散模型在更广泛图像复原任务中的应用开辟了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25014v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1292, \"height\": 482, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25014v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1295, \"height\": 831, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25014v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 770, \"height\": 443, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.25014v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 690, \"height\": 514, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25014v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 900, \"height\": 515, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25014v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 721, \"height\": 493, \"label\": \"Table\"}]"
motivation: 基于扩散的图像去模糊，采用卷积前向过程。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
近年来，扩散模型在图像复原任务中取得了显著的性能。其核心机制依赖于在添加噪声操作之前退化先验的严格假设。然而，作为研究最广泛的退化形式之一，模糊模型却违背了这一假设，因为它本质上基于卷积而非加法。在本文中，我们提出了 ConvDiff，一种新颖的基于扩散的框架，将加法操作替换为卷积，用于图像去模糊任务。在前向过程中，我们利用卷积的频域特性，构建了从清晰图像到其模糊版本的富有意义的轨迹，而不是逐步用加性噪声破坏图像。当前工作针对高斯模糊实现了该框架，其中频域分解产生封闭形式且物理上有效的中间状态；而基于模糊算子构建退化轨迹的基本原理自然地扩展到其他模糊族。该表述弥合了模糊的数学原理与基于扩散的复原算法的迭代设计之间的差距，使得图像复原模型更具物理依据和有效性。

## Abstract
In recent times, diffusion models have achieved remarkable performance in image restoration tasks. Their core mechanism relies on the restricted presumption of degradation prior to the additive noise operation. However, the blur model, one of the most widely studied degradation formulations, violates this assumption, as it is inherently based on convolution rather than addition. In this paper, we introduce ConvDiff, a novel diffusion based framework that substitutes the additive operation with convolution for the task of image deblurring. In the forward process, we construct a meaningful trajectory from the clean image to its blurred counterpart by exploiting the frequency domain characteristics of convolution, rather than progressively corrupting the image with additive noise. While the current work instantiates this framework for Gaussian blur, where frequency-domain decomposition yields closed-form and physically valid intermediate states, the underlying principle of constructing degradation trajectories from the blur operator extends naturally to other blur families. This formulation bridges the gap between the mathematical principles of blurring and the iterative design of diffusion-based restoration algorithms, enabling more physically grounded and effective image restoration models.

---

## 论文详细总结（自动生成）

好的，以下是根据论文《Stop Denoising Your Blurs》生成的结构化总结。

### 1. 论文的核心问题与整体含义

这篇论文挑战了当前基于扩散模型的图像复原方法中的一个根本性假设。其核心动机与背景如下：

-   **识别根本性错配**：主流的扩散模型（如 SR3）在图像复原中的前向过程是逐步添加高斯噪声，其核心机制是“去噪”。然而，图像模糊的物理本质是清晰图像与模糊核的**卷积**操作，而非加性噪声。这种“加法”与“卷积”的根本差异，意味着用“去噪”的思路去“去模糊”在物理上是不一致的。
-   **构建物理上有意义的轨迹**：该论文旨在提出一个针对去模糊任务的、物理上更精确的扩散框架。其核心思想是摒弃传统的加性噪声，改用**卷积操作**来构建从清晰图像到模糊图像的前向退化轨迹，使中间状态在物理上具有真实意义（即“部分模糊”的图像），而非毫无物理意义的“含噪”图像。

### 2. 论文提出的方法论

该论文提出了一个名为 **ConvDiff** 的新框架，其核心是进入频域，利用卷积定理来分解模糊操作。

-   **核心思想：频域下的模糊分解**
    卷积操作在时域是单步完成的，难以直接分解为多步迭代过程。ConvDiff 利用频域的乘法特性解决了这个问题：
    -   **空域关系**：模糊图像 `y = x * k_blur`
    -   **频域关系**：`Y = X · H_blur` (大写字母代表傅里叶变换)
    -   利用卷积的结合律，总模糊核 `k_blur` 可以在频域被分解为 `n` 个增益的乘积：`H_blur = (H_blur^(1/n))^n`。
    -   这使得生成一个**连续的、强度可控的退化轨迹**成为可能。

-   **技术细节与算法流程**
    -   **前向过程（训练）**：定义一个连续的退化强度参数 `β ∈ (0, 1]`。给定清晰图像 `X0`和模糊核 `H_blur`，通过公式 `X_β = X_0 · H_blur^β` 生成任意中间状态 `X_β`。当 `β=0` 时为清晰图像，`β=1` 时为完全模糊图像。这保证了中间状态是物理上有效的“部分模糊”图像（对于高斯核尤其如此，因为高斯函数的分数次幂仍是高斯函数）。训练目标是让神经网络 `I_θ` 学习从任意 `X_β` 直接预测出原始的清晰图像 `X_0`。
    -   **反向过程（推理/采样）**：这是一个迭代“去模糊”过程。
        1.  以模糊图像 `y` 作为初始的 `x_t` (t=n)。
        2.  模型 `I_θ` 根据当前 `x_t` 和强度 `t/n` 预测一个干净图像 `x_hat_0`。
        3.  使用维纳滤波，根据 `x_hat_0` 和最初的模糊图像 `y` 重新估计出当前的模糊核 `H̃_blur`。
        4.  利用估计出的核、当前强度 `(t-1)/n` 和 `x_hat_0`，通过前向过程生成一个“更不模糊”的下一个迭代状态 `x_{t-1}`。
        5.  重复步骤 2-4，直至 `t=0`，输出最终的清晰图像。

### 3. 实验设计

-   **数据集与退化模拟**：使用 **DIV2K** 数据集（800 张训练，100 张测试）。通过大小为 `15x15`、标准差 `σ` 在 `[2, 4]` 范围内随机采样的**高斯模糊核**与图像卷积来模拟退化。训练时从图像中提取 `128x128` 的补丁。
-   **对比基准与方法**：实验对比分为两组：
    -   **传统迭代方法**：
        -   **SR3**：传统的基于加性噪声的去噪扩散模型。
        -   **INDI**：基于清晰与模糊图像之间线性插值的迭代方法。
    -   **当前最优（SOTA）端到端方法**：
        -   **FFTformer**, **Restormer**, **DiffIR**, **HiDiff**。
-   **评估设置**：所有传统迭代方法（ConvDiff, SR3, INDI）都使用了相同的、经过增强的 U-Net 网络结构以确保公平。它们分别使用 **5**, **2000**, 和 **10** 个迭代步数。评估指标为 **PSNR**、**SSIM** 和 **LPIPS**。

### 4. 资源与算力

论文中**没有明确提及**使用的 GPU 型号、数量或具体的训练时长。

### 5. 实验数量与充分性

-   **实验组数**：论文只在一个数据集（DIV2K）和一种模糊类型（高斯模糊）上进行了实验。对比了 6 种其他方法，共一组定量结果和视觉对比。
-   **实验充分性评估**：
    -   **优点**：在于与物理原理一致的传统迭代方法（SR3, INDI）的对比非常公平，因为所有方法都基于完全相同的网络架构进行训练和比较，清晰地将性能提升归因于新的退化框架。这有力地支撑了其核心论点。
    -   **不足**：
        1.  **场景单一**：仅在合成高斯模糊上进行验证，缺乏对真实世界模糊、运动模糊等更多样退化的实验。
        2.  **缺少消融实验**：没有消融研究来探索不同迭代步数 `n`、不同维纳滤波正则化参数 `S`，或核估计误差对结果的具体影响。
        3.  **客观性**：对 SOTA 方法的比较，存在训练/评估协议不一致的潜在风险，因为 SOTA 方法的结果可能是从其原始论文中引用的。实验数量和多样性略显不足，对于充分证明一个全新框架的普适性和鲁棒性而言，证据稍显单薄。

### 6. 论文的主要结论与发现

-   **物理一致性优于噪声范式**：ConvDiff 在 PSNR（29.59 dB）和 SSIM（0.78）上显著优于传统基于噪声/插值的扩散模型 SR3（25.35 dB）和 INDI（26.09 dB），证明了在特定任务中，**遵从物理规律的退化过程比通用的噪声假设更有效**。
-   **对 SOTA 的差距**：尽管 ConvDiff 提升巨大，但其性能仍然落后于 FFTformer、HiDiff 等端到端训练的 SOTA 方法（PSNR ~31.2 dB）。论文将此差距归结于推理阶段的**迭代维纳核估计引入的近似误差**。
-   **新范式建立**：论文成功建立了一个“用卷积代替噪声”进行扩散的新范式，其核心贡献在于思想和框架的转变，而非追求绝对的最高性能。

### 7. 优点

-   **理论创新性强**：指出了去模糊任务中“扩散=去噪”的根本矛盾，并提出了一个优雅、数学上严谨的解决方案，利用频域特性将单步卷积转化为可微、可迭代的连续过程。
-   **物理可解释性高**：前向过程每一步都产生有意义的“部分模糊”图像，与物理世界的退化过程一致，而不仅仅是算法上的中间产物。这是一个重要的概念贡献。
-   **对比实验公平**：与 SR3 和 INDI 共享完全相同的网络基座进行对比，干净地剥离并凸显了“前向退化过程定义”这一单一变量所带来的巨大性能影响，论证极具说服力。

### 8. 不足与局限

-   **假设限制强，应用场景窄**：
    -   目前仅适用于**空间不变**的模糊，特别要求核的分数次幂变换在空间域仍为有效核（如高斯核），难以直接推广到更常见的、全局不一致的运动模糊等。
    -   依赖核已知（训练）或可被维纳滤波准确估计（推理），在真实复杂噪声和模糊共存的场景下，简单维纳滤波的估计能力存疑。
-   **推理机制引入误差**：迭代式维纳核估计步骤与神经网络预测步骤解耦，其误差会在迭代过程中传播和积累，限制了性能上界，成为通往更高性能的瓶颈。
-   **实验不够全面**：
    -   仅在合成高斯模糊的单一数据集上验证，缺乏对真实图像去模糊、更复杂退化类型或不同噪声水平下的测试，泛化能力未被证实。
    -   缺少超参数和算法构件的消融实验，无法量化各设计选择（如迭代步数、正则化参数）的贡献与影响的详细分析。

（完）
