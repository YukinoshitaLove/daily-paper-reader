---
title: Spatial Gram Alignment for Ultra-High-Resolution Image Synthesis
title_zh: 用于超高分辨率图像合成的空间格拉姆对齐
authors: "Jinjin Zhang, Xiefan Guo, Di Huang"
date: 2026-05-20
pdf: "https://arxiv.org/pdf/2605.20808v1"
tags: ["query:real-ir"]
score: 9.0
evidence: 通过潜在扩散模型和空间格拉姆对齐实现超高分辨率合成
tldr: 超高分辨率图像合成依赖大规模预训练潜在扩散模型（LDM），然而，将基础模型视觉先验直接蒸馏到生成特征会扰动预训练流形，导致极端分辨率下生成质量下降。本文提出空间格拉姆对齐（SGA），采用非侵入式空间约束，对齐生成特征与基础先验的内部自相似性，从而在不破坏预训练流形的前提下建立宏观结构一致性。实验显示，SGA在超高分辨率文本到图像合成上取得最先进性能，有效调和了全局结构完整性与细粒度细节。该通用策略无缝集成至LDM的扩散特征和VAE潜变量，为超高分辨率生成任务提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.20808v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1419, \"height\": 563, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.20808v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1431, \"height\": 1184, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.20808v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1449, \"height\": 1507, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.20808v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1443, \"height\": 405, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.20808v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1303, \"height\": 352, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.20808v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 662, \"height\": 272, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.20808v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 672, \"height\": 220, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.20808v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 583, \"height\": 223, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.20808v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1149, \"height\": 220, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.20808v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1442, \"height\": 133, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.20808v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 510, \"height\": 158, \"label\": \"Table\"}]"
motivation: 现有表示对齐方法在超高分辨率下强制逐块特征蒸馏，会扰动预训练LDM的潜在流形，引发可学习性与保真度冲突，导致生成退化。
method: 提出空间格拉姆对齐（SGA），通过约束生成特征与基础先验的内部自相似性（空间格拉姆矩阵），实现非侵入式结构对齐，保留原生生成能力。
result: SGA在超高分辨率文本到图像合成中实现SOTA性能，成功同时保持全局结构完整性和细粒度视觉细节。
conclusion: SGA提供了一种不破坏预训练流形的高效对齐策略，可无缝集成到LDM中，为超高分辨率生成开辟了新路径。
---

## 摘要
现代超高分辨率图像合成高度依赖于大规模预训练潜在扩散模型（LDMs）的强大生成能力。尽管最近的表示对齐方法通过将基础模型（如 SAM 或 DINO）的视觉先验蒸馏到生成式潜特征中已被证明有效，但在将此类方法扩展至极高分辨率的预训练 LDM 时，会暴露出关键的可学习性-保真度冲突。具体而言，强制性的逐块特征蒸馏会固有地扰乱预训练的潜在流形，最终导致生成质量下降。针对这一瓶颈，我们提出了空间格拉姆对齐（SGA），这是一种新颖的框架，能够显式利用视觉基础模型的表示先验，同时保留 LDM 的自然生成能力。SGA 突破了限制性的直接对齐，通过将生成特征的内在自相似性与基础先验的自相似性对齐，施加了非侵入性的空间约束。该空间约束有效建立了宏观结构一致性，而原生生成目标则保留了原始 LDM 固有的微观像素级保真度。值得注意的是，这一通用策略能够无缝集成到预训练 LDM 的中间扩散特征和 VAE 潜变量中。大量实验表明，SGA 在超高分辨率文本到图像合成中达到了最先进水平，实现了全局结构完整性与精细视觉细节之间的有效调和。代码可在 https://github.com/zhang0jhon/SGA 获取。

## Abstract
Modern ultra-high-resolution image synthesis relies heavily on the robust generative capacity of large-scale pre-trained Latent Diffusion Models (LDMs). While recent representation alignment methods have proven effective by distilling visual priors from foundation models (e.g., SAM or DINO) into generative latent features, scaling these approaches to pre-trained LDMs at extreme resolutions exposes a critical learnability-fidelity conflict. Specifically, forcing direct patch-wise feature distillation inherently perturbs the pre-trained latent manifold, ultimately leading to generation degradation. To address this bottleneck, we propose Spatial Gram Alignment (SGA), a novel framework that explicitly leverages the representation priors of vision foundation models while preserving the native generative capacity of LDMs. Moving beyond restrictive direct alignment, SGA imposes a non-invasive spatial constraint by aligning the internal self-similarities of the generative features with those of the foundation priors. This spatial constraint effectively establishes macroscopic structural coherence, while the native generative objectives retain the microscopic pixel-level fidelity inherent to the original LDMs. Notably, this versatile strategy integrates seamlessly across both intermediate diffusion features and VAE latents within pre-trained LDMs. Extensive experiments demonstrate that SGA achieves state-of-the-art performance for ultra-high-resolution text-to-image synthesis, yielding an effective reconciliation between global structural integrity and fine-grained visual details. Code is available at https://github.com/zhang0jhon/SGA.

---

## 论文详细总结（自动生成）

好的，根据您提供的论文，以下是为您生成的结构化总结。

### 1. 论文的核心问题与整体含义

*   **研究背景**：超高分辨率（如4K）图像合成目前严重依赖大规模预训练的潜在扩散模型（LDM）。为了进一步提升模型的空间结构和生成质量，一种主流的范式是**表示对齐（REPA）**，即将视觉基础模型（如DINO、SAM）的深层语义先验通过特征蒸馏的方式注入到生成模型的潜空间中。
*   **核心问题**：论文揭示了将现有的REPA方法直接应用于**微调**预训练LDM以进行超高分辨率合成时的一个根本性冲突，即**可学习性-保真度冲突**。
    *   **可学习性**：来自基础模型的特征编码了宏观的语义拓扑结构，有助于生成模型学习全局结构。
    *   **保真度**：预训练LDM的VAE潜空间则保留了微观的高频细节。
    *   **冲突所在**：强制性的、直接的逐块特征蒸馏会迫使生成特征的流形**均质化**，向基础模型的特征空间靠拢，从而扰乱和破坏预训练LDM中固有的、用于生成精细细节的潜在流形，最终导致生成质量在极端分辨率下严重退化。
*   **整体含义**：该研究旨在调和上述冲突，提出一种既能利用基础模型的结构先验，又能完整保留预训练LDM原生高保真生成能力的新范式。

### 2. 论文提出的方法论

*   **核心思想**：提出**空间格拉姆对齐（SGA）**，一种非侵入式（non-invasive）的表示对齐框架。它不强制生成特征去严格模仿基础先验的绝对特征值，而是对齐两者内部的空间自相似性结构。
*   **关键技术细节**：
    *   **特征提取与归一化**：给定清晰图像，从冻结的视觉基础模型提取深度语义表征；同时从目标生成模块（VAE编码器或扩散网络中间层）提取生成特征。两者经过投影头和L2归一化后映射到共享特征空间。
    *   **空间格拉姆矩阵构建**：通过矩阵乘法分别计算生成特征和基础先验的**空间格拉姆矩阵** 。该矩阵封装了所有空间块之间密集的结构相关性。
    *   **非侵入性空间约束**：SGA的核心目标是最小化两个格拉姆矩阵之间的**缩放平方Frobenius范数**。这种对齐是一种相对拓扑约束，允许生成特征在正交变换下保持不变（通道正交规范不变性），从而为生成特征保留了远多于直接块匹配的**自由度**，有效避免了跨模型特征坐标的均质化。
*   **公式或算法流程**：
    1.  **VAE阶段**：在标准VAE损失（重建、感知、对抗损失）基础上，增加**SGA损失**和**矩对齐损失**（锚定潜空间统计量）。此阶段旨在将SGA注入到潜压缩过程，在保证更高压缩比（如16倍）的同时增强复原保真度。
    2.  **扩散阶段**：在标准的流匹配或扩散损失基础上，直接增加应用于扩散网络中间隐藏状态的**SGA损失**。此阶段将结构先验注入到生成式去噪过程中，以在超高分辨率下合成连贯的全局构图。

### 3. 实验设计

*   **数据集与场景**：
    *   **VAE训练**：使用了一个包含超过1200万张图像的大规模高分辨率数据集，来源包括SA-1B、FFHQ、Mapillary Vistas等。
    *   **扩散模型训练与评估**：主要在 **Aesthetic-Train**（12,015张高质量图像）上微调，在 **Aesthetic-Eval** 基准上评估。评估场景为**超高分辨率图像合成**，具体包括**2K和4K**两种分辨率。
*   **对比方法**：
    *   **基准模型**：原始`Flux`。
    *   **强基线方法**：`Flux-WLF (Diffusion-4K)`，这是专门针对4K生成的方法。
    *   **主流对齐方法**：`iREPA`，在消融研究中作为直接块匹配对齐策略的代表进行对比。
*   **评估指标**：
    *   **整体指标**：gFID， CLIP Score， Aesthetics Score。
    *   **局部指标**：GLCM Score， Compression Ratio。
    *   **VAE重建指标**：rFID， NMSE， PSNR， SSIM, LPIPS。

### 4. 资源与算力

*   **VAE阶段**：在**16块** NVIDIA **H100** GPU上，以1024×1024分辨率，总批次大小160，微调了**2个epoch**。优化器为AdamW，学习率1e-5。
*   **扩散阶段**：在**8块** NVIDIA **H100** GPU上，使用DeepSpeed ZeRO加速，总批次大小32，训练了**20，000次迭代**（约1个epoch）。优化器为AdamW，学习率1e-6。

### 5. 实验数量与充分性

*   **实验数量**：论文包含了较为充分的实验。
    *   **主要结果**：在Aesthetic-Eval@2K和@4K两个基准上，使用3种不同基础模型（SAM2， DINOv2, DINOv3）与基准方法进行了全面的定量和定性对比。
    *   **消融研究**：设计了4组消融实验，分别验证了对齐策略（SGA vs iREPA）、对齐层深度、以及对VAE和扩散模块单独应用SGA的效果。这些实验有效支持了论文的核心论点。
*   **客观与公平性**：实验设计客观公平。对比方法采用了当前最先进的开源模型和基线，所有对比都在相同的数据集和评估协议下进行。消融实验严格控制变量，如在对齐策略对比中，使用了相同的基础先验、层位置和超参数。

### 6. 论文的主要结论与发现

*   SGA能有效调和在微调预训练LDM进行超高分辨率生成时遇到的**可学习性-保真度冲突**。
*   通过对齐空间自相似结构而非绝对特征值，SGA是一种**非侵入性**的方法，能够成功注入基础先验而不破坏预训练的生成流形。
*   SGA框架具有高度的**通用性**，可无缝集成到LDM的VAE和扩散模型两个阶段，并兼容多种视觉基础模型。
*   SGA在4K文本到图像合成任务上取得了**最先进性能**，能够在保持宏观结构完整性的同时，提升微观细节的保真度。

### 7. 优点

*   **问题洞察深刻**：明确识别并定义了“可学习性-保真度冲突”这一关键瓶颈。
*   **方法设计优雅**：采用空间格拉姆矩阵对齐，并以理论分析（附录A）论证了其非侵入性（通道规范不变性、零损失集更大），从原理上解释了为何优于直接块匹配。
*   **框架高度统一**：提出了一个模板式损失函数，将SGA统一应用于VAE压缩和扩散生成两个阶段，实现了端到端的结构先验注入。
*   **实验说服力强**：不仅有丰富的定量指标和可视化对比，消融实验也设计得很有针对性，清晰证明了SGA各组件和设计选择的有效性。

### 8. 不足与局限

*   **模型泛化性验证有限**：方法的验证主要基于一个代表性的先进模型（Flux），虽然文中将其作为案例研究并提出了未来拓展方向，但尚未在更多样化的预训练LDM（如SD3.5等）上验证其泛化能力。
*   **依赖特定基础模型的配置**：SGA的性能在一定程度上依赖于基础模型，例如在4K合成中，将DINOv2的对齐分辨率从32提升至64能带来显著性能提升，这暗示了方法性能可能对基础模型的内在结构（如块大小）较为敏感。
*   **计算开销**：虽然单次SGA损失计算代价与iREPA相当，但其绝对值依然存在，且文中未详细探讨在使用不同基础模型、不同层或不同分辨率进行对齐时，对总训练时间的具体影响。

（完）
