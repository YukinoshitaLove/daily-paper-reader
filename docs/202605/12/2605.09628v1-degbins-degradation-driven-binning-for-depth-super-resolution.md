---
title: "DegBins: Degradation-Driven Binning for Depth Super-Resolution"
title_zh: DegBins：基于退化驱动分箱的深度超分辨率
authors: "Zhiqiang Yan, Zhengxue Wang, Jian Yang, Gim Hee Lee"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09628v1"
tags: ["query:real-ir"]
score: 10.0
evidence: 退化驱动的分箱用于深度超分辨率
tldr: 本文提出DegBins框架，通过退化驱动的分箱机制将深度残差建模为混合分类-回归任务，并结合多阶段粗到细的优化策略，有效解决空间退化导致的重建误差问题，在多项基准测试中取得优异表现。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09628v1/fig-001.webp\", \"caption\": \"Table 1: Results on synthetic DSR benchmarks. The best and second-best metrics are highlighted. Method Scale Middlebury Lu NYU v2 RGB-D-D TOFDSR\", \"page\": 6, \"index\": 1, \"width\": 829, \"height\": 834}, {\"url\": \"assets/figures/arxiv/2605.09628v1/fig-002.webp\", \"caption\": \"Figure 4: Quantitative comparisons of arbitrary scaling factors on five synthetic DSR datasets.\", \"page\": 7, \"index\": 2, \"width\": 760, \"height\": 388}, {\"url\": \"assets/figures/arxiv/2605.09628v1/fig-003.webp\", \"caption\": \"Table 2: Cross-scale DSR on synthetic RGB-D-D and Lu.\", \"page\": 7, \"index\": 3, \"width\": 488, \"height\": 336}, {\"url\": \"assets/figures/arxiv/2605.09628v1/fig-004.webp\", \"caption\": \"Table 6: Complexity comparisons on the real-world RGB-D-D dataset. All methods are measured using a single 4090 GPU.\", \"page\": 14, \"index\": 4, \"width\": 535, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2605.09628v1/fig-005.webp\", \"caption\": \"Figure 2: Framework of DegBins, where DDB represents the degradation-driven binning strategy.\", \"page\": 3, \"index\": 5, \"width\": 822, \"height\": 320}, {\"url\": \"assets/figures/arxiv/2605.09628v1/fig-006.webp\", \"caption\": \"Table 3: Results on real-world DSR datasets: RGB-D-D, TOFDSR, and their noisy patterns.\", \"page\": 8, \"index\": 6, \"width\": 784, \"height\": 207}, {\"url\": \"assets/figures/arxiv/2605.09628v1/fig-007.webp\", \"caption\": \"Figure 5: Error map comparisons on the real-world RGB-D-D dataset.\", \"page\": 8, \"index\": 7, \"width\": 746, \"height\": 504}, {\"url\": \"assets/figures/arxiv/2605.09628v1/fig-008.webp\", \"caption\": \"Figure 7: Ablation of the stage and number of bins on the real-world RGB-D-D dataset.\", \"page\": 9, \"index\": 8, \"width\": 630, \"height\": 202}, {\"url\": \"assets/figures/arxiv/2605.09628v1/fig-009.webp\", \"caption\": \"Figure 3: Error map comparisons on synthetic NYU v2 with ×4 and ×16 upscaling factors.\", \"page\": 5, \"index\": 9, \"width\": 754, \"height\": 515}]"
motivation: 现有基于残差学习的方法难以应对空间变化的退化，导致深度重建精度不足。
method: 提出DegBins框架，通过退化驱动的分箱和多阶段细化机制，将深度残差建模为分类与回归的混合问题。
result: 在五个基准上，DegBins在准确率、鲁棒性及泛化性均优于最新方法。
conclusion: DegBins显著提升了深度超分辨任务的重建精度、鲁棒性和泛化能力。
---

## 摘要
深度超分辨率（Depth Super-Resolution，DSR）的目标是从低分辨率（LR）深度图恢复出高分辨率（HR）深度图。借助彩色图像的引导，这一任务通常被表述为在低维特征空间中学习 HR 与 LR 之间的残差。然而，这种简单的加法形式不足以准确捕捉 HR 与 LR 之间复杂的关系，尤其是在存在空间变化退化的情况下。本文提出了 DegBins，这是一种新颖的 DSR 框架，利用退化驱动的分箱（degradation-driven binning）来自适应地增强残差建模。具体而言，DegBins 将基于回归的 DSR 重新表述为一个混合分类-回归问题，其中残差深度被表示为离散深度分箱的线性组合，并由其学习到的概率分布加权，从而获得更灵活、更具表达力的表示。此外，DegBins 在高维特征空间中建模 HR 与 LR 之间的退化关系，使得分箱范围的自适应调整和概率优化能够根据局部退化特性进行。为逐步提升重建质量，DegBins 采用多阶段细化方案，每一阶段都基于前一阶段的估计执行更细粒度的分箱划分和概率更新。这种由粗到细的设计有助于实现更准确的深度恢复，特别是在存在严重退化或复杂结构变化的区域。大量在五个基准上的实验表明，DegBins 在精度、鲁棒性和泛化能力方面均持续优于现有的最新方法。

## Abstract
Depth super-resolution (DSR) aims to recover a high-resolution (HR) depth map from its low-resolution (LR) counterpart. With color image guidance, this task is typically formulated as learning the residual between HR and LR in a low-dimensional feature space. However, this additive formulation is insufficient to accurately capture the complex relationship between HR and LR, especially under spatially varying degradations. In this paper, we introduce DegBins, a novel DSR framework that leverages degradation-driven binning to adaptively enhance residual modeling. Specifically, DegBins reformulates the regression-based DSR as a hybrid classification-regression problem, where the residual depth is represented as a linear combination of discrete depth bins weighted by their learned probability distribution, yielding more flexible and expressive representations. Furthermore, DegBins models the degradation relationship between HR and LR in a high-dimensional feature space, enabling adaptive bin range adjustment and probability optimization conditioned on local degradation characteristics. To progressively improve reconstruction quality, DegBins adopts a multi-stage refinement scheme, where each stage performs finer-grained bin partitioning and probability updating based on the former estimation. This coarse-to-fine design facilitates more accurate depth recovery, particularly in regions with severe degradations or complex structural variations. Extensive experiments across five benchmarks demonstrate that DegBins consistently outperforms existing state-of-the-art methods in terms of accuracy, robustness, and generalization.

---

## 论文详细总结（自动生成）

# DegBins：基于退化驱动分箱的深度超分辨率研究总结

---

## 一、研究背景与问题动机

- **研究领域**：深度超分辨率（Depth Super-Resolution, DSR）旨在从低分辨率（LR）深度图恢复高分辨率（HR）深度图，是三维重建、增强现实与机器人视觉中的关键任务。
- **已有问题**：
  - 现有方法多数采用**残差学习（residual learning）**，假设 HR 与 LR 深度间存在简单的加法关系。
  - 实际应用中，**空间退化（spatially varying degradations）**导致深度关系复杂，如噪声、模糊、传感器误差与图像错位等。
  - 固定模型难以准确恢复结构复杂或退化严重的区域。
- **论文动机**：
  - 打破单纯的加法残差假设，引入**分布式建模思想**；
  - 结合退化表征（degradation representation）与概率建模，使深度超分辨在局部自适应调整下更加鲁棒和表达力强。

---

## 二、方法论：核心思想与算法流程

### 1. 核心思想
- 提出 **DegBins 框架**，将 DSR 问题从单纯的回归任务重新表述为**混合“分类+回归”问题**：
  - 将深度残差范围离散化为多个分箱（bins）；
  - 每个像素的深度由若干分箱中心的加权线性组合（按概率分布权重）计算得到；
  - 借助高维退化特征，自适应调整分箱范围与概率分布。

### 2. 网络结构与模块
- **主干网络**：使用预训练 **DuCos [45]** 作为特征编码器。
- **退化表示获取**：
  - 利用预训练的 **DORNet [38]** 模型获得退化关系 \( D_i = D(X_i, X_5) \)；
  - 在高维空间中计算退化标准差，用于动态扩展 bin 范围。
- **退化驱动分箱（Degradation-Driven Binning, DDB）**：
  - 将像素深度范围 \([v_{\text{min}}(p), v_{\text{max}}(p)]\) 均匀划分为 \( N=32 \) 个分箱；
  - 根据退化标准差 \(\sigma(p)\)，更新 bin 范围：
    \[
    [u_n(p) - \gamma \sigma(p),\, u_{n+1}(p) + \gamma \sigma(p)] \quad \text{其中} \gamma = 0.2
    \]
  - 使用 **GRU 单元 + Deformable Convolution (DCN)** 优化概率分布：
    \[
    P_n(p) = \text{Softmax}(\text{ReLU}(G(Q, H_t)))
    \]
  - 最终深度预测：
    \[
    X'_i(p) = \sum_{n=0}^{N-1} C_n(p) \cdot P_n(p)
    \]
- **多阶段粗到细优化**：
  - 模型由 4 个阶段组成，每阶段细化分箱与概率分布；
  - 表达式：\( X_{i+1} = X_i + P(X_i, G_i, D_i, Y) \)。

### 3. 损失函数
- **重建损失**：
  \[
  L_{\text{rec}} = \frac{1}{m} \|X - Z\|_1
  \]
- **分箱损失（Chamfer Loss）**：
  \[
  L_{\text{bin}} = \sum_{z \in Z}\min_{c \in C}\|c-z\|^2 + \sum_{c \in C}\min_{z \in Z}\|c-z\|^2
  \]
- **总损失**：
  \[
  L = L_{\text{rec}} + \alpha L_{\text{bin}}, \quad \alpha=0.1
  \]

---

## 三、实验设计

### 1. 数据集与场景
- 训练集：**Hypersim [23]** 合成数据集；
- 测试集（共五个 benchmark）：
  - **TOFDSR [42, 44]**
  - **RGB-D-D [9]**
  - **NYUv2 [28]**
  - **Middlebury [10, 24]**
  - **Lu [21]**
- 实验场景涵盖：
  - 合成 DSR；
  - 任意缩放因子（integer & non-integer）；
  - Cross-scale DSR；
  - 实际真实退化；
  - 压缩深度恢复。

### 2. 对比方法
- 对比了13种主流方法，包括 **DJF, DJFR, CUNet, DKN, FDKN, FDSR, DCTNet, DADA, DORNet, DuCos** 等。

---

## 四、资源与算力

- **训练平台**：
  - 使用 **4 × NVIDIA RTX A6000** GPU；
  - 训练 **200 epochs**；
  - 优化器：**Adam**，初始学习率 \(5×10^{-5}\)，每 40, 80, 120 epoch 减半。
- **复杂度分析**：
  - DegBins（DuCos 主干）参数规模 **42.15M**，速度 **约28 FPS**；
  - 替换轻量主干（Residual Group [53]）后参数降至 **16.28M**，速度提升至 **102 FPS**；
  - 推理时间较高但可平衡准确率与效率。
- **数据增强**：随机水平翻转与 90° 旋转，输入裁剪为 256×256。

---

## 五、实验数量与充分性

- **基准对比实验**：
  - 5个公开数据集 × 多缩放因子（×2, ×4, ×8, ×16, ×32），共计数十组量化结果。
- **跨尺度实验**：
  - 直接迁移 ×4 训练模型到 ×8、×16 验证任务。
- **真实场景与压缩深度实验**：
  - 分别测试噪声与模糊退化输入情况。
- **消融实验**：
  - 包括分箱策略对比（AdaBins, LocalBins, IEBins）、阶段数与分箱数量分析、退化特征影响分析。
- **充分性评价**：
  - 覆盖合成与真实场景、不同尺度与退化类型；
  - 对比模型数量充足；
  - 指标使用 RMSE、MAE、δ1，评估全面、公平。

---

## 六、主要结论与发现

- DegBins 在五大基准上均优于最新方法：
  - 提升 RMSE 平均约 20–30%，δ1 提升 0.5–2个百分点。
- 在高退化与复杂结构场景下表现尤为显著；
- 多阶段分箱策略可有效降低预测误差（如 ×16 比 DuCos 减少 0.26 cm RMSE）；
- 在真实传感器和压缩传输环境中具有极强鲁棒性；
- 同时具备较好的跨尺度泛化能力（无需微调即可适应不同放大倍数）。

---

## 七、方法与实验亮点

- **创新性**：
  - 首次将 DSR 从回归任务扩展为“退化驱动的分布式建模”；
  - 结合高维退化特征与概率建模，实现自适应 bin 调整。
- **技术亮点**：
  - GRU 与 DCN 融合概率估计；
  - 多阶段粗到细优化。
- **实验亮点**：
  - 实验覆盖全面，验证从合成到真实场景的泛化；
  - 消融实验系统地证明每一模块的贡献；
  - 提供复杂度分析与轻量版本讨论。

---

## 八、不足与局限

- **计算复杂度高**：
  - 主干网络依赖大型基础模型（如 DuCos），推理速度相对慢；
  - 实时应用仍然有限。
- **轻量化不足**：
  - 虽提供轻量版本 DegBins-RG，但仍需进一步改进以满足移动端要求。
- **实验范围局限**：
  - 当前未在户外真实场景或多传感器融合环境验证；
  - 主要聚焦于点云深度图像的二维重建。
- **理论局限**：
  - 分箱策略参数（如 γ, N）需人工设定，模型可能对超参数敏感。

---

（完）
