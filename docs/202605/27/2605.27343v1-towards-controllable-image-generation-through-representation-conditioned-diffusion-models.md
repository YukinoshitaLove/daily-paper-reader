---
title: Towards Controllable Image Generation through Representation-Conditioned Diffusion Models
title_zh: 面向可控图像生成的表示条件扩散模型
authors: "Nithesh Chandher Karthikeyan, Jonas Unger, Gabriel Eilertsen"
date: 2026-05-26
pdf: "https://arxiv.org/pdf/2605.27343v1"
tags: ["query:real-ir"]
score: 8.0
evidence: 基于自监督表示条件的扩散模型
tldr: 扩散模型在高质量图像生成中表现优异，但精确控制生成仍需大量标注数据。本文提出基于预训练自监督模型表示的条件扩散模型，利用自条件机制提升无条件生成质量，同时提供可控制的表示空间。通过识别表示空间中的变化方向，实现了平滑且可解耦的生成控制。该工作为无需标注数据的可控图像生成开辟了新途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27343v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 769, \"height\": 525, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.27343v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1579, \"height\": 403, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.27343v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 838, \"height\": 517, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.27343v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1489, \"height\": 490, \"label\": \"Figure\"}]"
motivation: 扩散模型依赖大量标注数据才能实现可控生成，亟需无监督的替代方案。
method: 使用预训练自监督模型提取表示作为条件，并通过探索表示空间中的变化方向实现生成控制。
result: 自条件机制提升了无条件生成质量，并实现了平滑可解耦的生成控制效果。
conclusion: 展示了自监督表示条件扩散模型在无需标注数据下进行可控图像生成的潜力。
---

## 摘要
扩散模型已成为高质量图像生成和编辑的强大工具，但引导这些模型产生特定输出仍是一大挑战。传统方法依赖于条件机制，如文本提示或语义图，这需要大量标注的数据集。在这项初步工作中，我们探索了基于预训练自监督模型表示的扩散模型条件机制。自条件机制不仅提高了无条件图像生成的质量，还提供了一个可用于控制生成的表示空间。我们通过识别变化方向来探索这一条件空间，并在平滑性和解耦性方面展示了有前景的特性。

## Abstract
Diffusion models have emerged as powerful tools for high-quality image generation and editing, but guiding these models to produce specific outputs remains a challenge. Conventional approaches rely on conditioning mechanisms, such as text prompts or semantic maps, which require extensively annotated datasets. In this preliminary work, we explore diffusion models conditioned on representations from a pre-trained self-supervised model. The self-conditioning mechanism not only improves the quality of unconditional image generation, but also provides a representation space that can be used to control the generation. We explore this conditioning space by identifying directions of variations, and demonstrate promising properties in terms of smoothness and disentanglement.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：扩散模型在高质量图像生成方面表现优异，但实现精确可控生成（如改变具体属性）仍然困难。传统方法依赖文本提示、语义图等条件机制，这些方法需要大量人工标注的数据集，且难以处理无标注或非结构化数据。
- **整体含义**：本文旨在探索一种无需标注数据即可实现可控图像生成的方法，通过利用预训练自监督模型（如DINO）提取的表示作为条件，为扩散模型提供一个**可操控的表示空间**，从而在提升无条件生成质量的同时，实现对生成过程的平滑、可解耦控制。

## 2. 论文提出的方法论

### 核心思想
- 使用**自监督学习（SSL）编码器**（本文采用 DINO）将图像映射到低维表示空间，然后训练一个潜在扩散模型（LDM），将该表示作为条件输入。
- 通过在该表示空间中执行**扰动、插值、寻找语义方向**等操作，实现对生成图像的控制，而无需依赖标注数据。

### 关键技术细节
1. **两阶段训练**：
   - **第一阶段**：利用预训练 DINO 编码器从图像中提取 768 维表示向量。
   - **第二阶段**：使用条件去噪 U-Net 训练扩散模型，去噪过程在 LDM 的潜在空间中由表示向量引导。
2. **控制操作**：
   - **扰动**：向表示向量添加高斯噪声 \(\hat{C} = C + \lambda \epsilon\)，观察不同噪声强度下的生成变化。
   - **插值**：对两个表示向量进行线性插值 \(\hat{C} = \alpha C_1 + (1-\alpha)C_2\)，生成过渡图像。
   - **监督语义方向**：计算具有特定属性（如“金发”）的样本表示均值，将其加到参考表示上，实现属性修改。
   - **无监督语义方向**：对表示空间做主成分分析（PCA），沿主成分方向扰动表示 \(\hat{C} = C + \alpha V_K\)，发现如额头大小、头发长度等变化。

### 公式/算法流程（文字说明）
- 训练：输入图像 → DINO 编码器 → 表示向量 → 条件去噪 U-Net → 生成图像。
- 控制：给定参考图像 → 提取表示 → 在该空间内进行数学操作 → 将修改后的表示送入条件 U-Net 生成新图像。

## 3. 实验设计

### 使用数据集
- **LSUN Churches**：用于训练和测试 RCDM（表示条件扩散模型）。
- **Celeb-A**：用于训练和监督/无监督语义方向实验。
- 另外，在扰动实验中使用了 **STL-10 数据集**（仅用于对比方法 Diffusion Inversion 的训练）。

### Benchmark
- 本文未使用标准的图像质量定量指标（如 FID、IS），仅进行定性展示。
- 对比的方法：
  - **Diffusion Inversion**（基于 DDIM 编码-解码的方法）
  - **Stable Diffusion**（文本条件潜在扩散模型）
  - 未对比 GAN 或其他代表性可控生成方法。

### 实验场景
1. **扰动实验**：对比 RCDM 与 Diffusion Inversion 在不同噪声强度下的生成质量与内容改变情况。
2. **插值实验**：对比 Stable Diffusion（文本嵌入插值）、Diffusion Inversion（学习嵌入插值）与 RCDM（表示插值）的平滑性。
3. **监督语义方向**：在 Celeb-A 上通过属性均值添加“金发”、“秃头”等。
4. **无监督语义方向**：在 Celeb-A 上使用 PCA 方向（取 K=3,4,6,9）进行控制。

## 4. 资源与算力

- **论文未明确说明**使用的 GPU 型号、数量、训练时长。仅提及训练了针对每个数据集的 RCDM，使用条件去噪 U-Net，具体计算资源信息缺失。

## 5. 实验数量与充分性

- **实验数量**：共进行了 4 组定性实验（扰动、插值、监督方向、无监督方向），分别对应图2、3、4(a)、4(b)。未进行消融实验（如不同表示编码器、不同插值方式）或定量评估。
- **充分性**：
  - **优点**：初步验证了表示空间的可控性和平滑性。
  - **不足**：
    - 缺乏定量指标（如 FID、LPIPS、属性精度）证明有效性。
    - 未与 GAN 的潜空间操控（如 StyleGAN、GANSpace）进行公平比较。
    - 实验仅基于两个数据集，且无监督方向解耦效果有限（文中自承不如 GANs）。
    - 未进行用户研究或统计显著性测试。

## 6. 主要结论与发现

- 表示条件扩散模型的**条件空间**具有以下优良特性：
  - **平滑性**：在表示空间中插值能生成语义平滑过渡的图像，优于 Stable Diffusion 的文本嵌入插值和 Diffusion Inversion 的学习嵌入插值。
  - **鲁棒性**：在高噪声扰动下，RCDM 仍能保持图像质量，而 Diffusion Inversion 则显著退化。
  - **部分解耦性**：通过监督或 PCA 方向可改变特定属性（如头发颜色、长度），尽管无监督方向解耦程度尚不如 GANs。
- 自条件机制在提升无条件生成质量的同时，为可控生成提供了新途径，且无需标注数据。

## 7. 优点

- **创新性**：将自监督表示条件扩散模型用于可控生成，拓展了无监督可控生成的研究方向。
- **无需标注数据**：利用预训练 SSL 模型，避免昂贵的人工标注。
- **表示空间可操作性强**：支持简单直接的数学操作（加/减、插值、PCA），且生成的图像保持整体一致性。
- **与现有条件机制互补**：可结合文本条件或其他控制方式。

## 8. 不足与局限

- **实验验证不足**：
  - 所有结果均为定性展示，缺乏定量指标（如 FID、属性控制准确率）。
  - 未与当前主流的可控扩散模型（如 DreamBooth、Custom Diffusion）在相同条件下对比。
  - 无监督语义方向解耦性有限，文中自身承认“不如 GANs”。
- **依赖预训练编码器**：使用固定的 DINO 表示，该表示的可能偏置会影响生成结果（如数据集相关性导致“秃头”属性同时改变性别）。
- **未独立训练表示生成器**：目前仅在测试集表示上操作，未实现完全的无条件生成控制（若能训练一个表示生成器，可提升实际可用性）。
- **计算资源未报告**：缺少训练和推理的时间/资源信息，难以评估可复现性。
- **应用范围有限**：仅在 Churches 和 Celeb-A 上验证，未推广到更复杂或多类别数据集。

（完）
