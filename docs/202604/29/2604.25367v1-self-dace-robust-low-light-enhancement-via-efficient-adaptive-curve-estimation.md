---
title: "Self-DACE++: Robust Low-Light Enhancement via Efficient Adaptive Curve Estimation"
title_zh: Self-DACE++：通过高效自适应曲线估计实现鲁棒的低光照增强
authors: "Jianyu Wen, Jun Xie, Feng Chen, Zhepeng Wang, Chenhao Wu, Tong Zhang, Yixuan Yu, Piotr Swierczynski"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25367v1"
tags: ["query:real-ir"]
score: 9.0
evidence: 低光照图像增强与修复框架
tldr: 本文提出Self-DACE++，一种基于自适应曲线估计的改进型无监督轻量框架，用于低光照图像增强。通过增强的自适应调整曲线、随机训练与网络融合机制，实现更高效的推理并保持图像的色彩与结构自然性。实验验证其在多数据集上性能优于现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.25367v1/fig-001.webp\", \"caption\": \"Fig. 3: Visual comparison of face detection on the DarkFace dataset. The blue box shows the detected human face, while the red box marks the GT. The number represents the confidence score from Retinaface, with only scores above 0.50 displayed.\", \"page\": 6, \"index\": 1, \"width\": 1056, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2604.25367v1/fig-002.webp\", \"caption\": \"TABLE I: Quantitative comparisons in terms of P (PSNR), S (SSIM), L (LPIPS), △ (CIEDE2000), and Average Precision (AP). We evaluate the face detection performance on the DarkFace dataset enhanced by different methods using RetinaFace with IoU thresholds of 0.25, 0.50, and 0.75. The table is organized into four sections: Supervised methods, followed by Unsupervised methods categorized into Large, Small, and Tiny scales based on model complexity and inference speed. The best result is in bold, the sub-optimal result is underlined. ◦ denotes the results are trained on LOL and tested on SCIE dataset. Results marked with • are retrained and tested on SCIE dataset by us.\", \"page\": 5, \"index\": 2, \"width\": 1064, \"height\": 376}, {\"url\": \"assets/figures/arxiv/2604.25367v1/fig-003.webp\", \"caption\": \"Fig. 2: Visual Comparison on LOL dataset.\", \"page\": 5, \"index\": 3, \"width\": 1066, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2604.25367v1/fig-004.webp\", \"caption\": \"Fig. 1: The framework of Self-DACE++. (a) The standard DM module employed by both the Standard and Small versions. (b) The lightweight and fast DM module designed specifically for the Tiny version.\", \"page\": 3, \"index\": 4, \"width\": 914, \"height\": 825}]"
motivation: 为平衡低光照图像增强中的计算效率和恢复质量，需开发更高效且鲁棒的模型。
method: 提出了改进版的无监督轻量框架Self-DACE++，利用增强的自适应调整曲线和随机训练及网络融合机制进行低光照图像增强。
result: 在多种真实数据集上，自DACE++的增强效果和实时性能优于现有方法。
conclusion: Self-DACE++实现了高质量、实时且稳定的低光照图像增强，为相关应用提供了高效解决方案。
---

## 摘要
本文提出了 Self-DACE++，一种改进的无监督且轻量的低光照图像增强（Low-Light Image Enhancement，LLIE）框架，基于我们先前的 Self-Reference Deep Adaptive Curve Estimation（Self-DACE）方法。为更好地平衡计算效率与复原质量，Self-DACE++ 引入了改进的自适应调整曲线（Adaptive Adjustment Curves，AACs）。这些曲线由极少的可训练参数控制，可灵活调整动态范围，同时保持增强图像的色彩保真度、结构完整性和自然感。为了在不牺牲性能的情况下实现极其轻量的架构，我们提出了一种随机顺序训练策略结合网络融合机制，将模型压缩为高效的迭代推理结构。此外，我们基于 Retinex 理论构建了物理驱动的目标函数，并引入专用的去噪模块，有效估计并抑制暗区域中的潜在噪声。大量的定性与定量实验表明，Self-DACE++ 在多个真实世界基准数据集上优于现有的先进方法，提供卓越的增强质量和实时推理能力。代码可在 https://github.com/John-Wendell/Self-DACE 获取。

## Abstract
In this paper, we present Self-DACE++, an improved unsupervised and lightweight framework for Low-Light Image Enhancement (LLIE), building upon our previous Self-Reference Deep Adaptive Curve Estimation (Self-DACE). To better address the trade-off between computational efficiency and restoration quality, Self-DACE++ introduces enhanced Adaptive Adjustment Curves (AACs). These curves, governed by minimal trainable parameters, flexibly adjust the dynamic range while preserving the color fidelity, structural integrity, and naturalness of the enhanced images. To achieve an extremely lightweight architecture without sacrificing performance, we propose a randomized order training strategy coupled with a network fusion mechanism, which compresses the model into an efficient iterative inference structure. Furthermore, we formulate a physics-grounded objective function based on Retinex theory and incorporate a dedicated denoising module to effectively estimate and suppress latent noise in dark regions. Extensive qualitative and quantitative evaluations on multiple real-world benchmark datasets demonstrate that Self-DACE++ outperforms existing state-of-the-art methods, delivering superior enhancement quality with real-time inference capability. The code is available at https://github.com/John-Wendell/Self-DACE.

---

## 论文详细总结（自动生成）

# Self-DACE++：通过高效自适应曲线估计实现鲁棒的低光照增强 — 中文总结

---

## 一、研究背景与核心问题

- **研究动机**：低光照条件下拍摄的图像常出现亮度不足、噪声增强、颜色失真等问题，严重影响计算机视觉任务（如检测与识别）的性能。  
- **问题关键**：现有基于深度学习的低光照图像增强方法在效率与质量上存在两难困境：
  - **曲线类方法**（如 ZeroDCE）虽高效但对复杂光照变化鲁棒性不足；
  - **生成式方法**（如 GAN、Transformer）增强效果好但计算代价高、泛化性差。
- **论文目标**：在不依赖成对监督数据的前提下，实现**高质量、实时、轻量化**的低光照增强。  
  自-DACE++ 继承并改进了作者早期的 Self-DACE 框架，针对噪声抑制与泛化能力提升进行优化。

---

## 二、方法论与技术创新

### 1. 核心思想
- 提出改进的 **自适应调整曲线（Adaptive Adjustment Curves, AACs）**，以极少参数实现对亮度与色彩的动态调节；
- 结合 **随机顺序训练策略 + 网络融合机制**，将复杂深度网络压缩成迭代推理的轻量结构，保持性能与实时性平衡；
- 基于 **Retinex 理论** 构建物理驱动的无监督损失函数体系；
- 引入 **专用去噪模块（Denoising Block）**，显式估计并抑制暗区的潜在噪声。

### 2. 关键技术细节
- **Adaptive Adjustment Curves（AACs）**：
  - 包括低亮区域增强（LAEC）与高亮区域抑制（HASC）两分支；
  - 以 sigmoid 函数控制像素级强度范围，避免曲线震荡与过曝；
  - α 控制增强幅度，β 控制照度范围（均为可训练标量图）。
  
- **框架结构**：
  - 由 **照度调节模块（IA Block）** 与 **去噪模块（DN Block）** 组成；
  - IA 模块融合随机训练的多子模块 (Disordered Modules, DMs)，通过权重平均得到压缩单元；
  - 形成类似 RNN 的迭代结构，明显减少参数与内存占用。
  
- **损失函数设计（基于 Retinex）**：
  - 多项无监督约束共同指导训练：
    - 反射一致性损失 L_RC（保持色彩分布）；
    - 白平衡损失 L_WB（防止过饱和）；
    - 照度损失 L_IL（实现亮度恢复）；
    - 曲线平滑损失 L_CS（保持空间一致性）；
    - 去噪损失 L_DN（SSIM + 梯度约束结合）。
  - 最终总体损失：  
    `L = wR·LRC + wW·LWB + wI·LIL + wζ·LCS + LDN`，配有精细权重调度。

- **模型变体**：
  - **Standard**：完整模型，最高质量；
  - **Small**：去除去噪模块，更快推理；
  - **Tiny**：极简结构，仅保留核心曲线，适合嵌入式设备。

---

## 三、实验设计与比较策略

- **训练数据**：使用 SCIE Part1 数据集训练；图像尺寸统一为 256×256。
- **测试与对比**：
  - **公开基准数据集**：
    - LOL-test dataset（标准低光照增强评估集）；
    - SCIE-part2（跨域泛化评估）；
    - DarkFace（下游人脸检测场景）。
  - **评估指标**：
    - PSNR（峰值信噪比）
    - SSIM（结构相似度）
    - LPIPS（感知质量）
    - CIEDE2000（颜色准确度）
    - AP（平均检测精度）
  - **对比方法**：
    - 监督方法：KinD++, URN, SNR-Net, Retinexformer；
    - 无监督方法：ZeroDCE, EnGAN, RUAS, SCI, PairLIE, RRDNet；
    - 分类别比较（Large / Small / Tiny），覆盖不同复杂度与实时性能。

---

## 四、资源与算力说明

- 文中未明确说明训练所用 GPU 类型、数量或训练时长。  
- 实验部分仅提到推理速度测评在 **NVIDIA A100 GPU** 上进行。  
- 未提供详细算力资源配置或时间消耗指标，因此算力信息不充分。

---

## 五、实验数量与充分性分析

- 含多种实验类型：
  1. **跨数据集测试**（LOL->SCIE）验证泛化；
  2. **多模型规模对比**（Standard、Small、Tiny）；
  3. **消融实验**（表 II 与表 III）分析模块与损失的作用；
  4. **下游检测实验**（DarkFace 上的 RetinaFace 性能验证）。
- 数据集与指标覆盖全面，涉及结构质量、感知质量与应用层性能。
- 对比方法涵盖主流 LLIE 框架，评价公平。
- 实验设计总体充分，能支持作者的结论。

---

## 六、主要结论与发现

- Self-DACE++ 相比当前主流方法，在 **视觉质量、泛化能力和实时性** 方面均表现优异：
  - 在 LOL-test 上取得最高的无监督性能（PSNR 19.69 dB、SSIM 0.78）；
  - 在未见过的 SCIE-part2 上保持稳健性能（PSNR 21.02 dB）；
  - Tiny 版本仅 0.00034M 参数、可达实时（＞50 FPS）。
- 去噪模块有效抑制暗区噪声，同时保留细节；
- 作为预处理增强，显著提升 DarkFace 上的人脸检测平均精度（AP 达 0.666）。

---

## 七、方法与实验的优点

- **创新性设计**：
  - 物理驱动的自适应曲线（AAC）兼具可解释性与可训练性；
  - 随机训练 + 网络融合机制，显著减轻模型复杂度。
- **高效性**：
  - 超轻量结构（Tiny版）仍保持高画质与实时性能；
  - 结构压缩技术具备嵌入式与移动端潜力。
- **泛化性强**：
  - 单一训练集（LOL）即可跨域表现稳定；
  - 无监督范式减少对合成数据依赖。
- **可作为视觉前处理模块**：
  - 实证显示在下游检测任务中能有效提升性能。

---

## 八、不足与局限

- **资源描述不充分**：未详细说明训练环境与硬件配置，限制复现性；
- **真实场景复杂度**：仅测试于标准数据集，未涉及动态视频或传感器噪声模型；
- **噪声去除与高频纹理平衡**：作者提到强去噪可能削弱细节，仍需定量控制；
- **未讨论可扩展性**：如是否适用于红外/多光谱低光照成像；
- **无明确信息公开范围**：代码开源但未附完整训练脚本或参数说明。

---

**（完）**
