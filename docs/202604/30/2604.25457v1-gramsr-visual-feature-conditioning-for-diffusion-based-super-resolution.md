---
title: "GramSR: Visual Feature Conditioning for Diffusion-Based Super-Resolution"
title_zh: GramSR：用于基于扩散的超分辨率的视觉特征条件
authors: "Fabio D'Oronzio, Federico Putamorsi, Leonardo Zini, Marcella Cornia, Lorenzo Baraldi"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25457v1"
tags: ["query:real-ir"]
score: 10.0
evidence: 基于视觉特征调节的扩散超分辨率
tldr: 本文提出GramSR，一种基于视觉特征条件的单步扩散超分辨率框架，用DINOv3密集特征取代文本描述，并通过三阶段LoRA模块分别优化退化去除、感知增强和纹理一致性，在多项基准测试中实现了领先的重建质量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.25457v1/fig-001.webp\", \"caption\": \"Table 4. Comparison of alternative training and conditioning strategies on the RealSR dataset. Top: LoRA training strategies for pixel-, semantic-, and texture-level objectives. Bottom: conditioning mechanisms for guiding the diffusion model.\", \"page\": 11, \"index\": 1, \"width\": 712, \"height\": 247}, {\"url\": \"assets/figures/arxiv/2604.25457v1/fig-002.webp\", \"caption\": \"Table 3. Effect of the texture-level LoRA during training and inference on the RealSR dataset. Top: varying the Gram matrix loss weight λ4 in Eq. 5. Bottom: varying the texture-level guidance scale λgram in Eq. 6.\", \"page\": 11, \"index\": 2, \"width\": 606, \"height\": 221}, {\"url\": \"assets/figures/arxiv/2604.25457v1/fig-003.webp\", \"caption\": \"Table 2. Ablation study results on the effects of visual conditioning and texture-level LoRA with Gram matrix loss, reported on the DIV2K, RealSR, and DRealSR datasets.\", \"page\": 10, \"index\": 3, \"width\": 712, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2604.25457v1/fig-004.webp\", \"caption\": \"Table 1. Comparison of one-step diffusion-based SR methods across different datasets.\", \"page\": 8, \"index\": 4, \"width\": 640, \"height\": 559}, {\"url\": \"assets/figures/arxiv/2604.25457v1/fig-005.webp\", \"caption\": \"Table 5. Generalization across different visual encoders on the RealSR dataset. Performance of GramSR using various frozen visual backbones for conditioning and Gram matrix alignment, compared to the baseline text-conditioned two-LoRA model. Best results are in bold, second best are underlined.\", \"page\": 12, \"index\": 5, \"width\": 723, \"height\": 219}, {\"url\": \"assets/figures/arxiv/2604.25457v1/fig-006.webp\", \"caption\": \"Fig. 2. Qualitative comparison on real-world images from the RealSR dataset. From left to right: low-resolution input, results of SinSR [35], OSEDiff [38], PiSA-SR [29], GramSR (Ours), and ground truth.\", \"page\": 9, \"index\": 6, \"width\": 710, \"height\": 370}, {\"url\": \"assets/figures/arxiv/2604.25457v1/fig-007.webp\", \"caption\": \"Fig. 1. Overview of the proposed three-stage training framework. The architecture consists of a frozen DINOv3 [25] encoder for visual conditioning, a frozen VAE encoderdecoder pair, and a diffusion U-Net equipped with three sequential LoRA [12] modules. In Stage 1, the pixel-level LoRA is trained with pixel-wise loss. In Stage 2, the pixel-level LoRA is frozen and the semantic-level LoRA is trained with perceptual and semantic losses. In Stage 3, both are frozen, and the texture-level LoRA is trained with Gram matrix loss to enhance texture consistency. The DINOv3 encoder remains frozen throughout all stages, and all LoRA modules are applied only to the U-Net module.\", \"page\": 5, \"index\": 7, \"width\": 725, \"height\": 377}]"
motivation: 现有扩散式超分辨率方法依赖文本条件，缺乏与图像空间对齐的视觉信息，导致细节还原不足。
method: GramSR采用基于DINOv3视觉特征的条件扩散模型，通过像素、语义、纹理三阶段LoRA模块训练完成超分辨率任务。
result: 在多个标准超分辨率数据集上，GramSR性能优于现有单步扩散方法，在结构和纹理还原上表现出色。
conclusion: GramSR证明以视觉特征替代文本条件能显著提升扩散式超分辨率的真实性与结构保真度。
---

## 摘要
尽管近年来取得了显著进展，单幅图像超分辨率（SR）仍然具有挑战性，尤其是在具有复杂退化的真实场景中。基于扩散模型的SR方法，特别是建立在Stable Diffusion上的方法，利用了强大的生成先验，但通常依赖于由语义描述生成的文本条件。这类文本描述仅提供高层次的语义信息，缺乏实现真实复原所需的空间对齐视觉信息，导致抽象语义与空间对齐视觉细节之间存在表示差距。为了解决这一问题，我们提出了GramSR，一种基于扩散的一步超分辨率框架，它将文本条件替换为由预训练的DINOv3编码器从低分辨率输入中提取的密集视觉特征。GramSR采用三阶段LoRA架构，其中像素级、语义级和纹理级LoRA模块依次训练。像素级模块通过$\ell_2$损失专注于退化去除，语义级模块利用LPIPS和CSD损失增强感知细节，而纹理级模块通过从DINOv3特征计算的Gram矩阵损失来强化特征相关一致性。在推理阶段，独立的引导尺度使退化去除、语义增强和纹理保真可以灵活控制。在标准SR基准上的大量实验表明，GramSR始终优于现有的一步扩散方法，在结构保真度和纹理真实感方面表现更佳。本工作的代码可在以下地址获取：https://github.com/aimagelab/GramSR。

## Abstract
Despite recent advances, single-image super-resolution (SR) remains challenging, especially in real-world scenarios with complex degradations. Diffusion-based SR methods, particularly those built on Stable Diffusion, leverage strong generative priors but commonly rely on text conditioning derived from semantic captioning. Such textual descriptions provide only high-level semantics and lack the spatially aligned visual information required for faithful restoration, leading to a representation gap between abstract semantics and spatially aligned visual details. To address this limitation, we propose GramSR, a one-step diffusion-based SR framework that replaces text conditioning with dense visual features extracted from the low-resolution input using a pre-trained DINOv3 encoder. GramSR adopts a three-stage LoRA architecture, where pixel-level, semantic-level, and texture-level LoRA modules are trained sequentially. The pixel-level module focuses on degradation removal using $\ell_2$ loss, the semantic-level module enhances perceptual details via LPIPS and CSD losses, and the texture-level module enforces feature correlation consistency through a Gram matrix loss computed from DINOv3 features. At inference, independent guidance scales enable flexible control over degradation removal, semantic enhancement, and texture preservation. Extensive experiments on standard SR benchmarks demonstrate that GramSR consistently outperforms existing one-step diffusion-based methods, achieving superior structural fidelity and texture realism. The code for this work is available at: https://github.com/aimagelab/GramSR.

---

## 论文详细总结（自动生成）

# GramSR：用于基于扩散的超分辨率的视觉特征条件 — 中文深度总结

---

## 一、研究背景与核心问题

- **研究动机**：单幅图像超分辨率（Single Image Super-Resolution，SR）任务旨在从低分辨率（LQ）图像恢复高分辨率（HQ）图像，是计算机视觉领域的基础问题。但是现有基于深度学习的SR方法在面对**真实复杂退化（噪声、模糊、混合失真等）**时往往效果不佳。
- **现有方法局限**：
  - 传统超分辨率模型常依赖像素级损失（如 PSNR、SSIM），难以保持真实感。
  - GAN类方法虽有较好视觉质量，但训练不稳定且易产生伪影。
  - 基于**扩散模型（Diffusion Models）**的SR近期取得进展，尤其利用 **Stable Diffusion** 等模型的强生成先验，但几乎都使用**文本条件（Text Conditioning）**来指导复原。
  - 文本提示仅提供高层语义信息，缺乏**空间对齐（spatial alignment）**的细粒度视觉线索，导致语义与视觉细节脱节。
- **核心问题**：解决扩散式SR中“文字语义与图像空间不匹配”的条件瓶颈，提升复杂退化情形下的结构与纹理还原能力。

---

## 二、方法论：GramSR 框架

### 核心思想
论文提出 **GramSR** ——一种**单步扩散（One-step Diffusion）**的超分辨率框架，以视觉特征取代文本条件，结合多层次学习策略，显式建模结构和纹理一致性。

### 主要技术组件

1. **视觉特征条件（Visual Feature Conditioning）**  
   - 使用预训练的 **DINOv3 ViT 编码器** 从低分辨率输入提取密集视觉特征。  
   - 特征包含图像的局部纹理及全局上下文，具备空间对齐性。  
   - 经轻量级 MLP 适配器投影到扩散 U-Net 的条件空间作为模型输入。

2. **三阶段 LoRA（Low-Rank Adaptation）训练架构**
   - 采用 Stable Diffusion 作为冻结主干，只在部分层插入 LoRA 模块：
     - **第1阶段：像素级 LoRA (Δθ_pix)**  
       - 任务：退化去除（噪声、模糊等）。  
       - 损失：均方误差 (ℓ₂)。
     - **第2阶段：语义级 LoRA (Δθ_sem)**  
       - 任务：感知细节与语义增强。  
       - 损失：LPIPS + Classifier Score Distillation (CSD)。
       - 保留前一阶段的像素保真。
     - **第3阶段：纹理级 LoRA (Δθ_gram)**  
       - 任务：通过 **Gram矩阵损失** 对纹理统计实现一致性。  
       - Gram矩阵计算特征间的二阶相关，用于保持局部纹理模式。
       - 总损失：  
         L = λ₁L_MSE + λ₂L_LPIPS + λ₃L_CSD + λ₄L_gram。

3. **推理阶段**
   - 模型为单步推理（One-Step Diffusion），通过三个指导系数 λ_pix、λ_sem、λ_gram 独立调节退化去除、语义增强和纹理强化，实现灵活的图像生成控制。

### 算法流程概述
1. 冻结 Stable Diffusion 主模型与 DINOv3 编码器；
2. 顺序训练三个 LoRA 模块，从低级到高级语义逐步增强；
3. 最终推理时，将三个模块输出线性组合生成超分辨结果。

---

## 三、实验设计

### 数据集与评价标准
- **训练数据**：
  - LSDIR + FFHQ 前 10k 图像，含丰富自然场景；
  - 退化处理依据 Real-ESRGAN 过程生成 LQ-HQ 对。
- **测试数据集**：
  - **DIV2K**（合成退化数据），  
  - **RealSR** 与 **DRealSR**（真实退化场景）。
- **评价指标**：
  - 像素保真：PSNR、SSIM；
  - 感知质量：LPIPS、DISTS；
  - 生成质量：FID；
  - 主观自然度：NIQE。

### 对比方法
与多种先进的一步扩散SR模型对比：
- SinSR、OSEDiff、PiSA-SR、AdcSR、S3Diff 等。

---

## 四、资源与算力

- **硬件配置**：单张 **NVIDIA L40S GPU**，批量大小为 16。
- **优化器**：Adam。
- **学习率**：
  - 前两阶段：5×10⁻⁵；
  - 纹理级阶段：5×10⁻⁶。
- **早停策略**：基于 NIQE 指标。
- 论文未详细说明训练时长或总 GPU 小时数，仅提及单卡训练。

---

## 五、实验数量与充分性

- **基准对比实验**：3 个主要数据集（DIV2K、RealSR、DRealSR），6 个对比模型。
- **消融实验（Ablation Studies）**：
  - 测试视觉条件替换文本条件的效果；
  - 测试增加纹理级 LoRA 的作用；
  - 研究纹理损失权重 λ₄ 和推理引导 λ_gram 的影响；
  - 比较不同 LoRA 训练策略（单一、双、三阶段）。
  - 测试多种视觉编码器（CLIP、SigLIP2、DINOv2、DINOv3 等）。
- 总体实验覆盖充分，分析系统且公平，评价指标多维度，验证结果一致。

---

## 六、主要结论与发现

- **GramSR显著提升图像结构与纹理真实感**：
  - 在所有基准上获得最高 PSNR 和 SSIM；
  - 在真实场景下（RealSR、DRealSR）优势更显著；
  - 纹理保真（LPIPS、DISTS、FID）表现全优。
- **视觉特征条件比文本条件更有效**；
- **三阶段 LoRA 设计实现了像素、语义、纹理的解耦优化**；
- **Gram矩阵约束显著增强了纹理一致性**；
- **显式控制引导尺度可平衡失真与视觉真实感**。

---

## 七、方法优点

- **创新性**：首次将密集视觉特征（DINOv3）引入扩散式超分辨率条件。
- **结构设计合理**：三阶段 LoRA 逐层优化，避免互相干扰。
- **效率高**：单步推理（One-step Diffusion）实现实时重建。
- **控制灵活**：独立指导尺度可根据任务需求调节不同增强效果。
- **泛化强**：适配多种视觉编码器，效果稳定。

---

## 八、不足与局限

- **训练代价**：虽然单步推理高效，但多阶段训练仍需额外计算；论文未披露训练耗时。
- **数据依赖**：主要使用合成退化与人工配对数据，实际低质场景可能更复杂。
- **视觉编码器冻结**：使用预训练 DINOv3，未探讨联合优化可能的增益。
- **缺乏高分辨率极端测试**：未考察在 >4× 放大或特定领域（医学、卫星图像）中的适应性。
- **主观评估较少**：虽然有 NIQE，但缺乏人类感知评测。

---

**（完）**
