---
title: Edge-Cloud Collaborative Reconstruction via Structure-Aware Latent Diffusion for Downstream Remote Sensing Perception
title_zh: 基于结构感知潜空间扩散的边云协同重建，用于下游遥感感知
authors: "Yun Li, Xianju Li"
date: 2026-04-28
pdf: "https://arxiv.org/pdf/2604.25319v1"
tags: ["query:real-ir"]
score: 9.5
evidence: 用于遥感超分辨率的结构感知潜扩散模型
tldr: 本研究针对卫星遥感图像传输带宽受限导致的结构细节丢失问题，提出结构感知潜变量扩散（SALD）框架，通过边缘端压缩与结构先验传输、云端结构门控大核卷积和语义引导重建机制，实现高效边云协同重建，显著提升图像质量与下游识别性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.25319v1/fig-001.webp\", \"caption\": \"Fig. 3. Visual comparison of different reconstruction methods on the MSCM dataset under extreme bandwidth constraints. SALD accurately preserves critical structural boundaries and restores high-fidelity textures without generative hallucinations.\", \"page\": 5, \"index\": 1, \"width\": 510, \"height\": 542}, {\"url\": \"assets/figures/arxiv/2604.25319v1/fig-002.webp\", \"caption\": \"TABLE I QUANTITATIVE COMPARISON ON MSCM AND UCMERCED DATASETS\", \"page\": 3, \"index\": 2, \"width\": 1052, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2604.25319v1/fig-003.webp\", \"caption\": \"Fig. 1. The overall architecture of the proposed Structure-Aware Latent Diffusion (SALD) framework. It adopts an asymmetric edge-cloud collaborative design to achieve bandwidth-efficient super-resolution.\", \"page\": 2, \"index\": 3, \"width\": 1054, \"height\": 393}, {\"url\": \"assets/figures/arxiv/2604.25319v1/fig-004.webp\", \"caption\": \"Fig. 2. Detailed architecture of the Structure-Gated Large Kernel (SGLK) module. It consists of three parallel branches: a Detail Branch for local feature extraction, a Context Branch that incorporates the structural prior (P(·)) via sigmoid-gated modulation, and a Projection Branch for linear learning.\", \"page\": 2, \"index\": 4, \"width\": 512, \"height\": 511}, {\"url\": \"assets/figures/arxiv/2604.25319v1/fig-005.webp\", \"caption\": \"TABLE VII SCENE CLASSIFICATION PERFORMANCE ON THE MSCM DATASET.\", \"page\": 4, \"index\": 5, \"width\": 504, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2604.25319v1/fig-006.webp\", \"caption\": \"TABLE VIII OBJECT DETECTION PERFORMANCE ON THE MSCM DATASET.\", \"page\": 4, \"index\": 6, \"width\": 500, \"height\": 308}]"
motivation: 高分辨率遥感图像传输受限带宽约束，过度压缩损失结构信息，影响后续感知任务。
method: 提出结构感知潜变量扩散（SALD）框架，在边缘端解耦低频与结构先验，在云端利用结构门控大卷积和语义引导模块进行重建。
result: 在MSCM和UCMerced数据集上，SALD显著提升LPIPS指标和场景分类、小目标检测性能。
conclusion: SALD在极端带宽条件下兼顾感知质量和下游任务表现，为遥感图像边云协同重建提供新范式。
---

## 摘要
高分辨率遥感数据的指数级增长在卫星到地面的传输过程中面临严重瓶颈。受限的下行带宽迫使采用高比率的压缩方式，这会不可逆地破坏对于下游机器感知任务（如目标检测）至关重要的高频结构细节。尽管当前的超分辨率技术试图恢复这些细节，但基于回归的方法常常产生过度平滑的纹理，而生成式扩散模型则频繁引入结构幻觉，误导检测系统。为解决这一权衡问题，我们提出了结构感知潜空间扩散（Structure-Aware Latent Diffusion，SALD）框架，这是一种非对称的边云协同超分辨率系统。在资源受限的边缘端，系统将图像解耦为高度压缩的低频负载和轻量级的软结构先验。传输这种解耦后的表示能够最大限度减少带宽消耗。在算力强大的云端，我们在扩散骨干中引入了结构门控大核（Structure-Gated Large Kernel，SGLK）模块和语义引导引擎（Semantic-Guidance Engine，SGE）。这些模块利用传输的结构先验对大核卷积进行门控，有效捕捉航空场景中的长程依赖，同时主动抑制生成性幻觉。基于 MSCM 和 UCMerced 数据集的大量实验表明，即使在极端带宽限制下，SALD 仍实现了卓越的感知质量（LPIPS），并显著提升了场景分类和小目标检测等下游任务的性能。

## Abstract
The exponential surge in high-resolution remote sensing data faces a severe bottleneck in satellite-to-ground transmission. Limited downlink bandwidth forces the use of extreme high-ratio compression, which irreversibly destroys high-frequency structural details essential for downstream machine perception tasks like object detection. While current super-resolution techniques attempt to recover these details, regression-based methods often yield over-smoothed textures, and generative diffusion models frequently introduce structural hallucinations that mislead detection systems. To address this trade-off, we propose the Structure-Aware Latent Diffusion (SALD) framework, an asymmetric edge-cloud collaborative SR system. At the resource-constrained edge, the system decouples imagery into a highly compressed low-frequency payload and a lightweight soft structural prior. Transmitting this decoupled representation minimizes bandwidth consumption. On the powerful cloud side, we introduce a Structure-Gated Large Kernel (SGLK) module and a Semantic-Guidance Engine (SGE) within the diffusion backbone. These modules leverage the transmitted structural priors to gate large-kernel convolutions, effectively capturing long-range dependencies inherent in aerial scenes while actively suppressing generative hallucinations. Extensive experiments on both the MSCM and UCMerced datasets demonstrate that, even under extreme bandwidth constraints, SALD achieves superior perceptual quality (LPIPS) and significantly enhances downstream performance in both scene classification and small-target detection.

---

## 论文详细总结（自动生成）

# 基于结构感知潜空间扩散的边云协同遥感重建 — 论文深度总结

---

## 一、研究背景与核心问题

- **问题背景**：高分辨率遥感影像（如卫星影像）在地面接收过程中受到**下行通信带宽**严重限制，传统的高压缩比传输过程会破坏关键的几何结构与高频细节。  
- **核心困境**：  
  - 过度压缩 → 关键边缘结构损失；  
  - 回归型超分辨率模型 → 纹理过度平滑；  
  - 扩散生成模型 → 容易产生错误纹理或“结构幻觉”；  
  - 导致下游机器感知任务（如目标检测、场景分类）失效。  
- **研究目标**：设计一个在**带宽极端受限条件下**，仍能有效恢复图像结构并保持下游识别能力的重建框架。

---

## 二、方法论与框架设计

### 2.1 整体思路

论文提出 **SALD（Structure-Aware Latent Diffusion）框架**，以“边云协同”的方式实现遥感图像的高保真超分辨率重建：

- **边缘端（Edge）**：轻量化编码与压缩，仅提取必要的低频信息和结构先验；
- **云端（Cloud）**：利用结构先验引导扩散生成过程，从潜空间中重建高分辨率图像；
- **目标**：以极低带宽传输的结构先验，指导云端生成模型结构感知式地恢复高频细节。

### 2.2 关键技术模块

1. **边缘端双流解耦（Dual-Stream Decoupling）**  
   - 输入高分辨率图像 \( I_{HR} \)，经过下采样得到低频负载 \( I_{LR} \)；  
   - 通过轻量 MobileNetV3-U-Net 提取结构先验掩码 \( M \)；  
   - 最终传输的内容：\((I_{LR}, M)\)，显著减少带宽占用。  

2. **云端潜空间扩散重建（Latent Diffusion Reconstruction）**  
   - 构建条件潜变量扩散模型，以噪声逆向去除实现重建；
   - 在扩散反演过程中，引入结构先验 \( F_{stru} \) 和上采样后的 \( I_{LR} \)，作为联合条件 \( c \)；
   - 损失函数为：
     \[
     L_{total} = \lambda_1 L_{diff} + \lambda_2 L_{rec} + \lambda_3 L_{per}
     \]
     结合扩散、重建和感知三个维度进行优化。

3. **语义引导引擎（SGE）**  
   - 将离散二值结构掩码 \( M \) 映射到连续潜空间；
   - 使用多层特征金字塔（Feature Pyramid Network）生成层级语义嵌入 \( F_{stru} \)，实现结构语义对齐。

4. **结构门控大核卷积（SGLK）模块**  
   - 三分支设计：
     - **Detail 分支**：局部特征提取（1×1卷积）；
     - **Context 分支**：大核（9×9卷积）捕捉远程依赖，并以 \( F_{stru} \) 经 Sigmoid 门控方式进行调制；
     - **Projection 分支**：线性残差投影，保持梯度通畅；
   - 最终输出：
     \[
     X_{out} = X_{detail} + \gamma X_{gated} + X_{res}
     \]
     用以融合多尺度结构特征，提高重建的空间一致性与稳定性。

### 2.3 异步架构优势
- **边缘计算**：低算力消耗（约1.2 GFLOPs）。
- **云端生成**：高效扩散反演＋结构引导，防止幻觉产生并保持边缘精度。
- **整体架构**具有高度异步性：先验传输与生成过程分离，可平行执行。

---

## 三、实验设计与Benchmark

### 3.1 数据集与测试场景
- **MSCM**：矿区遥感复杂场景（2025年Edg-Net论文提供）。  
- **UCMerced**：典型地物分类遥感数据集。  

两者均用于测试在**极端带宽约束（0.15MB传输预算）**下的重建效果。

### 3.2 对比方法
与多种主流超分辨率方法比较：
- **传统SR**：Bicubic、ESRGAN、SwinIR、SAFMN；
- **生成式**：ResShift、PLKSR、PFT-SR、InvSR；
- **最新扩散类**：EDiffSR、GRL 等。

### 3.3 评价指标
分为三大类指标：
1. **信号层质量**：PSNR、SSIM；
2. **感知层真实度**：LPIPS、FID；
3. **下游实用性**：YOLOv11 的 mAP@0.5、mAP@0.5:0.95，场景分类 Top-1/Top-5。

---

## 四、资源与算力说明

- 论文仅说明实现框架使用 **PyTorch + AdamW 优化器**，推理步数 T=50。  
- 未明确给出 GPU 型号、数量或训练时长。  
- 提及“云端具备充足算力以进行扩散反演”但未提供具体硬件参数。  
👉 **结论**：算力信息未公开。

---

## 五、实验数量与充分性分析

- **数据集维度**：2 个公开数据集（MSCM、UCMerced）；  
- **实验类型**：
  - 主对比实验（8+基线）；
  - 消融实验（SGE/SGLK模块、扩散步数T、卷积核大小K、结构权重λ2）；
  - 鲁棒性实验（掩码缺失率0–50%模拟误差）；  
  - 下游任务性能（场景分类与小目标检测）。
- **结论**：共包含7类实验类型，指标覆盖全面；所有基线均“重新训练”，并采用各自推荐超参配置，保证公正性。实验充分度高。

---

## 六、主要结论与发现

- SALD 在极端带宽下仍保持高保真重建：
  - LPIPS 降至 **0.1601**（最低）；
  - mAP@0.5 达 **87.45%**，接近未压缩上限（89.51%）。
- 降低了扩散生成中的幻觉问题，实现结构一致性重建。
- 在下游**场景分类 Top-1准确率提升至 90.91%**，几乎接近原始数据。
- 在掩码缺失实验中展现良好的鲁棒性（30%缺失时性能仅下降 <3%）。

---

## 七、方法和实验的亮点

- **结构感知条件扩散**：通过显式结构门控，大幅削减生成噪声与误差。  
- **边云协同设计**：在传输受限环境中兼顾“低带宽、高质量”。  
- **软先验机制**：掩码不是硬约束，增强对缺失结构的容错性。  
- **高效模块组合**：SGE + SGLK 互补性强，在多项消融实验中显著提升感知与识别性能。  
- **公平设定**：统一带宽预算与重训练策略，确保对比公正。

---

## 八、不足与局限

- **算力说明不完整**：缺乏 GPU 类型、云端推理时延等细节。  
- **数据覆盖有限**：仅测试两个遥感场景，泛化性尚待进一步验证。  
- **实时性评估不足**：虽分析扩散步数与延迟，但未测实地通信性能。  
- **模型复杂度较高**：云端模型参数量（约27M）对部署环境仍存在压力。  
- **未探讨量化或剪枝策略**：作者仅在未来工作中提出进一步硬件适配的设想。

---

**（完）**
