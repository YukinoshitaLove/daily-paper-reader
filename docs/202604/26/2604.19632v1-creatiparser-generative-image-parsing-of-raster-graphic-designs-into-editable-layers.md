---
title: "CreatiParser: Generative Image Parsing of Raster Graphic Designs into Editable Layers"
title_zh: CreatiParser：将光栅图形设计生成解析为可编辑图层的生成式模型
authors: "Weidong Chen, Dexiang Hong, Zhendong Mao, Yutao Cheng, Xinyan Liu, Lei Zhang, Yongdong Zhang"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19632v1"
tags: ["query:real-ir"]
score: 9.0
evidence: 将位图图形设计解析为可编辑图层
tldr: 本文提出CreatiParser，一种将光栅图形设计解析为可编辑层的生成式框架，结合视觉语言模型与多分支扩散架构，实现文本、背景和贴纸等层的自动拆解与重建，大幅提升编辑性和解析质量。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.19632v1/fig-001.webp\", \"caption\": \"Fig. 1: Illustration of graphic design image parsing. Given a rasterized design image, our task aims to decompose it into three editable layers: Text, Sticker, and Background.\", \"page\": 1, \"index\": 1, \"width\": 529, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2604.19632v1/fig-002.webp\", \"caption\": \"Fig. 4: From left to right column, we show: (a) the input poster image; (b) the reconstructed design by compositing the parsed layers; (c) the extracted background; (d) the extracted sticker layer; and (e) the extracted text layer. Our method accurately separates semantic layers, including text, decorative sticker elements, and background regions across diverse design styles, enabling clean layer extraction and faithful reconstruction.\", \"page\": 6, \"index\": 2, \"width\": 914, \"height\": 742}, {\"url\": \"assets/figures/arxiv/2604.19632v1/fig-003.webp\", \"caption\": \"Fig. 2: Overview of the proposed CreatiParser framework. The framework comprises three components: (a) VLM-based Text Layer Parsing module (upper left), where a QwenLM-based multimodal decoder with LoRA generates text rendering protocols from the input graphic design, then with render engine to generate text layer; (b) Multi-branch Diffusion module (lower left), where three SDXL U-Net branches with Layer Token Attention (LTA) and LoRA adapters generate disentangled background and sticker layers; (c) GRPO Training (right), where the text parsing module is optimized via GRPO. Gradient backpropagation updates only the QwenLM LoRA weights while all other components remain frozen.\", \"page\": 3, \"index\": 3, \"width\": 1064, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2604.19632v1/fig-004.webp\", \"caption\": \"Fig. 6: Visualization Comparison of full decomposition results between creatiparser and LayerD.\", \"page\": 8, \"index\": 4, \"width\": 412, \"height\": 604}]"
motivation: 现有生成模型难以从光栅设计图中获取可编辑层结构，影响后续编辑灵活性。
method: 作者设计了结合视觉语言文本解析与支持RGBA的多分支扩散生成的混合框架，并引入ParserReward与GRPO优化机制。
result: "在Parser-40K与Crello数据集上，方法在各项指标上平均提升23.7%，显著优于现有方案。"
conclusion: 研究表明，CreatiParser显著提升了图形设计解析的性能与可控性，为后续可编辑内容生成提供了新方向。
---

## 摘要
图形设计图像通常由多个可编辑图层组成，如文本、背景和装饰元素，而大多数生成模型仅输出无显式图层结构的光栅图像，从而限制了后续编辑。现有的图形设计解析方法通常依赖于包含布局预测、抠图和修复的多阶段流程，这类方法容易出现误差累积且可控性有限。我们提出了一种用于光栅到图层图形设计解析的混合生成框架，该框架将设计图像分解为可编辑的文本、背景和贴纸图层。文本区域通过视觉语言模型解析为文本渲染协议，从而实现高保真重建与灵活再编辑；背景与贴纸图层则通过支持 RGBA 的多分支扩散架构生成。我们进一步引入了 ParserReward，并结合组相对策略优化（Group Relative Policy Optimization）以使生成质量与人类设计偏好对齐。在两个具有挑战性的数据集——Parser-40K 和 Crello 数据集上的大量实验表明，我们的方法在所有指标上均优于现有方法，整体平均提升达 23.7%。

## Abstract
Graphic design images consist of multiple editable layers, such as text, background, and decorative elements, while most generative models produce rasterized outputs without explicit layer structures, limiting downstream editing. Existing graphic design parsing methods typically rely on multi-stage pipelines combining layout prediction, matting, and inpainting, which suffer from error accumulation and limited controllability. We propose a hybrid generative framework for raster-to-layer graphic design parsing that decomposes a design image into editable text, background, and sticker layers. Text regions are parsed using a vision-language model into a text rendering protocol, enabling faithful reconstruction and flexible re-editing, while background and sticker layers are generated using a multi-branch diffusion architecture with RGBA support. We further introduce ParserReward and integrate it with Group Relative Policy Optimization to align generation quality with human design preferences. Extensive experiments on two challenging datasets, \emph{i.e.,} the Parser-40K and Crello datasets, demonstrate superior performance over existing methods, \emph{eg.,} achieving an overall average improvement of 23.7\% across all metrics.

---

## 论文详细总结（自动生成）

# CreatiParser：将光栅图形设计解析为可编辑图层的生成式框架

---

## 一、核心问题与研究背景

- **研究动机**  
  图形设计广泛应用于广告、社交媒体、UI 等领域，其显著特征是**由多层可编辑元素组成（文本、贴纸、背景等）**。然而，当前多数生成模型（如扩散模型、GAN）仅生成**单层光栅化图像**，缺乏显式图层结构，使后续编辑（修改文本、调整元素、替换背景）困难且需要大量人工分离工作。

- **问题定义**  
  如何从光栅图形自动解析出具有语义独立性和可编辑性的图层结构，将设计图“反解”为文本协议与分离图层——即实现**raster-to-layer graphic design parsing**。

- **现有局限**  
  传统的解析管线依赖布局预测＋抠图＋修复的多阶段算法，面临：
  - 阶段性误差累积；
  - 模型间风格不一致；
  - 不支持可编辑文本结构（仅像素级分割）。

---

## 二、方法论与核心技术

### 1. 整体框架
**CreatiParser** 是一种**混合式生成框架**，融合视觉语言模型（VLM）与扩散生成模型，包含三大模块：

1. **文本层解析模块（VLM-based Text Parser）**  
   - 基于 Qwen3-VL 模型，输入光栅图像，输出**文本渲染协议（Text Rendering Protocol）**。  
   - 协议包含几何属性（位置、角度、曲率）、语义属性（文本内容、阅读方向）、外观属性（字体、颜色、阴影、描边等）和层级属性。  
   - 协议结构化输出为 JSON 格式，并通过渲染引擎生成可编辑文本层。

2. **多分支扩散生成模块（Multi-branch Diffusion Architecture）**  
   - 采用 **SDXL（Stable Diffusion XL）** 主干，扩展为三分支结构：
     - 条件分支：编码输入图。
     - 背景分支：生成 RGB 背景层。
     - 贴纸分支：生成 RGBA 前景层（支持透明度）。
   - 各分支配备 **LoRA** 轻量适配模块，参数化独立优化，避免破坏预训练生成先验。
   - 引入 **Layer Token Attention (LTA)**：在每个 U-Net block 内进行跨分支位置对应的注意力交互，实现空间对应的层间信息融合。

3. **强化学习驱动的文本协议优化（ParserReward + GRPO）**  
   - **ParserReward** 衡量渲染后文本层的像素一致性（Rpix）、空间位置交并比（Rloc）、语义相似度（Rsem）。  
   - **Group Relative Policy Optimization (GRPO)**：采样多组文本协议候选，通过组内归一化奖励更新策略，仅微调 Qwen3-VL 的 LoRA 权重。  
   - 奖励公式为：  
     \[
     R_{\text{text}} = \lambda_{\text{pix}} R_{\text{pix}} + \lambda_{\text{loc}} R_{\text{loc}} + \lambda_{\text{sem}} R_{\text{sem}}
     \]  
     并结合裁剪比率更新及 KL 正则以稳定训练。

---

## 三、实验设计

### 1. 数据集与场景
- **Parser-40K**：作者自建高质量图形设计数据集，来源于专业设计平台，包含可编辑源文件（PSD），提供原生层及文本元数据。  
- **Crello Dataset**：公共设计模板数据集，样式多样、排版复杂。该数据集用于**零样本（zero-shot）评估**，验证模型的泛化性能。

### 2. 对比方法（Benchmark）
- **Baseline**：多阶段传统管线，结合 Grounded-SAM-2 做分割、LaMa 做背景修复。
- **LayerD**：论文 [9] 提出的层分解与修复模型（matting-first + diffusion background completion）。

### 3. 评价指标
- 图层重建精度：T-IoU（文本层）、S-IoU（贴纸层）。  
- 文本可编辑性：字体识别精度（Font）、属性预测精度（Attr）。  
- 像素重建质量：RGB L1 误差（按层与平均值）。  

---

## 四、资源与算力信息

- 论文未明确说明使用的 GPU 型号、数量、或训练时长。  
- 提到 LoRA 的轻量适配与冻结主干参数，表明总体算力需求相对较低，但具体配置未公开。

---

## 五、实验数量与充分性

- **主要实验**：
  - 在两个数据集上进行完整指标对比；
  - **零样本泛化实验**验证模型跨风格能力；
  - **消融实验**涵盖：
    - 去除 VLM 模块；
    - 去除 LTA；
    - 去除 ParserReward；
    - LoRA 低秩参数（不同 rank 值）；
    - 不同 GRPO 组大小；
    - 奖励项权重比对。
  - 总计约 6 类主要实验维度、十余组表格与图示验证。

- **充分性与客观性**：
  - 涵盖结构、超参数、策略层面消融；
  - 与当前主流模型基线对比；
  - 评价指标多维且标准化；
  - 数据集覆盖专业和真实场景。  
  → 整体实验规模充足且设计公平。

---

## 六、主要结论与发现

- CreatiParser 在 **Parser-40K** 上：
  - T-IoU 提升至 0.896，S-IoU 提升至 0.862；
  - 文本字体识别准确率 87.3%，属性准确率 91.2%。  

- 在 **Crello 零样本测试**中：
  - 泛化性能显著，T-IoU 达 0.914；
  - 各层 RGB L1 均低于对比模型；
  - 平均优于现有方法约 23.7%。

- 该方法能产生**语义清晰、边界干净、可编辑性强**的分离层，有效避免传统管线的误差传播。

---

## 七、优点与亮点

- **方法创新性**
  - 首次将 **视觉语言模型解析**与**扩散生成层分解**结合；
  - 引入 **文本渲染协议**，实现从图像到结构化文字的可编辑映射。

- **架构设计**
  - 使用 **LTA（Layer Token Attention）** 实现分支间空间对应交互，提升层分离精度；
  - **RGBA-aware diffusion** 支持透明度生成，适应贴纸/图标场景。

- **优化策略**
  - **ParserReward + GRPO** 将训练与人类审美偏好对齐，具备任务特定奖励。

- **性能与泛化**
  - 在未见数据集上保持稳定性能，表现出强泛化性。

---

## 八、不足与局限

- **算力与训练细节**未公开，难以重现精确训练过程。  
- **奖励设计依赖较多经验参数**（λ 权重、组大小），需调试。  
- **文本渲染协议依赖语言模型的理解能力**，可能对艺术化复杂字体或非拉丁字符精度不足。  
- **仅支持三类图层（文本、贴纸、背景）**，实际设计中存在更多细分类型（形状、图像滤镜等），模型需要扩展。  
- **实验未涉及用户交互或实时编辑系统集成**，应用验证仍处离线阶段。

---

（完）
