---
title: Learned Nonlocal Feature Matching and Filtering for RAW Image Denoising
title_zh: 用于 RAW 图像去噪的可学习非局部特征匹配与滤波
authors: "Marco Sánchez-Beeckman, Antoni Buades"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17453v1"
tags: ["query:real-ir"]
score: 9.0
evidence: 作为修复任务的RAW图像去噪
tldr: 本文提出一种用于RAW图像去噪的可学习非局部特征匹配与滤波网络，将经典自相似去噪理念融入深度模型中，通过非局部块实现多尺度特征匹配与聚合，在真实与合成数据上训练后能跨传感器泛化，以更少参数获得接近或超越主流方法的效果，兼顾解释性与实用性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17453v1/fig-001.webp\", \"caption\": \"Figure 3: Local propagation of nonlocal neighboring features.\", \"page\": 6, \"index\": 1, \"width\": 498, \"height\": 310}, {\"url\": \"assets/figures/arxiv/2604.17453v1/fig-002.webp\", \"caption\": \"Figure 9: Visual comparison on in-the-wild smartphone photographs. Each column shows an image captured with a different\", \"page\": 11, \"index\": 2, \"width\": 1029, \"height\": 760}, {\"url\": \"assets/figures/arxiv/2604.17453v1/fig-003.webp\", \"caption\": \"Figure 4: Estimated noise level curves of the used camera\", \"page\": 7, \"index\": 3, \"width\": 466, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2604.17453v1/fig-004.webp\", \"caption\": \"Figure 5: Visual comparison between different values of 𝐾 at\", \"page\": 7, \"index\": 4, \"width\": 498, \"height\": 287}, {\"url\": \"assets/figures/arxiv/2604.17453v1/fig-005.webp\", \"caption\": \"Figure 8: Visual comparison against state-of-the-art RAW denoising methods on the DND dataset. Denoised images are retrieved\", \"page\": 10, \"index\": 5, \"width\": 1029, \"height\": 351}, {\"url\": \"assets/figures/arxiv/2604.17453v1/fig-006.webp\", \"caption\": \"Figure 7: Visual comparison between DRUNet, Restormer, CTNet and our method with 25/15/9 neighbors. The top image is\", \"page\": 10, \"index\": 6, \"width\": 1029, \"height\": 489}, {\"url\": \"assets/figures/arxiv/2604.17453v1/fig-007.webp\", \"caption\": \"Figure 6: Visual comparison between different feature matching strategies on two images from the CBSD68 dataset. A single scale\", \"page\": 8, \"index\": 7, \"width\": 1029, \"height\": 336}, {\"url\": \"assets/figures/arxiv/2604.17453v1/fig-008.webp\", \"caption\": \"Figure 1: Architecture of the proposed denoising network. We use a three-scale UNet with a single NL block per scale. The NL\", \"page\": 4, \"index\": 8, \"width\": 1029, \"height\": 336}, {\"url\": \"assets/figures/arxiv/2604.17453v1/fig-009.webp\", \"caption\": \"Figure 10: Visual comparison between DualDn (RAW) and our method with 25/15/9 neighbors on low-light outdoor photographs.\", \"page\": 12, \"index\": 9, \"width\": 1029, \"height\": 752}, {\"url\": \"assets/figures/arxiv/2604.17453v1/fig-010.webp\", \"caption\": \"Figure 2: Outline of the proposed Nonlocal Feature Matching and Filtering block. All linear transformations, as well as the\", \"page\": 5, \"index\": 10, \"width\": 1008, \"height\": 427}]"
motivation: 现有深度学习去噪模型参数庞大且缺乏传统方法的可解释性，难以在资源受限设备上部署。
method: 作者设计了一种结合非局部特征匹配与滤波的网络结构，模仿经典基于自相似性的去噪流程。
result: 该网络在真实RAW数据上泛化良好，并在定量与视觉效果上优于或媲美卷积与Transformer方法。
conclusion: 该方法实现了高效且可解释的RAW图像去噪，在多个数据集上表现与主流模型相当但参数更少。
---

## 摘要
作为图像处理中最古老且最基本的问题之一，图像去噪在深度学习的快速发展推动下再次成为研究热点。然而，大多数现代去噪架构对在神经网络广泛应用之前的经典去噪器研究中积累的技术知识利用有限，而是依赖于网络的深度和大量参数。这不仅给理解此类网络的特性带来挑战，也使得在受资源限制且具有不同噪声特征的真实设备上部署这些模型变得困难。针对这些问题，我们提出一种专为 RAW-to-RAW 去噪设计的架构，将经典的基于自相似性的去噪器的可解释结构融入到一个完全可学习的神经网络中。我们的设计核心是一个新颖的非局部模块，它与邻域匹配、协作滤波和聚合这一由基于非局部块方法所推广的既定处理流程相对应，并作用于学习到的多尺度特征表示上。这种内置的非局部特性有效地扩展了感受野，使得在每个尺度上仅需一个模块和适量的邻居即可获得高质量结果。我们在一个精心整理的数据集上对网络进行训练，该数据集包含干净的真实 RAW 数据和建模的合成噪声，并通过噪声水平图进行条件化，从而得到一种与传感器无关的去噪器，可以有效泛化到未知设备上。在基准测试和真实场景摄影中的定量与可视化结果表明，我们的方法是一种实用且可解释的真实世界 RAW 去噪解决方案，以显著更少的参数实现与最新卷积和 Transformer 去噪器竞争的性能。代码可在 https://github.com/MIA-UIB/nonlocal-matchfilter 获取。

## Abstract
Being one of the oldest and most basic problems in image processing, image denoising has seen a resurgence spurred by rapid advances in deep learning. Yet, most modern denoising architectures make limited use of the technical knowledge acquired researching the classical denoisers that came before the mainstream use of neural networks, instead relying on depth and large parameter counts. This poses a challenge not only for understanding the properties of such networks, but also for deploying them on real devices which may present resource constraints and diverse noise profiles. Tackling both issues, we propose an architecture dedicated to RAW-to-RAW denoising that incorporates the interpretable structure of classical self-similarity-based denoisers into a fully learnable neural network. Our design centers on a novel nonlocal block that parallels the established pipeline of neighbor matching, collaborative filtering and aggregation popularized by nonlocal patch-based methods, operating on learned multiscale feature representations. This built-in nonlocality efficiently expands the receptive field, sufficing a single block per scale with a moderate number of neighbors to obtain high-quality results. Training the network on a curated dataset with clean real RAW data and modeled synthetic noise while conditioning it on a noise level map yields a sensor-agnostic denoiser that generalizes effectively to unseen devices. Both quantitative and visual results on benchmarks and in-the-wild photographs position our method as a practical and interpretable solution for real-world RAW denoising, achieving results competitive with state-of-the-art convolutional and transformer-based denoisers while using significantly fewer parameters. The code is available at https://github.com/MIA-UIB/nonlocal-matchfilter .

---

## 论文详细总结（自动生成）

# 用于 RAW 图像去噪的可学习非局部特征匹配与滤波 —— 论文总结

---

## 一、研究动机与核心问题

- **研究背景**：图像去噪是图像处理领域最基础且经典的问题之一，尤其在数码摄影设备普及的今天，处理 RAW 图像（传感器直接输出的未处理数据）的噪声问题愈加重要。RAW 噪声通常是信号相关的（Poisson-Gaussian 分布），不同设备的传感器噪声特性差异显著。  
- **问题痛点**：
  - 现代基于深度学习的去噪网络虽然性能强大，但依赖深层结构和大量参数，计算代价高、可解释性低；
  - 与经典算法（如 BM3D）相比，现代深度网络较少利用图像的“非局部自相似性”这一关键先验；
  - 在移动端或低能耗设备上难以高效部署；
  - 不同设备的传感器让训练出来的网络难以跨设备泛化。
- **研究目标**：设计一种融合传统非局部自相似思想与深度学习优势的新型 RAW 去噪网络，使其具备：
  - 可解释性（对应传统匹配—滤波—聚合结构）；
  - 高效性（减少层数与参数）；
  - 泛化性（跨感光器的 RAW-to-RAW 传感器无关去噪）。

---

## 二、方法论与技术细节

### 2.1 核心思想

- 借鉴经典非局部补丁匹配法（如 BM3D）的三阶段框架：
  1. **匹配相似特征块（Feature Matching）**
  2. **协作滤波（Collaborative Filtering）**
  3. **特征聚合（Aggregation）**
- 将这一结构完全转化为一个**可学习的非局部模块（Nonlocal Feature Matching and Filtering Block, NLFeMF）**；
- 该模块嵌入在三尺度的 **U-Net** 框架中，形成轻量而可解释的 RAW 去噪网络。

### 2.2 网络架构

- **输入**：RAW 图像经 Bayer CFA 打包为四通道；另加入每像素噪声标准差图（4 通道），形成 8 通道输入。
- **主干结构**：
  - 三尺度 U-Net，每个尺度仅包含一个 **NL 块**；
  - 每个 NL 块由三部分组成：
    - 前/后各一个 **ConvNeXt** 层（提取局部上下文）；
    - 中间是 **非局部特征匹配与滤波模块**。
- **输出**：返回 4 通道干净 RAW 图像。

### 2.3 非局部特征匹配与滤波模块（NLFeMF）

1. **特征匹配阶段**：  
   - 由一个 6 层 3×3 卷积网络预测每个像素的 K 个偏移量（邻居位置）；  
   - 偏移可为非整数，使用双线性插值提取特征。
2. **协作滤波阶段**：  
   - 将每像素及其邻居堆叠成 C×K 通道；
   - 对每个通道独立施加**分组卷积**(Group Conv)实现线性变换；
   - 使用一个小 CNN 生成 [0,1] 范围的**调制图（modulation map）**，对变换结果逐通道衰减噪声；
   - 再次线性反变换重构特征堆栈。
3. **聚合阶段**：  
   - 通过 1×1 卷积学习性地对邻居特征进行加权聚合（避免简单平均造成信息丢失）。

### 2.4 噪声建模与训练过程

- **噪声模型**：基于信号强度的异方差高斯分布：  
  \( \mathcal{N}(0, a \cdot x + b) \)，其中 a, b 分别为光子噪声与读出噪声的参数。  
  合成噪声时使用更真实的 **Poisson-Gaussian** 模型。
- **损失函数**：L1 损失（预测去噪 RAW 与真值差异）。
- **优化器**：Adam，初始学习率 1e-4，Cosine Annealing 降至 5e-7。
- **数据增强**：128×128 裁剪 + D8 群旋转/翻转随机增强。
- **训练轮次**：共 10000 epoch，batch size=4，共约 115 万 iteration。

---

## 三、实验设计

### 3.1 训练数据集

- 从多个真实 RAW 数据集中选取高质量图像：  
  SID、ELD、SIDD、RawNIND、Nikon、CRVD、ReCRVD。  
  → 过滤残留噪声后共计 460 张训练图，30 张验证图。
- 依据 7 款传感器的噪声参数（来自 SIDD 等），生成多种 ISO 情况下的合成噪声样本 → 构造“跨传感器”训练集。

### 3.2 基准评测（Benchmark）

- **RAW 领域**：DND (Darmstadt Noise Dataset)；
- **合成 Gaussian 噪声领域**：CBSD68, Kodak24, McMaster；
- **实际手机拍摄测试**：Li et al. (2024b) 采集的 in-the-wild RAW 照片。

### 3.3 对比方法

- **经典方法**：BM3D；
- **CNN 系列**：DnCNN、FFDNet、DRUNet；
- **Transformer 系列**：Restormer、CTNet、DSCA-Former；
- **RAW 专用方法**：UPI、CycleISP、PseudoISP、DualDn。

### 3.4 消融实验

- **邻居数量 K 的影响**；
- **多尺度结构与单尺度结构对比**；
- **特征匹配策略**：固定局部邻域 vs 可学习 CNN 偏移 vs 可微 PatchMatch；
- **与高斯噪声去噪网络性能对比**（客观 PSNR/主观视觉）。

---

## 四、资源与算力

- 文中未明确公开使用的 GPU 型号、数量或训练耗时；
- 仅提供训练超参数与数据量信息；
- 可推断训练在支持 CUDA 的常规 GPU 集群上完成；
- 无明确算力统计。

---

## 五、实验数量与充分性

- **实验维度广泛、充分**：
  - 多达 6 个模块消融实验；
  - 使用 3 个高斯噪声 benchmark + 1 个 RAW benchmark + 真实手机场景；
  - 涵盖**定量（PSNR、SSIM）**与**定性（可视化）**；
  - 对比国内外主流多种架构（CNN、Transformer）；
  - 并验证泛化性能（跨设备、不同 ISO）。
- **公平性**：对比均在相同噪声标准差及已公开模型下进行，训练/推理流程一致。

---

## 六、主要结论与发现

1. 提出 NLFeMF 非局部模块，在可学习框架中复现经典去噪流程；
2. 三尺度结构使得模型仅需少量层数与较少邻居即可获得大感受野；
3. 在 AWGN 任务上（CBSD68、Kodak、McMaster），7.5M 参数的多尺度模型性能与 DRUNet、Restormer 等持平；
4. 在 DND RAW benchmark 上达：
   - **PSNR = 40.63 dB**，SSIM = 0.9644；
   - 超越 CycleISP、PseudoISP，接近 DualDn；
5. 在 in-the-wild 实拍 RAW 图像上，表现干净、细节保真、无伪影；
6. 优秀的跨传感器泛化能力和轻量化能力。

---

## 七、优点与创新亮点

- **结构可解释**：完全基于传统非局部三步思想，能直观理解每一模块的作用；
- **轻量高效**：参数量仅为 DRUNet 的四分之一，优于大多数 Transformer；
- **泛化性出色**：通过噪声曲线建模与多相机噪声采样，实现传感器无关训练；
- **实验系统完备**：包含从合成高斯到真实 RAW 的跨域验证；
- **高视觉质量**：有效抑制低频残噪、保持纹理精度，不产生虚构细节；
- **开源实现**：代码公开，可复现性高。

---

## 八、不足与局限性

- **算力信息缺失**：训练资源和时间未披露，限制复现可控性；
- **未测试视频 RAW 去噪**：仅做静态图像；
- **数据覆盖局限**：训练数据集虽多来源，但总量（约 460 图像）仍略少；
- **极端低照场景性能尚未深入量化**；
- **未与最新蒸馏或轻量化 Transformer 直接比较性能/速度权衡**；
- **跨模态（RAW→sRGB）的联合优化未涉及，仅为 RAW-to-RAW。**

---

（完）
