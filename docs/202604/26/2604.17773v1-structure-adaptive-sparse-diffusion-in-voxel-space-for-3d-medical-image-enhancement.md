---
title: Structure-Adaptive Sparse Diffusion in Voxel Space for 3D Medical Image Enhancement
title_zh: 基于体素空间的结构自适应稀疏扩散用于三维医学图像增强
authors: "Hongxu Jiang, Fei Li, Boxiao Yu, Ying Zhang, Kaleb Smith, Kuang Gong, Wei Shao"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17773v1"
tags: ["query:real-ir"]
score: 9.5
evidence: 利用扩散模型进行包括去噪和超分辨率在内的3D医学图像增强
tldr: 本文提出一种结构自适应稀疏扩散模型，用于三维医学图像增强任务，通过在体素空间中采用稀疏时间步采样和结构感知时序调制模块，在保持细节的同时显著降低计算成本，实现CT、PET、MRI图像的高效去噪与超分辨。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17773v1/fig-001.webp\", \"caption\": \"Table 1. Quantitative comparison on denoising and 4× super-resolution tasks across four datasets. Best results are in red, second-best are blue.\", \"page\": 6, \"index\": 1, \"width\": 688, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2604.17773v1/fig-002.webp\", \"caption\": \"Fig. 2. Qualitative denoising results on lung CT and brain PET. Purple and blue boxes highlight lung fissures and subtle anatomical boundaries, respectively. Our model preserves sharper, more structurally consistent details than all baselines.\", \"page\": 7, \"index\": 2, \"width\": 639, \"height\": 389}, {\"url\": \"assets/figures/arxiv/2604.17773v1/fig-003.webp\", \"caption\": \"Fig. 3. Qualitative 4× super-resolution results on aorta CTA and brain MRI. Highlighted regions show vessel boundaries and cortical structures. Our model recovers finer anatomical details with fewer artifacts than all baselines.\", \"page\": 7, \"index\": 3, \"width\": 639, \"height\": 397}, {\"url\": \"assets/figures/arxiv/2604.17773v1/fig-004.webp\", \"caption\": \"Fig. 1. Overview of the proposed framework.\", \"page\": 3, \"index\": 4, \"width\": 723, \"height\": 202}]"
motivation: 三维医学影像的高维性使传统扩散模型计算代价过高，需设计高效的体素级增强方法。
method: 作者设计稀疏体素空间扩散框架，并引入结构感知时序调制模块自适应调整扩散过程。
result: 在CT、PET、MRI四个数据集上，该方法实现最高性能并达成约10倍训练加速效果。
conclusion: 该方法在多种医学影像任务中显著提升性能并加速训练，为三维影像增强提供了高效新范式。
---

## 摘要
三维（3D）医学图像增强（包括去噪和超分辨）在 CT、PET 和 MRI 的临床诊断中至关重要。尽管扩散模型在二维医学成像中取得了显著成功，但将其扩展到高分辨率三维体数据时，由于高维体数据上的扩散轨迹过长，计算开销极为巨大。我们观察到，在条件增强中，退化输入中的强解剖先验使得密集的噪声调度大多变得冗余。基于这一洞见，我们提出了一种稀疏体素空间扩散框架，在一组均匀子采样的时间步上进行训练和采样。网络在数据流形上直接预测干净数据，并在速度空间中进行监督，以实现稳定的梯度缩放。一个轻量级的结构感知轨迹调制（Structure-aware Trajectory Modulation, STM）模块在每个网络块中根据局部解剖结构内容重新校准时间嵌入，从而在共享的稀疏调度上实现结构自适应去噪。我们的框架直接在体素空间中运行，在无需有损压缩的情况下保留精细的解剖细节，同时实现最高可达 10 倍的训练加速。在涵盖 CT、PET 和 MRI 的四个数据集上的实验表明，该方法在去噪和超分辨任务上均达到了当前最先进的性能。我们的代码已在以下地址公开：https://github.com/mirthAI/sparse-3d-diffusion。

## Abstract
Three-dimensional (3D) medical image enhancement, including denoising and super-resolution, is critical for clinical diagnosis in CT, PET, and MRI. Although diffusion models have shown remarkable success in 2D medical imaging, scaling them to high-resolution 3D volumes remains computationally prohibitive due to lengthy diffusion trajectories over high-dimensional volumetric data. We observe that in conditional enhancement, strong anatomical priors in the degraded input render dense noise schedules largely redundant. Leveraging this insight, we propose a sparse voxel-space diffusion framework that trains and samples on a compact set of uniformly subsampled timesteps. The network predicts clean data directly on the data manifold, supervised in velocity space for stable gradient scaling. A lightweight Structure-aware Trajectory Modulation (STM) module recalibrates time embeddings at each network block based on local anatomical content, enabling structure-adaptive denoising over the shared sparse schedule. Operating directly in voxel space, our framework preserves fine anatomical detail without lossy compression while achieving up to $10\times$ training acceleration. Experiments on four datasets spanning CT, PET, and MRI demonstrate state-of-the-art performance on both denoising and super-resolution tasks. Our code is publicly available at: https://github.com/mirthAI/sparse-3d-diffusion.

---

## 论文详细总结（自动生成）

# 论文总结：基于体素空间的结构自适应稀疏扩散用于三维医学图像增强

---

## 一、核心问题与研究背景

- **研究动机**：三维（3D）医学影像（CT、MRI、PET）在临床诊断中极具价值，但图像采集常受制于辐射剂量、扫描时间和设备性能，导致信噪比低、分辨率不足，进而影响医生对复杂解剖结构的判断。  
- **现有问题**：传统的扩散模型在二维任务中表现优异，但直接应用到高分辨率三维体素空间时，会产生巨大计算开销——迭代步数多、维度高，使模型训练极其缓慢。  
- **整体目标**：作者旨在设计一种**高效且保持结构细节的三维扩散方法**，既能在体素空间中直接生成高质量结果，又能减少训练与采样成本，适用于多种医学图像增强任务（去噪与超分辨）。

---

## 二、方法论：核心思想与关键技术

### 1. 方法总体框架
该方法称为 **Structure-Adaptive Sparse Diffusion (SASD)**，核心思想是：
> 在体素空间中进行条件扩散，但利用稀疏时间步调度和结构自适应调制策略，从而显著减少计算负担。

整个流程包含三个关键组件：

#### (1) 稀疏体素空间扩散  
- 输入数据为高质量体积数据 \(x_0\) 和对应退化版本 \(y\)。  
- 扩散的推进公式依然采用标准加噪过程：
  \[
  x_t = \sqrt{\bar{\alpha}_t} x_0 + \sqrt{1-\bar{\alpha}_t}\epsilon
  \]
  其中 \(\epsilon \sim N(0,I)\)。  
- 传统的扩散模型需多达上千步（如 T=1000），本文通过**均匀子采样 K 个关键步**（实验证明 K=5 最优），形成稀疏时间轨迹，大幅减少冗余的去噪步骤。

#### (2) 数据流形上的 x₀ 预测与速度监督
- 作者反对使用“噪声预测”或“速度预测”的传统做法，因为这些输出偏离数据流形。  
- 该方法直接预测干净图像 \( \hat{x}_0 = f_\theta(x_t, y, t) \)，保持与原始数据空间一致。  
- 为稳定不同时间步上的梯度规模，转换为**速度空间监督**：
  \[
  v_t = \frac{\sqrt{\bar{\alpha}_t}x_t - x_0}{\sqrt{1-\bar{\alpha}_t}}, \quad
  \hat{v}_t = \frac{\sqrt{\bar{\alpha}_t}x_t - \hat{x}_0}{\sqrt{1-\bar{\alpha}_t}}
  \]
  并最小化 \( L = \|v_t - \hat{v}_t\|^2 \)。  
- 此设计保证训练更加平滑稳定，避免梯度消失或爆炸。

#### (3) 结构感知轨迹调制（STM）
- 三维医学图像结构差异显著，如平滑软组织与复杂血管区在去噪难度上不同。  
- STM 模块引入 **多结构编码器**：
  - 包含三个分支：3D卷积（体积结构）、2D卷积（冠状面、矢状面平均投影），各自经 GAP 后融合为紧凑特征向量 \(c\)。  
- 在 UNet 每层的时间嵌入处，STM 计算对应的仿射调制参数：
  \[
  (\gamma_k, \beta_k) = \mathrm{MLP}_k(c), \quad t^{(k)}_{mod} = (1+\gamma_k)e_t + \beta_k
  \]
- 即同一扩散步在不同结构区域可产生不同“去噪强度”，实现**结构自适应的时序轨迹调整**。
  
---

## 三、实验设计

### 1. 数据集与任务
论文在四个公开数据集上验证模型，涵盖三种主要影像模态与两类任务：

| 任务类型 | 数据集 | 模态 | 数据规模 | 用途 |
|-----------|---------|------|----------|------|
| 去噪 | Lung LDCT (CT低剂量肺部) | CT | 50例（40训练+10测试） | CT去噪 |
| 去噪 | Brain FDG PET | PET | 88例（70训练+18测试） | PET去噪 |
| 超分辨率 (4×) | AortaSeg24 | CTA | 60例（50训练+10测试） | 血管结构高分辨恢复 |
| 超分辨率 (4×) | UHB FCD Brain MRI | MRI | 120例（96训练+24测试） | 脑部皮层结构恢复 |

所有体积均正则化至 [-1, 1] 区间。

### 2. 对比基线
与四种代表性3D扩散模型比较：
- **3D LDM**（在潜空间训练）  
- **3D WDM**（在小波域训练）  
- **3D DDPM**（标准噪声预测）  
- **3D DDIM**（隐式采样扩散）

使用评价指标：
- PSNR、SSIM、MS-SSIM、HFEN（高频误差归一指标）。

### 3. 结果摘要
- 在所有数据集和任务上，该方法均获得最优或次优结果。  
- 在 CT 去噪中比 3D DDIM 提升 +0.57 dB PSNR、+0.016 SSIM；在 CTA 超分中提升 +0.46 dB PSNR。  
- 多模态一致性表现突出，验证了模型的跨模态泛化能力。

---

## 四、资源与算力说明

- 论文未明确说明训练所用 GPU 型号或数量，也未给出绝对训练时长。  
- 作者仅报告相对性能：在相同硬件条件下，该方法能实现 **约 10× 加速训练和采样**。  
- 没有涉及详细的显存使用或硬件配置描述，需后续研究补充算力评估。

---

## 五、实验数量与充分性

- **实验广度**：覆盖 4 个不同模态与任务，体现较强的通用性。  
- **消融实验**：  
  - 时间步数 \(K\) 的影响（K=5 为最佳）。  
  - 模块组合对性能及训练时间的影响（x₀预测/稀疏调度/STM 增益均有量化）。  
- **充分性评价**：  
  - 消融与对比齐全、指标多维，说明实验设计系统且客观。  
  - 然而无跨设备性能评测或真实临床推理测试，需进一步验证临床实用性。

---

## 六、主要结论与发现

- 提出的 **稀疏体素空间扩散框架** 能在条件增强任务中显著降低去噪冗余。  
- 结合 **x₀预测 + 速度监督 + STM 模块**，模型在仅五个扩散步的情况下即可恢复高质量三维图像。  
- 相比主流扩散模型，实现了：
  - **性能提升**：PSNR/SSIM 等指标全面优于基线；
  - **效率提升**：训练与采样速度最高提高约十倍；
  - **细节保留**：能更好地恢复小血管、脑皮层复杂结构。  
- 表明在体素空间中进行条件扩散、结合结构自适应调节，是实现高效三维医学图像增强的可行新范式。

---

## 七、优点与创新点

- **算法层面**：
  - 稀疏时间步采样，大幅减少训练与采样成本；
  - 速度空间监督避免梯度不稳定；
  - STM 模块实现结构敏感的时序调制，使不同结构区自适应去噪。  
- **工程与实验层面**：
  - 在多模态、多任务上验证效果；
  - 保持全体素空间操作，不使用潜空间压缩，保留高频细节；
  - 模型代码开源，具有可复现性。  

---

## 八、不足与局限

- **算力与硬件信息缺失**：缺乏具体 GPU 和训练时间说明，无法评估真实效率与部署可行性。  
- **临床实用验证不足**：实验均基于公共数据集，缺少真实临床环境的评测。  
- **方法可能局限于条件增强任务**：对于无条件生成或截然不同模态的任务，稀疏步骤策略是否有效仍待验证。  
- **潜在的超参数敏感性**：稀疏步数 K、STM 模块结构可能需要根据任务重新调整。  

---

（完）
