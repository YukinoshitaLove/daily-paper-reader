---
title: "sketch-plot: Progressive Editing for Text-to-Image Academic Figures"
title_zh: sketch-plot：面向文本到图像学术插图的渐进式编辑
authors: "Yinghao Tang, Yupeng Xie, Yingchaojie Feng, Tingfeng Lan, Jiale Lao, Wei Chen"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.09171v2"
tags: ["query:real-ir"]
score: 9.0
evidence: 用于文本到图像学术图的渐进式编辑管道，实现可控图像编辑
tldr: 文本到图像模型生成学术图表后，局部修改要求重生成整个图像，无法保留已满意部分。sketch-plot提出三层渐进编辑管道：生成的PNG、可寻址拼图块、逐块SVG，用户只在需修改层介入，通过人在回路逐块接受或拒绝分解与矢量化决策。专家用户研究表明，该方法能有效进行针对性编辑，用户偏好优于整体重生成，缩小了AI生成学术图的可控性差距。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09171v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 860, \"height\": 567, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09171v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1807, \"height\": 1279, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09171v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 736, \"height\": 535, \"label\": \"Figure\"}]"
motivation: 现有T2I模型生成图表后难以局部修改，重生成会破坏原有满意部分，缺乏细粒度可控编辑手段。
method: 三层渐进管道：PNG、可编辑拼图块、逐块SVG；用户在最适层停止，通过人在回路界面逐块确认分解与矢量化结果。
result: 专家用户研究证实sketch-plot能有效进行针对性编辑，参与者偏好其胜过重生成整个图像。
conclusion: sketch-plot通过渐进编辑与人在回路，弥合AI生成学术图表的可控性缺口，提升其实用性。
---

## 摘要
诸如 gpt-image-2 等文本到图像（T2I）模型如今能够根据简短的提示生成出版级质量的学术插图，但其输出是平面的栅格图像：若用户希望修改一个箭头、一个标签或一个图标，则必须重新生成整个图像，同时也会干扰到他们希望保留的部分。我们提出了 sketch-plot，一个交互式系统，通过三层渐进式编辑流水线来弥补这一可控性差距：生成的 PNG、由可编辑图块组成的可寻址拼图，以及每个图块的 SVG。用户在能够对手头修改提供足够控制的层级停止，因此分解和矢量化的开销仅出现在需要的图块上。实现这一流水线并非易事。通用的分割模型缺乏语义区分能力，无法干净地分解研究插图，而端到端的图像矢量化会产生不完整的形状并丢失语义结构。因此，我们将这两个阶段都通过人在环接口进行引导，让用户能够逐块接受、优化或拒绝分解与矢量化的决策。我们通过一项专家用户研究验证了该设计，参与者发现 sketch-plot 对于对 AI 生成的学术插图进行定向编辑非常有效，并且优于重新生成整个图像。演示视频可在 https://paper-plot.dev/sketch 上观看。

## Abstract
Text to image (T2I) models such as gpt-image-2 can now generate publication grade academic figures from a short prompt, but the output is a flat raster: a user who wants to change one arrow, one label, or one icon has to regenerate the whole image, which also disturbs the parts they wanted to keep. We present sketch-plot, an interactive system that closes this controllability gap with a three layer progressive editing pipeline: a generated PNG, an addressable puzzle of editable pieces, and a per piece SVG. The user stops at the layer that gives them enough control for the change at hand, so the cost of decomposition and vectorisation is paid only on the pieces that need it. Realising this pipeline is not trivial. General segmentation models lack the semantic discriminability to decompose a research figure cleanly, and end to end image vectorisation produces incomplete shapes and loses semantic structure. We therefore route both stages through a human in the loop interface that lets the user accept, refine, or reject decomposition and vectorisation decisions on a piece by piece basis. We validate the design with an expert user study, in which participants found sketch-plot effective for making targeted edits to AI generated academic figures and preferred it over regenerating the whole image. A demonstration video is available at https://paper-plot.dev/sketch.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

*   **核心问题**：当前先进的文本到图像（T2I）模型（如 gpt‑image‑2）可快速生成出版级质量的学术插图，但输出为不可编辑的平面栅格图。若用户希望仅修改一个箭头、标签或图标，就必须重新生成整张图，这会同时破坏已满意的部分，形成 AI 生成插图的“**可控性差距**”。
*   **整体含义**：论文旨在解决这一差距，使研究人员既能享受 T2I 生成的视觉质量和速度，又能像在 PowerPoint 或 TikZ 中一样对生成结果进行**细粒度、局部、可保留式的编辑**。其核心矛盾是“快速生成”与“可控编辑”的权衡，论文试图通过一种人在回路中的渐进编辑范式将两者统一。

## 2. 论文提出的方法论

*   **核心思想**：**三层渐进式可控制流水线（PNG → 谜题拼图 → 逐块 SVG）**，用户根据修改需求在合适的层级停止，仅在必要时才对特定图块进行分解和矢量化，从而避免全局重生成。
*   **三层结构**：
    *   **L1（生成）**：使用 T2I 模型将文本提示转化为栅格 PNG。
    *   **L2（分解/谜题）**：将 PNG 分割为可独立寻址、带语义标签的“拼图块”。每个像素归属唯一一块，每块暴露统一的交互词汇（悬停、选择、调整边界、重新提示编辑等）。
    *   **L3（矢量化）**：对选定的拼图块进行图像到 SVG 的按需转换，使文本、颜色、描边等成为可直接编辑的属性。
*   **关键技术细节**：
    *   **VLM 粗布局 + 残差补全**：使用视觉语言模型（如 gemini‑3.5‑flash）读取栅格图，输出包含语义标签的顶层边界框，但模型输出通常不完美（有间隙、重叠）。通过**确定性残差补全步骤**：按区域面积升序排序，较小、更具体的框优先；进行轴对齐矩形裁剪，使框之间无重叠；剩余空白区域自动补全为背景块，最终形成完整铺满画布的精确分割。
    *   **人在环接口**：用户可逐块接受、优化或拒绝分解及矢量化结果。操作包括检查元数据、拖拽调整公共边界、对单块发出编辑提示（利用蒙版和指令重新生成该区域），以及按需将栅格块转换为可编辑 SVG。
    *   **交互模式**：所有区域（包括背景）均为可编辑块，模型错误被局限在单个块内，不会污染全局。

## 3. 实验设计

*   **实验类型**：专家用户研究，以验证系统帮助用户创作和精修 AI 生成学术图的可控性。
*   **参与者**：3 名来自人工智能和人机交互领域，有学术插图制作经验，且未使用过该系统的人员。
*   **流程与任务**：介绍系统后，参与者在约 40 分钟内，使用 sketch‑plot 创建一幅基于自己研究工作的学术图（如方法概览、管道图、系统架构图等），并通过 L1‑L3 流程进行生成和精细编辑，同时出声思考。
*   **评估方式**：5 点李克特量表（5 个问题：易学性、易用性、L1‑L3 流程有效性、L2 分解、L3 渲染）及结束后半结构化访谈。
*   **对比**：未设置基线方法对比，仅收集系统使用反馈和与以往“重新生成全图”经验的主观比较。

## 4. 资源与算力

*   **文中未明确说明**：论文未提及系统训练或运行所需的 GPU 型号、数量、训练时长等算力信息。系统以 Web 应用形式实现，通过 OpenRouter 调用外部模型（如 gpt‑image‑2、gemini 系列），推理成本由模型提供商承担，本地无额外训练开销。

## 5. 实验数量与充分性

*   **实验组数**：仅包含一项用户研究，未设计自动化 benchmark、消融实验或不同配置间的定量对比。
*   **充分性与客观性**：
    *   **样本量极小**（3 人），结果仅具初步探索意义，难以代表广泛用户群体。
    *   **无对照组**，未与“全图重生成”或“纯手动编辑”等策略进行定量比较，结论可信度受限。
    *   **依赖单系统实例**，缺乏不同模型选择或界面变体的分析。
    *   因此，实验更偏向于概念验证和设计迭代反馈，而非严格的效果验证。

## 6. 论文的主要结论与发现

*   **总体肯定**：参与者对 sketch‑plot 的易学性和易用性给予满分评价，并高度认同 L1‑L3 流程有助于将研究想法转化为可编辑的学术图（平均 4.67）。
*   **分层价值**：L2 分解阶段被认为有助于保留和重用生成图中满意的部分（4.67）；L3 矢量化虽能提供可编辑的 SVG，但用户指出复杂图标和密集区域仍需额外加工（4.33）。
*   **关键偏好**：参与者明确表示，更倾向于使用 sketch‑plot 进行定向编辑，而不是反复重新生成整个图像。
*   **改进建议**：用户希望增加对已选区域的进一步分解、更精确的边界吸附，以及撤销、预览、版本历史等编辑功能。

## 7. 优点

*   **创新范式**：提出“谜题拼图”概念，将 AI 生成后的编辑转化为按块可寻址的操作，弥合了 T2I 生成的可控性缺口。
*   **容错设计**：通过“任意布局 + 残差补全”的策略，将 VLM 的不完美输出转化为可用交互表面，避免了对完美分割模型的依赖。
*   **按需成本**：渐进式编辑使用户仅在必要块上投入分解和矢量化开销，保持了对大部分保留区域的零成本。
*   **人在环中的有效结合**：将人的判断与模型能力有机结合，用户可就地解决模型错误，错误影响被局限在单个块内。
*   **开源性**：提供了开源 Web 实例和演示视频，易于复现和后续研究。

## 8. 不足与局限

*   **实验规模不足**：用户研究仅 3 人，无定量基线对比，结论的普遍性和说服力有限。
*   **模型依赖性**：VLM 对结构化视觉内容的解析仍存在融合、遗漏和边界偏差，需进一步通过提示优化或边缘吸附来降低修复成本；图像到 SVG 转换对细部元素（图标、细线、字形）的处理依然薄弱。
*   **范围局限**：当前设计主要针对流程图和架构图，未扩展至表格、数据图表、代码驱动图形等其他常见学术插图类型。
*   **功能缺失**：缺乏进一步子块分解、精确边界调整辅助、编辑历史、撤销等功能，影响高精度任务的使用体验。
*   **缺乏自动化评估**：未提供基于度量（如编辑成功率、保留区域完整性）的客观指标，难以量化系统对可控性的改善程度。

（完）
