---
title: Local Coverage Governs Memorization in Diffusion Models
title_zh: 局部覆盖决定扩散模型中的记忆化
authors: "Claudia Merger, Sebastian Goldt"
date: 2026-06-12
pdf: "https://arxiv.org/pdf/2606.14390v1"
tags: ["query:real-ir"]
score: 9.0
evidence: 局部覆盖理论解释扩散模型记忆行为
tldr: 扩散模型生成样本时，可能复现训练数据（记忆）或产生新数据（泛化）。现有研究多从全局角度分析记忆，但本文发现记忆由训练点邻域的局部数据覆盖决定。利用扩散模型与核密度估计的数学联系，作者推导了依据局部密度和数据集大小判别记忆的理论条件，并证明在高维极限下存在局部相变：稀疏区域导致记忆，密集区域促进泛化。实验验证了理论，并扩展到多类场景，发现类内稀疏性越高的类别记忆越强。该研究揭示了数据几何如何塑造扩散模型的记忆行为，为理解生成模型的泛化与隐私提供了新视角。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.14390v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1638, \"height\": 1156, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14390v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1623, \"height\": 1080, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14390v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1646, \"height\": 1177, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14390v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1640, \"height\": 675, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14390v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1650, \"height\": 1130, \"label\": \"Figure\"}]"
motivation: 扩散模型可同时记忆和泛化，但何种训练样本易被记忆尚不清楚，缺乏局部视角的理论解释。
method: 将扩散模型与核密度估计相联系，推导基于局部数据密度和数据集大小的记忆预测准则，分析高维极限下的局部转变。
result: 理论和实验表明，低局部覆盖区域孤立样本被记忆，密集区域支持插值；记忆随局部稀疏性增加，多类设置中类内稀疏性越高记忆越强。
conclusion: 局部数据覆盖决定扩散模型的记忆行为，解释了记忆发生的几何条件，为生成模型泛化与隐私提供新见解。
---

## 摘要
扩散模型中的记忆化通常被视为模型或数据集的全局性质。然而在实践中，单个扩散模型可以同时生成记忆样本和新颖样本。哪些训练样本最有可能被记忆？在这项工作中，我们表明记忆化受局部数据覆盖支配。利用扩散模型与核密度估计（KDE）之间的联系，我们推导出一个理论准则，该准则基于训练样本在其邻域内的密度和训练数据集的大小，预测一个点是否被记忆。在高维极限下，这导致一个急剧的局部转变：低覆盖区域由孤立的训练样本主导，这些样本被记忆；而密集区域支持插值和泛化。我们通过实验验证了这些预测，表明记忆化随局部稀疏性增加，并且扩散模型在同一模型内表现出记忆样本与新样本的共存。将该框架扩展到多类设置，我们进一步表明，类内稀疏性更高（因此局部覆盖更低）的类别被更强烈地记忆。我们的结果提供了扩散模型中记忆化的局部视角，从数据几何的角度解释了记忆化发生的时间和地点。

## Abstract
Memorization in diffusion models is often treated as a global property of the model or dataset. In practice, however, a single diffusion model can simultaneously generate both memorized and novel samples. Which training samples are most likely to be memorized? In this work, we show that memorization is governed by \emph{local data coverage}. Leveraging the connection between diffusion models and kernel density estimation (KDE), we derive a theoretical criterion that predicts whether a point is memorized based on the density of training data in its neighborhood and the size of the training dataset. In the high-dimensional limit, this leads to a sharp, local transition: regions of low coverage are dominated by isolated training samples, which are memorized, while dense regions support interpolation and generalization. We validate these predictions empirically, showing that memorization increases with local sparsity and that diffusion models exhibit a coexistence of memorized and novel samples within the same model. Extending this framework to multi-class settings, we further show that classes with higher intra-class sparsity (and thus lower local coverage) are more strongly memorized. Our results provide a local view of memorization in diffusion models, explaining when and where memorization occurs in terms of data geometry.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义
- **研究动机**：扩散模型在有限数据训练时往往会复现训练样本（记忆化），而非生成全新内容，这带来隐私泄露、版权风险等问题。  
- **核心问题**：为什么同一个扩散模型可以同时产生记忆样本和新颖样本？哪些训练样本或数据区域更容易被记忆？  
- **整体含义**：论文提出“记忆化由局部数据覆盖决定”的观点，即邻域内训练数据的密度决定了该点是被记忆还是被泛化。该工作从数据几何角度解释了记忆何时、何地发生，挑战了将记忆视为全局特性的传统认知。

## 2. 方法论
- **核心思想**：利用扩散模型与核密度估计（KDE）的数学联系，将扩散模型的生成行为看作对训练数据分布的高斯混合估计。在局部尺度上，若某点邻域内训练样本稠密，则多个核叠加支持插值/泛化；若邻域稀疏，则训练点孤立，样本趋向于直接复制该训练点。  
- **关键技术细节**：  
  - 定义局部覆盖量 `pin(x)`：以 `x` 为中心、半径为 `h` 的球内数据的概率质量。  
  - 在高维极限 `P = e^{αN}` （ `P` 为训练样本数，`N` 为维度）下，定义指数增长率 `νin(x) = lim_{N→∞} (1/N) ln pin(x)` 。  
  - 主要理论结果（定理 1 和引理 2）：点 `x` 的 log-密度 `z_N(x)` 的行为由 `νin(x)` 与 `α` 的比较决定：  
    - 若 `νin(x) > -α`，该区域包含指数级多样本，KDE 平滑估计，模型泛化；  
    - 若 `νin(x) < -α`，高概率下无邻近样本，局部密度集中于孤立训练点，导致记忆化。  
  - 利用硬球核（hard-sphere kernel）解析展示这一转变，定性等同于高斯核。  
  - 采用指数变换（exponential transform）计算累积分布函数，联系 Gumbel 极值统计。
- **扩展**：将理论推广至多类场景，此时局部覆盖由最稠密类别决定，密度低（类内稀疏性高）的类别更易被记忆。

## 3. 实验设计
- **数据集与场景**：  
  - CIFAR-10（彩色图像，32×32）不同规模子集。  
  - CelebA（下采样至 32×32 灰度图像）。  
  - MNIST + CIFAR-10 混合数据集（平衡两类，用以构造不同局部密度的类别）。  
- **基准与评估指标**：  
  - 局部稀疏性：每个训练样本与其 k 个最近邻的平均平方距离。  
  - 局部主导性：生成样本的最近邻距离与次近邻距离之比的对数 `log(d2/d1)`。  
  - 记忆率：生成样本满足 `d1/d2 < τ` 的比例，`τ` 取多个阈值。  
  - 通过与测试数据（未参与训练）的对比提供无记忆时的基线。  
- **模型与训练**：  
  - 使用标准 U-Net 架构，离散时间步 T=1000。  
  - 优化器：Adam，学习率 1e-4，批次大小 100，训练固定 10^6 梯度步。  
- **对比方法**：主要验证自身理论，未与其他记忆分析或缓解方法直接对比。

## 4. 资源与算力
- 论文未明确提及 GPU 型号、数量及具体训练时间。  
- 训练配置提及：固定 10^6 梯度步，批次大小 100，在 CIFAR-10、CelebA 等数据集上进行，推断需中等算力，但缺少确切硬件描述。

## 5. 实验数量与充分性
- **实验组数**：  
  - 多个训练集大小 `P`（如 1000, 2000, 4000, 10000, 20000, 40000 等）的对比。  
  - 多个数据集（CIFAR-10，CelebA，CIFAR-10+MNIST）。  
  - 多个 k 值（5,10,20,50）和多个记忆阈值 `τ`。  
  - 类间分析：CIFAR 子类（飞机、船 vs 卡车、汽车）、CelebA 属性（金发、戴帽）、整体 MNIST vs CIFAR-10。  
- **充分性与客观性**：实验覆盖了不同数据集、样本量、度量方式，结果一致支持理论，消融实验较为齐全，但缺少与其他基线记忆分析技术的定量比较，评估属于自验证性质。

## 6. 主要结论与发现
- 记忆化由局部训练样本覆盖决定：稀疏区域孤立样本被记忆，稠密区域支持插值和泛化。  
- 同一扩散模型可同时产生记忆样本和新颖样本（共存现象）。  
- 随着训练集增大，密集区域首先不再记忆，稀疏区域记忆最后消失。  
- 类别内多样性高（即局部覆盖低）的类更易被记忆。  
- 记忆不是全局二值属性，而是与数据几何相关的局部现象。

## 7. 优点
- 提供了首个性地从局部数据覆盖角度解释扩散模型记忆机制的理论框架。  
- 将记忆与核密度估计明确关联，推导出简明的局部转变准则。  
- 实验设计系统，在多数据集、多尺度下验证了理论预测的一致性。  
- 通过类别属性分析（类内稀疏性）进一步扩展了理论的普适性。  
- 揭示的“共存”现象对理解生成模型的失败模式具有重要启示。

## 8. 不足与局限
- **理论假设**：基于无限维极限和指数样本复杂度，预测为尖锐转变，而实际观察到的是渐变；有限 N 的波动影响未充分解耦。  
- **空间假设**：理论假设模型在数据环境空间直接操作，而实际模型可能利用低维表示或对称性，有效核形状可能不同。  
- **核形式**：定性等价于高维高斯核，但未探究不同核函数的具体影响。  
- **实验局限**：仅在较小规模图像数据集上验证，未涉及大规模或文本条件模型；未与现有记忆缓解方法对比有效性。  
- **训练动态缺失**：未考虑训练时长、学习率等因素如何影响从记忆到泛化的收敛过程，尽管已有其他工作指出这些因素的重要性。  
- **实用性延伸**：未直接给出缓解记忆的具体策略，仅提供了理论解释。

（完）
