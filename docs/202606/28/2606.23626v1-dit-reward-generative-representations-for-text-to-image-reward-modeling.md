---
title: "DiT-Reward: Generative Representations for Text-to-Image Reward Modeling"
title_zh: "DiT-Reward: 面向文本到图像奖励建模的生成式表征"
authors: "Yuanming Yang, Guoqing Ma, Bo Wang, Yuan Zhang, Wei Tang, Chenyi Li, Haoyang Huang, Nan Duan"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23626v1"
tags: ["query:real-ir"]
score: 8.0
evidence: 将扩散变换器表示用于文本到图像奖励建模以评估生成质量
tldr: 研究生成表示能否用于评估生成图像质量。DiT-Reward将预训练的文本到图像扩散变换器改造为奖励模型，通过处理近干净潜变量并聚合文本条件的图像表示，在多个偏好基准上超越现有模型，证明了生成表示在图像生成评价中的有效性，为生成模型评估提供了新的高效途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23626v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1387, \"height\": 1293, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23626v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1443, \"height\": 665, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23626v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1328, \"height\": 1959, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23626v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1158, \"height\": 921, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23626v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1447, \"height\": 944, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23626v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1445, \"height\": 620, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23626v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1454, \"height\": 865, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23626v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1205, \"height\": 460, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23626v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1080, \"height\": 208, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23626v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1270, \"height\": 209, \"label\": \"Table\"}]"
motivation: 将扩散变换器表示用于文本到图像奖励建模以评估生成质量。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
为图像生成而学习的表征能否也支持生成图像的评估？我们将文本到图像的奖励预测作为生成式表征学习的一个下游任务展开研究。为此，我们引入了 DiT-Reward，它通过处理接近干净的图像潜变量并在变换器各层之间聚合文本条件下的图像表征，将预训练的文本到图像扩散变换器转换成了奖励模型。在使用与 HPSv3 相同的训练数据混合下，DiT-Reward 在所有四个评估偏好基准上都优于 HPSv3，在 HPDv2 上达到 85.6%，在 HPDv3 上达到 77.6%。当生成式主干网络被冻结时，一个轻量级的学习头仍能从其表征中提取有意义的偏好预测。跨深度的探测进一步揭示，下游奖励性能在中间到靠后的层中最强，并受益于结合不同阶段的表征。我们还观察到生成式主干网络容量带来了一致的正向扩展。最后，当用于优化 Stable Diffusion 3.5 Large 并结合 Flow-GRPO 时，DiT-Reward 在匹配的训练轨迹上优于 HPSv3，尤其在真实感方面有显著提升。直接潜变量评分相比 HPSv3 还实现了 1.65 倍的推理加速，且峰值内存相当。这些结果表明，预训练的生成式 DiT 为奖励建模和策略优化提供了可迁移的表征。

## Abstract
Can representations learned for image generation also support the evaluation of generated images? We study text-to-image reward prediction as a downstream task of generative representation learning. To this end, we introduce DiT-Reward, which converts a pretrained text-to-image Diffusion Transformer into a reward model by processing near-clean image latents and aggregating text-conditioned image representations across transformer layers. Under the same training data mixture as HPSv3, DiT-Reward outperforms HPSv3 on all four evaluated preference benchmarks, reaching 85.6% on HPDv2 and 77.6% on HPDv3. When the generative backbone is frozen, a lightweight learned head can still extract meaningful preference predictions from its representations. Probing across depth further reveals that downstream reward performance is strongest in the middle-to-late layers and benefits from combining representations across different stages. We also observe consistent positive scaling with generative backbone capacity. Finally, when used to optimize Stable Diffusion 3.5 Large with Flow-GRPO, DiT-Reward outperforms HPSv3 along the matched training trajectory, with particularly clear gains in realism. Direct latent scoring also achieves a 1.65x inference speedup over HPSv3 with comparable peak memory. These results show that pretrained generative DiTs provide transferable representations for reward modeling and policy optimization.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **核心问题**：能否将预训练文本到图像扩散变换器（DiT）内部用于生成的表征，直接改造为评估生成质量的奖励模型？换言之，生成器学到的“理解”能否服务于“评价”。
- **研究动机**：现有奖励模型多依赖独立的判别式视觉‑语言编码器（如 CLIP、BLIP），需要解码到像素空间再编码，与生成策略处于分离的表征空间。本文试图利用生成器自身的表征来构建奖励模型，使评价与生成在统一空间中完成，从而桥接生成与理解。
- **整体含义**：探索生成模型的表征可迁移性，证明大型 DiT 不仅是图像生成器，也可作为通用的、可迁移的视觉‑文本奖励表征主干，为文本到图像的强化学习和对齐提供新范式。

## 2. 论文提出的方法论

### 2.1 核心思想
将预训练的 MMDiT（多模态扩散变换器）的中间层隐藏状态作为图像‑提示对的特征，附加一个轻量级 MLP 头输出偏好标量，并通过 Bradley‑Terry 偏好损失微调。同时允许直接对生成策略输出的潜变量评分，省去解码‑再编码步骤。

### 2.2 关键技术流程

1. **近干净潜变量构建**：对输入图像 \( x \) 经 VAE 编码得到潜变量 \( z_0 \)，加入极少量高斯噪声：  
   \[
   z_\tau = (1-\tau) z_0 + \tau \epsilon,\quad \epsilon \sim \mathcal{N}(0,I),\;0<\tau\ll1
   \]  
   使之接近 DiT 训练时的输入分布。

2. **多尺度特征提取**：选择 DiT 的 K 个层 \( S=\{\ell_1,\ell_2,\dots,\ell_K\} \)，提取每层的图像标记隐藏状态 \( \mathbf{H}_\ell(x,p) \)，通过均值池化得到 \( \mathbf{f}_\ell \)，再将所有层的池化特征拼接：  
   \[
   \mathbf{f}(x,p) = \text{Concat}(\mathbf{f}_{\ell_1},\mathbf{f}_{\ell_2},\dots,\mathbf{f}_{\ell_K}) \in \mathbb{R}^{K\cdot d}.
   \]

3. **奖励映射**：拼接特征经轻量级 MLP 映射为标量奖励 \( r_\theta(x,p) \)。

4. **训练目标**：使用标准 Bradley‑Terry 损失：  
   \[
   \mathcal{L}(\theta) = -\log\sigma\big(r_\theta(x^+,p)-r_\theta(x^-,p)\big)
   \]  
   优化奖励之间的相对排序。

5. **潜变量空间中的强化学习**：当策略与奖励模型共享同一 VAE 潜空间时，策略产生的潜变量可直接送入 DiT‑Reward 评分，跳过 VAE 解码和像素编码，提升推理效率并降低内存占用。文中结合 Flow‑GRPO 优化 SD3.5 Large。

### 2.3 实现细节
- 默认使用 SD3.5‑Large 的 38 层 MMDiT，特征层选取 {15,23,28,37}。
- 训练时 \(\tau=0.005\)，文本编码器和 VAE 冻结，DiT 骨干和奖励头可微调（学习率分别为 5e-6 和 2e-5）。
- 奖励头参数量约 4M–16M，随层数变化。

## 3. 实验设计

### 3.1 基准与数据集
- **偏好基准**：ImageReward、PickScore、HPDv2、HPDv3（成对偏好预测准确率）。
- **训练数据混合**：完全遵循 HPSv3 的公开配方，包括 HPDv3 成对数据、HPDv2 黄金子集、Pick‑a‑Pic 样本、ImageReward 样本以及 Midjourney 用户选择数据，无额外数据。

### 3.2 对比方法
CLIP ViT‑H/14、Aesthetic Score Predictor、ImageReward、PickScore、HPS 系列（HPS、HPSv2、MPS、HPSv3）。其中 HPSv3 为主要对比对象。

### 3.3 下游 RL 评估
- **策略优化**：使用 SD3.5‑Large + Flow‑GRPO，与 HPSv3 共享完全相同的训练配置（采样步数、引导尺度、组大小、批次、LoRA 参数等）。
- **外部评估**：GPT‑5 和 Gemini‑3‑Flash 对 2048个 PickScore 提示生成的图像在“提示对齐、视觉质量、真实感、细节丰富度”四个维度独立打分，整体轨迹可视化。
- **效率对比**：比较 HPSv3、DiT‑Reward 像素接口和潜变量接口在 RTX PRO 6000 Blackwell GPU 上的单张推理延迟与峰值内存。

## 4. 资源与算力

- **推理效率实验**：使用 1 块 NVIDIA RTX PRO 6000 Blackwell GPU，batch size=1。
- **RL 训练**：使用 16 块 GPU 进行分布式梯度累积，每个更新步骤的有效优化批量为 384 个样本；未明确提及总训练时长和具体 GPU 型号（仅提 16 GPUs，可能是同级别计算卡）。
- 常规奖励模型训练的具体 GPU 数量和训练时间未给出，只提训练 2–3 个 epoch，warmup 比率 0.05。

## 5. 实验数量与充分性

- **偏好预测实验**：4 个主流基准，覆盖不同数据分布，与至少 7 种基线进行对比。
- **消融研究**：
  - 冻结骨干 vs. 完全微调，对比两种容量（SD3.5‑Medium 2.5B 与 Large 8.1B）。
  - 单层探针与层对探针，系统分析每层以及跨层组合的奖励表征能力（HPDv3）。
  - 跨去噪步骤的层间相似性演变，支撑表征可分块互补的结论。
- **RL 实验**：长达 1080 步的训练轨迹，每 60 步检查一次，外部双法官评估，包含定性和定量对比。
- **效率测试**：三种奖励推理接口的延迟和内存对比。
- 实验设计较全面，覆盖了算法有效性、表征可迁移性、伸缩性及系统工程收益，对比公平（相同训练数据/RL 超参）。

## 6. 论文的主要结论与发现

- **DiT‑Reward 在多个偏好基准上超越 HPSv3**，尤其在 HPDv3 和 HPDv2 上取得最佳（77.6% 和 85.6%），证明生成式 DiT 可有效转化为奖励模型。
- **生成表征本身包含大量偏好信息**：冻结主干仅训练轻量级头即能获得中等性能，说明预训练阶段已编码了与人类偏好相关的知识。
- **表征分布与互补性**：中层至后层（约第24层）对奖励预测最重要，不同块（B1–B4）的表征具有互补性，跨块融合可进一步提升性能。
- **模型容量与奖励性能正相关**：从 2.5B 扩充到 8.1B 带来一致增益，表明更大生成主干能提供更强的可迁移表征。
- **RL 应用中的优势**：DiT‑Reward 优化的策略在真实感、视觉质量和细节丰富度上显著优于 HPSv3 优化策略，同时保持推理加速（1.65×）和内存节约。
- **奖励攻击与局限性**：两种奖励模型在晚期均出现奖励攻击，但表现形色不同；DiT‑Reward 的提示对齐能力在后期稍弱，可能源于扩散骨干的文本跟踪能力不及 VLM。

## 7. 优点

- **原生表征复用**：无需额外视觉编码器，直接利用生成模型自身的多模态表征，保持策略与奖励模型空间一致。
- **高效推理**：潜变量直接评分省去解码‑编码开销，在内存相近的条件下实现更快推理。
- **系统化验证**：从冻结探针、层选择、容量扩展到 RL 对齐，多角度验证了生成表征的可迁移性和可比性。
- **训练数据控制**：严格使用与 HPSv3 相同的数据混合物，排除了数据分布差异的干扰，突出架构优势。
- **可解释性强**：通过层相似性与单层/块探针揭示了 MMDiT 内部表征的组织结构及其对偏好预测的贡献。

## 8. 不足与局限

- **提示对齐弱势**：在 RL 后期，DiT‑Reward 训练的模型在精确遵循文本指令方面逊于 HPSv3，可能因为扩散骨干对细粒度语言理解能力不如大型 VLM。未来可考虑融合文本侧表征或显式建模图文交互。
- **奖励攻击未根本解决**：两种奖励均导致后期输出失真，需更多正则化或动态奖励技术。
- **架构单一**：仅测试了 Stable Diffusion 3.5 系列的 MMDiT，未探索其他基于流的扩散模型或不同规模的 DiT 变体，泛化性待验证。
- **强化学习实验局限**：仅使用 Flow‑GRPO 一种算法和 SD3.5 Large 一种策略，不同 RL 范式下的表现未比较。
- **计算开销细节缺失**：未报告奖励模型训练的具体 GPU 小时或总能耗，以及 RL 训练的完整资源消耗，难以精确复现计算需求。

（完）
