---
title: "Chorus II: Cross-Request Sparsity Reuse for Efficient Image-to-Video Generation"
title_zh: Chorus II：面向高效图像到视频生成的跨请求稀疏性重用
authors: "Hao Liu, Chenghuan Huang, Hao Liu, Xing Cai, Chen Li, Ziyang Ma, Jing Lyu, Nong Xiao, Jiangsu Du"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.25040v1"
tags: ["query:real-ir"]
score: 8.0
evidence: 扩散模型中通过跨请求稀疏复用的高效图像到视频生成
tldr: 图像到视频生成服务面临高计算成本问题，相似请求中稀疏注意力模式高度一致。提出跨请求稀疏重用框架Chorus II，通过共享稀疏掩码避免在线预测，结合可选特征重用和轻量引导增强来维持质量。实验表明，默认配置下生成质量不变，速度提升2.16倍，为高效部署提供新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25040v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1322, \"height\": 820, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25040v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 588, \"height\": 393, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25040v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1433, \"height\": 804, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25040v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1296, \"height\": 692, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25040v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 656, \"height\": 339, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25040v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1230, \"height\": 624, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25040v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 646, \"height\": 426, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25040v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1451, \"height\": 588, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25040v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1448, \"height\": 373, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25040v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1445, \"height\": 237, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25040v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 773, \"height\": 184, \"label\": \"Table\"}]"
motivation: 大规模部署图像到视频扩散模型时，相似请求的冗余计算导致高昂成本，现有特征重用方法未充分利用注意力稀疏性。
method: 跨请求重用历史稀疏掩码作为先验，消除在线预测开销；可选特征重用在下采样区域计算；引导增强缓解语义漂移。
result: 默认稀疏重用配置在保持生成质量的同时，实现2.16倍加速，无明显边界伪影或条件偏离。
conclusion: Chorus II利用跨请求稀疏性显著降低计算负担，为高效图像到视频推理提供可行方案。
---

## 摘要
为图像到视频生成提供服务级扩散模型计算开销高昂，给大规模部署带来显著挑战。真实图像到视频工作负载常包含相似请求，如重复的效果模板、相关主题和反复出现的镜头构图。现有跨请求加速方法主要通过特征重用利用此类冗余。我们观察到，相似请求还共享高度一致的稀疏注意力模式，使得历史稀疏掩码可作为请求条件先验，几乎无在线掩码预测开销。我们提出一个以稀疏性重用为核心的跨请求重用框架，并将特征重用作为可选扩展，由轻量级引导增强加以保护。稀疏性重用以共享稀疏掩码重用的形式实现，即重用相似历史请求的高质量稀疏掩码，避免逐请求在线掩码预测。可选特征重用将下采样计算应用于高度冗余的时空区域，在保持效率增益的同时抑制边界伪影。引导增强在重用后强化图像/文本条件，缓解语义漂移和条件遵循问题。实验表明，默认稀疏性重用配置在保持生成质量的同时实现2.16倍加速。

## Abstract
Serving diffusion models for image-to-video generation is computationally expensive, posing significant challenges for large-scale deployment. Real I2V workloads often contain similar requests, such as repeated effect templates, related subjects, and recurring shot layouts. Existing cross-request acceleration methods mainly exploit this redundancy through feature reuse. We observe that similar I2V requests also share highly consistent sparse attention patterns, enabling historical sparse masks to serve as request-conditioned priors with almost no online mask-prediction overhead. We propose a cross-request reuse framework centered on \textbf{sparsity reuse}, with \textbf{feature reuse} as an optional extension safeguarded by a lightweight \textbf{guidance enhancement}. Our sparsity reuse is implemented as shared sparse mask reuse, which reuses high-quality sparse masks from similar historical requests to avoid per-request online mask prediction. Optional feature reuse applies downsampled computation to highly redundant spatiotemporal regions, mitigating boundary artifacts while preserving efficiency gains. Guidance enhancement reinforces image/text conditioning after reuse, mitigating semantic drift and condition-adherence issues. Experiments show that default sparsity reuse configuration preserves generation quality with a \textbf{2.16$\times$} speedup.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：在图像到视频（I2V）生成的大规模部署中，扩散模型（DiTs）的推理计算开销巨大，尤其在步骤蒸馏后，传统的单请求内冗余利用方法（如动态稀疏注意力、特征缓存）因步间相关性降低而效率折损，难以持续提供加速。
- **整体含义**：文章观察到现实I2V服务中充斥着语义或结构相似请求，这些请求不仅共享浅层特征冗余，更在深层注意力机制中表现出高度一致的稀疏模式。因此，提出利用跨请求的稀疏性重用作为新的加速维度，通过将历史高质量稀疏掩码复用为当前请求的先验，几乎消除在线预测成本，突破单请求内加速的瓶颈，实现更高效的部署。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程（用文字说明即可）
- **核心思想**：构建一个以“跨请求稀疏性重用”为中心的框架，将特征重用作为可选加速扩展，并用轻量级“引导增强”保护生成质量。默认质量优先路径基于稀疏掩码复用，避免逐请求的昂贵预测；激进速度优先路径额外启用下采样特征重用。
- **关键技术细节**：
  - **共享稀疏掩码重用**：将历史请求的高质量块级稀疏掩码（通过top-p与最小top-k产生）缓存于向量数据库，新请求到来时基于文本与图像嵌入的余弦相似度检索并直接应用。若相似度低于阈值则回退至在线预测或稠密注意力。
  - **安全回退机制**：
    - *块对访问刷新*：在每层组（如8层）内维护已访问块对映射，组内最后一层强制覆盖未访块，保证信息流下限。
    - *最小top-k保证*：缓存掩码时施加最低保留比例（如10\%），防止注意力极度集中的头部在跨请求分布微小漂移时丢失关键信息。
  - **头部级负载均衡调度**：已知头部稀疏度分布后，在多头序列并行前采用贪心算法重排头部分配，平衡GPU间计算负载。
  - **下采样潜特征重用**：在可选特征重用路径中，替代直接丢弃非计算区或缓存海量KV状态，改用时空下采样池化处理重用区域的上下文计算，再与计算区域的完整序列拼接，以极低成本消除区域边界撕裂伪影。
  - **引导增强**（训练无关）：
    - *首帧键增强*：自注意力的键张量中，首帧标记乘以步长依赖因子βstep，放大首帧信息在softmax中的竞争力。
    - *图像嵌入增强与衰减调度*：在patch嵌入阶段，对条件图像分支的卷积权重施加系数αstep进行放大。两者均在重用启动步后生效并随去噪步数衰减。
- **公式或算法流程**：
  - 首帧键增强：`K[:, 0:N0, :, :] ← β_{step} K[:, 0:N0, :, :]`
  - 图像嵌入增强：`e = Conv3d(x, W_x) + α_{step} Conv3d(y, W_y) + b`，等价于 `Conv3d([x, α_{step} y], W)`。

## 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法
- **数据集与场景**：使用COCO 2017图像作为条件图像，文本提示来自真实业务场景。所有视频在720p分辨率、61帧、每帧3600个令牌、4步蒸馏的轻量x2v LoRA模型上评估。
- **基准模型（Benchmark）**：以4步蒸馏的Wan2.2-I2V模型配合稠密FlashAttention作为质量与速度的绝对参照。
- **对比方法**：
  - 稀疏注意力类：动态高精度稀疏SVG2、粗粒度动态稀疏Sparge-Attn、静态稀疏Radial-Attn。各方法在不同稀疏阈值下比较。
  - 加速量化类：SageAttention。
  - 本文变体：默认稀疏性重用路径、稀疏+下采样特征重用（速度优先）、稀疏重用的“turbo”激进配置。
- **评估指标**：
  - 视频通用质量：VBench-Q。
  - 条件遵循度：图像条件遵循DINO-I、文本条件遵循CLIP-T。
  - 保真度（对稠密基线）：PSNR、SSIM、LPIPS、CLIP-F。
  - 效率：端到端延迟与加速比。

## 4. 资源与算力：如果文中有提到，请总结使用了多少算力
- **硬件平台**：所有实验在**NVIDIA H20 GPUs**上进行。文中虽未明确训练过程（该工作是推理加速，不涉及训练），但展示了单卡至8卡的负载均衡测试，即实验可扩展至多卡并行环境。
- **推理算力**：明确记载了单H20上，默认稀疏性重用配置的端到端延迟为78.9秒，结合负载均衡调度后8卡时可降至15.1秒。

## 5. 实验数量与充分性：大概做了多少组实验，这些实验是否充分、是否客观、公平
- **实验组数**：论文提供了至少四组详实实验表格和分析图表：
  1. **主要对比表**（Table 1）：在4步蒸馏模型上比较1种基线、1种量化方法、3类稀疏方法（含多阈值）及本文4种配置，跨越**质量、条件遵循、保真度、效率**十余项指标。
  2. **掩码质量与开销微基准测试**（Table 2, Fig. 7）：横向对比SVG2（多速度档）、Sparge-Attn和本文重用方法的路由开销、掩码覆盖率、注意力延迟；纵向探讨了不同语义相似度下掩码覆盖率的变化。
  3. **消融实验**（Table 3）：对可选特征重用路径的“下采样”与“引导增强”两个组件进行2x2全面消融。
  4. **系统优化验证**（Table 4）：对负载均衡调度在1/2/4/8卡下的加速收益进行独立验证。
- **充分性、客观性与公平性**：
  - **充分**：实验覆盖了动态/静态稀疏、量化及跨请求缓存等多个主流加速范式，并对自身方法的关键设计维度（稀疏度、特征重用组件、工程优化）进行了拆解。
  - **客观公平**：所有对比方法均使用其论文默认或官方库推荐的超参数，在统一的4步蒸馏模型、分辨率和求解器下测量端到端延迟。评估指标兼顾无参考质量（VBench-Q）、条件对齐（DINO-I/CLIP-T）和有参考保真度（PSNR/CLIP-F），多维度衡量避免了单一指标偏颇。

## 6. 论文的主要结论与发现
- **发现**：相似I2V请求的块级稀疏注意力模式具有高度可复用性。当DINO相似度>0.4时，理想top-p(90%)掩码的IoU超过85%。这为跨请求稀疏先验提供了实证基础。
- **结论**：
  - 提出的**稀疏性重用**作为默认路径，成功将高昂的在线掩码搜索转为几乎零开销的检索，在保持与稠密基线近乎一致的生成质量（VBench-Q 0.787, CLIP-F 0.9859）下，实现了**2.16倍**端到端加速，其保真度媲美高成本动态稀疏方法SVG2，而延迟仅为其一半。
  - 可选的下采样特征重用结合引导增强，可在降低边界伪影和语义漂移的同时，将加速比进一步提升至**2.59倍**，代表了一种更激进的质量-速度权衡点。
  - 轻量的工程优化（如头部负载均衡调度）可额外带来~7%的系统吞吐增益，展示了算法与系统协同设计的价值。

## 7. 优点：方法或实验设计上有哪些亮点
- **方法亮点**：
  - **新维度挖掘**：首次将加速视角从“单请求内去冗余”拓展到“跨请求结构级模式复用”，切中了轻步数模型内部冗余减少的痛点。
  - **精巧的工程权衡**：通过共享掩码缓存打破动态稀疏“精度-开销”僵局，用“空间换时间”和“检索替代预测”实现近乎零额外开销的高精确稀疏计算。
  - **全面的质量保障机制**：设计两级安全回退和轻量引导增强，系统性地对抗了跨请求复用引入的信息丢失、条件漂移和区域伪影等典型复用风险，使激进加速路径也可用。
  - **系统协同优化**：利用已知掩码稀疏度进行头部级负载均衡，是推理层面切实有效的工程优化。
- **实验亮点**：
  - **评估维度立体**：构建了包含通用视觉质量、条件遵循、稠密基线保真度和绝对效率的四维评价体系，能全面反映不同方法的质量-速度权衡本质。
  - **对比基准扎实**：涵盖了从稠密基线到最新的量化（Sage）、静态（Radial）、动态（SVG2, Sparge-Attn）各类加速技术，并在统一的平台和模型上进行端到端公平比较。

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
- **实验覆盖**：
  - 实验基于单个4步蒸馏的Wan2.2-I2V模型，未在其他主流I2V架构或不同蒸馏步数（如8步、2步）上验证方法的泛化性。
  - 数据集场景使用COCO图像和自建提示，评估更多依赖自动化指标，缺少大规模用户主观质量研究来印证无参考得分。
- **偏差风险与应用限制**：
  - **强相似性依赖**：方法加速效率直接受请求流语义相似度波动影响。在请求多样化、缺乏相似历史请求的冷启动或长尾场景下，会频繁回退至基线推理，稀释整体加速收益。
  - **数据库维护代价**：为保障高命中率，尤其在通用场景下，需要构建和维护一个大规模的、持续更新的向量数据库与掩码缓存，此运维复杂度在论文中未作深入讨论或测算。
  - **特征重用风险**：尽管有增强措施，可选特征重用路径仍导致对稠密基线的保真度显著下降（如PSNR/SSIM/LPIPS大幅恶化），在追求极高内容保真度的应用中，仍可能面临身份漂移等不可接受的风险。
  - **推理限制**：该框架仅针对生成推理阶段的加速，无法加速模型训练过程。

（完）
