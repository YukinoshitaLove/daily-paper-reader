---
title: "IV-CoT: Implicit Visual Chain-of-Thought for Structure-Aware Text-to-Image Generation"
title_zh: IV-CoT：面向结构感知文本到图像生成的隐式视觉思维链
authors: "Zixuan Li, Haokun Lin, Yicheng Xiao, Zhiwei Li, Xinyang Song, Zelong Zheng, Yong He, Heng Yao, Ke Ding, Chao Yu, Chuan Yuan, Qi Li, Zhenan Sun"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24849v1"
tags: ["query:real-ir"]
score: 8.0
evidence: 文本到图像生成的隐式视觉思维链
tldr: 多模态大语言模型在结构感知文生图任务中因结构规划与外观渲染耦合而效果受限。本文提出隐式视觉思维链（IV-CoT），通过潜在视觉规划将结构查询与语义查询级联解耦，训练时引入草图监督但推理无需草图。仅单次前向传递即可隐式推理，在GenEval和T2I-CompBench上取得最优结果，证实结构查询与语义查询的互补性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24849v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1653, \"height\": 506, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24849v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1455, \"height\": 876, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24849v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 739, \"height\": 410, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24849v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1657, \"height\": 757, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24849v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 806, \"height\": 798, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24849v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 761, \"height\": 815, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24849v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1604, \"height\": 1605, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24849v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1645, \"height\": 1953, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24849v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 811, \"height\": 221, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24849v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1662, \"height\": 965, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24849v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1670, \"height\": 361, \"label\": \"Table\"}]"
motivation: 结构感知文生图中，结构规划与外观渲染在单一条件流中纠缠，导致对象计数、空间关系等结构难题。
method: IV-CoT框架将视觉条件查询分解为结构到语义的级联：先用结构查询生成潜在视觉计划，再基于此计划用语义查询渲染外观。
result: 在GenEval和T2I-CompBench基准上，IV-CoT以单次前向隐式推理达到结构感知生成最优性能。
conclusion: 结构查询与语义查询互补，训练时草图监督有效注入结构知识，无需推理时草图提取，提升文生图结构遵循能力。
---

## 摘要
统一多模态大语言模型（MLLMs）在文本到图像生成方面已取得了高质量，但在遵循结构感知提示方面仍面临挑战，这类提示要求保留物体数量、空间关系、属性绑定以及粗略布局。我们将这一限制部分归因于单一条件流中结构规划和外观渲染的纠缠。为解决这一问题，我们提出了隐式视觉思维链（IV-CoT），这是一种用于查询条件图像生成的潜空间视觉推理框架。IV-CoT将视觉条件查询分解为从结构到语义的级联：结构查询首先形成潜空间视觉规划，然后语义查询在该规划的条件下渲染外观。为引导结构查询，我们引入了仅训练时使用的草图监督，鼓励它们从草图中捕获结构，而无需在推理时进行草图提取或中间解码。IV-CoT在单次前向传播中执行隐式CoT推理，并在GenEval和T2I-CompBench上取得了优越的结果。可视化与分析表明，所学到的结构查询和语义查询在结构感知生成中发挥着互补作用。

## Abstract
Unified multi-modal large language models (MLLMs) have achieved strong text-to-image generation quality, but still struggle with structure-aware prompt following, where object counts, spatial relations, attribute bindings, and coarse layouts must be preserved. We attribute this limitation in part to the entanglement of structural planning and appearance rendering within a single conditioning stream. To address this issue, we propose Implicit Visual Chain-of-Thought (IV-CoT), a latent visual reasoning framework for query-conditioned image generation. IV-CoT decomposes the visual conditioning queries into a structural-to-semantic cascade, where structural queries first form a latent visual plan and semantic queries then render appearance conditioned on this plan. To guide the structural queries, we introduce training-only sketch supervision, which encourages them to capture structure from sketches without requiring sketch extraction or intermediate decoding at inference time. IV-CoT performs implicit CoT reasoning in a single forward pass and achieves superior results on GenEval and T2I-CompBench. Visualizations and analyses demonstrate that the learned structural and semantic queries play complementary roles in structure-aware generation.

---

## 论文详细总结（自动生成）

### 一、论文的核心问题与整体含义

- **研究背景与动机**：当前统一多模态大语言模型（MLLM）在文本到图像生成中已能产生逼真图像，但在**结构感知提示遵循**（structure‑aware prompt following）方面表现不佳。当提示中包含多个物体、特定空间关系、属性绑定和布局要求时，模型常常交换属性、遗漏物体或违反请求的布局。
- **核心问题**：作者将失败归因于**结构规划与外观渲染在单一条件流中的纠缠**。MLLM将场景结构、物体身份、属性等压缩为一条平坦的视觉条件序列，使得生成器难以区分哪些信息决定结构、哪些控制外观。
- **整体含义**：论文提出一种**隐式视觉思维链（IV‑CoT）**框架，将结构规划和语义渲染解耦，通过在潜空间中进行结构优先的推理来改善结构感知生成，而不需要显式的中间文本或图像输出。

### 二、方法论

- **核心思想**：将视觉条件查询分成**结构查询（structural queries）与语义查询（semantic queries）**，并利用因果自注意力构建一个单向依赖：结构查询先生成潜空间视觉规划，语义查询再以此规划为条件进行外观渲染。
- **关键技术与流程**：
    - **查询级联**：将可学习的视觉查询输入分为两组 \(Q_0^s\)（结构查询输入）和 \(Q_0^m\)（语义查询输入），以有序序列 \([y, Q_0^s, Q_0^m]\) 输入 MLLM。由于因果注意力，结构查询无法访问语义查询，但语义查询可以访问结构查询的输出，形成“结构 → 语义”的级联。
    - **训练第一阶段（冻结生成器、仅结构约束）**：使用固定的边缘检测器（PiDiNet）从训练图像提取草图，并用预训练VAE编码为草图潜变量 \(z_s\)。冻结 MLLM 与扩散模型，仅优化结构查询输入 \(Q_0^s\)，使其能以草图潜变量为干净目标进行扩散去噪。损失为 \(L_{\text{struct}} = \mathbb{E}_{z_s,t,\epsilon}\|\epsilon - \epsilon_\theta(z_{s,t}, t, Q_s)\|_2^2\)。该阶段迫使结构查询编码轮廓、形状、数量和布局等信息，排斥颜色、纹理等外观因素。
    - **训练第二阶段（语义渲染与结构正则）**：解冻全部组件，加入图像生成损失 \(L_{\text{img}}\)，并将 \(L_{\text{struct}}\) 作为正则项保留（\(\lambda = 0.3\)），防止结构查询退化为仅编码外观特征。总损失 \(L_{\text{total}} = L_{\text{img}} + \lambda L_{\text{struct}}\)。
    - **推理**：仅需一次 MLLM 前向传播，输入完整有序查询，得到 \([Q_s, Q_m]\)，直接输入扩散模型生成最终图像。**无需提取草图、解码中间图像或产生显式推理轨迹**。
- **公式与符号**：文中使用的主要符号包括：\(Q = \Phi_{\text{MLLM}}(y, Q_0)\) 表示 MLLM 生成的条件查询；\(Q_s\)、\(Q_m\) 分别为结构查询和语义查询；扩散模型以 \([Q_s, Q_m]\) 为条件进行去噪。

### 三、实验设计

- **数据集与训练**：使用混合数据集训练，包含 BLIP3‑o、ShareGPT‑4o‑Image 和 Echo‑4o，共约 128,393 个图文对。草图仅在训练时用固定 PiDiNet 从训练图像提取。
- **基准评估**：
    - **GenEval**：评估单物体、双物体、计数、颜色、位置、颜色属性等维度的整体得分。
    - **T2I‑CompBench**：评估颜色、形状、纹理、空间关系、非空间关系、复杂组合等维度的整体得分。
- **对比方法**：
    - **统一生成模型**：Janus‑Pro、Emu3、Show‑o、MetaQuery‑XL、OpenUni‑L‑1024（基线）、BAGEL、TUNA‑2。
    - **显式CoT增强生成模型**：T2I‑R1、GoT‑R1、TWIG‑RL、Uni‑CoT、Draco。
- **消融实验**：设置了多种变体，包括`Base + More Queries`（仅增加查询数）、`Flat Sketch Aux`（扁平查询加草图辅助）、`Parallel Two-Query`（并行两组查询，无级联）、`IV-CoT w/o Structural Constraint`（有级联但移除结构正则）等，以分析查询组织与结构监督各自的作用。
- **零样本重组实验**：将不同提示得到的结构查询与语义查询交叉组合，直接输入扩散模型，观察生成图像是否继承结构布局与外观属性。

### 四、资源与算力

- **GPU 配置**：使用 NVIDIA A800 80GB GPU 进行训练，推理延迟同样以单张 A800 测量。
- **混合精度**：bfloat16 混合精度训练。
- **优化器与超参**：AdamW 优化器，学习率 \(2\times10^{-5}\)，\(\beta=(0.9,0.95)\)，权重衰减 0.05，梯度裁剪 1.0；学习率线性预热 10% 步数，余弦退火至 \(1\times10^{-7}\)。
- **模型规模**：基于 OpenUni‑L 主干，MLLM 为 2B InternVL3，扩散生成器为 1.6B Sana DiT，总参数约 3.6B。
- **训练时长未明确提及**，但给出了训练步数相关的预热与衰减策略。

### 五、实验数量与充分性

- **主要性能实验**：在两个广泛使用的基准（GenEval、T2I‑CompBench）上进行全面评估，对比了多种统一模型与显式 CoT 方法，覆盖较多相关前沿工作。
- **消融实验**：设计了 5 种变体，逐一控制查询容量、草图监督、查询分组、级联顺序和正则化等因素，较为系统。
- **分析实验**：包括结构查询可视化、随机扰动实验、跨注意力比例分析（不同去噪步数和层组）以及零样本重组实验，从多个角度验证了结构查询的功能和可解释性。
- **效率对比**：对 IV‑CoT 与三种显式 CoT 方法（T2I‑R1、GoT‑R1、TWIG‑RL）的推理时间进行了定量比较，并给出相对加速比。
- **总体评价**：实验数量较充足，设计层次清晰，消融与分析覆盖了主要贡献点，对比公平（同一训练数据和计算预算），能够有力支撑论文结论。

### 六、主要结论与发现

- IV‑CoT 在 **GenEval** 上整体得分从基线 0.86 提升至 0.88，在 **T2I‑CompBench** 上从 0.5448 提升至 0.5743，尤其在位置、颜色归属、空间关系、形状等结构敏感维度提升显著。
- **效率优势**：IV‑CoT 单次前向推理仅需 1.693 秒，而显式 CoT 方法如 T2I‑R1、GoT‑R1、TWIG‑RL 分别需 15.3、16.7、25.4 秒，加速比达 9‑15 倍。
- 结构查询成功编码了可恢复的**潜视觉规划**（通过草图域可视化），随机扰动会破坏布局和物体几何。
- 跨注意力分析显示**结构查询在轮廓和空间边界处分配更高注意力，语义查询则在物体内部和外观区域更活跃**，表明软性功能分工。
- 零样本重组实验表明，结构查询编码了可操控的布局信息，与另一提示的语义查询组合后能部分实现结构-外观分离。
- 消融结果证实，**结构监督和结构到语义级联是互补的**，缺一不可。

### 七、优点

- **新颖的隐式推理设计**：首次在 MLLM‑DiT 生成框架中将视觉规划完全保持在潜查询中，避免了显式文本或视觉中间状态带来的解码开销与误差累积。
- **高效的单轮推理**：推理时无需任何中间步骤，仅一次 MLLM 前向加标准扩散解码，显著优于现有的显式 CoT 生成方法。
- **训练策略巧妙**：利用训练专用的草图监督来注入结构先验，却不引入推理依赖，实现了“训练可监督、推理全隐式”的优雅设计。
- **可解释性与操控性**：通过可视化、扰动和跨注意力分析充分揭示了结构查询的功能，且提供了零样本重组等初步的可控性潜力。
- **扎实的消融对比**：通过系统的控制变量实验清晰地证明了各组件（查询级联、结构正则等）的独立贡献。

### 八、不足与局限

- **未覆盖文本渲染**：IV‑CoT 未专门优化图像内文字生成（如拼写准确、排版精细），草图监督无法提供文字级别的结构指导。
- **任务范围受限**：评估仅聚焦于文本到图像生成，未探索图像编辑（如局部编辑、指令驱动的修改、多轮细化），而这类任务同样对结构感知有需求。
- **重组可控性有限**：零样本结构-外观重组虽然有部分效果，但分离并非完全正交，产物的可控性和鲁棒性有待进一步提升。
- **算力细节缺失**：文中未明确给出训练总时长或总迭代步数，仅提供了优化器参数，难以精确复现训练开销。
- **方法依赖特定底层模型**：IV‑CoT 基于 OpenUni‑L 主干（MLLM + DiT），在更广泛的多模态生成架构上的泛化性尚待验证。

（完）
