---
title: Wasserstein Convergence of ODE-Based Samplers in Decentralized Diffusion Model via Velocity Field Decomposition
title_zh: 基于速度场分解的去中心化扩散模型中基于ODE的采样器的Wasserstein收敛性
authors: "Chencheng Tang, Xuanyu Xue, Fangyikang Wang, Chao Zhang, Hubery Yin"
date: 2026-06-14
pdf: "https://arxiv.org/pdf/2606.15835v1"
tags: ["query:real-ir"]
score: 9.0
evidence: 论文为去中心化扩散模型提供了收敛性保证。
tldr: "扩散模型去中心化架构通过多专家路由提升隐私与可扩展性，但随机专家切换使其收敛理论空白。本文研究基于ODE采样的去中心化扩散框架，提出速度场分解方法，首次证明离散分布以O(N^{-1/2}+ε)速率在Wasserstein-2距离下收敛至解析解，ε度量神经逼近误差。该结果填补了分布式扩散模型的理论缺口，为可靠生成奠定了重要基础。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15835v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1702, \"height\": 467, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15835v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1710, \"height\": 470, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15835v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1190, \"height\": 941, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.15835v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1293, \"height\": 311, \"label\": \"Table\"}]"
motivation: 去中心化扩散模型引入随机专家切换，标准扩散收敛分析失效，亟需建立其采样过程的收敛理论。
method: 通过速度场分解分析随机ODE采样动态，量化离散误差与神经网络逼近误差，导出Wasserstein-2距离界。
result: "证明N步离散分布收敛到真实解，Wasserstein-2速率达O(N^{-1/2}+ε)，ε为神经逼近误差。"
conclusion: 首次为去中心化ODE采样扩散模型提供Wasserstein-2收敛保证，为隐私可扩展开辟了理论支持。
---

## 摘要
扩散模型在生成任务中取得了令人印象深刻的经验性成功，其收敛理论现已相对清楚。受隐私和可扩展性驱动，最近的去中心化扩散架构用多个局部专家和一个路由机制取代单一的全局速度场，产生了具有随机专家切换的采样动力学，这超出了标准的扩散收敛分析。在本文中，我们研究了一个具有随机速度场和基于ODE的采样的去中心化扩散框架。我们在Wasserstein-2距离下建立了收敛保证，表明$N$步离散化的分布在$W_2$中以速率$\mathcal{O}(N^{-1/2}+\varepsilon)$收敛到解析解，其中$\varepsilon$捕获了神经近似误差。据我们所知，这是第一个针对采用基于ODE的采样方案的分散扩散模型的$W_2$收敛结果。

## Abstract
Diffusion models have achieved impressive empirical success in generative tasks, and their convergence theory is now relatively well understood. Motivated by privacy and scalability, recent decentralized diffusion architectures replace a single global velocity field with multiple local experts and a routing mechanism, yielding a sampling dynamics with stochastic expert switching that falls outside standard diffusion convergence analyses. In this work, We study a decentralized diffusion framework with stochastic velocity fields and ODE-based sampling. We establish a convergence guarantee in Wasserstein-2 distance, showing that the distribution of the $N$-step discretization converges to the analytical solution at rate $\mathcal{O}(N^{-1/2}+\varepsilon)$ in $W_2$, where $\varepsilon$ captures the neural approximation errors. To our knowledge, this is the first $W_2$ convergence result for decentralized diffusion models with an ODE-based sampling scheme.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：扩散模型在中心化训练下虽有成熟收敛理论，但去中心化、多专家架构（如 Decentralized Diffusion Models）引入随机路由与专家切换，使得全局速度场不再满足经典 ODE 收敛分析所依赖的平滑性假设。现有理论无法覆盖这种“混合专家+随机路由”的 ODE 采样过程。
- **整体含义**：本文旨在填补这一理论空白，首次为去中心化扩散模型的概率流 ODE（PF-ODE）采样建立严格的 Wasserstein-2 收敛保证，并量化神经网络近似误差与离散化误差对生成质量的影响，从而为隐私保护、可扩展的分布式扩散框架提供理论支撑。

### 2. 论文提出的方法论

- **核心思想**：将全局速度场分解为按后验概率加权的组件速度场之和：\(v(x,t) = \sum_{l=1}^L a_l(x,t) v_l(x,t)\)，其中 \(a_l\) 为样本属于第 \(l\) 个组件的后验概率。在采样时，采用随机路由从分类分布中抽取组件索引，并用对应组件神经网络预测的速度场进行 Euler 步进。
- **关键技术细节**：
  - **误差分解**：将单步全局误差 \(\hat{e}_{k+1}\) 拆分为四个部分：
    - *确定性空间传播误差*（由真实速度场 Lipschitz 连续控制）
    - *局部截断误差* \(T_k\)（由 Euler 格式引起，量级 \(O(\Delta\tau^2)\)）
    - *鞅差分噪声* \(\hat{M}_k\)（随机组件选择引入，条件期望为零）
    - *神经网络近似误差* \(E_{NN,k}\)（由组件后验概率网络和去噪网络的不完美拟合产生）
  - **数学推导**：利用速度场在有界支撑及截断时间域上的 Lipschitz 性、线性增长性及时序导数界，结合双重 Young 不等式和离散 Grönwall 不等式，得到以初始状态为条件的轨迹均方误差界：
    \[
    \sup_{0\le k\le N} \hat{E}_k \le S(T_\delta)\left[ C_{\text{approx}}(\hat{x}_0)\varepsilon^2 + C_{\text{disc}}(\hat{x}_0)\Delta\tau \right]
    \]
    再通过对标准高斯初始分布的矩积分，得到最终的 \(W_2\) 距离显式界，收敛速率 \(O(N^{-1/2} + \varepsilon)\)。
  - **算法流程**：在逆向时间 \(\tau\) 上，以步长 \(\Delta\tau = T_\delta/N\) 迭代：
    \[
    \hat{x}_{k+1} = \hat{x}_k + \Delta\tau \tilde{v}_{\hat{l}_k}^\theta(\hat{x}_k, \tau_k), \quad \hat{l}_k \sim \text{Categorical}(a^\phi(\hat{x}_k, \tau_k))
    \]

### 3. 实验设计

- **数据集/场景**：
  - CIFAR-10（60k 自然图像，10 类）
  - CelebA-HQ（30k 高分辨率人脸图像，256×256）
- ** Benchmark 与方法对比**：
  - DDIM（无条件基线）
  - “+ Naive”：直接类别预测，无时间导数网络
  - “+ MoG-a net”：仅用后验概率网络 \(a_\theta\)
  - “+ MoG-da net”（本文方法）：同时使用 \(a_\theta\) 和 \(\frac{da^\phi}{dt}\) 时间导数网络
- **评估指标**：FID 分数（不同采样步数 10/25/50/100），以及轨迹误差分析（与 1000 步 DDIM 参考轨迹的均方 L2 误差）

### 4. 资源与算力

- **硬件配置**：8 块 NVIDIA A800 GPU（每块 80 GB 显存）。
- **训练时长**：每个数据集约训练 2 天，共约 200k 优化步数。
- **优化器与参数**：Adam，学习率 \(\eta = 10^{-4}\)，批大小 128，线性调度器。

### 5. 实验数量与充分性

- **实验量**：
  - 2 个数据集 × 4 种方法 × 4 种采样步数，合计至少 32 组 FID 对比。
  - 额外包含轨迹误差幂律拟合实验（CIFAR‑10，7 种步数）。
  - 定性结果展示生成样本图。
- **充分性与客观性**：
  - 对比基线涵盖无条件 DDIM 及两种消融变体，可分析各组件贡献。
  - 轨迹误差拟合 \(R^2=0.999963\)，印证理论预测的加速收敛。
  - 但仅使用两个中等规模数据集，未在更大规模或更多模态上验证；消融实验种类有限，缺乏对截断时间 \(\delta\)、组件数 \(L\) 等关键参数的敏感性分析。

### 6. 论文的主要结论与发现

- 在速度场分解框架下，首次证明去中心化 ODE 采样离散分布的 \(W_2\) 收敛速率为 \(O(N^{-1/2}+\varepsilon)\)，\(\varepsilon\) 为神经网络一致逼近误差。
- 理论界明确揭示误差对数据维度 \(d\)、支撑半径 \(R\)、混合组件数 \(L\) 及截断噪声水平 \(\sigma_\delta\) 的依赖。
- 实验表明，引入时间导数正则化（学习 \(\frac{da}{dt}\)）能在少步采样中提升分类引导与生成质量，且轨迹误差呈超线性衰减（幂指数约 1.29），优于标准一阶数值方法。

### 7. 优点

- **理论贡献突出**：填补了分布式扩散模型中 ODE 采样收敛理论的空白，证明过程严谨，误差分解清晰。
- **实际指导性强**：显式给出各常数对参数 \(d, R, L, \sigma_\delta\) 的依赖，为加密、分布式学习中的采样设计提供定量基线。
- **方法创新**：将随机路由建模为鞅差分噪声，成功将确定性 ODE 分析技术与随机近似融合。
- **实验验证方向正确**：高阶训练目标（时间导数）带来的加速收敛和 FID 改善与理论预测一致。

### 8. 不足与局限

- **假设较为严格**：要求初始分布有界支撑，且神经网络近似误差需满足 \(L^\infty\) 界，而实际训练多为 \(L^2\) 损失，理论与实践存在间阂。
- **时间截断依赖**：收敛速率隐含通过 \(\delta\) 对 \(1/\sigma_\delta\) 的依赖，当 \(\delta \to 0\) 时界爆炸，未根本解决数据流形附近的奇异问题。
- **实验覆盖不足**：仅在 CIFAR-10、CelebA-HQ 上评估，未测试更大规模或更复杂的多模态数据集；缺少对关键超参数（如 \(L\)、\(\delta\)、网络容量）的系统消融研究。
- **理论依赖 \(L^\infty\) 误差**：未能直接衔接分数匹配等常用的 \(L^2\) 损失，限制了在大网络、大数据下的直接适用性。
- **算力需求较高**：8 块 A800、2 天训练，对复现和推广有一定门槛。

（完）
