---
title: "Mean Velocity Matching: Rethinking Generative Dynamics in Diffusion Models"
title_zh: 均值速度匹配：重新思考扩散模型中的生成动力学
authors: "Yunhong Zhang, Changjie Cao, Zhihua Zhang, Bingli Liu, Zongjie Cao, Zongyong Cui, Ying Yang"
date: 2026-09-21
pdf: "https://arxiv.org/pdf/2609.25444v1"
tags: ["query:real-ir"]
score: 8.0
evidence: 随机扩散生成动力学的预测参数化
tldr: 扩散模型的 velocity 参数化虽可单场预测，但通常只支持确定性动力学，随机扩展需额外 score 信息。为此提出 Mean Velocity Matching（MVM），构造高斯扰动过程，使恢复导向速度的条件期望直接成为反向 SDE 漂移，从而用单一学习场参数化随机反向过程。针对 t→0 无界问题，引入 sqrt(t) 缩放参数化，并统一导出概率流 ODE。实验在 ImageNet 32×32 与 256×256 上取得有竞争力 FID，并揭示低 NFE 下 ODE 更优、充足 NFE 下随机过程 FID 更低。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.25444v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1035, \"height\": 472, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.25444v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 598, \"height\": 412, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.25444v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1239, \"height\": 1852, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.25444v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1232, \"height\": 1849, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.25444v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 638, \"height\": 636, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.25444v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 562, \"height\": 637, \"label\": \"Table\"}]"
motivation: 现有 velocity 生成模型虽单场简洁，但标准形式是确定性的；随机扩展需额外估计或重建 score，增加复杂度。
method: MVM 构造高斯扰动，使 (x0-x_t)/t 的条件期望直接构成反向 SDE 漂移，并用 sqrt(t) 缩放保证训练目标有界，同时导出概率流 ODE。
result: ImageNet 32×32 与 256×256 上取得有竞争力 FID；SDE-ODE 对比显示低 NFE 时 ODE 更优，NFE 充足时随机反向过程 FID 更低。
conclusion: MVM 为随机反向动力学提供直接的单场参数化，统一随机与确定性采样，并保持竞争性生成质量。
---

## 摘要
本文研究扩散模型中随机生成动力学的预测参数化。现有基于速度的生成模型具有学习单一传输场的简洁性，但其标准形式是确定性的，而随机扩展通常需要额外的分数信息或中间的速度到分数重构。为了在直接支持随机反向动力学的同时保留单场预测，本文提出均值速度匹配（Mean Velocity Matching, MVM）。MVM 构造了一个高斯扰动过程，其中面向恢复的速度 $(x_0-x_t)/t$ 的条件期望直接构成反向 SDE 的漂移项。因此，仅需一个学习到的场即可参数化随机反向过程，而无需单独估计或重构分数。由于该速度的直接回归在 $t=0$ 附近会变得无界，MVM 进一步引入 $\sqrt{t}$ 缩放的参数化，其在保持反向动力学的同时产生有界的训练目标。同一个学习到的场还诱导出一个确定性的概率流 ODE，使得随机采样和确定性采样可以在统一形式下进行研究。基于 Transformer 的生成模型实验在 ImageNet $32\times32$ 上以 \MVMImageNetThirtyTwoNFE\ NFE 达到 FID 为 $\MVMImageNetThirtyTwoFID$，并在 ImageNet $256\times256$ 上以 \MVMImageNetTwoFiftySixNFE\ NFE 达到 $\MVMImageNetTwoFiftySixFID$。受控的 SDE--ODE 对比进一步表明，在极低 NFE 下 ODE 表现更好，而当有足够的函数评估时，随机反向过程能达到更低的 FID。这些结果表明，MVM 为随机反向动力学提供了一种直接的单场参数化，同时保持了具有竞争力的生成质量。

## Abstract
This work studies prediction parameterization for stochastic generative dynamics in diffusion models. Existing velocity-based generative models provide the simplicity of learning a single transport field, but their standard formulation is deterministic, whereas stochastic extensions generally require additional score information or an intermediate velocity-to-score reconstruction. To retain single-field prediction while directly supporting stochastic reverse dynamics, this paper introduces Mean Velocity Matching (MVM). MVM constructs a Gaussian perturbation process for which the conditional expectation of a restoration-oriented velocity, $(x_0-x_t)/t$, directly forms the reverse-SDE drift. Consequently, a single learned field is sufficient to parameterize the stochastic reverse process without separately estimating or reconstructing the score. Because direct regression of this velocity becomes unbounded near $t=0$, MVM further introduces a $\sqrt{t}$-scaled parameterization that preserves the reverse dynamics while yielding a bounded training target. The same learned field also induces a deterministic probability-flow ODE, enabling stochastic and deterministic sampling to be studied within a unified formulation. Experiments with Transformer-based generative models achieve an FID of $\MVMImageNetThirtyTwoFID$ at \MVMImageNetThirtyTwoNFE\ NFE on ImageNet $32\times32$ and $\MVMImageNetTwoFiftySixFID$ at \MVMImageNetTwoFiftySixNFE\ NFE on ImageNet $256\times256$. Controlled SDE--ODE comparisons further show that the ODE performs better under very low NFE, whereas the stochastic reverse process achieves lower FID when sufficient function evaluations are available. These results demonstrate that MVM provides a direct single-field parameterization of stochastic reverse dynamics while maintaining competitive generation quality.

---

## 论文详细总结（自动生成）

> 说明：以下总结严格基于所给论文提取文本（元数据与摘要）。正文、公式细节、实验表格与具体数值未完整提供；涉及“未提及”处均表示提取文本中无法确认。

# 论文总结：Mean Velocity Matching

## 1. 核心问题与整体含义
- **研究背景**：扩散模型中，基于速度（velocity）的生成模型具有“学习单一传输场”的简洁性，但其标准形式通常是确定性的。
- **核心问题**：若要将速度参数化扩展到随机反向动力学，通常需要额外估计 score，或进行 velocity-to-score 重构，增加了模型与训练复杂度。
- **整体含义**：论文提出 **Mean Velocity Matching（MVM，均值速度匹配）**，目标是在保留单场预测简洁性的同时，直接支持随机反向 SDE，并统一随机采样与确定性采样。

## 2. 方法论
- **核心思想**：构造一个高斯扰动过程，使面向恢复的速度  
  \[
  \frac{x_0-x_t}{t}
  \]
  的条件期望直接构成反向 SDE 的漂移项。
- **关键结果**：因此只需学习一个场，即可参数化随机反向过程，无需单独估计或重构 score。
- **训练稳定性处理**：直接回归该速度在 \(t=0\) 附近会变得无界；MVM 引入 \(\sqrt{t}\) 缩放参数化，在保持反向动力学的同时，使训练目标有界。
- **统一采样框架**：同一个学习到的场还可诱导出确定性的概率流 ODE，从而将随机采样和确定性采样放在统一形式下研究。
- **算法流程（文字概述）**：
  1. 构造高斯扰动过程；
  2. 定义恢复导向速度 \((x_0-x_t)/t\)；
  3. 以条件期望作为反向 SDE 漂移；
  4. 使用 \(\sqrt{t}\) 缩放构造有界训练目标；
  5. 训练单一网络学习该均值速度场；
  6. 采样时既可用随机反向 SDE，也可用概率流 ODE。

## 3. 实验设计
- **数据集 / 场景**：
  - ImageNet \(32\times32\)
  - ImageNet \(256\times256\)
- **模型类型**：基于 Transformer 的生成模型。
- **评价指标**：FID、NFE（函数评估次数）。
- **Benchmark**：ImageNet 图像生成；摘要称取得有竞争力的 FID。
- **对比方式**：
  - 主要进行受控的 SDE–ODE 对比；
  - 比较低 NFE 与充足 NFE 下随机反向过程与确定性 ODE 的表现。
- **具体基线**：摘要未列出具体外部对比方法名称，因此无法确认与哪些模型逐项比较。
- **具体数值**：提取文本中的 FID 与 NFE 数值以占位符形式出现（如 `\MVMImageNetThirtyTwoFID`、`\MVMImageNetTwoFiftySixNFE`），未能还原实际数字。

## 4. 资源与算力
- 提取文本**未提及** GPU 型号、数量、训练时长、训练步数、模型参数量或总体算力预算。
- 因此无法对资源消耗与可复现成本进行总结。

## 5. 实验数量与充分性
- **可见实验数量**：
  - ImageNet \(32\times32\) 生成实验；
  - ImageNet \(256\times256\) 生成实验；
  - SDE 与 ODE 的受控对比，可能覆盖不同 NFE 设置。
- **充分性判断**：
  - 从摘要看，实验覆盖了两个常见 ImageNet 分辨率，并考察了随机/确定性采样的权衡。
  - 但缺少消融实验、基线列表、数据划分、重复实验、模型规模等细节，无法全面判断实验充分性。
- **客观与公平性**：
  - SDE–ODE 在同一学习场下比较，内部对比具有一定受控性；
  - 外部方法对比、超参公平性和计算预算公平性因信息不足而无法评估。

## 6. 主要结论与发现
- MVM 为随机反向动力学提供了一种**直接的单场参数化**，无需额外 score 估计或速度到 score 的重构。
- 同一学习场可统一支持随机反向 SDE 与确定性概率流 ODE。
- 在 ImageNet \(32\times32\) 与 \(256\times256\) 上取得有竞争力的 FID。
- 采样权衡明显：
  - 在**极低 NFE** 下，ODE 表现更好；
  - 当**NFE 充足**时，随机反向过程能达到更低 FID。

## 7. 优点
- **方法简洁**：保留单场预测，避免额外 score 网络或中间重构。
- **统一性强**：随机 SDE 与确定性 ODE 可由同一学习场导出，便于比较与选择采样策略。
- **训练稳定性设计**：\(\sqrt{t}\) 缩放缓解 \(t=0\) 附近速度无界问题。
- **实验洞察实用**：明确揭示 NFE 预算下 SDE 与 ODE 的取舍，对采样器设计有参考价值。
- **生成质量有竞争力**：在 ImageNet 两个分辨率上报告了有竞争力的 FID。

## 8. 不足与局限
- **信息完整度有限**：所给文本仅为元数据与摘要，缺少方法推导、算法伪代码、实验细节和完整结果。
- **具体数值缺失**：FID 与 NFE 在提取文本中为占位符，无法核验实际性能。
- **实验覆盖有限**：仅见 ImageNet \(32\times32\) 与 \(256\times256\)，未涉及文本、视频、条件生成或其他模态任务。
- **公平性难以判断**：未报告具体

的基线配置、超参数、训练预算与评估协议；因此无法判断其与外部方法比较是否在同等条件下完成。

- **关键消融缺失**：摘要未体现对 \(\sqrt{t}\) 缩放、扰动过程设计、SDE/ODE 切换方式、噪声调度等关键组件的消融实验，难以判断各设计选择的独立贡献。
- **理论推导与假设未呈现**：虽然核心主张是条件期望速度可直接作为反向 SDE 漂移，但提取文本未给出推导、正则性假设、边界条件或误差分析；尤其 \(t\to 0\) 附近的无界问题仅有缩放描述，实际数值行为仍需正文验证。
- **采样细节不足**：随机反向 SDE 的具体离散化方式、扩散系数/方差调度、NFE 统计口径、ODE 求解器与步长策略均未在摘要中说明，影响复现与跨方法比较。
- **可复现性信息不足**：未提及代码、模型权重、数据预处理、评价脚本或训练配置；FID/NFE 又以占位符形式出现，当前无法独立核验结果。
- **应用范围与影响未讨论**：实验仅见于图像生成，未覆盖文本、视频、三维、科学数据等模态；同时未见对生成模型滥用、偏见、版权或安全风险的讨论。

## 9. 总体评价与后续关注点
- **方法定位**：若正文支持摘要主张，MVM 的主要价值在于把 velocity matching 从确定性生成扩展到随机反向动力学，同时保持“单场预测”的简洁训练形式，减少额外 score 估计或重构带来的复杂度。
- **证据强度**：摘要层面可支持“在 ImageNet \(32\times32\) 与 \(256\times256\) 上有竞争力”以及“低 NFE 下 ODE 更优、充足 NFE 下随机 SDE 更优”的结论；但由于缺少完整数值、基线和消融，尚不足以判断其是否全面优于现有扩散/流匹配方法。
- **最值得核验的后续信息**：完整 FID–NFE 曲线、与 score-based / flow matching / 其他 velocity 参数化方法的逐项对比、\(\sqrt{t}\) 缩放消融、训练稳定性实验、理论推导与采样器伪代码。
- **适用读者与场景**：适合关注扩散模型、流匹配、随机反向 SDE、概率流 ODE 以及低 NFE 采样器的研究者；对需要统一随机/确定性采样接口的生成建模工作有潜在参考价值。
- **一句话结论**：MVM 在摘要层面提出了一个简洁且有吸引力的单场随机生成框架，但其实际效果、理论完备性与公平比较仍需依赖完整正文和可复现实验来确认。

（完）
