---
title: "STAR: Structure-aware Test-time Adaptation for diffusion-based light field Reconstruction"
title_zh: STAR：用于基于扩散的光场重建的结构感知测试时自适应
authors: "Wontae Choi, Ki Ryum Moon, Jae Young Lee, Hyung Sup Yun, Il Yong Chun"
date: 2026-09-17
pdf: "https://arxiv.org/pdf/2609.19747v1"
tags: ["query:real-ir"]
score: 8.0
evidence: 扩散模型结合测试时自适应的逆问题重建
tldr: 从有限且含噪的焦栈重建光场是高度病态逆问题。虽然给定光学装置的LF-to-FS成像几何固定，但各场景的光场空间-角度结构（视内细节、跨视角依赖与视差）差异显著，固定预训练先验难以适配每个测试光场。STAR首次将测试时适应引入扩散式光场重建：冻结预训练扩散先验，仅针对观测焦栈拟合三个轻量适配器，联合调整空间-角度结构的三类成分。在双焦面与三焦面设置下，STAR均优于现有最先进方法，且推理时间短于需测试时更新参数的方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.19747v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 887, \"height\": 561, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.19747v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 838, \"height\": 737, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.19747v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 900, \"height\": 730, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.19747v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 820, \"height\": 426, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.19747v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 849, \"height\": 537, \"label\": \"Table\"}]"
motivation: 固定预训练先验难适应各测试光场不同的空间-角度结构，而有限含噪焦栈使重建高度病态。
method: STAR冻结预训练扩散先验，仅用观测焦栈拟合三个轻量适配器，联合适应视内细节、跨视角依赖和视差。
result: 在双焦面和三焦面设置下，STAR均超越现有最先进方法，且推理时间短于需测试时更新参数的方法。
conclusion: 作为首个面向焦栈到光场重建的测试时适应框架，STAR验证了结构感知适配对扩散先验的有效性。
---

## 摘要
从有限且含噪的焦栈（FS）测量中重建光场（LF）是一个高度不适定的逆问题。尽管对于给定光学装置，LF到FS的成像几何是固定的，但LF的空间-角度结构——包括视图内空间细节、跨视角角度依赖以及视图间视差——会随场景而变化。因此，固定的预训练先验可能无法最优地捕捉每个测试LF的空间-角度结构。我们提出用于基于扩散的光场重建的结构感知测试时自适应（STAR），这是首个从FS重建LF的测试时自适应框架。对于每个测试LF，STAR冻结预训练的扩散先验，并针对观测到的FS拟合三个轻量级适配器，以联合自适应LF空间-角度结构的三个组成部分。在双焦平面片和三焦平面片设置下，STAR均优于现有最先进方法，且推理时间短于那些进行测试时参数更新的方法。

## Abstract
Light field (LF) reconstruction from limited and noisy focal stack (FS) measurements is a highly ill-posed inverse problem. Although the LF-to-FS imaging geometry is fixed for a given optical setup, LF spatial-angular structure---including within-view spatial details, cross-view angular dependencies, and disparity across views---varies across scenes. Consequently, a fixed pre-trained prior may not optimally capture the spatial-angular structure of each test LF. We propose Structure-aware Test-time Adaptation for diffusion-based light field Reconstruction (STAR), the first test-time adaptation framework for reconstructing an LF from FS. For each test LF, STAR freezes a pre-trained diffusion prior and fits three lightweight adapters to the observed FS to jointly adapt the three components of the LF's spatial-angular structure. STAR outperforms existing state-of-the-art methods in both two- and three-focal-sheet settings, with shorter inference times than those with test-time parameter updates.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义

- 光场（LF）是包含两个空间维度和两个角度维度的 4D 信号，可用于数字重聚焦、深度估计、新视角合成等任务。
- 从有限且含噪的焦栈（FS）测量中重建 LF 是高度病态的逆问题：系统矩阵高度欠定，测量数量远少于待恢复 LF 维度。
- 给定光学装置后，LF-to-FS 成像几何固定；但每个测试场景的 LF 空间-角度结构会变化，包括：
  - 视图内空间细节；
  - 跨视角角度依赖；
  - 视图间视差。
- 因此，固定预训练先验可能无法最优适配每个测试 LF。已有扩散类方法多保持参数固定，或进行昂贵的测试时参数优化，但未显式针对 LF 的空间-角度结构进行适配。
- 论文提出 STAR：首个面向“从 FS 重建 LF”的测试时自适应框架。核心含义是：冻结预训练扩散先验，仅用观测 FS 拟合轻量适配器，从而在保留共享知识的同时适配场景相关结构。

### 2. 方法论

- **整体框架**：
  - 使用预训练 LF-HDiT 作为扩散先验。LF-HDiT 在 HDiT 基础上加入专门的 angular attention，并在 LF 数据上预训练。
  - 测试时冻结 LF-HDiT 主干，仅更新三个轻量适配器。
  - 每个反向扩散步中，LF-HDiT 从 \(x_t\) 预测干净 LF 估计 \(\hat{x}_{0|t}\)，DDRM 再利用 FS 测量 \(y\) 做测量一致性更新，得到 \(x_{t-1}\)。

- **三个结构感知适配器**：
  - **LF-CA：空间适配器**
    - 并行加在 spatial attention 的 Q、K、V 投影上。
    - 不是单纯 token-wise LoRA，而是低秩卷积分支：先将 patch token 还原为空间张量，经 \(P\times P\) 卷积聚合邻域并将通道降到 \(R\)，再经 \(1\times1\) 卷积升回 \(D\)。
    - 形式上等效于一个 \(P\times P\) 卷积层，其展开核矩阵秩不超过 \(R\)，参数量从 \(D^2P^2\) 降至 \(R(DP^2+D)\)。
  - **LoRA：角度适配器**
    - 并行加在 angular attention 的 Q、K、V 投影上。
    - 在每个空间 token 位置，跨视角 token 通过低秩矩阵更新，适配跨视角角度依赖。
  - **LF-MA：多域适配器**
    - 并行加在 MLP 上。
    - 将输入重排为 MacPI 特征网格，并行提取四类特征：
      - 空间特征；
      - 角度特征；
      - 水平 EPI 特征；
      - 垂直 EPI 特征。
    - EPI 特征可显式建模视差结构；四类特征拼接后融合，输出加到冻结 MLP 输出上。

- **DDRM 测量一致性**：
  - 采用 DDRM 作为扩散逆问题求解器。
  - 由于系统矩阵 \(A\) 尺寸巨大，直接 SVD 不可行；论文沿用可处理分解：在周期边界条件下，各子矩阵块循环，可被 2D DFT 对角化，并通过排列将完整 SVD 化为 \(N\) 个独立的 \(C\times S\) 小矩阵 SVD。
  - 该分解用于高效计算 DDRM 中的谱域变换与测量更新。

- **自监督测试时适配**：
  - 测试时只有含噪 FS 测量 \(y\)，没有干净 LF。
  - 使用自监督目标：  
    \[
    \mathcal{L}=\sum_{t=1}^{T}\left\|x_{t-1}-\sqrt{\bar{\alpha}_{t-1}}\hat{x}_{0|t}\right\|_2^2
    \]
  - DDRM 生成的 \(x_{t-1}\) 作为固定
