---
title: Scale-Consistent Posterior Dynamics for Diffusion Inverse Problems
title_zh: 尺度一致的后验动力学用于扩散逆问题
authors: "Zhaoqiang Liu, Tongyao Pang, Ruibing Wang, Yang Zheng"
date: 2026-08-15
pdf: "https://arxiv.org/pdf/2608.15144v1"
tags: ["query:real-ir"]
score: 9.0
evidence: 直接针对图像修复与超分辨率的扩散逆问题方法
tldr: 扩散逆问题后验采样受制于难解的中间似然条件得分。本文提出尺度一致的后验动力学，从理想单参数后验SDE族出发，利用重缩放坐标和对数信噪比组织似然代理，并注入冻结目标Langevin校正器。采用Lie-Trotter分裂和IMEX预测-校正离散化，在FFHQ/ImageNet上以100次评分实现高保真重建。理论证明边缘不变性、收敛性和弱误差界，消融实验验证尺度一致性与创新注入位置的关键作用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.15144v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1609, \"height\": 611, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.15144v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1599, \"height\": 485, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.15144v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1532, \"height\": 514, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.15144v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1616, \"height\": 613, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.15144v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1606, \"height\": 926, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.15144v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1470, \"height\": 692, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.15144v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1394, \"height\": 354, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.15144v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1152, \"height\": 331, \"label\": \"Table\"}]"
motivation: 扩散逆问题的后验采样中，条件似然得分通常不可解，导致现有方法缺乏理论保证和尺度一致性。
method: 构建单参数后验SDE族，用重缩放坐标和对数信噪比设计似然代理，结合Langevin校正器和IMEX预测-校正离散化。
result: FFHQ/ImageNet超分辨率和去模糊任务上100次评分重建保真度高；消融证实尺度一致性和创新注入位置的关键性。
conclusion: 给出可证明收敛的扩散逆问题求解框架，兼顾理论严谨与实用效率。
---

## 摘要
以预训练扩散先验进行后验采样受条件分数控制，其中间似然分量通常难以处理。我们从理想的一参数后验SDE族出发，其中随机性参数控制概率流输运和随机探索，而不改变后验边缘分布。为获得可处理的模型，我们在重标定的干净图像坐标中表达似然，并使用对数信噪比来组织所得的后验代理。通过前向算子投影扩散不确定性，产生噪声条件协方差路径，其目标逼近干净后验。由于这些目标的端点一致性并不能确保替代输运会跟随它们，我们将输运与冻结目标的Langevin校正器交错，生成连续的替代SDE。我们用外部Lie--Trotter分裂和方差匹配的裂步IMEX预测器对该模型离散化，其中显式处理学习先验，隐式处理线性似然，并在隐式求解后处理随机创新。我们证明了理想族的边际不变性、连续替代在混合和输运缺陷条件下的后验收敛性，以及离散算法的一阶弱误差界。在FFHQ和ImageNet上使用100次分数评估的实验表明，超分辨率和去模糊的重建保真度具有竞争力。一项受控的100图像消融研究将尺度一致性与随机增量放置、延续和校正器分配的有限步效应区分开来。一项单独的无噪声框内修复研究表明，仅当匹配的创新在刚性似然求解后注入时，大规模探索才达到性能平台。

## Abstract
Posterior sampling with a pretrained diffusion prior is governed by a conditional score whose intermediate likelihood component is generally intractable. We begin from an ideal one-parameter posterior SDE family in which a stochasticity parameter controls probability-flow transport and stochastic exploration without changing the posterior marginals. To obtain a tractable model, we express the likelihood in a rescaled clean-image coordinate and use log-SNR to organize the resulting posterior proxies. Projecting the diffusion uncertainty through the forward operator then yields a noise-conditioned covariance path whose targets approach the clean posterior. Because endpoint consistency of these targets does not ensure that a surrogate transport follows them, we interleave the transport with a frozen-target Langevin corrector, producing a continuous surrogate SDE. We discretize this model with an outer Lie--Trotter splitting and a variance-matched split-step IMEX predictor that treats the learned prior explicitly, the linear likelihood implicitly, and the stochastic innovation after the implicit solve. We prove marginal invariance of the ideal family, posterior convergence of the continuous surrogate under mixing and transport-defect conditions, and a first-order weak error bound for the discrete algorithm. Experiments on FFHQ and ImageNet with 100 score evaluations demonstrate competitive reconstruction fidelity for super-resolution and deblurring. A controlled 100-image ablation separates scale consistency from the finite-step effects of stochastic-increment placement, continuation, and corrector allocation. A separate noiseless box-inpainting study shows that large exploration reaches a performance plateau only when the matched innovation is injected after the stiff likelihood solve.

---

## 论文详细总结（自动生成）

## 论文详细总结

### 1. 核心问题与整体含义（研究动机和背景）

- **研究问题**：扩散逆问题（如去模糊、超分辨率、断层摄影、加速MRI等）中，给定线性测量 \(y = Ax + n\)，需要从后验分布 \(p(x|y)\) 采样。预训练扩散模型可以作为可复用的图像先验，但后验采样的关键障碍是**中间似然得分（conditional score）不可解**——即 \(p_t(y|x_t) = \int p(y|x_0)p(x_0|x_t)dx_0\) 需要边际化不可得的后验转移核，导致似然引导项无法精确计算。
- **现有方法的不足**：已有的DPS、DDRM、DiffPIR等方法多依赖去噪点估计或算子特定分解，在**尺度一致性、目标路径的连续性、以及数值稳定性**上缺乏理论保证，容易出现引导梯度被过度放大或抑制的问题。
- **本文的意义**：构建了一个具有理论保证的预测-校正框架，从**理想的一参数后验SDE族**出发，逐步构造可计算、可证明收敛的离散采样算法，将目标一致性、采样器跟踪能力和离散化误差三个层面明确分离。

### 2. 方法论：核心思想、关键技术细节与算法流程

#### 2.1 核心思想
- **理想后验SDE族**：在假设条件得分已知的前提下，构造单参数后验反向SDE族（式3.7），随机性参数 \(\zeta\) 在**不改变后验边缘分布**的前提下控制概率流输运与随机探索之间的平衡（Proposition 3.1）。
- **尺度一致（Scale Consistency）** ：将干净似然与学习先验统一在重标定坐标 \(z = x_t/\sqrt{\alpha_t}\) 中（式3.8–3.9），避免扩散时间尺度失配。
- **噪声条件协方差路径（Continuation Path）** ：考虑扩散不确定性经前向算子投影的影响，构造 \(\Gamma_\lambda = \sigma_d^2 I + s_\lambda AA^\top\)（式3.14），在低信噪比时软化不可靠测量方向，在高信噪比时恢复原始似然，形成目标密度路径（式3.16）。
- **冻结目标Langevin校正器**：由于目标路径一致不代表采样器跟踪该路径，额外注入针对当前目标的Langevin校正器（式3.20），形成连续替代SDE（式3.21）。

#### 2.2 关键技术细节
- **方差匹配裂步IMEX预测器**（式3.26）：
  - 似然项用隐式后向欧拉求解（\(R_k = (I + \omega_k H_{k+1})^{-1}\)），学习先验显式处理；
  - 随机创新在隐式求解**之后**注入，方差 \(q^{vm}_k = \tau^2_{k+1}(1 - e^{-2\zeta h_k})\) 与冻结去噪器先验流的条件方差匹配（Proposition 3.2），避免被似然解析因子衰减。
- **Crank–Nicolson校正器**（式3.28）：对似然采用中心化隐式处理，保持固定水平似然高斯动力学的协方差，降低不变律偏差。
- **外层Lie–Trotter分裂**：先预测输运到下一噪声水平，再对右端点冻结目标执行校正（式3.24）。

#### 2.3 算法流程（Algorithm 3.1）
1. 计算 \(\alpha_{\lambda_k}\)、\(h_k\) 等网格参数；
2. 从高斯噪声初始化 \(z_0\)；
3. 对每个 log-SNR 层级 \(k = 0, \dots, K-1\)：
   - 计算 \(\Gamma_{\lambda_{k+1}}\)、\(H_{\lambda_{k+1}}\)、\(b_{\lambda_{k+1}}\)；
   - 执行 IMEX 预测器（式3.26）；
   - 执行 \(M_{k+1}\) 步 Crank–Nicolson 校正器（式3.28）；
4. 终端去噪得到近似后验样本 \(\hat{x}_0\)。

#### 2.4 理论结果
- **Theorem 3.3（后验收敛性）**：在 log-Sobolev 不等式和输运缺陷条件（式3.34）下，连续替代动力学以 KL 散度指数收敛到目标后验。
- **Theorem 3.4（一阶弱误差界）**：离散算法在常规光滑性条件下的弱误差为 \(O(h + \eta_* + \delta_{bud})\)。

### 3. 实验设计

- **数据集**：FFHQ 256×256（100张测试图，预训练先验来自DPS）、ImageNet 256×256（100张测试图，预训练先验来自Dhariwal & Nichol的unconditional prior）。
- **任务与设置**：
  - 四倍双三次超分辨率（SR）；
  - 高斯去模糊（61×61核，标准差3.0）；
  - 运动去模糊（61×61核，强度0.5）；
  - 独立高斯测量噪声 \(\sigma_d = 0.05\)。
- **评估指标**：PSNR、SSIM、LPIPS（较高PSNR/SSIM和较低LPIPS为更优）。
- **对比方法**：DPS、DiffPIR、DDRM、RED-Diff、MGPS、DAPS，均在相同预训练先验和测量设置下比较。
- **预算**：本文方法固定100次评分网络评估（NFEs），包含预测器、校正器和终端去噪调用；对比方法按各自释放配置报告外层级与NFE。

### 4. 资源与算力

- 论文文本**未明确说明**实验中使用的GPU型号、数量、训练时长或推理时间等算力资源信息，也没有报告单个任务的运行时间或内存占用。

### 5. 实验数量与充分性

- **实验组数**：
  - 主实验：FFHQ和ImageNet上各3类任务 × 7种方法（含本文方法），即42个指标表项；
  - 受控100图消融（表2）：在FFHQ高斯去模糊和ImageNet运动去模糊两个代表性任务上，覆盖SC（尺度一致）、VM（方差匹配创新注入）、NC（噪声条件延续）、PC（Langevin校正器）4个组件的5组配置；
  - 随机性（EX）扫描：3个任务面板（盒修复、FFHQ高斯去模糊、ImageNet运动去模糊），变化 \(\zeta \in \{0, 0.01, \dots, 256\}\)，同时报告PSNR和LPIPS；
  - 无噪声盒修复压力测试（表3）：DPS、DiffPIR、DDRM、RED-Diff、MGPS、DAPS和本文方法，在100张FFHQ图上比较缺失区域PSNR/SSIM/LPIPS和边界梯度误差（BGE）；
  - 网格细化测试：25至400 NFE区间，配对随机数下对比VM-IMEX与直接后向欧拉；
  - 可视化对比：图1（跨方法重建）、图4（盒修复重建）、图5（随机性影响）。
- **充分性评价**：
  - **优点**：消融设计严格控制单变量变化（SC0→SC、VM0→VM、VM→NC、NC→PC），能够逐层分离组件贡献；无噪声盒修复实验特意用于暴露隐式似然求解对创新的滤波效应；随机性扫描覆盖了从确定性输运到强探索的完整区间。
  - **不足**：未覆盖更多真实医学成像任务（断层摄影、MRI等）；LPIPS指标上本文并未全域最优（如DDRM在某些LPIPS上更优），说明失真-感知权衡的分析有待更系统的刻画；无噪声场景仅测试了盒修复单一掩膜类型。

### 6. 主要结论与发现

- **尺度一致是主导性结构要素**：SC使FFHQ高斯去模糊PSNR提升10.001 dB，ImageNet运动去模糊提升13.776 dB。
- **噪声条件延续高度依赖任务**：NC在ImageNet运动去模糊上贡献2.856 dB，但在FFHQ高斯去模糊上仅0.021 dB，说明延续的作用是有限预算下的条件化工具而非普适增益。
- **Langevin校正器在固定预算下出现权衡**：PC使ImageNet LPIPS从.1968降至.1802，但以0.179 dB PSNR损失为代价。
- **方差匹配创新（VM）在低\(\zeta\)加噪任务上影响较小**，但在无噪声盒修复中起决定性作用：\(\zeta=256\)时VM-IMEX得到19.410 dB缺失区PSNR，直接后向欧拉仅6.527 dB，且网格细化不能弥补这一差距。
- **随机性\(\zeta\)存在非单调的有限步效应**：盒修复中 \(2 \le \zeta \le 12\) 为部分刷新区间，\(\zeta > 64\) 后进入性能平台期；在去噪任务上，最佳\(\zeta\)取值很小且有任务特异性。
- 总体而言，本文方法在多数据集的PSNR/SSIM上表现领先，尤其运动去模糊效果突出，但LPIPS上与DDRM等仍互有胜负。

### 7. 优点

- **理论严密**：从精确后验SDE族出发，逐层建立目标收敛（TV/得分一致性）、连续采样的后验收敛（Theorem 3.3）及离散弱误差界（Theorem 3.4），使模型误差与数值误差有清晰边界。
- **结构清晰**：将尺度一致、延续路径、Langevin校正、方差匹配IMEX四个机制各自定位，消融实验精确验证了每个机制的作用。
- **算法设计巧妙**：将随机创新放在隐式似然求解之后，既保持确定性方向的数值稳定性，又防止解析因子衰减随机探索方差（Proposition 3.2），并以直接后向欧拉为对照给出明确的分布性差异证据。
- **实验控制严谨**：100图固定种子、配对随机数比较、100-NFE统一预算、明确报告MGPS/DAPS实际评分调用数，保证了比较的公平性。

### 8. 不足与局限

- **计算资源信息缺失**：未报告GPU型号、数量、训练/推理时间，实际部署成本不透明。
- **实验覆盖有限**：未涉及MRI、断层摄影等更广泛的线性逆问题；无噪声实验仅覆盖单一盒状掩膜。
- **LPIPS并非最优**：在多个FFHQ任务上DDRM保持最佳LPIPS，本文方法主要偏向失真优化，感知质量的全面优势尚未确立。
- **学术分误差未纳入定量分析**：定理3.4假设精确得分，学习得分的误差被单独标注但不量化，实际模型与理论的可控差距未做敏感性分析。
- **校正器机制的多模态局限**：论文承认局部Langevin校正器无法从根本上解决多模态混合瓶颈，但其衰减机制（\(\rho_\lambda\) 随目标变化的实际值）未被定量刻画。
- **延续路径的通用性受限**：\(s(\tau)\) 的具体形式和 \(\tau_c = 6\) 的设置是否跨任务最优未做系统扫描。
- **\(\zeta\) 的调节缺乏自动选择**：\(\zeta\) 的最优取值具有数据/任务特异性，但论文未提供自适应选择策略或参数敏感性区间。

（完）
