---
title: Physics-Guided Flow Matching for CT Image Reconstruction
title_zh: 物理引导的流匹配用于CT图像重建
authors: Davide Evangelista
date: 2026-08-28
pdf: "https://arxiv.org/pdf/2608.28256v1"
tags: ["query:real-ir"]
score: 9.0
evidence: 物理引导流匹配作为CT重建生成先验
tldr: CT图像重建是典型的病态逆问题，扩散模型虽性能优异但采样路径长、计算量大且稳定性欠佳。本文提出以Flow Matching作为生成先验，训练高分辨率Rectified Flow模型，并通过两阶段训练策略缓解过拟合。在多个CT重建任务中，Flow Matching方法在PSNR、SSIM及感知质量上均优于扩散方法，且所需采样步数更少。该工作验证了Flow Matching作为高效稳定替代方案的潜力，并开源模型与代码。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.28256v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1541, \"height\": 710, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.28256v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1559, \"height\": 817, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.28256v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1674, \"height\": 958, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.28256v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1700, \"height\": 751, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.28256v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1533, \"height\": 677, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.28256v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1533, \"height\": 682, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.28256v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 868, \"height\": 904, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.28256v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1047, \"height\": 973, \"label\": \"Table\"}]"
motivation: 扩散模型在CT重建中虽表现优异，但采样过程慢、数值稳定性差，需探索更高效的生成先验。
method: 训练高分辨率Rectified Flow Matching模型，采用两阶段训练（先强增强后微调），并评估多种Flow重建算法。
result: Flow Matching方法在PSNR、SSIM和感知质量上全面优于扩散方法，且采样步数更少。
conclusion: Flow Matching是CT图像重建的高效稳定替代方案，公开代码和模型以推动后续研究。
---

## 摘要
深度生成模型最近已成为解决CT中不适定逆问题的强大先验，基于扩散的方法实现了最先进的重建性能。然而，扩散模型通常依赖于随机采样过程、长的推理轨迹以及精心调整的噪声调度，这可能限制计算效率和数值稳定性，尤其是在高空间分辨率下。在本工作中，我们研究流匹配作为CT重建的替代生成先验。我们在Mayo Clinic低剂量CT数据集的256x256胸部图像上训练了一个高分辨率的整流流匹配模型。为了减轻过拟合和有限的解剖变异性，我们采用两阶段训练策略：初始阶段使用强解剖信息数据增强，随后微调阶段减少或去除增强以细化结构保真度。生成的模型能够产生高质量且解剖上连贯的CT类图像，作为强大的学习先验。然后我们评估了专门为流匹配模型设计的多种重建方法，包括Plug-and-Play Flow、FlowDPS、Flower和Flow-Priors (ICTM)，并将它们与最先进的基于扩散的重建算法如DDRM、DPS和DiffPIR进行比较。在多个CT逆问题设置中的实验结果表明，基于流匹配的方法在PSNR、SSIM和感知质量方面 consistently 优于基于扩散的方法，同时需要更少的采样步骤。最后，我们公开释放训练好的流匹配模型和附带代码，以促进可重复性和未来研究。总体而言，本工作表明流匹配为高分辨率CT图像重建提供了扩散模型的稳定、高效且有效的替代方案。

## Abstract
Deep generative models have recently emerged as powerful priors for solving ill-posed inverse problems in CT, with diffusion-based approaches achieving state-of-the-art reconstruction performance. However, diffusion models typically rely on stochastic sampling procedures, long inference trajectories, and carefully tuned noise schedules, which can limit computational efficiency and numerical stability, especially at high spatial resolutions. In this work, we investigate Flow Matching as an alternative generative prior for CT reconstruction. We train a high-resolution Rectified Flow Matching model on 256x256 chest images from the Mayo Clinic Low-Dose CT dataset. To mitigate overfitting and limited anatomical variability, we employ a two-stage training strategy consisting of an initial phase with strong, anatomically informed data augmentation, followed by a fine-tuning phase with reduced or no augmentation to refine structural fidelity. The resulting model is capable of generating high-quality and anatomically coherent CT-like images, serving as a strong learned prior. We then evaluate multiple reconstruction methods specifically designed for Flow Matching models, including Plug-and-Play Flow, FlowDPS, Flower, and Flow-Priors (ICTM), and compare them against state-of-the-art diffusion-based reconstruction algorithms such as DDRM, DPS, and DiffPIR. Experimental results across several CT inverse problem settings show that Flow Matching-based approaches consistently outperform diffusion-based methods in terms of PSNR, SSIM, and perceptual quality, while requiring fewer sampling steps. Finally, we publicly release the trained Flow Matching model and accompanying code to facilitate reproducibility and future research. Overall, this work demonstrates that Flow Matching provides a stable, efficient, and effective alternative to diffusion models for high-resolution CT image reconstruction.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **背景**：CT图像重建是经典的病态逆问题，尤其在稀疏视图、低剂量或有限角度采集下，满足 Hadamard 适定性条件中的非单射性，导致解不唯一。传统解析/迭代方法依赖手工正则项，难以在噪声抑制与细节保留间取得平衡。
- **现状**：扩散模型作为生成先验在 CT 重建中性能优越（如 DDRM、DPS），但存在随机采样过程、长推理轨迹、噪声调度敏感、数值稳定性差和高计算成本等缺陷，在高分辨率医学图像场景中尤为突出。
- **核心问题**：能否采用 **Flow Matching（流匹配）** 作为替代性的生成先验，在保证重建质量的同时提升效率、稳定性和可复现性？
- **整体含义**：论文训练了一个高分辨率（256×256）的 Rectified Flow Matching 模型，系统比较了多种流匹配重建方法与扩散基线，证明流匹配可成为扩散模型的高效、稳定替代方案，并开源模型与代码。

## 2. 方法论

- **逆问题建模**：给定测量模型 \(y = Kx + e\)，其中 \(K\) 为 CT 投影算子，\(e \sim \mathcal{N}(0,\sigma^2 I)\)。目标是估计 \(x\)，采用 MAP 估计框架，先验由流匹配模型提供。
- **Flow Matching 先验**：
  - 学习一个时间依赖的速度场 \(v_\Theta(x_t, t)\)，通过 ODE \(\frac{dx_t}{dt} = v^*(x_t, t)\) 将基分布 \(p_0\)（高斯噪声）确定性传输到数据分布 \(p_{\text{data}}\)。
  - 采用 **Rectified Flow**：插值路径 \(x_t = (1-t)x_0 + t x_1\)，真实速度场 \(v^*(x_t,t) = x_1 - x_0\)。
  - 训练损失为 \(L_{\text{FM}}(\Theta) = \mathbb{E}_{x_0,x_1,t} \| v_\Theta(x_t, t) - (x_1 - x_0) \|_2^2\)。
- **网络架构**：时间条件 ResidualAttentionUNet，四个分辨率层（64、128、256、256 通道），在两个最深分辨率层使用自注意力；正弦位置嵌入 + 上采样实现时间条件；每个块前使用 GroupNorm；批大小为 4。
- **两阶段训练策略**：
  - **阶段一（强增强）**：旋转（-90°~90°）、水平翻转（p=0.5）、弹性变形（α=120, σ=6）、强度扰动；500 epochs，AdamW，学习率 1e-4，约 50 小时。
  - **阶段二（微调）**：减少或去除增强（无旋转，弹性变形 α=15, σ=4）；100 epochs，学习率 5e-5，以提升解剖保真度。
- **重建策略**：将待重建图像视为流初始状态 \(x_0\)，通过修改速度场引入数据一致性。基于 Tweedie 公式可得到去噪估计 \(\hat{x}_{0|t} = x_t - t v_\Theta(x_t,t)\) 和 \(\hat{x}_{1|t} = x_t + (1-t)v_\Theta(x_t,t)\)。四种方法：
  - **FlowDPS**：在流更新中加入归一化的似然梯度修正。
  - **PnP-Flow**：交替进行流预测和数据一致性梯度步。
  - **FLOWER**：使用 \(K^T(K\hat{x}_{1|t}-y)\) 作为数据一致性更新。
  - **ICTM（Flow-Priors）**：执行 CGLS 投影更新，用 \(\nu_t\) 平衡流估计与投影解。
- **数值稳定性处理**：
  - 图像范围映射：\(P(x)=(x+1)/2\) 用于匹配物理域 [0,1] 与归一化域 [-1,1]。
  - 梯度缩放因子 \(\gamma = 1/(H\sqrt{N_\alpha})\)，防止 \(K^TK\) 导致的梯度爆炸，并统一不同角度配置。
  - 采用 CGLS 近似求解法方程 \(K^TKx=K^Ty\)，缓解 ASTRA 背投影不精确于离散伴随的问题。

## 3. 实验设计

- **数据集**：Mayo Clinic Low-Dose CT 数据集，256×256 胸部切片，共 3,306 次采集、10 位患者。图像归一化至 [-1,1]。
- **任务场景**：稀疏视图 CT 重建，设置 60°、90°、120° 三种投影角度（均在 [0,π] 内），噪声 \(\sigma=0.01\)。投影算子用 ASTRA 并行束实现。
- **评估指标**：PSNR、SSIM、LPIPS；生成质量用 KID（与 350 张训练时未见过的图像比较）。
- **对比方法**：
  - 扩散基线：DPS、DDRM。
  - 流匹配：FlowDPS、PnP-Flow、FLOWER、ICTM。
  - 为保证公平，扩散与流匹配使用相同 backbone（UNet 架构和超参数）。
- **超参数选择**：在验证集上通过网格搜索调优各方法的数据一致性参数 \(\nu_t\)（或 DPS 的 \(\zeta_t\)）和步数。例如 FlowDPS 在 60/90° 时 \(\nu_t=5.0\)，120° 时 \(\nu_t=1.0\)，步数 N=500；PnP-Flow 步数 200，FLOWER 步数 300，ICTM 步数 100；DPS 步数 100-150，DDRM 步数 50。
- **实验类型**：
  - 无条件生成质量比较（KID 随步数变化，N∈{5,10,20,50,100}），对比第一阶段、第二阶段模型和扩散基线。
  - 三种角度配置下的重建定量与定性比较。
  - 附录中对两个具体样本（C081-45、C081-79）的详细结果，验证结论的普遍性。

## 4. 资源与算力

- **GPU**：实验使用 NVIDIA RTX A4000 GPU（16GB 显存），未明确说明 GPU 数量（通常单卡）。
- **训练时长**：
  - 第一阶段（强增强）约 50 小时。
  - 第二阶段（微调）未报告具体时长，但只训练 100 epochs。
- **推理步数**：各方法步数不同，FlowDPS 500 步、FLOWER 300 步、PnP-Flow 200 步、ICTM 100 步、DPS 100-150 步、DDRM 50 步。
- **内存限制**：batch size 设为 4，说明显存受限。

## 5. 实验数量与充分性

- **实验数量**：
  - 无条件生成实验：5 种步数 × 3 种模型（阶段一、阶段二、扩散），报告 KID 均值/标准差。
  - 重建主实验：3 种角度 × 6 种方法，报告 PSNR/SSIM/LPIPS。
  - 附录个例实验：2 个特定样本 × 3 种角度 × 6 种方法。
- **充分性**：
  - 全面比较了 4 种流匹配重建方法与 2 种扩散方法，覆盖不同稀疏度，定量+定性+生成质量评估，并包含两阶段训练的消融验证。
  - 超参数通过网格搜索优化，保证方法间相对公平。
  - 不足：仅在单一数据集（Mayo Clinic 胸部 CT）上验证，患者数少（10 人），缺乏跨数据集或跨解剖部位的泛化性测试；没有进行临床专家评估或下游任务验证；没有比较使用高阶 ODE 求解器的效果（作者留作未来工作）；摘要中提及与 DiffPIR 对比，但正文实验部分并未出现 DiffPIR 的结果，存在不一致。

## 6. 主要结论与发现

- **流匹配作为生成先验在 CT 重建中有效**：在所有角度配置下，流匹配方法均优于扩散基线。
  - 以 **FlowDPS** 为例：60° 时 PSNR 36.19 dB、SSIM 0.9162、LPIPS 0.1759；90° 时 36.56 dB、0.9230、0.1653；120° 时 36.64 dB、0.9260、0.1576。相比 DDRM，PSNR 提升 0.4~0.9 dB，LPIPS 大幅下降（相对改善约 36%~41%）。
- **两阶段训练策略有效**：微调后 KID 降低 10%~20%，生成的图像解剖连贯性更好，减少强增强导致的伪影。
- **流匹配在低步数下优势显著**：无条件生成中，即使步数为 5，流匹配 KID 仍低于扩散模型，说明确定性 ODE 采样比随机扩散采样更高效。
- **数值稳定化处理是关键**：范围映射、梯度缩放、CGLS 校正对保证重建收敛和公平比较非常重要。
- **ICTM/PnP-Flow 能恢复某些极细小结构**（如切片底部对称骨结构），但会引入轻微噪声；FlowDPS 整体视觉效果最佳。

## 7. 优点

- **方法创新**：首次将高分辨率 Rectified Flow Matching 应用于 CT 重建，并公开模型与代码，填补了该领域空白。
- **两阶段训练策略**：有效平衡了有限医学数据下泛化能力与解剖保真，具有普适参考价值。
- **系统性的方法比较**：在同一 backbone、同一数据集和控制条件下，横向比较了多种流匹配和扩散重建算法，结论可信度高。
- **工程上注重实际**：针对 CT 成像中的数值问题（伴随不匹配、梯度放大、范围不匹配）提出了具体且实用的解决方案。
- **可复现性**：代码和训练后模型将公开，有利于后续研究。

## 8. 不足与局限

- **数据与任务覆盖有限**：仅使用 Mayo Clinic 胸部 2D 切片，10 位患者的解剖变异性有限；未评估低剂量噪声、有限角度、3D 体数据重建等更广泛临床场景。
- **摘要与正文不一致**：摘要提到与 DiffPIR 相比，但正文实验未包含 DiffPIR。
- **推理步数仍较多**：FlowDPS 需 500 步，虽少于某些扩散模型，但在医疗实时性要求下仍有改进空间；高阶/自适应求解器未探索。
- **计算资源限制**：batch size 仅 4，可能影响训练稳定性和模型质量；训练时间长（>50 小时）。
- **对超参数敏感**：各方法的 \(\nu_t\) 需针对角度配置精细调优，实际部署时可能需要自适应调节机制。
- **缺乏临床评价**：仅使用数值指标和视觉检查，未进行放射科医生评估或诊断任务验证。
- **背投影不精确**：依赖 CGLS 近似，且 CGLS 迭代次数少（2 次），可能引入半收敛误差。

（完）
